/**
 * Created by Tivie on 13-07-2015.
 */// eslint-disable-next-line no-unused-vars -- called from sibling src files via the shared concat scope (see scripts/concat.mjs)
function getDefaultOpts(simple){'use strict';var defaultOptions={omitExtraWLInCodeBlocks:{defaultValue:false,describe:'Omit the default extra whiteline added to code blocks',type:'boolean'},headerIds:{defaultValue:{},describe:'Controls the id attribute generated on headings. Accepts either `false` (no ids) or an object `{prefix, raw}`. `{}` (the default) generates github-compatible ids with no prefix. `prefix` (string) is prepended to every id. `raw: true` uses minimal sanitization (only spaces, \', ", > and < become dashes, including in any prefix) instead of the default github-compatible sanitization. WARNING: raw ids might be malformed',type:'object'},headerLevelStart:{defaultValue:1,describe:'The header blocks level start',type:'number'},parseImgDimensions:{defaultValue:false,describe:'Turn on/off image dimension parsing',type:'boolean'},simplifiedAutoLink:{defaultValue:false,describe:'Turn on/off GFM autolink style',type:'boolean'},httpsAutoLinks:{defaultValue:false,describe:'Use \'https://\' for auto-generated links',type:'boolean'},literalMidWordUnderscores:{defaultValue:false,describe:'Parse midword underscores as literal underscores',type:'boolean'},strikethrough:{defaultValue:true,describe:'Turn on/off strikethrough support',type:'boolean'},tables:{defaultValue:false,describe:'Turn on/off tables support',type:'boolean'},tablesHeaderId:{defaultValue:false,describe:'Add an id to table headers',type:'boolean'},ghCodeBlocks:{defaultValue:true,describe:'Turn on/off GFM fenced code blocks support',type:'boolean'},tasklists:{defaultValue:false,describe:'Turn on/off GFM tasklist support',type:'boolean'},footnotes:{defaultValue:false,describe:'Turn on/off GFM footnotes support',type:'boolean'},smartIndentationFix:{defaultValue:false,describe:'Tries to smartly fix indentation in es6 strings',type:'boolean'},disableForced4SpacesIndentedSublists:{defaultValue:false,describe:'Disables the requirement of indenting nested sublists by 4 spaces',type:'boolean'},simpleLineBreaks:{defaultValue:false,describe:'Parses simple line breaks as <br>',type:'boolean'},requireSpaceBeforeHeadingText:{defaultValue:false,describe:'Makes adding a space between `#` and the header text mandatory (GFM Style)',type:'boolean'},ghMentions:{defaultValue:false,describe:'Enables github @mentions',type:'boolean'},ghMentionsLink:{defaultValue:'https://github.com/{u}',describe:'Changes the link generated by @mentions. Only applies if ghMentions option is enabled.',type:'string'},encodeEmails:{defaultValue:true,describe:'Encode e-mail addresses through the use of Character Entities, transforming ASCII e-mail addresses into its equivalent decimal entities',type:'boolean'},backslashEscapesHTMLTags:{defaultValue:false,describe:'Support for HTML Tag escaping. ex: \\<div>foo\\</div>',type:'boolean'},emoji:{defaultValue:false,describe:'Enable emoji support. Ex: `this is a :smile: emoji`',type:'boolean'},underline:{defaultValue:false,describe:'Enable support for underline. Syntax is double or triple underscores: `__underline word__`. With this option enabled, underscores no longer parses into `<em>` and `<strong>`',type:'boolean'},ellipsis:{defaultValue:true,describe:'Replaces three dots with the ellipsis unicode character',type:'boolean'},completeHTMLDocument:{defaultValue:false,describe:'Outputs a complete html document, including `<html>`, `<head>` and `<body>` tags',type:'boolean'},metadata:{defaultValue:false,describe:'Enable support for document metadata (defined at the top of the document between `«««` and `»»»` or between `---` and `---`).',type:'boolean'},splitAdjacentBlockquotes:{defaultValue:false,describe:'Split adjacent blockquote blocks',type:'boolean'},moreStyling:{defaultValue:false,describe:'Adds some useful styling css classes in the generated html',type:'boolean'},relativePathBaseUrl:{defaultValue:'',describe:'Prepends a base URL to relative paths',type:'string'},decodeEntities:{defaultValue:false,describe:'Resolve HTML5 named and numeric character references to their characters (CommonMark behavior)',type:'boolean'},cmSpec:{defaultValue:false,describe:'Enable CommonMark spec compliance: parse blocks (lists, block quotes, HTML blocks, containers, tabs) and inlines (emphasis, links, images, autolinks, raw HTML) per the CommonMark spec instead of Showdown\'s legacy matching',type:'boolean'},disallowRawHTML:{defaultValue:false,describe:'Filter the GFM "disallowed raw HTML" tags (title, textarea, style, xmp, iframe, noembed, noframes, script, plaintext) by escaping their leading < to &lt; in the output',type:'boolean'},safeMode:{defaultValue:false,describe:'Defense-in-depth hardening for untrusted Markdown: (1) block dangerous URL schemes (javascript:, vbscript:, data: except data:image for image src) in generated links/images, and (2) escape ALL raw HTML tags so embedded <script>/<img onerror>/event handlers cannot execute. NOTE: this is not a full HTML sanitizer — for fully untrusted input still run the output through a dedicated sanitizer (e.g. DOMPurify) and serve a Content-Security-Policy',type:'boolean'}};if(simple===false){return JSON.parse(JSON.stringify(defaultOptions));}var ret={};for(var opt in defaultOptions){if(Object.prototype.hasOwnProperty.call(defaultOptions,opt)){ret[opt]=defaultOptions[opt].defaultValue;}}return ret;}/**
 * Created by Tivie on 06-01-2015.
 */// Private properties
var showdown={},parsers={},extensions={},globalOptions=getDefaultOpts(true),setFlavor='vanilla',flavor={commonmark:{headerIds:false,requireSpaceBeforeHeadingText:true,decodeEntities:true,cmSpec:true,strikethrough:false,encodeEmails:false},gfm:{headerIds:false,requireSpaceBeforeHeadingText:true,decodeEntities:true,cmSpec:true,strikethrough:true,encodeEmails:false,ghCodeBlocks:true,tables:true,tasklists:true,footnotes:true,ghMentions:true,simplifiedAutoLink:true,emoji:true,omitExtraWLInCodeBlocks:true,disallowRawHTML:true// GFM tagfilter extension (spec §6.11)
//literalMidWordUnderscores:            true,
//disableForced4SpacesIndentedSublists: true,
//backslashEscapesHTMLTags:             true,
},original:{headerIds:false,ghCodeBlocks:false,strikethrough:false},vanilla:getDefaultOpts(true)};// alias github to gfm for backwards compatibility
flavor.github=flavor.gfm;/**
 * helper namespace
 * @type {{}}
 */showdown.helper={};/**
 * Set a global option
 * @static
 * @param {string} key
 * @param {*} value
 * @returns {showdown}
 */showdown.setOption=function(key,value){'use strict';globalOptions[key]=value;return this;};/**
 * Get a global option
 * @static
 * @param {string} key
 * @returns {*}
 */showdown.getOption=function(key){'use strict';return globalOptions[key];};/**
 * Get the global options
 * @static
 * @returns {{}}
 */showdown.getOptions=function(){'use strict';return globalOptions;};/**
 * Reset global options to the default values
 * @static
 */showdown.resetOptions=function(){'use strict';globalOptions=getDefaultOpts(true);};/**
 * Set the flavor showdown should use as default
 * @param {string} name
 */showdown.setFlavor=function(name){'use strict';if(!Object.prototype.hasOwnProperty.call(flavor,name)){throw Error(name+' flavor was not found');}showdown.resetOptions();var preset=flavor[name];setFlavor=name;for(var option in preset){if(Object.prototype.hasOwnProperty.call(preset,option)){globalOptions[option]=preset[option];}}};/**
 * Get the currently set flavor
 * @returns {string}
 */showdown.getFlavor=function(){'use strict';return setFlavor;};/**
 * Get the options of a specified flavor. Returns undefined if the flavor was not found
 * @param {string} name Name of the flavor
 * @returns {{}|undefined}
 */showdown.getFlavorOptions=function(name){'use strict';if(Object.prototype.hasOwnProperty.call(flavor,name)){return flavor[name];}};/**
 * Get the list of available flavor names
 * @static
 * @returns {string[]}
 */showdown.getFlavors=function(){'use strict';return Object.keys(flavor);};/**
 * Get the default options
 * @static
 * @param {boolean} [simple=true]
 * @returns {{}}
 */showdown.getDefaultOptions=function(simple){'use strict';return getDefaultOpts(simple);};/**
 * Get or set a subParser
 *
 * subParser(name)       - Get a registered subParser
 * subParser(name, func) - Register a subParser
 * @static
 * @param {string} name
 * @param {function} [func]
 * @returns {*}
 */showdown.subParser=function(name,func){'use strict';if(showdown.helper.isString(name)){if(typeof func!=='undefined'){parsers[name]=func;}else{if(Object.prototype.hasOwnProperty.call(parsers,name)){return parsers[name];}else{throw Error('SubParser named '+name+' not registered!');}}}else{throw Error('showdown.subParser function first argument must be a string (the name of the subparser)');}};/**
 *
 * @returns {{}}
 */showdown.getSubParserList=function(){return parsers;};/**
 * Gets or registers an extension
 * @static
 * @param {string} name
 * @param {object|object[]|function=} ext
 * @returns {*}
 */showdown.extension=function(name,ext){'use strict';if(!showdown.helper.isString(name)){throw Error('Extension \'name\' must be a string');}name=showdown.helper.stdExtName(name);// Getter
if(showdown.helper.isUndefined(ext)){if(!Object.prototype.hasOwnProperty.call(extensions,name)){throw Error('Extension named '+name+' is not registered!');}return extensions[name];// Setter
}else{// Expand extension if it's wrapped in a function
if(typeof ext==='function'){ext=ext();}// Ensure extension is an array
if(!showdown.helper.isArray(ext)){ext=[ext];}var validExtension=validate(ext,name);if(validExtension.valid){extensions[name]=ext;}else{throw Error(validExtension.error);}}};/**
 * Gets all extensions registered
 * @returns {{}}
 */showdown.getAllExtensions=function(){'use strict';return extensions;};/**
 * Remove an extension
 * @param {string} name
 */showdown.removeExtension=function(name){'use strict';delete extensions[name];};/**
 * Removes all extensions
 */showdown.resetExtensions=function(){'use strict';extensions={};};/**
 * Validate extension
 * @param {array} extension
 * @param {string} name
 * @returns {{valid: boolean, error: string}}
 */function validate(extension,name){'use strict';var errMsg=name?'Error in '+name+' extension->':'Error in unnamed extension',ret={valid:true,error:''};if(!showdown.helper.isArray(extension)){extension=[extension];}for(var i=0;i<extension.length;++i){var baseMsg=errMsg+' sub-extension '+i+': ',ext=extension[i];if(_typeof(ext)!=='object'){ret.valid=false;ret.error=baseMsg+'must be an object, but '+_typeof(ext)+' given';return ret;}if(!showdown.helper.isString(ext.type)){ret.valid=false;ret.error=baseMsg+'property "type" must be a string, but '+_typeof(ext.type)+' given';return ret;}var type=ext.type=ext.type.toLowerCase();// normalize extension type
if(type==='language'){type=ext.type='lang';}if(type==='html'){type=ext.type='output';}if(type!=='lang'&&type!=='output'&&type!=='listener'){ret.valid=false;ret.error=baseMsg+'type '+type+' is not recognized. Valid values: "lang/language", "output/html" or "listener"';return ret;}if(type==='listener'){if(showdown.helper.isUndefined(ext.listeners)){ret.valid=false;ret.error=baseMsg+'. Extensions of type "listener" must have a property called "listeners"';return ret;}}else{if(showdown.helper.isUndefined(ext.filter)&&showdown.helper.isUndefined(ext.regex)){ret.valid=false;ret.error=baseMsg+type+' extensions must define either a "regex" property or a "filter" method';return ret;}}if(ext.listeners){if(_typeof(ext.listeners)!=='object'){ret.valid=false;ret.error=baseMsg+'"listeners" property must be an object but '+_typeof(ext.listeners)+' given';return ret;}for(var ln in ext.listeners){if(Object.prototype.hasOwnProperty.call(ext.listeners,ln)){if(typeof ext.listeners[ln]!=='function'){ret.valid=false;ret.error=baseMsg+'"listeners" property must be an hash of [event name]: [callback]. listeners.'+ln+' must be a function but '+_typeof(ext.listeners[ln])+' given';return ret;}}}}if(ext.filter){if(typeof ext.filter!=='function'){ret.valid=false;ret.error=baseMsg+'"filter" must be a function, but '+_typeof(ext.filter)+' given';return ret;}}else if(ext.regex){if(showdown.helper.isString(ext.regex)){ext.regex=new RegExp(ext.regex,'g');}if(!(ext.regex instanceof RegExp)){ret.valid=false;ret.error=baseMsg+'"regex" property must either be a string or a RegExp object, but '+_typeof(ext.regex)+' given';return ret;}if(showdown.helper.isUndefined(ext.replace)){ret.valid=false;ret.error=baseMsg+'"regex" extensions must implement a replace string or function';return ret;}}}return ret;}/**
 * Validate extension
 * @param {object} ext
 * @returns {boolean}
 */showdown.validateExtension=function(ext){'use strict';var validateExtension=validate(ext,null);if(!validateExtension.valid){console.warn(validateExtension.error);return false;}return true;};// noinspection HtmlDeprecatedAttribute,CheckImageSize
/**
 * showdownjs helper functions
 */if(!Object.prototype.hasOwnProperty.call(showdown,'helper')){showdown.helper={};}// makeMarkdown needs a DOM to parse the input HTML. Resolution is lazy (first access of
// `showdown.helper.document`) so Node users that only call makeHtml never pay the cost of
// loading happy-dom; in environments with an ambient window/document (browsers, DOM test
// environments) the require branch is never reached, so happy-dom never ends up in bundles.
var lazyDocument=null;Object.defineProperty(showdown.helper,'document',{get:function get(){'use strict';if(lazyDocument===null){if(typeof window!=='undefined'&&window.document){lazyDocument=window.document;}else if(typeof document!=='undefined'){lazyDocument=document;}else if(typeof require==='function'){var happyDom=require('happy-dom');lazyDocument=new happyDom.Window().document;}else{throw new Error('makeMarkdown requires a DOM: provide a global window/document, '+'or run in Node (CommonJS) with happy-dom installed.');}}return lazyDocument;}});/**
 * Parse an untrusted HTML string into a detached <div> and return it, for
 * makeMarkdown to walk. The HTML is parsed inside an *inert* document (one with
 * no browsing context, obtained via document.implementation.createHTMLDocument)
 * whenever the host DOM supports it. In an inert document, assigning to innerHTML
 * never executes <script> and never triggers resource loads or fires on* handlers
 * such as `<img onerror>` / `<svg onload>` — so parsing attacker HTML client-side
 * cannot run script. happy-dom (Node) is already inert; using the same path keeps
 * behavior uniform.
 * @param {string} html
 * @returns {Object} a detached div element whose innerHTML is the parsed html
 */var lazyInertDocument=null;showdown.helper.parseHTML=function(html){'use strict';var ownerDoc=showdown.helper.document;if(ownerDoc.implementation&&typeof ownerDoc.implementation.createHTMLDocument==='function'){if(lazyInertDocument===null){lazyInertDocument=ownerDoc.implementation.createHTMLDocument('');}ownerDoc=lazyInertDocument;}var div=ownerDoc.createElement('div');div.innerHTML=html;return div;};/**
 * Check if var is string
 * @static
 * @param {*} a
 * @returns {boolean}
 */showdown.helper.isString=function(a){'use strict';return typeof a==='string'||a instanceof String;};/**
 * Check if var is a number
 * @static
 * @param {*} a
 * @returns {boolean}
 */showdown.helper.isNumber=function(a){return!isNaN(a);};/**
 * Check if var is a function
 * @static
 * @param {*} a
 * @returns {boolean}
 */showdown.helper.isFunction=function(a){'use strict';var getType={};return a&&getType.toString.call(a)==='[object Function]';};/**
 * isArray helper function
 * @static
 * @param {*} a
 * @returns {boolean}
 */showdown.helper.isArray=function(a){'use strict';var isArray;if(!Array.isArray){isArray=function isArray(arg){return Object.prototype.toString.call(arg)==='[object Array]';};}else{isArray=Array.isArray;}return isArray(a);};/**
 * Check if value is undefined
 * @static
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is `undefined`, else `false`.
 */showdown.helper.isUndefined=function(value){'use strict';return typeof value==='undefined';};/**
 * Check if value is an object (excluding arrays)
 * @param {*} value
 * @returns {boolean}
 */showdown.helper.isObject=function(value){'use strict';return _typeof(value)==='object'&&!Array.isArray(value)&&value!==null;};/**
 * ForEach helper function
 * Iterates over Arrays and Objects (own properties only)
 * @static
 * @param {*} obj
 * @param {function} callback Accepts 3 params: 1. value, 2. key, 3. the original array/object
 */showdown.helper.forEach=function(obj,callback){'use strict';// check if obj is defined
if(showdown.helper.isUndefined(obj)){throw new Error('obj param is required');}if(showdown.helper.isUndefined(callback)){throw new Error('callback param is required');}if(!showdown.helper.isFunction(callback)){throw new Error('callback param must be a function/closure');}if(typeof obj.forEach==='function'){obj.forEach(callback);}else if(showdown.helper.isArray(obj)){for(var i=0;i<obj.length;i++){callback(obj[i],i,obj);}}else if(_typeof(obj)==='object'){for(var prop in obj){if(Object.prototype.hasOwnProperty.call(obj,prop)){callback(obj[prop],prop,obj);}}}else{throw new Error('obj does not seem to be an array or an iterable object');}};/**
 * Standardize extension name
 * @static
 * @param {string} s extension name
 * @returns {string}
 */showdown.helper.stdExtName=function(s){'use strict';return s.replace(/[_?*+/\\.^-]/g,'').replace(/\s/g,'').toLowerCase();};function escapeCharactersCallback(wholeMatch,m1){'use strict';var charCodeToEscape=m1.charCodeAt(0);return'¨E'+charCodeToEscape+'E';}/**
 * Callback used to escape characters when passing through String.replace
 * @static
 * @param {string} wholeMatch
 * @param {string} m1
 * @returns {string}
 */showdown.helper.escapeCharactersCallback=escapeCharactersCallback;/**
 * Escape characters in a string
 * @static
 * @param {string} text
 * @param {string} charsToEscape
 * @param {boolean} afterBackslash
 * @returns {string|void|*}
 */showdown.helper.escapeCharacters=function(text,charsToEscape,afterBackslash){'use strict';// First we have to escape the escape characters so that
// we can build a character class out of them
var regexString='(['+charsToEscape.replace(/([[\]\\])/g,'\\$1')+'])';if(afterBackslash){regexString='\\\\'+regexString;}var regex=new RegExp(regexString,'g');text=text.replace(regex,escapeCharactersCallback);return text;};var rgxFindMatchPos=function rgxFindMatchPos(str,left,right,flags){'use strict';var f=flags||'',g=f.indexOf('g')>-1,x=new RegExp(left+'|'+right,'g'+f.replace(/g/g,'')),l=new RegExp(left,f.replace(/g/g,'')),pos=[],t,s,m,start,end;do{t=0;while(m=x.exec(str)){if(l.test(m[0])){if(!t++){s=x.lastIndex;start=s-m[0].length;}}else if(t){if(! --t){end=m.index+m[0].length;var obj={left:{start:start,end:s},match:{start:s,end:m.index},right:{start:m.index,end:end},wholeMatch:{start:start,end:end}};pos.push(obj);if(!g){return pos;}}}}}while(t&&(x.lastIndex=s));return pos;};/**
 * matchRecursiveRegExp
 *
 * (c) 2007 Steven Levithan <stevenlevithan.com>
 * MIT License
 *
 * Accepts a string to search, a left and right format delimiter
 * as regex patterns, and optional regex flags. Returns an array
 * of matches, allowing nested instances of left/right delimiters.
 * Use the "g" flag to return all matches, otherwise only the
 * first is returned. Be careful to ensure that the left and
 * right format delimiters produce mutually exclusive matches.
 * Backreferences are not supported within the right delimiter
 * due to how it is internally combined with the left delimiter.
 * When matching strings whose format delimiters are unbalanced
 * to the left or right, the output is intentionally as a
 * conventional regex library with recursion support would
 * produce, e.g. "<<x>" and "<x>>" both produce ["x"] when using
 * "<" and ">" as the delimiters (both strings contain a single,
 * balanced instance of "<x>").
 *
 * examples:
 * matchRecursiveRegExp("test", "\\(", "\\)")
 * returns: []
 * matchRecursiveRegExp("<t<<e>><s>>t<>", "<", ">", "g")
 * returns: ["t<<e>><s>", ""]
 * matchRecursiveRegExp("<div id=\"x\">test</div>", "<div\\b[^>]*>", "</div>", "gi")
 * returns: ["test"]
 */showdown.helper.matchRecursiveRegExp=function(str,left,right,flags){'use strict';var matchPos=rgxFindMatchPos(str,left,right,flags),results=[];for(var i=0;i<matchPos.length;++i){results.push([str.slice(matchPos[i].wholeMatch.start,matchPos[i].wholeMatch.end),str.slice(matchPos[i].match.start,matchPos[i].match.end),str.slice(matchPos[i].left.start,matchPos[i].left.end),str.slice(matchPos[i].right.start,matchPos[i].right.end)]);}return results;};/**
 *
 * @param {string} str
 * @param {string|function} replacement
 * @param {string} left
 * @param {string} right
 * @param {string} flags
 * @returns {string}
 */showdown.helper.replaceRecursiveRegExp=function(str,replacement,left,right,flags){'use strict';if(!showdown.helper.isFunction(replacement)){var repStr=replacement;replacement=function replacement(){return repStr;};}var matchPos=rgxFindMatchPos(str,left,right,flags),finalStr=str,lng=matchPos.length;if(lng>0){var bits=[];if(matchPos[0].wholeMatch.start!==0){bits.push(str.slice(0,matchPos[0].wholeMatch.start));}for(var i=0;i<lng;++i){bits.push(replacement(str.slice(matchPos[i].wholeMatch.start,matchPos[i].wholeMatch.end),str.slice(matchPos[i].match.start,matchPos[i].match.end),str.slice(matchPos[i].left.start,matchPos[i].left.end),str.slice(matchPos[i].right.start,matchPos[i].right.end)));if(i<lng-1){bits.push(str.slice(matchPos[i].wholeMatch.end,matchPos[i+1].wholeMatch.start));}}if(matchPos[lng-1].wholeMatch.end<str.length){bits.push(str.slice(matchPos[lng-1].wholeMatch.end));}finalStr=bits.join('');}return finalStr;};/**
 * Returns the index within the passed String object of the first occurrence of the specified regex,
 * starting the search at fromIndex. Returns -1 if the value is not found.
 *
 * @param {string} str string to search
 * @param {RegExp} regex Regular expression to search
 * @param {int} [fromIndex = 0] Index to start the search
 * @returns {Number}
 * @throws InvalidArgumentError
 */showdown.helper.regexIndexOf=function(str,regex,fromIndex){'use strict';if(!showdown.helper.isString(str)){throw'InvalidArgumentError: first parameter of showdown.helper.regexIndexOf function must be a string';}if(!(regex instanceof RegExp)){throw'InvalidArgumentError: second parameter of showdown.helper.regexIndexOf function must be an instance of RegExp';}var indexOf=str.substring(fromIndex||0).search(regex);return indexOf>=0?indexOf+(fromIndex||0):indexOf;};/**
 * Splits the passed string object at the defined index, and returns an array composed of the two substrings
 * @param {string} str string to split
 * @param {int} index index to split string at
 * @returns {[string,string]}
 * @throws InvalidArgumentError
 */showdown.helper.splitAtIndex=function(str,index){'use strict';if(!showdown.helper.isString(str)){throw'InvalidArgumentError: first parameter of showdown.helper.regexIndexOf function must be a string';}return[str.substring(0,index),str.substring(index)];};/**
 * MurmurHash3's mixing function
 * https://stackoverflow.com/questions/521295/seeding-the-random-number-generator-in-javascript/47593316#47593316
 *
 * @param {string} string
 * @returns {Number}
 *//*jshint bitwise: false*/function xmur3(str){var h;for(var i=0,_h=1779033703^str.length;i<str.length;i++){_h=Math.imul(_h^str.charCodeAt(i),3432918353);_h=_h<<13|_h>>>19;}return function(){h=Math.imul(h^h>>>16,2246822507);h=Math.imul(h^h>>>13,3266489909);return(h^=h>>>16)>>>0;};}/**
 * Random Number Generator
 * https://stackoverflow.com/questions/521295/seeding-the-random-number-generator-in-javascript/47593316#47593316
 *
 * @param {Number} seed
 * @returns {Number}
 *//*jshint bitwise: false*/function mulberry32(a){return function(){var t=a+=0x6D2B79F5;t=Math.imul(t^t>>>15,t|1);t^=t+Math.imul(t^t>>>7,t|61);return((t^t>>>14)>>>0)/4294967296;};}/**
 * Obfuscate an e-mail address through the use of Character Entities,
 * transforming ASCII characters into their equivalent decimal or hex entities.
 *
 *
 * @param {string} mail
 * @returns {string}
 */showdown.helper.encodeEmailAddress=function(mail){'use strict';var encode=[function(ch){return'&#'+ch.charCodeAt(0)+';';},function(ch){return'&#x'+ch.charCodeAt(0).toString(16)+';';},function(ch){return ch;}];// RNG seeded with mail, so that we can get determined results for each email.
var rand=mulberry32(xmur3(mail));mail=mail.replace(/./g,function(ch){if(ch==='@'){// this *must* be encoded. I insist.
ch=encode[Math.floor(rand()*2)](ch);}else{var r=rand();// roughly 10% raw, 45% hex, 45% dec
ch=r>0.9?encode[2](ch):r>0.45?encode[1](ch):encode[0](ch);}return ch;});return mail;};/**
 * String.prototype.repeat polyfill
 *
 * @param {string} str
 * @param {int} count
 * @returns {string}
 */showdown.helper.repeat=function(str,count){'use strict';// use built-in method if it's available
if(!showdown.helper.isUndefined(String.prototype.repeat)){return str.repeat(count);}str=''+str;if(count<0){throw new RangeError('repeat count must be non-negative');}if(count===Infinity){throw new RangeError('repeat count must be less than infinity');}count=Math.floor(count);if(str.length===0||count===0){return'';}// Ensuring count is a 31-bit integer allows us to heavily optimize the
// main part. But anyway, most current (August 2014) browsers can't handle
// strings 1 << 28 chars or longer, so:
/*jshint bitwise: false*/if(str.length*count>=1<<28){throw new RangeError('repeat count must not overflow maximum string size');}/*jshint bitwise: true*/var maxCount=str.length*count;count=Math.floor(Math.log(count)/Math.log(2));while(count){str+=str;count--;}str+=str.substring(0,maxCount-str.length);return str;};/**
 * String.prototype.padEnd polyfill
 *
 * @param {string} str
 * @param {int} targetLength
 * @param {string} [padString]
 * @returns {string}
 */showdown.helper.padEnd=function padEnd(str,targetLength,padString){'use strict';// eslint-disable-next-line @stylistic/space-infix-ops
targetLength=targetLength>>0;//floor if number or convert non-number to 0;
padString=String(padString||' ');if(str.length>targetLength){return String(str);}else{targetLength=targetLength-str.length;if(targetLength>padString.length){padString+=showdown.helper.repeat(padString,targetLength/padString.length);//append to original to ensure we are longer than needed
}return String(str)+padString.slice(0,targetLength);}};/**
 * Unescape HTML entities
 * @param txt
 * @returns {string}
 */showdown.helper.unescapeHTMLEntities=function(txt){'use strict';return txt.replace(/&quot;/g,'"').replace(/&lt;/g,'<').replace(/&gt;/g,'>').replace(/&amp;/g,'&');};showdown.helper._hashHTMLSpan=function(html,globals){return'¨C'+(globals.gHtmlSpans.push(html)-1)+'C';};/**
 * Prepends a base URL to relative paths.
 *
 * @param {string} baseUrl the base URL to prepend to a relative path
 * @param {string} url the path to modify, which may be relative
 * @returns {string} the full URL
 */showdown.helper.applyBaseUrl=function(baseUrl,url){// Only prepend if given a base URL and the path is not absolute.
if(baseUrl&&baseUrl!==''&&!showdown.helper.isAbsolutePath(url)){var urlResolve=new showdown.helper.URLUtils(url,baseUrl);url=urlResolve.href;}return url;};/**
 * Checks if the given path is absolute.
 *
 * @param {string} path the path to test for absolution
 * @returns {boolean} `true` if the given path is absolute, else `false`
 */showdown.helper.isAbsolutePath=function(path){// Absolute paths begin with '[protocol:]//' or '#' (anchors)
return /(^([a-z]+:)?\/\/)|(^#)/i.test(path);};// URL schemes allowed by safeMode. Relative URLs, fragments (#...) and
// protocol-relative URLs (//host) carry no scheme and are always allowed.
showdown.helper.safeUrlSchemes=['http','https','ftp','ftps','mailto','tel','sms'];/**
 * safeMode URL guard: decide whether a link/image destination is safe to emit.
 * Rejects dangerous schemes (javascript:, vbscript:, data:text/html, ...). The
 * scheme is resolved after decoding character references and stripping the
 * characters browsers ignore when parsing a scheme (whitespace and control
 * chars), so evasions like `java&#115;cript:` or `java\tscript:` are caught.
 * `data:` is only permitted when opts.allowDataImage is set and the payload is a
 * `data:image/...` URL (so inline base64 images keep working under safeMode).
 * @param {string} url
 * @param {{allowDataImage?: boolean}} [opts]
 * @returns {boolean}
 */showdown.helper.isSafeUrl=function(url,opts){'use strict';var allowDataImage=!!(opts&&opts.allowDataImage);// resolve character references (numeric + named) that could hide the scheme
var decoded=showdown.helper.cmDecodeEntities(String(url));// restore showdown's internal escape placeholders (¨E<code>E) to their chars
decoded=decoded.replace(/¨E(\d+)E/g,function(wm,code){return String.fromCharCode(+code);});// remove whitespace/control chars browsers ignore while resolving the scheme
// eslint-disable-next-line no-control-regex -- intentionally stripping ASCII control chars used to obfuscate schemes
var stripped=decoded.replace(/[\u0000-\u0020\u00a0]+/g,'');var m=/^([a-zA-Z][a-zA-Z0-9+.-]*):/.exec(stripped);if(!m){// no scheme: relative path, fragment (#...) or protocol-relative (//host)
return true;}var scheme=m[1].toLowerCase();if(scheme==='data'){return allowDataImage&&/^data:image\//i.test(stripped);}return showdown.helper.safeUrlSchemes.indexOf(scheme)!==-1;};/**
 * Polyfill method for trimStart
 * @param {string} text
 * @returns {string}
 */showdown.helper.trimStart=function(text){return!String.prototype.trimStart?// eslint-disable-next-line no-control-regex -- Unicode whitespace class (incl. \x09-\x0D) in trim polyfill
text.replace(/^[\x09\x0A\x0B\x0C\x0D\x20\xA0\u1680\u2000\u2001\u2002\u2003\u2004\u2005\u2006\u2007\u2008\u2009\u200A\u202F\u205F\u3000\u2028\u2029\uFEFF]+/,''):text.trimStart();};/**
 * Polyfill method for trimEnd
 * @param {string} text
 * @returns {string}
 */showdown.helper.trimEnd=function(text){return!String.prototype.trimEnd?// eslint-disable-next-line no-control-regex -- Unicode whitespace class (incl. \x09-\x0D) in trim polyfill
text.replace(/[\x09\x0A\x0B\x0C\x0D\x20\xA0\u1680\u2000\u2001\u2002\u2003\u2004\u2005\u2006\u2007\u2008\u2009\u200A\u202F\u205F\u3000\u2028\u2029\uFEFF]+$/,''):text.trimEnd();};showdown.helper.URLUtils=function(url,baseURL){var pattern2=/^([^:/?#]+:)?(?:\/\/(?:([^:@/?#]*)(?::([^:@/?#]*))?@)?(([^:/?#]*)(?::(\d*))?))?([^?#]*)(\?[^#]*)?(#[\s\S]*)?/;var m=String(url).trim().match(pattern2);if(!m){throw new RangeError();}var protocol=m[1]||'';var username=m[2]||'';var password=m[3]||'';var host=m[4]||'';var hostname=m[5]||'';var port=m[6]||'';var pathname=m[7]||'';var search=m[8]||'';var hash=m[9]||'';if(baseURL!==undefined){var base=new showdown.helper.URLUtils(baseURL);var flag=protocol===''&&host===''&&username==='';if(flag&&pathname===''&&search===''){search=base.search;}if(flag&&pathname.charAt(0)!=='/'){pathname=pathname!==''?((base.host!==''||base.username!=='')&&base.pathname===''?'/':'')+base.pathname.slice(0,base.pathname.lastIndexOf('/')+1)+pathname:base.pathname;}// dot segments removal
var output=[];pathname.replace(/^(\.\.?(\/|$))+/,'').replace(/\/(\.(\/|$))+/g,'/').replace(/\/\.\.$/,'/../').replace(/\/?[^/]*/g,function(p){if(p==='/..'){output.pop();}else{output.push(p);}});pathname=output.join('').replace(/^\//,pathname.charAt(0)==='/'?'/':'');if(flag){port=base.port;hostname=base.hostname;host=base.host;password=base.password;username=base.username;}if(protocol===''){protocol=base.protocol;}}this.origin=protocol+(protocol!==''||host!==''?'//':'')+host;this.href=protocol+(protocol!==''||host!==''?'//':'')+(username!==''?username+(password!==''?':'+password:'')+'@':'')+host+pathname+search+hash;this.protocol=protocol;this.username=username;this.password=password;this.host=host;this.hostname=hostname;this.port=port;this.pathname=pathname;this.search=search;this.hash=hash;};/**
 *
 * @param {string} url
 * @returns {string}
 */showdown.helper.urlASCIIEncoding=function(url){url=url.replace(/\\/g,'%5C').replace(/ /g,'%20');return url;};/**
 * Resolve HTML5 named (`&ouml;`), decimal (`&#246;`) and hexadecimal (`&#xf6;`)
 * character references to their corresponding characters (CommonMark behavior).
 * Unlike makehtml.decodeEntities, this returns the raw decoded characters (no
 * HTML re-escaping) so the result can be percent-encoded for a URL or further
 * processed. Invalid references are left verbatim.
 * @param {string} str
 * @returns {string}
 */showdown.helper.cmDecodeEntities=function(str){var entities=showdown.helper.htmlEntities||{};function fromCodePoint(cp){if(cp===0||cp>0x10FFFF||cp>=0xD800&&cp<=0xDFFF){return'�';}try{return String.fromCodePoint(cp);}catch(_unused){return'�';}}return str.replace(/&([#0-9a-zA-Z]+);/g,function(wholeMatch,body){var m;if(m=/^#([0-9]{1,7})$/.exec(body)){return fromCodePoint(parseInt(m[1],10));}if(m=/^#[xX]([0-9a-fA-F]{1,6})$/.exec(body)){return fromCodePoint(parseInt(m[1],16));}if(/^[a-zA-Z][a-zA-Z0-9]*$/.test(body)&&Object.prototype.hasOwnProperty.call(entities,body)){return entities[body];}return wholeMatch;});};/**
 * CommonMark URL percent-encoding: percent-encode every character outside the
 * "safe" set, while preserving sequences that are already percent-encoded.
 * Mirrors mdurl.encode's default behavior.
 * @param {string} uri
 * @returns {string}
 */showdown.helper.cmEncodeURI=function(uri){var safe=';/?:@&=+$,-_.!~*\'()#';var out='';for(var i=0;i<uri.length;++i){var ch=uri.charAt(i),code=uri.charCodeAt(i);if(ch==='%'&&/^[0-9a-fA-F]{2}$/.test(uri.slice(i+1,i+3))){out+=uri.slice(i,i+3);i+=2;}else if(code>=48&&code<=57||code>=65&&code<=90||code>=97&&code<=122||safe.indexOf(ch)!==-1){out+=ch;}else{out+=encodeURIComponent(ch);}}return out;};/**
 * Full CommonMark URL normalization for a link/image destination:
 * 1. restore showdown's `¨E<code>E` backslash-escape placeholders to their literal
 *    characters (so escaped punctuation is treated literally, not re-processed);
 * 2. resolve raw backslash escapes of ASCII punctuation (`\*` -> `*`); a backslash
 *    before a non-punctuation character stays literal (and is later percent-encoded);
 * 3. resolve HTML character references (`&ouml;` -> `ö`);
 * 4. percent-encode the result;
 * 5. HTML-escape any residual bare `&` so the href stays valid HTML.
 * @param {string} url
 * @returns {string}
 */showdown.helper.cmNormalizeURL=function(url){url=url.replace(/¨E(\d+)E/g,function(wholeMatch,code){return String.fromCharCode(parseInt(code,10));});url=url.replace(/\\([!-/:-@[-`{-~])/g,'$1');url=showdown.helper.cmDecodeEntities(url);url=showdown.helper.cmEncodeURI(url);return url.replace(/&(?![a-zA-Z#0-9]+;)/g,'&amp;');};/**
 * HTML-escape the characters that are significant in element and (double-quoted)
 * attribute contexts: `&`, `<`, `>`, `"`. Ampersand is escaped unconditionally,
 * so callers must pass a decoded/plain string (not one that already contains
 * entities they wish to preserve). Used to harden values that are concatenated
 * straight into generated markup (e.g. document metadata).
 * @param {string} str
 * @returns {string}
 */showdown.helper.escapeHTMLEntities=function(str){'use strict';return str.replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;');};/**
 * makeMarkdown emitters: backslash-escape the characters that would otherwise let
 * an attribute value break out of the generated Markdown syntax and inject new
 * constructs when the Markdown is re-rendered (round-trip injection).
 *
 * - Destination: emitted inside `<...>`, so `\`, `<` and `>` must be escaped.
 * - Title: emitted inside `"..."`, so `\` and `"` must be escaped.
 * - Text (link/image alt): emitted inside `[...]`, so `\`, `[` and `]` must be escaped.
 */showdown.helper.escapeMarkdownDestination=function(url){'use strict';return String(url).replace(/([\\<>])/g,'\\$1');};showdown.helper.escapeMarkdownTitle=function(title){'use strict';return String(title).replace(/([\\"])/g,'\\$1');};showdown.helper.escapeMarkdownText=function(text){'use strict';return String(text).replace(/([\\[\]])/g,'\\$1');};/**
 * CommonMark link/image title processing: resolve character references, then
 * HTML-escape the significant characters so the title attribute is valid HTML.
 * @param {string} title
 * @returns {string}
 */showdown.helper.cmEscapeTitle=function(title){return showdown.helper.cmDecodeEntities(title).replace(/&(?![a-zA-Z#0-9]+;)/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;');};/**
 * Normalize a CommonMark link label for reference matching: resolve backslash
 * escapes (both raw `\x` and showdown's `¨E<code>E` placeholders), collapse
 * internal whitespace runs to a single space, trim and case-fold. The same
 * normalization must be applied to both the definition label and the use label
 * so that they compare equal.
 * @param {string} label
 * @returns {string}
 */showdown.helper.cmNormalizeLabel=function(label){// CommonMark matches labels by case-fold + whitespace collapse only; backslash
// escapes are NOT resolved (so `[foo\!]` does not match a `[foo!]` definition).
return showdown.helper.caseFold(label.replace(/¨E(\d+)E/g,function(wholeMatch,code){return String.fromCharCode(parseInt(code,10));}).replace(/\s+/g,' ').trim());};/**
 * Unicode case folding for case-insensitive matching of link reference labels.
 * Uses `toLowerCase().toUpperCase()` (the round-trip used by commonmark.js) so that
 * characters like `ß`, `ẞ` and `SS` all fold together - which plain `toLowerCase`
 * does not (`ẞ` -> `ß`, not `ss`).
 * @param {string} str
 * @returns {string}
 */showdown.helper.caseFold=function(str){return str.toLowerCase().toUpperCase();};/**
 * Scan a CommonMark link destination starting at index `j`. Handles both
 * `<...>` destinations (no raw newline or unescaped `<`) and bare destinations
 * with balanced parentheses. Returns `{url, end, angle}` (url may be empty for
 * `<>`) or `null` when the destination is malformed.
 * @param {string} str
 * @param {number} j
 * @returns {{url: string, end: number, angle: boolean}|null}
 */showdown.helper.cmScanDestination=function(str,j){var n=str.length;if(str.charAt(j)==='<'){j++;var _buf='';while(j<n&&str.charAt(j)!=='>'){var c=str.charAt(j);if(c==='\n'||c==='<'){return null;}if(c==='\\'&&j+1<n){_buf+=c+str.charAt(j+1);j+=2;continue;}_buf+=c;j++;}if(j>=n||str.charAt(j)!=='>'){return null;}return{url:_buf,end:j+1,angle:true};}var depth=0,buf='';while(j<n){var _c=str.charAt(j),code=str.charCodeAt(j);if(_c==='\\'&&j+1<n){buf+=_c+str.charAt(j+1);j+=2;continue;}if(_c===' '||_c==='\t'||_c==='\n'){break;}if(code<0x20||code===0x7f){break;}if(_c==='('){depth++;buf+=_c;j++;continue;}if(_c===')'){if(depth===0){break;}depth--;buf+=_c;j++;continue;}buf+=_c;j++;}if(depth!==0){return null;}return{url:buf,end:j,angle:false};};/**
 * Scan a CommonMark link title starting at index `j` (which must point at the
 * opening delimiter `"`, `'` or `(`). The title may span multiple lines but not
 * contain a blank line. Returns `{title, end}` or `null` if malformed.
 * @param {string} str
 * @param {number} j
 * @returns {{title: string, end: number}|null}
 */showdown.helper.cmScanTitle=function(str,j){var n=str.length,open=str.charAt(j),close=open==='('?')':open;if(open!=='"'&&open!=='\''&&open!=='('){return null;}j++;var buf='';while(j<n){var c=str.charAt(j);if(c==='\\'&&j+1<n){buf+=c+str.charAt(j+1);j+=2;continue;}if(open==='('&&c==='('){return null;}if(c===close){if(/\n[ \t]*\n/.test(buf)){return null;}return{title:buf,end:j+1};}buf+=c;j++;}return null;};/**
 * Clones an object . If the second parameter is true, it deep clones the object.
 * Note: It should not be used in other contexts than showdown, since this algorithm might fail for
 * cyclic references, and dataypes such as Dates, RegExps, Typed Arrays, etc...
 * @param {{}} obj Object to clone
 * @param {boolean} [deep] [optional] If it should deep clone the object. Default is false
 */showdown.helper.cloneObject=function(obj,deep){deep=!!deep;if(obj===null||_typeof(obj)!=='object'){return obj;}if(obj instanceof Date){return new Date(obj);}if(!deep){var newObj={};for(var key in obj){if(Object.prototype.hasOwnProperty.call(obj,key)){newObj[key]=obj[key];}}return newObj;}if(typeof structuredClone==='function'){return structuredClone(obj);}else{// note: This is not a real deep clone, and might work in weird ways if used in a dif
//this is costly and should be used sparsly
return JSON.parse(JSON.stringify(obj));}};/**
 * Populate attributes in output text
 * @param {{}} attributes
 * @returns {string}
 */showdown.helper._populateAttributes=function(attributes){var text='';if(!attributes||!showdown.helper.isObject(attributes)){return text;}for(var attr in attributes){if(Object.prototype.hasOwnProperty.call(attributes,attr)){var key=attr,val=void 0;// since class is a javascript reserved word we use classes
if(attr==='classes'){key='class';}if(attributes[attr]===null||showdown.helper.isUndefined(attributes[attr])){val=null;}else if(showdown.helper.isArray(attributes[attr])){val=attributes[attr].join(' ');if(val===''){val=null;}}else if(showdown.helper.isString(attributes[attr])){val=attributes[attr];}else if(showdown.helper.isNumber(attributes[attr])){val=String(attributes[attr]);}else{throw new TypeError('Attribute "'+attr+'" must be either an array or string but '+_typeof(attributes[attr])+' given');}// special attributes
switch(attr){// these attributes don't expect a value. If they are false, they should be removed. If they are true,
// they should be present but without any value
case'checked':case'disabled':if(val===true||val==='true'||val===attr){text+=' '+key;}// if falsy, they are just ignored
break;// default behavior for all other attributes types
default:// Encode any literal double-quote so an attribute value cannot break out of
// the quoted attribute. Core callers already pre-escape their values (so this
// is a no-op for them); this closes the hole for values injected via an
// extension/listener through the setAttributes() event API.
text+=val===null?'':' '+key+'="'+String(val).replace(/"/g,'&quot;')+'"';break;}}}return text;};showdown.helper.normalizeLeadingTabs=function(text){// 1. (1 to 3 spaces followed by a tab at the start of the line) becomes (1 tab)
text=text.replace(/^ {1,3}\t/gm,'\t');// 2.
return text;};/**
 * CommonMark tab expansion: tabs are expanded to spaces using 4-column tab stops,
 * but only in the part of a line that defines block structure - the leading
 * whitespace plus an optional single list/block-quote marker and the whitespace
 * after it. Tabs in content (after that prefix) are preserved. Lines without a tab
 * in their prefix are returned unchanged.
 * @param {string} text
 * @returns {string}
 */showdown.helper.expandCmTabs=function(text){if(text.indexOf('\t')===-1){return text;}var prefixRgx=/^[ \t]*(?:(?:[-+*]|\d{1,9}[.)]|>)[ \t]*)?/;var thematicBreakRgx=/^ {0,3}([-*_])(?:[ \t]*\1){2,}[ \t]*$/;return text.split('\n').map(function(line){// a thematic break (e.g. `*\t*\t*`) is not a list item: do not let the marker
// branch of the prefix expansion mangle it
if(thematicBreakRgx.test(line)){return line;}var prefix=prefixRgx.exec(line)[0];if(prefix.indexOf('\t')===-1){return line;}var out='',col=0;for(var k=0;k<prefix.length;++k){var ch=prefix.charAt(k);if(ch==='\t'){var adv=4-col%4;out+=new Array(adv+1).join(' ');col+=adv;}else{out+=ch;col++;}}return out+line.slice(prefix.length);}).join('\n');};/**
 * Remove one level of line-leading tabs or spaces
 * @param {string} text
 * @returns {string}
 */showdown.helper.outdent=function(text){'use strict';if(!showdown.helper.isString(text)){return text;}return text.replace(/^(\t| {1,4})/gm,'');};/**
 * Validate options
 * @param {{}} options
 * @returns {{}}
 */showdown.helper.validateOptions=function(options){if(!showdown.helper.isObject(options)){throw new TypeError('Options must be an object, but '+_typeof(options)+' given');}var defaultOptions=getDefaultOpts(false);for(var opt in defaultOptions){if(!Object.prototype.hasOwnProperty.call(defaultOptions,opt)){continue;}if(!Object.prototype.hasOwnProperty.call(options,opt)){options[opt]=defaultOptions[opt].defaultValue;}switch(opt){case'headerIds':// accepts `false` (or any boolean) or a plain object {prefix, raw}
if(typeof options[opt]!=='boolean'&&!showdown.helper.isObject(options[opt])){throw new TypeError('Option headerIds must be `false` or an object but '+_typeof(options[opt])+' given');}break;default:if(_typeof(options[opt])!==defaultOptions[opt].type){throw new TypeError('Option '+opt+' must be of type '+defaultOptions[opt].type+' but '+_typeof(options[opt])+' given');}}}//options.headerLevelStart = (isNaN(parseInt(options.headerLevelStart))) ? 1 : parseInt(options.headerLevelStart);
return options;};/**
 * POLYFILLS
 */// use this instead of builtin is undefined for IE8 compatibility
if(typeof console==='undefined'){// noinspection JSValidateTypes
// eslint-disable-next-line no-global-assign -- deliberate polyfill for environments without console
console={warn:function warn(msg){'use strict';alert(msg);},log:function log(msg){'use strict';alert(msg);},error:function error(msg){'use strict';throw msg;}};}// Math.imul() polyfill
// https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Math/imul
if(!Math.imul){Math.imul=function(opA,opB){opB|=0;// ensure that opB is an integer. opA will automatically be coerced.
// floating points give us 53 bits of precision to work with plus 1 sign bit
// automatically handled for our convienence:
// 1. 0x003fffff /*opA & 0x000fffff*/ * 0x7fffffff /*opB*/ = 0x1fffff7fc00001
//    0x1fffff7fc00001 < Number.MAX_SAFE_INTEGER /*0x1fffffffffffff*/
var result=(opA&0x003fffff)*opB;// 2. We can remove an integer coersion from the statement above because:
//    0x1fffff7fc00001 + 0xffc00000 = 0x1fffffff800001
//    0x1fffffff800001 < Number.MAX_SAFE_INTEGER /*0x1fffffffffffff*/
if(opA&0xffc00000/*!== 0*/){result+=(opA&0xffc00000)*opB|0;}return result|0;};}/**
 * Common regexes.
 * We declare some common regexes to improve performance
 */// CommonMark inline raw-HTML grammar (mirrors commonmark.js' HTMLTAG): an open tag,
// close tag, HTML comment, processing instruction, declaration or CDATA section.
var cmAttributeName='[a-zA-Z_:][a-zA-Z0-9:._-]*',cmAttributeValue='(?:[^"\'=<>`\\x00-\\x20]+|\'[^\']*\'|"[^"]*")',cmAttribute='(?:\\s+'+cmAttributeName+'(?:\\s*=\\s*'+cmAttributeValue+')?)',cmOpenTag='<[A-Za-z][A-Za-z0-9\\-]*'+cmAttribute+'*\\s*/?>',cmCloseTag='</[A-Za-z][A-Za-z0-9\\-]*\\s*>',cmHTMLComment='<!-->|<!--->|<!--(?:[^-]|-[^-]|--[^>])*-->',cmProcessingInstruction='<[?][\\s\\S]*?[?]>',cmDeclaration='<![A-Za-z]+[^>]*>',cmCDATA='<!\\[CDATA\\[[\\s\\S]*?\\]\\]>';showdown.helper.regexes={asteriskDashTildeAndColon:/([*_:~])/g,asteriskDashAndTilde:/([*_~])/g,// Source string (not a RegExp) for the CommonMark inline raw-HTML grammar.
// hashHTMLSpans builds a *fresh* global RegExp from this per call, because that
// subparser runs re-entrantly and a shared stateful /g regex would corrupt its
// lastIndex between nested invocations.
cmHTMLTagSource:'(?:'+cmOpenTag+'|'+cmCloseTag+'|'+cmHTMLComment+'|'+cmProcessingInstruction+'|'+cmDeclaration+'|'+cmCDATA+')',// Open and close tag sources on their own, used for the CommonMark "type 7" HTML
// block start condition (a complete tag that fills the line).
cmOpenTagSource:cmOpenTag,cmCloseTagSource:cmCloseTag};/**
 * EMOJIS LIST
 */showdown.helper.emojis={'100':"\uD83D\uDCAF",'1234':"\uD83D\uDD22",'+1':"\uD83D\uDC4D",'-1':"\uD83D\uDC4E",'1st_place_medal':"\uD83E\uDD47",'2nd_place_medal':"\uD83E\uDD48",'3rd_place_medal':"\uD83E\uDD49",'8ball':"\uD83C\uDFB1",'a':"\uD83C\uDD70\uFE0F",'ab':"\uD83C\uDD8E",'abacus':"\uD83E\uDDEE",'abc':"\uD83D\uDD24",'abcd':"\uD83D\uDD21",'accept':"\uD83C\uDE51",'adhesive_bandage':"\uD83E\uDE79",'adult':"\uD83E\uDDD1",'aerial_tramway':"\uD83D\uDEA1",'afghanistan':"\uD83C\uDDE6\uD83C\uDDEB",'airplane':"\u2708\uFE0F",'aland_islands':"\uD83C\uDDE6\uD83C\uDDFD",'alarm_clock':"\u23F0",'albania':"\uD83C\uDDE6\uD83C\uDDF1",'alembic':"\u2697\uFE0F",'algeria':"\uD83C\uDDE9\uD83C\uDDFF",'alien':"\uD83D\uDC7D",'ambulance':"\uD83D\uDE91",'american_samoa':"\uD83C\uDDE6\uD83C\uDDF8",'amphora':"\uD83C\uDFFA",'anchor':"\u2693",'andorra':"\uD83C\uDDE6\uD83C\uDDE9",'angel':"\uD83D\uDC7C",'anger':"\uD83D\uDCA2",'angola':"\uD83C\uDDE6\uD83C\uDDF4",'angry':"\uD83D\uDE20",'anguilla':"\uD83C\uDDE6\uD83C\uDDEE",'anguished':"\uD83D\uDE27",'ant':"\uD83D\uDC1C",'antarctica':"\uD83C\uDDE6\uD83C\uDDF6",'antigua_barbuda':"\uD83C\uDDE6\uD83C\uDDEC",'apple':"\uD83C\uDF4E",'aquarius':"\u2652",'argentina':"\uD83C\uDDE6\uD83C\uDDF7",'aries':"\u2648",'armenia':"\uD83C\uDDE6\uD83C\uDDF2",'arrow_backward':"\u25C0\uFE0F",'arrow_double_down':"\u23EC",'arrow_double_up':"\u23EB",'arrow_down':"\u2B07\uFE0F",'arrow_down_small':"\uD83D\uDD3D",'arrow_forward':"\u25B6\uFE0F",'arrow_heading_down':"\u2935\uFE0F",'arrow_heading_up':"\u2934\uFE0F",'arrow_left':"\u2B05\uFE0F",'arrow_lower_left':"\u2199\uFE0F",'arrow_lower_right':"\u2198\uFE0F",'arrow_right':"\u27A1\uFE0F",'arrow_right_hook':"\u21AA\uFE0F",'arrow_up':"\u2B06\uFE0F",'arrow_up_down':"\u2195\uFE0F",'arrow_up_small':"\uD83D\uDD3C",'arrow_upper_left':"\u2196\uFE0F",'arrow_upper_right':"\u2197\uFE0F",'arrows_clockwise':"\uD83D\uDD03",'arrows_counterclockwise':"\uD83D\uDD04",'art':"\uD83C\uDFA8",'articulated_lorry':"\uD83D\uDE9B",'artificial_satellite':"\uD83D\uDEF0\uFE0F",'artist':"\uD83E\uDDD1\u200D\uD83C\uDFA8",'aruba':"\uD83C\uDDE6\uD83C\uDDFC",'ascension_island':"\uD83C\uDDE6\uD83C\uDDE8",'asterisk':"*\uFE0F\u20E3",'astonished':"\uD83D\uDE32",'astronaut':"\uD83E\uDDD1\u200D\uD83D\uDE80",'athletic_shoe':"\uD83D\uDC5F",'atm':"\uD83C\uDFE7",'atom_symbol':"\u269B\uFE0F",'australia':"\uD83C\uDDE6\uD83C\uDDFA",'austria':"\uD83C\uDDE6\uD83C\uDDF9",'auto_rickshaw':"\uD83D\uDEFA",'avocado':"\uD83E\uDD51",'axe':"\uD83E\uDE93",'azerbaijan':"\uD83C\uDDE6\uD83C\uDDFF",'b':"\uD83C\uDD71\uFE0F",'baby':"\uD83D\uDC76",'baby_bottle':"\uD83C\uDF7C",'baby_chick':"\uD83D\uDC24",'baby_symbol':"\uD83D\uDEBC",'back':"\uD83D\uDD19",'bacon':"\uD83E\uDD53",'badger':"\uD83E\uDDA1",'badminton':"\uD83C\uDFF8",'bagel':"\uD83E\uDD6F",'baggage_claim':"\uD83D\uDEC4",'baguette_bread':"\uD83E\uDD56",'bahamas':"\uD83C\uDDE7\uD83C\uDDF8",'bahrain':"\uD83C\uDDE7\uD83C\uDDED",'balance_scale':"\u2696\uFE0F",'bald_man':"\uD83D\uDC68\u200D\uD83E\uDDB2",'bald_woman':"\uD83D\uDC69\u200D\uD83E\uDDB2",'ballet_shoes':"\uD83E\uDE70",'balloon':"\uD83C\uDF88",'ballot_box':"\uD83D\uDDF3\uFE0F",'ballot_box_with_check':"\u2611\uFE0F",'bamboo':"\uD83C\uDF8D",'banana':"\uD83C\uDF4C",'bangbang':"\u203C\uFE0F",'bangladesh':"\uD83C\uDDE7\uD83C\uDDE9",'banjo':"\uD83E\uDE95",'bank':"\uD83C\uDFE6",'bar_chart':"\uD83D\uDCCA",'barbados':"\uD83C\uDDE7\uD83C\uDDE7",'barber':"\uD83D\uDC88",'baseball':"\u26BE",'basket':"\uD83E\uDDFA",'basketball':"\uD83C\uDFC0",'basketball_man':"\u26F9\uFE0F\u200D\u2642\uFE0F",'basketball_woman':"\u26F9\uFE0F\u200D\u2640\uFE0F",'bat':"\uD83E\uDD87",'bath':"\uD83D\uDEC0",'bathtub':"\uD83D\uDEC1",'battery':"\uD83D\uDD0B",'beach_umbrella':"\uD83C\uDFD6\uFE0F",'bear':"\uD83D\uDC3B",'bearded_person':"\uD83E\uDDD4",'bed':"\uD83D\uDECF\uFE0F",'bee':"\uD83D\uDC1D",'beer':"\uD83C\uDF7A",'beers':"\uD83C\uDF7B",'beetle':"\uD83D\uDC1E",'beginner':"\uD83D\uDD30",'belarus':"\uD83C\uDDE7\uD83C\uDDFE",'belgium':"\uD83C\uDDE7\uD83C\uDDEA",'belize':"\uD83C\uDDE7\uD83C\uDDFF",'bell':"\uD83D\uDD14",'bellhop_bell':"\uD83D\uDECE\uFE0F",'benin':"\uD83C\uDDE7\uD83C\uDDEF",'bento':"\uD83C\uDF71",'bermuda':"\uD83C\uDDE7\uD83C\uDDF2",'beverage_box':"\uD83E\uDDC3",'bhutan':"\uD83C\uDDE7\uD83C\uDDF9",'bicyclist':"\uD83D\uDEB4",'bike':"\uD83D\uDEB2",'biking_man':"\uD83D\uDEB4\u200D\u2642\uFE0F",'biking_woman':"\uD83D\uDEB4\u200D\u2640\uFE0F",'bikini':"\uD83D\uDC59",'billed_cap':"\uD83E\uDDE2",'biohazard':"\u2623\uFE0F",'bird':"\uD83D\uDC26",'birthday':"\uD83C\uDF82",'black_circle':"\u26AB",'black_flag':"\uD83C\uDFF4",'black_heart':"\uD83D\uDDA4",'black_joker':"\uD83C\uDCCF",'black_large_square':"\u2B1B",'black_medium_small_square':"\u25FE",'black_medium_square':"\u25FC\uFE0F",'black_nib':"\u2712\uFE0F",'black_small_square':"\u25AA\uFE0F",'black_square_button':"\uD83D\uDD32",'blond_haired_man':"\uD83D\uDC71\u200D\u2642\uFE0F",'blond_haired_person':"\uD83D\uDC71",'blond_haired_woman':"\uD83D\uDC71\u200D\u2640\uFE0F",'blonde_woman':"\uD83D\uDC71\u200D\u2640\uFE0F",'blossom':"\uD83C\uDF3C",'blowfish':"\uD83D\uDC21",'blue_book':"\uD83D\uDCD8",'blue_car':"\uD83D\uDE99",'blue_heart':"\uD83D\uDC99",'blue_square':"\uD83D\uDFE6",'blush':"\uD83D\uDE0A",'boar':"\uD83D\uDC17",'boat':"\u26F5",'bolivia':"\uD83C\uDDE7\uD83C\uDDF4",'bomb':"\uD83D\uDCA3",'bone':"\uD83E\uDDB4",'book':"\uD83D\uDCD6",'bookmark':"\uD83D\uDD16",'bookmark_tabs':"\uD83D\uDCD1",'books':"\uD83D\uDCDA",'boom':"\uD83D\uDCA5",'boot':"\uD83D\uDC62",'bosnia_herzegovina':"\uD83C\uDDE7\uD83C\uDDE6",'botswana':"\uD83C\uDDE7\uD83C\uDDFC",'bouncing_ball_man':"\u26F9\uFE0F\u200D\u2642\uFE0F",'bouncing_ball_person':"\u26F9\uFE0F",'bouncing_ball_woman':"\u26F9\uFE0F\u200D\u2640\uFE0F",'bouquet':"\uD83D\uDC90",'bouvet_island':"\uD83C\uDDE7\uD83C\uDDFB",'bow':"\uD83D\uDE47",'bow_and_arrow':"\uD83C\uDFF9",'bowing_man':"\uD83D\uDE47\u200D\u2642\uFE0F",'bowing_woman':"\uD83D\uDE47\u200D\u2640\uFE0F",'bowl_with_spoon':"\uD83E\uDD63",'bowling':"\uD83C\uDFB3",'boxing_glove':"\uD83E\uDD4A",'boy':"\uD83D\uDC66",'brain':"\uD83E\uDDE0",'brazil':"\uD83C\uDDE7\uD83C\uDDF7",'bread':"\uD83C\uDF5E",'breast_feeding':"\uD83E\uDD31",'bricks':"\uD83E\uDDF1",'bride_with_veil':"\uD83D\uDC70",'bridge_at_night':"\uD83C\uDF09",'briefcase':"\uD83D\uDCBC",'british_indian_ocean_territory':"\uD83C\uDDEE\uD83C\uDDF4",'british_virgin_islands':"\uD83C\uDDFB\uD83C\uDDEC",'broccoli':"\uD83E\uDD66",'broken_heart':"\uD83D\uDC94",'broom':"\uD83E\uDDF9",'brown_circle':"\uD83D\uDFE4",'brown_heart':"\uD83E\uDD0E",'brown_square':"\uD83D\uDFEB",'brunei':"\uD83C\uDDE7\uD83C\uDDF3",'bug':"\uD83D\uDC1B",'building_construction':"\uD83C\uDFD7\uFE0F",'bulb':"\uD83D\uDCA1",'bulgaria':"\uD83C\uDDE7\uD83C\uDDEC",'bullettrain_front':"\uD83D\uDE85",'bullettrain_side':"\uD83D\uDE84",'burkina_faso':"\uD83C\uDDE7\uD83C\uDDEB",'burrito':"\uD83C\uDF2F",'burundi':"\uD83C\uDDE7\uD83C\uDDEE",'bus':"\uD83D\uDE8C",'business_suit_levitating':"\uD83D\uDD74\uFE0F",'busstop':"\uD83D\uDE8F",'bust_in_silhouette':"\uD83D\uDC64",'busts_in_silhouette':"\uD83D\uDC65",'butter':"\uD83E\uDDC8",'butterfly':"\uD83E\uDD8B",'cactus':"\uD83C\uDF35",'cake':"\uD83C\uDF70",'calendar':"\uD83D\uDCC6",'call_me_hand':"\uD83E\uDD19",'calling':"\uD83D\uDCF2",'cambodia':"\uD83C\uDDF0\uD83C\uDDED",'camel':"\uD83D\uDC2B",'camera':"\uD83D\uDCF7",'camera_flash':"\uD83D\uDCF8",'cameroon':"\uD83C\uDDE8\uD83C\uDDF2",'camping':"\uD83C\uDFD5\uFE0F",'canada':"\uD83C\uDDE8\uD83C\uDDE6",'canary_islands':"\uD83C\uDDEE\uD83C\uDDE8",'cancer':"\u264B",'candle':"\uD83D\uDD6F\uFE0F",'candy':"\uD83C\uDF6C",'canned_food':"\uD83E\uDD6B",'canoe':"\uD83D\uDEF6",'cape_verde':"\uD83C\uDDE8\uD83C\uDDFB",'capital_abcd':"\uD83D\uDD20",'capricorn':"\u2651",'car':"\uD83D\uDE97",'card_file_box':"\uD83D\uDDC3\uFE0F",'card_index':"\uD83D\uDCC7",'card_index_dividers':"\uD83D\uDDC2\uFE0F",'caribbean_netherlands':"\uD83C\uDDE7\uD83C\uDDF6",'carousel_horse':"\uD83C\uDFA0",'carrot':"\uD83E\uDD55",'cartwheeling':"\uD83E\uDD38",'cat':"\uD83D\uDC31",'cat2':"\uD83D\uDC08",'cayman_islands':"\uD83C\uDDF0\uD83C\uDDFE",'cd':"\uD83D\uDCBF",'central_african_republic':"\uD83C\uDDE8\uD83C\uDDEB",'ceuta_melilla':"\uD83C\uDDEA\uD83C\uDDE6",'chad':"\uD83C\uDDF9\uD83C\uDDE9",'chains':"\u26D3\uFE0F",'chair':"\uD83E\uDE91",'champagne':"\uD83C\uDF7E",'chart':"\uD83D\uDCB9",'chart_with_downwards_trend':"\uD83D\uDCC9",'chart_with_upwards_trend':"\uD83D\uDCC8",'checkered_flag':"\uD83C\uDFC1",'cheese':"\uD83E\uDDC0",'cherries':"\uD83C\uDF52",'cherry_blossom':"\uD83C\uDF38",'chess_pawn':"\u265F\uFE0F",'chestnut':"\uD83C\uDF30",'chicken':"\uD83D\uDC14",'child':"\uD83E\uDDD2",'children_crossing':"\uD83D\uDEB8",'chile':"\uD83C\uDDE8\uD83C\uDDF1",'chipmunk':"\uD83D\uDC3F\uFE0F",'chocolate_bar':"\uD83C\uDF6B",'chopsticks':"\uD83E\uDD62",'christmas_island':"\uD83C\uDDE8\uD83C\uDDFD",'christmas_tree':"\uD83C\uDF84",'church':"\u26EA",'cinema':"\uD83C\uDFA6",'circus_tent':"\uD83C\uDFAA",'city_sunrise':"\uD83C\uDF07",'city_sunset':"\uD83C\uDF06",'cityscape':"\uD83C\uDFD9\uFE0F",'cl':"\uD83C\uDD91",'clamp':"\uD83D\uDDDC\uFE0F",'clap':"\uD83D\uDC4F",'clapper':"\uD83C\uDFAC",'classical_building':"\uD83C\uDFDB\uFE0F",'climbing':"\uD83E\uDDD7",'climbing_man':"\uD83E\uDDD7\u200D\u2642\uFE0F",'climbing_woman':"\uD83E\uDDD7\u200D\u2640\uFE0F",'clinking_glasses':"\uD83E\uDD42",'clipboard':"\uD83D\uDCCB",'clipperton_island':"\uD83C\uDDE8\uD83C\uDDF5",'clock1':"\uD83D\uDD50",'clock10':"\uD83D\uDD59",'clock1030':"\uD83D\uDD65",'clock11':"\uD83D\uDD5A",'clock1130':"\uD83D\uDD66",'clock12':"\uD83D\uDD5B",'clock1230':"\uD83D\uDD67",'clock130':"\uD83D\uDD5C",'clock2':"\uD83D\uDD51",'clock230':"\uD83D\uDD5D",'clock3':"\uD83D\uDD52",'clock330':"\uD83D\uDD5E",'clock4':"\uD83D\uDD53",'clock430':"\uD83D\uDD5F",'clock5':"\uD83D\uDD54",'clock530':"\uD83D\uDD60",'clock6':"\uD83D\uDD55",'clock630':"\uD83D\uDD61",'clock7':"\uD83D\uDD56",'clock730':"\uD83D\uDD62",'clock8':"\uD83D\uDD57",'clock830':"\uD83D\uDD63",'clock9':"\uD83D\uDD58",'clock930':"\uD83D\uDD64",'closed_book':"\uD83D\uDCD5",'closed_lock_with_key':"\uD83D\uDD10",'closed_umbrella':"\uD83C\uDF02",'cloud':"\u2601\uFE0F",'cloud_with_lightning':"\uD83C\uDF29\uFE0F",'cloud_with_lightning_and_rain':"\u26C8\uFE0F",'cloud_with_rain':"\uD83C\uDF27\uFE0F",'cloud_with_snow':"\uD83C\uDF28\uFE0F",'clown_face':"\uD83E\uDD21",'clubs':"\u2663\uFE0F",'cn':"\uD83C\uDDE8\uD83C\uDDF3",'coat':"\uD83E\uDDE5",'cocktail':"\uD83C\uDF78",'coconut':"\uD83E\uDD65",'cocos_islands':"\uD83C\uDDE8\uD83C\uDDE8",'coffee':"\u2615",'coffin':"\u26B0\uFE0F",'cold_face':"\uD83E\uDD76",'cold_sweat':"\uD83D\uDE30",'collision':"\uD83D\uDCA5",'colombia':"\uD83C\uDDE8\uD83C\uDDF4",'comet':"\u2604\uFE0F",'comoros':"\uD83C\uDDF0\uD83C\uDDF2",'compass':"\uD83E\uDDED",'computer':"\uD83D\uDCBB",'computer_mouse':"\uD83D\uDDB1\uFE0F",'confetti_ball':"\uD83C\uDF8A",'confounded':"\uD83D\uDE16",'confused':"\uD83D\uDE15",'congo_brazzaville':"\uD83C\uDDE8\uD83C\uDDEC",'congo_kinshasa':"\uD83C\uDDE8\uD83C\uDDE9",'congratulations':"\u3297\uFE0F",'construction':"\uD83D\uDEA7",'construction_worker':"\uD83D\uDC77",'construction_worker_man':"\uD83D\uDC77\u200D\u2642\uFE0F",'construction_worker_woman':"\uD83D\uDC77\u200D\u2640\uFE0F",'control_knobs':"\uD83C\uDF9B\uFE0F",'convenience_store':"\uD83C\uDFEA",'cook':"\uD83E\uDDD1\u200D\uD83C\uDF73",'cook_islands':"\uD83C\uDDE8\uD83C\uDDF0",'cookie':"\uD83C\uDF6A",'cool':"\uD83C\uDD92",'cop':"\uD83D\uDC6E",'copyright':"\xA9\uFE0F",'corn':"\uD83C\uDF3D",'costa_rica':"\uD83C\uDDE8\uD83C\uDDF7",'cote_divoire':"\uD83C\uDDE8\uD83C\uDDEE",'couch_and_lamp':"\uD83D\uDECB\uFE0F",'couple':"\uD83D\uDC6B",'couple_with_heart':"\uD83D\uDC91",'couple_with_heart_man_man':"\uD83D\uDC68\u200D\u2764\uFE0F\u200D\uD83D\uDC68",'couple_with_heart_woman_man':"\uD83D\uDC69\u200D\u2764\uFE0F\u200D\uD83D\uDC68",'couple_with_heart_woman_woman':"\uD83D\uDC69\u200D\u2764\uFE0F\u200D\uD83D\uDC69",'couplekiss':"\uD83D\uDC8F",'couplekiss_man_man':"\uD83D\uDC68\u200D\u2764\uFE0F\u200D\uD83D\uDC8B\u200D\uD83D\uDC68",'couplekiss_man_woman':"\uD83D\uDC69\u200D\u2764\uFE0F\u200D\uD83D\uDC8B\u200D\uD83D\uDC68",'couplekiss_woman_woman':"\uD83D\uDC69\u200D\u2764\uFE0F\u200D\uD83D\uDC8B\u200D\uD83D\uDC69",'cow':"\uD83D\uDC2E",'cow2':"\uD83D\uDC04",'cowboy_hat_face':"\uD83E\uDD20",'crab':"\uD83E\uDD80",'crayon':"\uD83D\uDD8D\uFE0F",'credit_card':"\uD83D\uDCB3",'crescent_moon':"\uD83C\uDF19",'cricket':"\uD83E\uDD97",'cricket_game':"\uD83C\uDFCF",'croatia':"\uD83C\uDDED\uD83C\uDDF7",'crocodile':"\uD83D\uDC0A",'croissant':"\uD83E\uDD50",'crossed_fingers':"\uD83E\uDD1E",'crossed_flags':"\uD83C\uDF8C",'crossed_swords':"\u2694\uFE0F",'crown':"\uD83D\uDC51",'cry':"\uD83D\uDE22",'crying_cat_face':"\uD83D\uDE3F",'crystal_ball':"\uD83D\uDD2E",'cuba':"\uD83C\uDDE8\uD83C\uDDFA",'cucumber':"\uD83E\uDD52",'cup_with_straw':"\uD83E\uDD64",'cupcake':"\uD83E\uDDC1",'cupid':"\uD83D\uDC98",'curacao':"\uD83C\uDDE8\uD83C\uDDFC",'curling_stone':"\uD83E\uDD4C",'curly_haired_man':"\uD83D\uDC68\u200D\uD83E\uDDB1",'curly_haired_woman':"\uD83D\uDC69\u200D\uD83E\uDDB1",'curly_loop':"\u27B0",'currency_exchange':"\uD83D\uDCB1",'curry':"\uD83C\uDF5B",'cursing_face':"\uD83E\uDD2C",'custard':"\uD83C\uDF6E",'customs':"\uD83D\uDEC3",'cut_of_meat':"\uD83E\uDD69",'cyclone':"\uD83C\uDF00",'cyprus':"\uD83C\uDDE8\uD83C\uDDFE",'czech_republic':"\uD83C\uDDE8\uD83C\uDDFF",'dagger':"\uD83D\uDDE1\uFE0F",'dancer':"\uD83D\uDC83",'dancers':"\uD83D\uDC6F",'dancing_men':"\uD83D\uDC6F\u200D\u2642\uFE0F",'dancing_women':"\uD83D\uDC6F\u200D\u2640\uFE0F",'dango':"\uD83C\uDF61",'dark_sunglasses':"\uD83D\uDD76\uFE0F",'dart':"\uD83C\uDFAF",'dash':"\uD83D\uDCA8",'date':"\uD83D\uDCC5",'de':"\uD83C\uDDE9\uD83C\uDDEA",'deaf_man':"\uD83E\uDDCF\u200D\u2642\uFE0F",'deaf_person':"\uD83E\uDDCF",'deaf_woman':"\uD83E\uDDCF\u200D\u2640\uFE0F",'deciduous_tree':"\uD83C\uDF33",'deer':"\uD83E\uDD8C",'denmark':"\uD83C\uDDE9\uD83C\uDDF0",'department_store':"\uD83C\uDFEC",'derelict_house':"\uD83C\uDFDA\uFE0F",'desert':"\uD83C\uDFDC\uFE0F",'desert_island':"\uD83C\uDFDD\uFE0F",'desktop_computer':"\uD83D\uDDA5\uFE0F",'detective':"\uD83D\uDD75\uFE0F",'diamond_shape_with_a_dot_inside':"\uD83D\uDCA0",'diamonds':"\u2666\uFE0F",'diego_garcia':"\uD83C\uDDE9\uD83C\uDDEC",'disappointed':"\uD83D\uDE1E",'disappointed_relieved':"\uD83D\uDE25",'diving_mask':"\uD83E\uDD3F",'diya_lamp':"\uD83E\uDE94",'dizzy':"\uD83D\uDCAB",'dizzy_face':"\uD83D\uDE35",'djibouti':"\uD83C\uDDE9\uD83C\uDDEF",'dna':"\uD83E\uDDEC",'do_not_litter':"\uD83D\uDEAF",'dog':"\uD83D\uDC36",'dog2':"\uD83D\uDC15",'dollar':"\uD83D\uDCB5",'dolls':"\uD83C\uDF8E",'dolphin':"\uD83D\uDC2C",'dominica':"\uD83C\uDDE9\uD83C\uDDF2",'dominican_republic':"\uD83C\uDDE9\uD83C\uDDF4",'door':"\uD83D\uDEAA",'doughnut':"\uD83C\uDF69",'dove':"\uD83D\uDD4A\uFE0F",'dragon':"\uD83D\uDC09",'dragon_face':"\uD83D\uDC32",'dress':"\uD83D\uDC57",'dromedary_camel':"\uD83D\uDC2A",'drooling_face':"\uD83E\uDD24",'drop_of_blood':"\uD83E\uDE78",'droplet':"\uD83D\uDCA7",'drum':"\uD83E\uDD41",'duck':"\uD83E\uDD86",'dumpling':"\uD83E\uDD5F",'dvd':"\uD83D\uDCC0",'e-mail':"\uD83D\uDCE7",'eagle':"\uD83E\uDD85",'ear':"\uD83D\uDC42",'ear_of_rice':"\uD83C\uDF3E",'ear_with_hearing_aid':"\uD83E\uDDBB",'earth_africa':"\uD83C\uDF0D",'earth_americas':"\uD83C\uDF0E",'earth_asia':"\uD83C\uDF0F",'ecuador':"\uD83C\uDDEA\uD83C\uDDE8",'egg':"\uD83E\uDD5A",'eggplant':"\uD83C\uDF46",'egypt':"\uD83C\uDDEA\uD83C\uDDEC",'eight':"8\uFE0F\u20E3",'eight_pointed_black_star':"\u2734\uFE0F",'eight_spoked_asterisk':"\u2733\uFE0F",'eject_button':"\u23CF\uFE0F",'el_salvador':"\uD83C\uDDF8\uD83C\uDDFB",'electric_plug':"\uD83D\uDD0C",'elephant':"\uD83D\uDC18",'elf':"\uD83E\uDDDD",'elf_man':"\uD83E\uDDDD\u200D\u2642\uFE0F",'elf_woman':"\uD83E\uDDDD\u200D\u2640\uFE0F",'email':"\u2709\uFE0F",'end':"\uD83D\uDD1A",'england':"\uD83C\uDFF4\uDB40\uDC67\uDB40\uDC62\uDB40\uDC65\uDB40\uDC6E\uDB40\uDC67\uDB40\uDC7F",'envelope':"\u2709\uFE0F",'envelope_with_arrow':"\uD83D\uDCE9",'equatorial_guinea':"\uD83C\uDDEC\uD83C\uDDF6",'eritrea':"\uD83C\uDDEA\uD83C\uDDF7",'es':"\uD83C\uDDEA\uD83C\uDDF8",'estonia':"\uD83C\uDDEA\uD83C\uDDEA",'ethiopia':"\uD83C\uDDEA\uD83C\uDDF9",'eu':"\uD83C\uDDEA\uD83C\uDDFA",'euro':"\uD83D\uDCB6",'european_castle':"\uD83C\uDFF0",'european_post_office':"\uD83C\uDFE4",'european_union':"\uD83C\uDDEA\uD83C\uDDFA",'evergreen_tree':"\uD83C\uDF32",'exclamation':"\u2757",'exploding_head':"\uD83E\uDD2F",'expressionless':"\uD83D\uDE11",'eye':"\uD83D\uDC41\uFE0F",'eye_speech_bubble':"\uD83D\uDC41\uFE0F\u200D\uD83D\uDDE8\uFE0F",'eyeglasses':"\uD83D\uDC53",'eyes':"\uD83D\uDC40",'face_with_head_bandage':"\uD83E\uDD15",'face_with_thermometer':"\uD83E\uDD12",'facepalm':"\uD83E\uDD26",'facepunch':"\uD83D\uDC4A",'factory':"\uD83C\uDFED",'factory_worker':"\uD83E\uDDD1\u200D\uD83C\uDFED",'fairy':"\uD83E\uDDDA",'fairy_man':"\uD83E\uDDDA\u200D\u2642\uFE0F",'fairy_woman':"\uD83E\uDDDA\u200D\u2640\uFE0F",'falafel':"\uD83E\uDDC6",'falkland_islands':"\uD83C\uDDEB\uD83C\uDDF0",'fallen_leaf':"\uD83C\uDF42",'family':"\uD83D\uDC6A",'family_man_boy':"\uD83D\uDC68\u200D\uD83D\uDC66",'family_man_boy_boy':"\uD83D\uDC68\u200D\uD83D\uDC66\u200D\uD83D\uDC66",'family_man_girl':"\uD83D\uDC68\u200D\uD83D\uDC67",'family_man_girl_boy':"\uD83D\uDC68\u200D\uD83D\uDC67\u200D\uD83D\uDC66",'family_man_girl_girl':"\uD83D\uDC68\u200D\uD83D\uDC67\u200D\uD83D\uDC67",'family_man_man_boy':"\uD83D\uDC68\u200D\uD83D\uDC68\u200D\uD83D\uDC66",'family_man_man_boy_boy':"\uD83D\uDC68\u200D\uD83D\uDC68\u200D\uD83D\uDC66\u200D\uD83D\uDC66",'family_man_man_girl':"\uD83D\uDC68\u200D\uD83D\uDC68\u200D\uD83D\uDC67",'family_man_man_girl_boy':"\uD83D\uDC68\u200D\uD83D\uDC68\u200D\uD83D\uDC67\u200D\uD83D\uDC66",'family_man_man_girl_girl':"\uD83D\uDC68\u200D\uD83D\uDC68\u200D\uD83D\uDC67\u200D\uD83D\uDC67",'family_man_woman_boy':"\uD83D\uDC68\u200D\uD83D\uDC69\u200D\uD83D\uDC66",'family_man_woman_boy_boy':"\uD83D\uDC68\u200D\uD83D\uDC69\u200D\uD83D\uDC66\u200D\uD83D\uDC66",'family_man_woman_girl':"\uD83D\uDC68\u200D\uD83D\uDC69\u200D\uD83D\uDC67",'family_man_woman_girl_boy':"\uD83D\uDC68\u200D\uD83D\uDC69\u200D\uD83D\uDC67\u200D\uD83D\uDC66",'family_man_woman_girl_girl':"\uD83D\uDC68\u200D\uD83D\uDC69\u200D\uD83D\uDC67\u200D\uD83D\uDC67",'family_woman_boy':"\uD83D\uDC69\u200D\uD83D\uDC66",'family_woman_boy_boy':"\uD83D\uDC69\u200D\uD83D\uDC66\u200D\uD83D\uDC66",'family_woman_girl':"\uD83D\uDC69\u200D\uD83D\uDC67",'family_woman_girl_boy':"\uD83D\uDC69\u200D\uD83D\uDC67\u200D\uD83D\uDC66",'family_woman_girl_girl':"\uD83D\uDC69\u200D\uD83D\uDC67\u200D\uD83D\uDC67",'family_woman_woman_boy':"\uD83D\uDC69\u200D\uD83D\uDC69\u200D\uD83D\uDC66",'family_woman_woman_boy_boy':"\uD83D\uDC69\u200D\uD83D\uDC69\u200D\uD83D\uDC66\u200D\uD83D\uDC66",'family_woman_woman_girl':"\uD83D\uDC69\u200D\uD83D\uDC69\u200D\uD83D\uDC67",'family_woman_woman_girl_boy':"\uD83D\uDC69\u200D\uD83D\uDC69\u200D\uD83D\uDC67\u200D\uD83D\uDC66",'family_woman_woman_girl_girl':"\uD83D\uDC69\u200D\uD83D\uDC69\u200D\uD83D\uDC67\u200D\uD83D\uDC67",'farmer':"\uD83E\uDDD1\u200D\uD83C\uDF3E",'faroe_islands':"\uD83C\uDDEB\uD83C\uDDF4",'fast_forward':"\u23E9",'fax':"\uD83D\uDCE0",'fearful':"\uD83D\uDE28",'feet':"\uD83D\uDC3E",'female_detective':"\uD83D\uDD75\uFE0F\u200D\u2640\uFE0F",'female_sign':"\u2640\uFE0F",'ferris_wheel':"\uD83C\uDFA1",'ferry':"\u26F4\uFE0F",'field_hockey':"\uD83C\uDFD1",'fiji':"\uD83C\uDDEB\uD83C\uDDEF",'file_cabinet':"\uD83D\uDDC4\uFE0F",'file_folder':"\uD83D\uDCC1",'film_projector':"\uD83D\uDCFD\uFE0F",'film_strip':"\uD83C\uDF9E\uFE0F",'finland':"\uD83C\uDDEB\uD83C\uDDEE",'fire':"\uD83D\uDD25",'fire_engine':"\uD83D\uDE92",'fire_extinguisher':"\uD83E\uDDEF",'firecracker':"\uD83E\uDDE8",'firefighter':"\uD83E\uDDD1\u200D\uD83D\uDE92",'fireworks':"\uD83C\uDF86",'first_quarter_moon':"\uD83C\uDF13",'first_quarter_moon_with_face':"\uD83C\uDF1B",'fish':"\uD83D\uDC1F",'fish_cake':"\uD83C\uDF65",'fishing_pole_and_fish':"\uD83C\uDFA3",'fist':"\u270A",'fist_left':"\uD83E\uDD1B",'fist_oncoming':"\uD83D\uDC4A",'fist_raised':"\u270A",'fist_right':"\uD83E\uDD1C",'five':"5\uFE0F\u20E3",'flags':"\uD83C\uDF8F",'flamingo':"\uD83E\uDDA9",'flashlight':"\uD83D\uDD26",'flat_shoe':"\uD83E\uDD7F",'fleur_de_lis':"\u269C\uFE0F",'flight_arrival':"\uD83D\uDEEC",'flight_departure':"\uD83D\uDEEB",'flipper':"\uD83D\uDC2C",'floppy_disk':"\uD83D\uDCBE",'flower_playing_cards':"\uD83C\uDFB4",'flushed':"\uD83D\uDE33",'flying_disc':"\uD83E\uDD4F",'flying_saucer':"\uD83D\uDEF8",'fog':"\uD83C\uDF2B\uFE0F",'foggy':"\uD83C\uDF01",'foot':"\uD83E\uDDB6",'football':"\uD83C\uDFC8",'footprints':"\uD83D\uDC63",'fork_and_knife':"\uD83C\uDF74",'fortune_cookie':"\uD83E\uDD60",'fountain':"\u26F2",'fountain_pen':"\uD83D\uDD8B\uFE0F",'four':"4\uFE0F\u20E3",'four_leaf_clover':"\uD83C\uDF40",'fox_face':"\uD83E\uDD8A",'fr':"\uD83C\uDDEB\uD83C\uDDF7",'framed_picture':"\uD83D\uDDBC\uFE0F",'free':"\uD83C\uDD93",'french_guiana':"\uD83C\uDDEC\uD83C\uDDEB",'french_polynesia':"\uD83C\uDDF5\uD83C\uDDEB",'french_southern_territories':"\uD83C\uDDF9\uD83C\uDDEB",'fried_egg':"\uD83C\uDF73",'fried_shrimp':"\uD83C\uDF64",'fries':"\uD83C\uDF5F",'frog':"\uD83D\uDC38",'frowning':"\uD83D\uDE26",'frowning_face':"\u2639\uFE0F",'frowning_man':"\uD83D\uDE4D\u200D\u2642\uFE0F",'frowning_person':"\uD83D\uDE4D",'frowning_woman':"\uD83D\uDE4D\u200D\u2640\uFE0F",'fu':"\uD83D\uDD95",'fuelpump':"\u26FD",'full_moon':"\uD83C\uDF15",'full_moon_with_face':"\uD83C\uDF1D",'funeral_urn':"\u26B1\uFE0F",'gabon':"\uD83C\uDDEC\uD83C\uDDE6",'gambia':"\uD83C\uDDEC\uD83C\uDDF2",'game_die':"\uD83C\uDFB2",'garlic':"\uD83E\uDDC4",'gb':"\uD83C\uDDEC\uD83C\uDDE7",'gear':"\u2699\uFE0F",'gem':"\uD83D\uDC8E",'gemini':"\u264A",'genie':"\uD83E\uDDDE",'genie_man':"\uD83E\uDDDE\u200D\u2642\uFE0F",'genie_woman':"\uD83E\uDDDE\u200D\u2640\uFE0F",'georgia':"\uD83C\uDDEC\uD83C\uDDEA",'ghana':"\uD83C\uDDEC\uD83C\uDDED",'ghost':"\uD83D\uDC7B",'gibraltar':"\uD83C\uDDEC\uD83C\uDDEE",'gift':"\uD83C\uDF81",'gift_heart':"\uD83D\uDC9D",'giraffe':"\uD83E\uDD92",'girl':"\uD83D\uDC67",'globe_with_meridians':"\uD83C\uDF10",'gloves':"\uD83E\uDDE4",'goal_net':"\uD83E\uDD45",'goat':"\uD83D\uDC10",'goggles':"\uD83E\uDD7D",'golf':"\u26F3",'golfing':"\uD83C\uDFCC\uFE0F",'golfing_man':"\uD83C\uDFCC\uFE0F\u200D\u2642\uFE0F",'golfing_woman':"\uD83C\uDFCC\uFE0F\u200D\u2640\uFE0F",'gorilla':"\uD83E\uDD8D",'grapes':"\uD83C\uDF47",'greece':"\uD83C\uDDEC\uD83C\uDDF7",'green_apple':"\uD83C\uDF4F",'green_book':"\uD83D\uDCD7",'green_circle':"\uD83D\uDFE2",'green_heart':"\uD83D\uDC9A",'green_salad':"\uD83E\uDD57",'green_square':"\uD83D\uDFE9",'greenland':"\uD83C\uDDEC\uD83C\uDDF1",'grenada':"\uD83C\uDDEC\uD83C\uDDE9",'grey_exclamation':"\u2755",'grey_question':"\u2754",'grimacing':"\uD83D\uDE2C",'grin':"\uD83D\uDE01",'grinning':"\uD83D\uDE00",'guadeloupe':"\uD83C\uDDEC\uD83C\uDDF5",'guam':"\uD83C\uDDEC\uD83C\uDDFA",'guard':"\uD83D\uDC82",'guardsman':"\uD83D\uDC82\u200D\u2642\uFE0F",'guardswoman':"\uD83D\uDC82\u200D\u2640\uFE0F",'guatemala':"\uD83C\uDDEC\uD83C\uDDF9",'guernsey':"\uD83C\uDDEC\uD83C\uDDEC",'guide_dog':"\uD83E\uDDAE",'guinea':"\uD83C\uDDEC\uD83C\uDDF3",'guinea_bissau':"\uD83C\uDDEC\uD83C\uDDFC",'guitar':"\uD83C\uDFB8",'gun':"\uD83D\uDD2B",'guyana':"\uD83C\uDDEC\uD83C\uDDFE",'haircut':"\uD83D\uDC87",'haircut_man':"\uD83D\uDC87\u200D\u2642\uFE0F",'haircut_woman':"\uD83D\uDC87\u200D\u2640\uFE0F",'haiti':"\uD83C\uDDED\uD83C\uDDF9",'hamburger':"\uD83C\uDF54",'hammer':"\uD83D\uDD28",'hammer_and_pick':"\u2692\uFE0F",'hammer_and_wrench':"\uD83D\uDEE0\uFE0F",'hamster':"\uD83D\uDC39",'hand':"\u270B",'hand_over_mouth':"\uD83E\uDD2D",'handbag':"\uD83D\uDC5C",'handball_person':"\uD83E\uDD3E",'handshake':"\uD83E\uDD1D",'hankey':"\uD83D\uDCA9",'hash':"#\uFE0F\u20E3",'hatched_chick':"\uD83D\uDC25",'hatching_chick':"\uD83D\uDC23",'headphones':"\uD83C\uDFA7",'health_worker':"\uD83E\uDDD1\u200D\u2695\uFE0F",'hear_no_evil':"\uD83D\uDE49",'heard_mcdonald_islands':"\uD83C\uDDED\uD83C\uDDF2",'heart':"\u2764\uFE0F",'heart_decoration':"\uD83D\uDC9F",'heart_eyes':"\uD83D\uDE0D",'heart_eyes_cat':"\uD83D\uDE3B",'heartbeat':"\uD83D\uDC93",'heartpulse':"\uD83D\uDC97",'hearts':"\u2665\uFE0F",'heavy_check_mark':"\u2714\uFE0F",'heavy_division_sign':"\u2797",'heavy_dollar_sign':"\uD83D\uDCB2",'heavy_exclamation_mark':"\u2757",'heavy_heart_exclamation':"\u2763\uFE0F",'heavy_minus_sign':"\u2796",'heavy_multiplication_x':"\u2716\uFE0F",'heavy_plus_sign':"\u2795",'hedgehog':"\uD83E\uDD94",'helicopter':"\uD83D\uDE81",'herb':"\uD83C\uDF3F",'hibiscus':"\uD83C\uDF3A",'high_brightness':"\uD83D\uDD06",'high_heel':"\uD83D\uDC60",'hiking_boot':"\uD83E\uDD7E",'hindu_temple':"\uD83D\uDED5",'hippopotamus':"\uD83E\uDD9B",'hocho':"\uD83D\uDD2A",'hole':"\uD83D\uDD73\uFE0F",'honduras':"\uD83C\uDDED\uD83C\uDDF3",'honey_pot':"\uD83C\uDF6F",'honeybee':"\uD83D\uDC1D",'hong_kong':"\uD83C\uDDED\uD83C\uDDF0",'horse':"\uD83D\uDC34",'horse_racing':"\uD83C\uDFC7",'hospital':"\uD83C\uDFE5",'hot_face':"\uD83E\uDD75",'hot_pepper':"\uD83C\uDF36\uFE0F",'hotdog':"\uD83C\uDF2D",'hotel':"\uD83C\uDFE8",'hotsprings':"\u2668\uFE0F",'hourglass':"\u231B",'hourglass_flowing_sand':"\u23F3",'house':"\uD83C\uDFE0",'house_with_garden':"\uD83C\uDFE1",'houses':"\uD83C\uDFD8\uFE0F",'hugs':"\uD83E\uDD17",'hungary':"\uD83C\uDDED\uD83C\uDDFA",'hushed':"\uD83D\uDE2F",'ice_cream':"\uD83C\uDF68",'ice_cube':"\uD83E\uDDCA",'ice_hockey':"\uD83C\uDFD2",'ice_skate':"\u26F8\uFE0F",'icecream':"\uD83C\uDF66",'iceland':"\uD83C\uDDEE\uD83C\uDDF8",'id':"\uD83C\uDD94",'ideograph_advantage':"\uD83C\uDE50",'imp':"\uD83D\uDC7F",'inbox_tray':"\uD83D\uDCE5",'incoming_envelope':"\uD83D\uDCE8",'india':"\uD83C\uDDEE\uD83C\uDDF3",'indonesia':"\uD83C\uDDEE\uD83C\uDDE9",'infinity':"\u267E\uFE0F",'information_desk_person':"\uD83D\uDC81",'information_source':"\u2139\uFE0F",'innocent':"\uD83D\uDE07",'interrobang':"\u2049\uFE0F",'iphone':"\uD83D\uDCF1",'iran':"\uD83C\uDDEE\uD83C\uDDF7",'iraq':"\uD83C\uDDEE\uD83C\uDDF6",'ireland':"\uD83C\uDDEE\uD83C\uDDEA",'isle_of_man':"\uD83C\uDDEE\uD83C\uDDF2",'israel':"\uD83C\uDDEE\uD83C\uDDF1",'it':"\uD83C\uDDEE\uD83C\uDDF9",'izakaya_lantern':"\uD83C\uDFEE",'jack_o_lantern':"\uD83C\uDF83",'jamaica':"\uD83C\uDDEF\uD83C\uDDF2",'japan':"\uD83D\uDDFE",'japanese_castle':"\uD83C\uDFEF",'japanese_goblin':"\uD83D\uDC7A",'japanese_ogre':"\uD83D\uDC79",'jeans':"\uD83D\uDC56",'jersey':"\uD83C\uDDEF\uD83C\uDDEA",'jigsaw':"\uD83E\uDDE9",'jordan':"\uD83C\uDDEF\uD83C\uDDF4",'joy':"\uD83D\uDE02",'joy_cat':"\uD83D\uDE39",'joystick':"\uD83D\uDD79\uFE0F",'jp':"\uD83C\uDDEF\uD83C\uDDF5",'judge':"\uD83E\uDDD1\u200D\u2696\uFE0F",'juggling_person':"\uD83E\uDD39",'kaaba':"\uD83D\uDD4B",'kangaroo':"\uD83E\uDD98",'kazakhstan':"\uD83C\uDDF0\uD83C\uDDFF",'kenya':"\uD83C\uDDF0\uD83C\uDDEA",'key':"\uD83D\uDD11",'keyboard':"\u2328\uFE0F",'keycap_ten':"\uD83D\uDD1F",'kick_scooter':"\uD83D\uDEF4",'kimono':"\uD83D\uDC58",'kiribati':"\uD83C\uDDF0\uD83C\uDDEE",'kiss':"\uD83D\uDC8B",'kissing':"\uD83D\uDE17",'kissing_cat':"\uD83D\uDE3D",'kissing_closed_eyes':"\uD83D\uDE1A",'kissing_heart':"\uD83D\uDE18",'kissing_smiling_eyes':"\uD83D\uDE19",'kite':"\uD83E\uDE81",'kiwi_fruit':"\uD83E\uDD5D",'kneeling_man':"\uD83E\uDDCE\u200D\u2642\uFE0F",'kneeling_person':"\uD83E\uDDCE",'kneeling_woman':"\uD83E\uDDCE\u200D\u2640\uFE0F",'knife':"\uD83D\uDD2A",'koala':"\uD83D\uDC28",'koko':"\uD83C\uDE01",'kosovo':"\uD83C\uDDFD\uD83C\uDDF0",'kr':"\uD83C\uDDF0\uD83C\uDDF7",'kuwait':"\uD83C\uDDF0\uD83C\uDDFC",'kyrgyzstan':"\uD83C\uDDF0\uD83C\uDDEC",'lab_coat':"\uD83E\uDD7C",'label':"\uD83C\uDFF7\uFE0F",'lacrosse':"\uD83E\uDD4D",'lantern':"\uD83C\uDFEE",'laos':"\uD83C\uDDF1\uD83C\uDDE6",'large_blue_circle':"\uD83D\uDD35",'large_blue_diamond':"\uD83D\uDD37",'large_orange_diamond':"\uD83D\uDD36",'last_quarter_moon':"\uD83C\uDF17",'last_quarter_moon_with_face':"\uD83C\uDF1C",'latin_cross':"\u271D\uFE0F",'latvia':"\uD83C\uDDF1\uD83C\uDDFB",'laughing':"\uD83D\uDE06",'leafy_green':"\uD83E\uDD6C",'leaves':"\uD83C\uDF43",'lebanon':"\uD83C\uDDF1\uD83C\uDDE7",'ledger':"\uD83D\uDCD2",'left_luggage':"\uD83D\uDEC5",'left_right_arrow':"\u2194\uFE0F",'left_speech_bubble':"\uD83D\uDDE8\uFE0F",'leftwards_arrow_with_hook':"\u21A9\uFE0F",'leg':"\uD83E\uDDB5",'lemon':"\uD83C\uDF4B",'leo':"\u264C",'leopard':"\uD83D\uDC06",'lesotho':"\uD83C\uDDF1\uD83C\uDDF8",'level_slider':"\uD83C\uDF9A\uFE0F",'liberia':"\uD83C\uDDF1\uD83C\uDDF7",'libra':"\u264E",'libya':"\uD83C\uDDF1\uD83C\uDDFE",'liechtenstein':"\uD83C\uDDF1\uD83C\uDDEE",'light_rail':"\uD83D\uDE88",'link':"\uD83D\uDD17",'lion':"\uD83E\uDD81",'lips':"\uD83D\uDC44",'lipstick':"\uD83D\uDC84",'lithuania':"\uD83C\uDDF1\uD83C\uDDF9",'lizard':"\uD83E\uDD8E",'llama':"\uD83E\uDD99",'lobster':"\uD83E\uDD9E",'lock':"\uD83D\uDD12",'lock_with_ink_pen':"\uD83D\uDD0F",'lollipop':"\uD83C\uDF6D",'loop':"\u27BF",'lotion_bottle':"\uD83E\uDDF4",'lotus_position':"\uD83E\uDDD8",'lotus_position_man':"\uD83E\uDDD8\u200D\u2642\uFE0F",'lotus_position_woman':"\uD83E\uDDD8\u200D\u2640\uFE0F",'loud_sound':"\uD83D\uDD0A",'loudspeaker':"\uD83D\uDCE2",'love_hotel':"\uD83C\uDFE9",'love_letter':"\uD83D\uDC8C",'love_you_gesture':"\uD83E\uDD1F",'low_brightness':"\uD83D\uDD05",'luggage':"\uD83E\uDDF3",'luxembourg':"\uD83C\uDDF1\uD83C\uDDFA",'lying_face':"\uD83E\uDD25",'m':"\u24C2\uFE0F",'macau':"\uD83C\uDDF2\uD83C\uDDF4",'macedonia':"\uD83C\uDDF2\uD83C\uDDF0",'madagascar':"\uD83C\uDDF2\uD83C\uDDEC",'mag':"\uD83D\uDD0D",'mag_right':"\uD83D\uDD0E",'mage':"\uD83E\uDDD9",'mage_man':"\uD83E\uDDD9\u200D\u2642\uFE0F",'mage_woman':"\uD83E\uDDD9\u200D\u2640\uFE0F",'magnet':"\uD83E\uDDF2",'mahjong':"\uD83C\uDC04",'mailbox':"\uD83D\uDCEB",'mailbox_closed':"\uD83D\uDCEA",'mailbox_with_mail':"\uD83D\uDCEC",'mailbox_with_no_mail':"\uD83D\uDCED",'malawi':"\uD83C\uDDF2\uD83C\uDDFC",'malaysia':"\uD83C\uDDF2\uD83C\uDDFE",'maldives':"\uD83C\uDDF2\uD83C\uDDFB",'male_detective':"\uD83D\uDD75\uFE0F\u200D\u2642\uFE0F",'male_sign':"\u2642\uFE0F",'mali':"\uD83C\uDDF2\uD83C\uDDF1",'malta':"\uD83C\uDDF2\uD83C\uDDF9",'man':"\uD83D\uDC68",'man_artist':"\uD83D\uDC68\u200D\uD83C\uDFA8",'man_astronaut':"\uD83D\uDC68\u200D\uD83D\uDE80",'man_cartwheeling':"\uD83E\uDD38\u200D\u2642\uFE0F",'man_cook':"\uD83D\uDC68\u200D\uD83C\uDF73",'man_dancing':"\uD83D\uDD7A",'man_facepalming':"\uD83E\uDD26\u200D\u2642\uFE0F",'man_factory_worker':"\uD83D\uDC68\u200D\uD83C\uDFED",'man_farmer':"\uD83D\uDC68\u200D\uD83C\uDF3E",'man_firefighter':"\uD83D\uDC68\u200D\uD83D\uDE92",'man_health_worker':"\uD83D\uDC68\u200D\u2695\uFE0F",'man_in_manual_wheelchair':"\uD83D\uDC68\u200D\uD83E\uDDBD",'man_in_motorized_wheelchair':"\uD83D\uDC68\u200D\uD83E\uDDBC",'man_in_tuxedo':"\uD83E\uDD35",'man_judge':"\uD83D\uDC68\u200D\u2696\uFE0F",'man_juggling':"\uD83E\uDD39\u200D\u2642\uFE0F",'man_mechanic':"\uD83D\uDC68\u200D\uD83D\uDD27",'man_office_worker':"\uD83D\uDC68\u200D\uD83D\uDCBC",'man_pilot':"\uD83D\uDC68\u200D\u2708\uFE0F",'man_playing_handball':"\uD83E\uDD3E\u200D\u2642\uFE0F",'man_playing_water_polo':"\uD83E\uDD3D\u200D\u2642\uFE0F",'man_scientist':"\uD83D\uDC68\u200D\uD83D\uDD2C",'man_shrugging':"\uD83E\uDD37\u200D\u2642\uFE0F",'man_singer':"\uD83D\uDC68\u200D\uD83C\uDFA4",'man_student':"\uD83D\uDC68\u200D\uD83C\uDF93",'man_teacher':"\uD83D\uDC68\u200D\uD83C\uDFEB",'man_technologist':"\uD83D\uDC68\u200D\uD83D\uDCBB",'man_with_gua_pi_mao':"\uD83D\uDC72",'man_with_probing_cane':"\uD83D\uDC68\u200D\uD83E\uDDAF",'man_with_turban':"\uD83D\uDC73\u200D\u2642\uFE0F",'mandarin':"\uD83C\uDF4A",'mango':"\uD83E\uDD6D",'mans_shoe':"\uD83D\uDC5E",'mantelpiece_clock':"\uD83D\uDD70\uFE0F",'manual_wheelchair':"\uD83E\uDDBD",'maple_leaf':"\uD83C\uDF41",'marshall_islands':"\uD83C\uDDF2\uD83C\uDDED",'martial_arts_uniform':"\uD83E\uDD4B",'martinique':"\uD83C\uDDF2\uD83C\uDDF6",'mask':"\uD83D\uDE37",'massage':"\uD83D\uDC86",'massage_man':"\uD83D\uDC86\u200D\u2642\uFE0F",'massage_woman':"\uD83D\uDC86\u200D\u2640\uFE0F",'mate':"\uD83E\uDDC9",'mauritania':"\uD83C\uDDF2\uD83C\uDDF7",'mauritius':"\uD83C\uDDF2\uD83C\uDDFA",'mayotte':"\uD83C\uDDFE\uD83C\uDDF9",'meat_on_bone':"\uD83C\uDF56",'mechanic':"\uD83E\uDDD1\u200D\uD83D\uDD27",'mechanical_arm':"\uD83E\uDDBE",'mechanical_leg':"\uD83E\uDDBF",'medal_military':"\uD83C\uDF96\uFE0F",'medal_sports':"\uD83C\uDFC5",'medical_symbol':"\u2695\uFE0F",'mega':"\uD83D\uDCE3",'melon':"\uD83C\uDF48",'memo':"\uD83D\uDCDD",'men_wrestling':"\uD83E\uDD3C\u200D\u2642\uFE0F",'menorah':"\uD83D\uDD4E",'mens':"\uD83D\uDEB9",'mermaid':"\uD83E\uDDDC\u200D\u2640\uFE0F",'merman':"\uD83E\uDDDC\u200D\u2642\uFE0F",'merperson':"\uD83E\uDDDC",'metal':"\uD83E\uDD18",'metro':"\uD83D\uDE87",'mexico':"\uD83C\uDDF2\uD83C\uDDFD",'microbe':"\uD83E\uDDA0",'micronesia':"\uD83C\uDDEB\uD83C\uDDF2",'microphone':"\uD83C\uDFA4",'microscope':"\uD83D\uDD2C",'middle_finger':"\uD83D\uDD95",'milk_glass':"\uD83E\uDD5B",'milky_way':"\uD83C\uDF0C",'minibus':"\uD83D\uDE90",'minidisc':"\uD83D\uDCBD",'mobile_phone_off':"\uD83D\uDCF4",'moldova':"\uD83C\uDDF2\uD83C\uDDE9",'monaco':"\uD83C\uDDF2\uD83C\uDDE8",'money_mouth_face':"\uD83E\uDD11",'money_with_wings':"\uD83D\uDCB8",'moneybag':"\uD83D\uDCB0",'mongolia':"\uD83C\uDDF2\uD83C\uDDF3",'monkey':"\uD83D\uDC12",'monkey_face':"\uD83D\uDC35",'monocle_face':"\uD83E\uDDD0",'monorail':"\uD83D\uDE9D",'montenegro':"\uD83C\uDDF2\uD83C\uDDEA",'montserrat':"\uD83C\uDDF2\uD83C\uDDF8",'moon':"\uD83C\uDF14",'moon_cake':"\uD83E\uDD6E",'morocco':"\uD83C\uDDF2\uD83C\uDDE6",'mortar_board':"\uD83C\uDF93",'mosque':"\uD83D\uDD4C",'mosquito':"\uD83E\uDD9F",'motor_boat':"\uD83D\uDEE5\uFE0F",'motor_scooter':"\uD83D\uDEF5",'motorcycle':"\uD83C\uDFCD\uFE0F",'motorized_wheelchair':"\uD83E\uDDBC",'motorway':"\uD83D\uDEE3\uFE0F",'mount_fuji':"\uD83D\uDDFB",'mountain':"\u26F0\uFE0F",'mountain_bicyclist':"\uD83D\uDEB5",'mountain_biking_man':"\uD83D\uDEB5\u200D\u2642\uFE0F",'mountain_biking_woman':"\uD83D\uDEB5\u200D\u2640\uFE0F",'mountain_cableway':"\uD83D\uDEA0",'mountain_railway':"\uD83D\uDE9E",'mountain_snow':"\uD83C\uDFD4\uFE0F",'mouse':"\uD83D\uDC2D",'mouse2':"\uD83D\uDC01",'movie_camera':"\uD83C\uDFA5",'moyai':"\uD83D\uDDFF",'mozambique':"\uD83C\uDDF2\uD83C\uDDFF",'mrs_claus':"\uD83E\uDD36",'muscle':"\uD83D\uDCAA",'mushroom':"\uD83C\uDF44",'musical_keyboard':"\uD83C\uDFB9",'musical_note':"\uD83C\uDFB5",'musical_score':"\uD83C\uDFBC",'mute':"\uD83D\uDD07",'myanmar':"\uD83C\uDDF2\uD83C\uDDF2",'nail_care':"\uD83D\uDC85",'name_badge':"\uD83D\uDCDB",'namibia':"\uD83C\uDDF3\uD83C\uDDE6",'national_park':"\uD83C\uDFDE\uFE0F",'nauru':"\uD83C\uDDF3\uD83C\uDDF7",'nauseated_face':"\uD83E\uDD22",'nazar_amulet':"\uD83E\uDDFF",'necktie':"\uD83D\uDC54",'negative_squared_cross_mark':"\u274E",'nepal':"\uD83C\uDDF3\uD83C\uDDF5",'nerd_face':"\uD83E\uDD13",'netherlands':"\uD83C\uDDF3\uD83C\uDDF1",'neutral_face':"\uD83D\uDE10",'new':"\uD83C\uDD95",'new_caledonia':"\uD83C\uDDF3\uD83C\uDDE8",'new_moon':"\uD83C\uDF11",'new_moon_with_face':"\uD83C\uDF1A",'new_zealand':"\uD83C\uDDF3\uD83C\uDDFF",'newspaper':"\uD83D\uDCF0",'newspaper_roll':"\uD83D\uDDDE\uFE0F",'next_track_button':"\u23ED\uFE0F",'ng':"\uD83C\uDD96",'ng_man':"\uD83D\uDE45\u200D\u2642\uFE0F",'ng_woman':"\uD83D\uDE45\u200D\u2640\uFE0F",'nicaragua':"\uD83C\uDDF3\uD83C\uDDEE",'niger':"\uD83C\uDDF3\uD83C\uDDEA",'nigeria':"\uD83C\uDDF3\uD83C\uDDEC",'night_with_stars':"\uD83C\uDF03",'nine':"9\uFE0F\u20E3",'niue':"\uD83C\uDDF3\uD83C\uDDFA",'no_bell':"\uD83D\uDD15",'no_bicycles':"\uD83D\uDEB3",'no_entry':"\u26D4",'no_entry_sign':"\uD83D\uDEAB",'no_good':"\uD83D\uDE45",'no_good_man':"\uD83D\uDE45\u200D\u2642\uFE0F",'no_good_woman':"\uD83D\uDE45\u200D\u2640\uFE0F",'no_mobile_phones':"\uD83D\uDCF5",'no_mouth':"\uD83D\uDE36",'no_pedestrians':"\uD83D\uDEB7",'no_smoking':"\uD83D\uDEAD",'non-potable_water':"\uD83D\uDEB1",'norfolk_island':"\uD83C\uDDF3\uD83C\uDDEB",'north_korea':"\uD83C\uDDF0\uD83C\uDDF5",'northern_mariana_islands':"\uD83C\uDDF2\uD83C\uDDF5",'norway':"\uD83C\uDDF3\uD83C\uDDF4",'nose':"\uD83D\uDC43",'notebook':"\uD83D\uDCD3",'notebook_with_decorative_cover':"\uD83D\uDCD4",'notes':"\uD83C\uDFB6",'nut_and_bolt':"\uD83D\uDD29",'o':"\u2B55",'o2':"\uD83C\uDD7E\uFE0F",'ocean':"\uD83C\uDF0A",'octopus':"\uD83D\uDC19",'oden':"\uD83C\uDF62",'office':"\uD83C\uDFE2",'office_worker':"\uD83E\uDDD1\u200D\uD83D\uDCBC",'oil_drum':"\uD83D\uDEE2\uFE0F",'ok':"\uD83C\uDD97",'ok_hand':"\uD83D\uDC4C",'ok_man':"\uD83D\uDE46\u200D\u2642\uFE0F",'ok_person':"\uD83D\uDE46",'ok_woman':"\uD83D\uDE46\u200D\u2640\uFE0F",'old_key':"\uD83D\uDDDD\uFE0F",'older_adult':"\uD83E\uDDD3",'older_man':"\uD83D\uDC74",'older_woman':"\uD83D\uDC75",'om':"\uD83D\uDD49\uFE0F",'oman':"\uD83C\uDDF4\uD83C\uDDF2",'on':"\uD83D\uDD1B",'oncoming_automobile':"\uD83D\uDE98",'oncoming_bus':"\uD83D\uDE8D",'oncoming_police_car':"\uD83D\uDE94",'oncoming_taxi':"\uD83D\uDE96",'one':"1\uFE0F\u20E3",'one_piece_swimsuit':"\uD83E\uDE71",'onion':"\uD83E\uDDC5",'open_book':"\uD83D\uDCD6",'open_file_folder':"\uD83D\uDCC2",'open_hands':"\uD83D\uDC50",'open_mouth':"\uD83D\uDE2E",'open_umbrella':"\u2602\uFE0F",'ophiuchus':"\u26CE",'orange':"\uD83C\uDF4A",'orange_book':"\uD83D\uDCD9",'orange_circle':"\uD83D\uDFE0",'orange_heart':"\uD83E\uDDE1",'orange_square':"\uD83D\uDFE7",'orangutan':"\uD83E\uDDA7",'orthodox_cross':"\u2626\uFE0F",'otter':"\uD83E\uDDA6",'outbox_tray':"\uD83D\uDCE4",'owl':"\uD83E\uDD89",'ox':"\uD83D\uDC02",'oyster':"\uD83E\uDDAA",'package':"\uD83D\uDCE6",'page_facing_up':"\uD83D\uDCC4",'page_with_curl':"\uD83D\uDCC3",'pager':"\uD83D\uDCDF",'paintbrush':"\uD83D\uDD8C\uFE0F",'pakistan':"\uD83C\uDDF5\uD83C\uDDF0",'palau':"\uD83C\uDDF5\uD83C\uDDFC",'palestinian_territories':"\uD83C\uDDF5\uD83C\uDDF8",'palm_tree':"\uD83C\uDF34",'palms_up_together':"\uD83E\uDD32",'panama':"\uD83C\uDDF5\uD83C\uDDE6",'pancakes':"\uD83E\uDD5E",'panda_face':"\uD83D\uDC3C",'paperclip':"\uD83D\uDCCE",'paperclips':"\uD83D\uDD87\uFE0F",'papua_new_guinea':"\uD83C\uDDF5\uD83C\uDDEC",'parachute':"\uD83E\uDE82",'paraguay':"\uD83C\uDDF5\uD83C\uDDFE",'parasol_on_ground':"\u26F1\uFE0F",'parking':"\uD83C\uDD7F\uFE0F",'parrot':"\uD83E\uDD9C",'part_alternation_mark':"\u303D\uFE0F",'partly_sunny':"\u26C5",'partying_face':"\uD83E\uDD73",'passenger_ship':"\uD83D\uDEF3\uFE0F",'passport_control':"\uD83D\uDEC2",'pause_button':"\u23F8\uFE0F",'paw_prints':"\uD83D\uDC3E",'peace_symbol':"\u262E\uFE0F",'peach':"\uD83C\uDF51",'peacock':"\uD83E\uDD9A",'peanuts':"\uD83E\uDD5C",'pear':"\uD83C\uDF50",'pen':"\uD83D\uDD8A\uFE0F",'pencil':"\uD83D\uDCDD",'pencil2':"\u270F\uFE0F",'penguin':"\uD83D\uDC27",'pensive':"\uD83D\uDE14",'people_holding_hands':"\uD83E\uDDD1\u200D\uD83E\uDD1D\u200D\uD83E\uDDD1",'performing_arts':"\uD83C\uDFAD",'persevere':"\uD83D\uDE23",'person_bald':"\uD83E\uDDD1\u200D\uD83E\uDDB2",'person_curly_hair':"\uD83E\uDDD1\u200D\uD83E\uDDB1",'person_fencing':"\uD83E\uDD3A",'person_in_manual_wheelchair':"\uD83E\uDDD1\u200D\uD83E\uDDBD",'person_in_motorized_wheelchair':"\uD83E\uDDD1\u200D\uD83E\uDDBC",'person_red_hair':"\uD83E\uDDD1\u200D\uD83E\uDDB0",'person_white_hair':"\uD83E\uDDD1\u200D\uD83E\uDDB3",'person_with_probing_cane':"\uD83E\uDDD1\u200D\uD83E\uDDAF",'person_with_turban':"\uD83D\uDC73",'peru':"\uD83C\uDDF5\uD83C\uDDEA",'petri_dish':"\uD83E\uDDEB",'philippines':"\uD83C\uDDF5\uD83C\uDDED",'phone':"\u260E\uFE0F",'pick':"\u26CF\uFE0F",'pie':"\uD83E\uDD67",'pig':"\uD83D\uDC37",'pig2':"\uD83D\uDC16",'pig_nose':"\uD83D\uDC3D",'pill':"\uD83D\uDC8A",'pilot':"\uD83E\uDDD1\u200D\u2708\uFE0F",'pinching_hand':"\uD83E\uDD0F",'pineapple':"\uD83C\uDF4D",'ping_pong':"\uD83C\uDFD3",'pirate_flag':"\uD83C\uDFF4\u200D\u2620\uFE0F",'pisces':"\u2653",'pitcairn_islands':"\uD83C\uDDF5\uD83C\uDDF3",'pizza':"\uD83C\uDF55",'place_of_worship':"\uD83D\uDED0",'plate_with_cutlery':"\uD83C\uDF7D\uFE0F",'play_or_pause_button':"\u23EF\uFE0F",'pleading_face':"\uD83E\uDD7A",'point_down':"\uD83D\uDC47",'point_left':"\uD83D\uDC48",'point_right':"\uD83D\uDC49",'point_up':"\u261D\uFE0F",'point_up_2':"\uD83D\uDC46",'poland':"\uD83C\uDDF5\uD83C\uDDF1",'police_car':"\uD83D\uDE93",'police_officer':"\uD83D\uDC6E",'policeman':"\uD83D\uDC6E\u200D\u2642\uFE0F",'policewoman':"\uD83D\uDC6E\u200D\u2640\uFE0F",'poodle':"\uD83D\uDC29",'poop':"\uD83D\uDCA9",'popcorn':"\uD83C\uDF7F",'portugal':"\uD83C\uDDF5\uD83C\uDDF9",'post_office':"\uD83C\uDFE3",'postal_horn':"\uD83D\uDCEF",'postbox':"\uD83D\uDCEE",'potable_water':"\uD83D\uDEB0",'potato':"\uD83E\uDD54",'pouch':"\uD83D\uDC5D",'poultry_leg':"\uD83C\uDF57",'pound':"\uD83D\uDCB7",'pout':"\uD83D\uDE21",'pouting_cat':"\uD83D\uDE3E",'pouting_face':"\uD83D\uDE4E",'pouting_man':"\uD83D\uDE4E\u200D\u2642\uFE0F",'pouting_woman':"\uD83D\uDE4E\u200D\u2640\uFE0F",'pray':"\uD83D\uDE4F",'prayer_beads':"\uD83D\uDCFF",'pregnant_woman':"\uD83E\uDD30",'pretzel':"\uD83E\uDD68",'previous_track_button':"\u23EE\uFE0F",'prince':"\uD83E\uDD34",'princess':"\uD83D\uDC78",'printer':"\uD83D\uDDA8\uFE0F",'probing_cane':"\uD83E\uDDAF",'puerto_rico':"\uD83C\uDDF5\uD83C\uDDF7",'punch':"\uD83D\uDC4A",'purple_circle':"\uD83D\uDFE3",'purple_heart':"\uD83D\uDC9C",'purple_square':"\uD83D\uDFEA",'purse':"\uD83D\uDC5B",'pushpin':"\uD83D\uDCCC",'put_litter_in_its_place':"\uD83D\uDEAE",'qatar':"\uD83C\uDDF6\uD83C\uDDE6",'question':"\u2753",'rabbit':"\uD83D\uDC30",'rabbit2':"\uD83D\uDC07",'raccoon':"\uD83E\uDD9D",'racehorse':"\uD83D\uDC0E",'racing_car':"\uD83C\uDFCE\uFE0F",'radio':"\uD83D\uDCFB",'radio_button':"\uD83D\uDD18",'radioactive':"\u2622\uFE0F",'rage':"\uD83D\uDE21",'railway_car':"\uD83D\uDE83",'railway_track':"\uD83D\uDEE4\uFE0F",'rainbow':"\uD83C\uDF08",'rainbow_flag':"\uD83C\uDFF3\uFE0F\u200D\uD83C\uDF08",'raised_back_of_hand':"\uD83E\uDD1A",'raised_eyebrow':"\uD83E\uDD28",'raised_hand':"\u270B",'raised_hand_with_fingers_splayed':"\uD83D\uDD90\uFE0F",'raised_hands':"\uD83D\uDE4C",'raising_hand':"\uD83D\uDE4B",'raising_hand_man':"\uD83D\uDE4B\u200D\u2642\uFE0F",'raising_hand_woman':"\uD83D\uDE4B\u200D\u2640\uFE0F",'ram':"\uD83D\uDC0F",'ramen':"\uD83C\uDF5C",'rat':"\uD83D\uDC00",'razor':"\uD83E\uDE92",'receipt':"\uD83E\uDDFE",'record_button':"\u23FA\uFE0F",'recycle':"\u267B\uFE0F",'red_car':"\uD83D\uDE97",'red_circle':"\uD83D\uDD34",'red_envelope':"\uD83E\uDDE7",'red_haired_man':"\uD83D\uDC68\u200D\uD83E\uDDB0",'red_haired_woman':"\uD83D\uDC69\u200D\uD83E\uDDB0",'red_square':"\uD83D\uDFE5",'registered':"\xAE\uFE0F",'relaxed':"\u263A\uFE0F",'relieved':"\uD83D\uDE0C",'reminder_ribbon':"\uD83C\uDF97\uFE0F",'repeat':"\uD83D\uDD01",'repeat_one':"\uD83D\uDD02",'rescue_worker_helmet':"\u26D1\uFE0F",'restroom':"\uD83D\uDEBB",'reunion':"\uD83C\uDDF7\uD83C\uDDEA",'revolving_hearts':"\uD83D\uDC9E",'rewind':"\u23EA",'rhinoceros':"\uD83E\uDD8F",'ribbon':"\uD83C\uDF80",'rice':"\uD83C\uDF5A",'rice_ball':"\uD83C\uDF59",'rice_cracker':"\uD83C\uDF58",'rice_scene':"\uD83C\uDF91",'right_anger_bubble':"\uD83D\uDDEF\uFE0F",'ring':"\uD83D\uDC8D",'ringed_planet':"\uD83E\uDE90",'robot':"\uD83E\uDD16",'rocket':"\uD83D\uDE80",'rofl':"\uD83E\uDD23",'roll_eyes':"\uD83D\uDE44",'roll_of_paper':"\uD83E\uDDFB",'roller_coaster':"\uD83C\uDFA2",'romania':"\uD83C\uDDF7\uD83C\uDDF4",'rooster':"\uD83D\uDC13",'rose':"\uD83C\uDF39",'rosette':"\uD83C\uDFF5\uFE0F",'rotating_light':"\uD83D\uDEA8",'round_pushpin':"\uD83D\uDCCD",'rowboat':"\uD83D\uDEA3",'rowing_man':"\uD83D\uDEA3\u200D\u2642\uFE0F",'rowing_woman':"\uD83D\uDEA3\u200D\u2640\uFE0F",'ru':"\uD83C\uDDF7\uD83C\uDDFA",'rugby_football':"\uD83C\uDFC9",'runner':"\uD83C\uDFC3",'running':"\uD83C\uDFC3",'running_man':"\uD83C\uDFC3\u200D\u2642\uFE0F",'running_shirt_with_sash':"\uD83C\uDFBD",'running_woman':"\uD83C\uDFC3\u200D\u2640\uFE0F",'rwanda':"\uD83C\uDDF7\uD83C\uDDFC",'sa':"\uD83C\uDE02\uFE0F",'safety_pin':"\uD83E\uDDF7",'safety_vest':"\uD83E\uDDBA",'sagittarius':"\u2650",'sailboat':"\u26F5",'sake':"\uD83C\uDF76",'salt':"\uD83E\uDDC2",'samoa':"\uD83C\uDDFC\uD83C\uDDF8",'san_marino':"\uD83C\uDDF8\uD83C\uDDF2",'sandal':"\uD83D\uDC61",'sandwich':"\uD83E\uDD6A",'santa':"\uD83C\uDF85",'sao_tome_principe':"\uD83C\uDDF8\uD83C\uDDF9",'sari':"\uD83E\uDD7B",'sassy_man':"\uD83D\uDC81\u200D\u2642\uFE0F",'sassy_woman':"\uD83D\uDC81\u200D\u2640\uFE0F",'satellite':"\uD83D\uDCE1",'satisfied':"\uD83D\uDE06",'saudi_arabia':"\uD83C\uDDF8\uD83C\uDDE6",'sauna_man':"\uD83E\uDDD6\u200D\u2642\uFE0F",'sauna_person':"\uD83E\uDDD6",'sauna_woman':"\uD83E\uDDD6\u200D\u2640\uFE0F",'sauropod':"\uD83E\uDD95",'saxophone':"\uD83C\uDFB7",'scarf':"\uD83E\uDDE3",'school':"\uD83C\uDFEB",'school_satchel':"\uD83C\uDF92",'scientist':"\uD83E\uDDD1\u200D\uD83D\uDD2C",'scissors':"\u2702\uFE0F",'scorpion':"\uD83E\uDD82",'scorpius':"\u264F",'scotland':"\uD83C\uDFF4\uDB40\uDC67\uDB40\uDC62\uDB40\uDC73\uDB40\uDC63\uDB40\uDC74\uDB40\uDC7F",'scream':"\uD83D\uDE31",'scream_cat':"\uD83D\uDE40",'scroll':"\uD83D\uDCDC",'seat':"\uD83D\uDCBA",'secret':"\u3299\uFE0F",'see_no_evil':"\uD83D\uDE48",'seedling':"\uD83C\uDF31",'selfie':"\uD83E\uDD33",'senegal':"\uD83C\uDDF8\uD83C\uDDF3",'serbia':"\uD83C\uDDF7\uD83C\uDDF8",'service_dog':"\uD83D\uDC15\u200D\uD83E\uDDBA",'seven':"7\uFE0F\u20E3",'seychelles':"\uD83C\uDDF8\uD83C\uDDE8",'shallow_pan_of_food':"\uD83E\uDD58",'shamrock':"\u2618\uFE0F",'shark':"\uD83E\uDD88",'shaved_ice':"\uD83C\uDF67",'sheep':"\uD83D\uDC11",'shell':"\uD83D\uDC1A",'shield':"\uD83D\uDEE1\uFE0F",'shinto_shrine':"\u26E9\uFE0F",'ship':"\uD83D\uDEA2",'shirt':"\uD83D\uDC55",'shit':"\uD83D\uDCA9",'shoe':"\uD83D\uDC5E",'shopping':"\uD83D\uDECD\uFE0F",'shopping_cart':"\uD83D\uDED2",'shorts':"\uD83E\uDE73",'shower':"\uD83D\uDEBF",'shrimp':"\uD83E\uDD90",'shrug':"\uD83E\uDD37",'shushing_face':"\uD83E\uDD2B",'sierra_leone':"\uD83C\uDDF8\uD83C\uDDF1",'signal_strength':"\uD83D\uDCF6",'singapore':"\uD83C\uDDF8\uD83C\uDDEC",'singer':"\uD83E\uDDD1\u200D\uD83C\uDFA4",'sint_maarten':"\uD83C\uDDF8\uD83C\uDDFD",'six':"6\uFE0F\u20E3",'six_pointed_star':"\uD83D\uDD2F",'skateboard':"\uD83D\uDEF9",'ski':"\uD83C\uDFBF",'skier':"\u26F7\uFE0F",'skull':"\uD83D\uDC80",'skull_and_crossbones':"\u2620\uFE0F",'skunk':"\uD83E\uDDA8",'sled':"\uD83D\uDEF7",'sleeping':"\uD83D\uDE34",'sleeping_bed':"\uD83D\uDECC",'sleepy':"\uD83D\uDE2A",'slightly_frowning_face':"\uD83D\uDE41",'slightly_smiling_face':"\uD83D\uDE42",'slot_machine':"\uD83C\uDFB0",'sloth':"\uD83E\uDDA5",'slovakia':"\uD83C\uDDF8\uD83C\uDDF0",'slovenia':"\uD83C\uDDF8\uD83C\uDDEE",'small_airplane':"\uD83D\uDEE9\uFE0F",'small_blue_diamond':"\uD83D\uDD39",'small_orange_diamond':"\uD83D\uDD38",'small_red_triangle':"\uD83D\uDD3A",'small_red_triangle_down':"\uD83D\uDD3B",'smile':"\uD83D\uDE04",'smile_cat':"\uD83D\uDE38",'smiley':"\uD83D\uDE03",'smiley_cat':"\uD83D\uDE3A",'smiling_face_with_three_hearts':"\uD83E\uDD70",'smiling_imp':"\uD83D\uDE08",'smirk':"\uD83D\uDE0F",'smirk_cat':"\uD83D\uDE3C",'smoking':"\uD83D\uDEAC",'snail':"\uD83D\uDC0C",'snake':"\uD83D\uDC0D",'sneezing_face':"\uD83E\uDD27",'snowboarder':"\uD83C\uDFC2",'snowflake':"\u2744\uFE0F",'snowman':"\u26C4",'snowman_with_snow':"\u2603\uFE0F",'soap':"\uD83E\uDDFC",'sob':"\uD83D\uDE2D",'soccer':"\u26BD",'socks':"\uD83E\uDDE6",'softball':"\uD83E\uDD4E",'solomon_islands':"\uD83C\uDDF8\uD83C\uDDE7",'somalia':"\uD83C\uDDF8\uD83C\uDDF4",'soon':"\uD83D\uDD1C",'sos':"\uD83C\uDD98",'sound':"\uD83D\uDD09",'south_africa':"\uD83C\uDDFF\uD83C\uDDE6",'south_georgia_south_sandwich_islands':"\uD83C\uDDEC\uD83C\uDDF8",'south_sudan':"\uD83C\uDDF8\uD83C\uDDF8",'space_invader':"\uD83D\uDC7E",'spades':"\u2660\uFE0F",'spaghetti':"\uD83C\uDF5D",'sparkle':"\u2747\uFE0F",'sparkler':"\uD83C\uDF87",'sparkles':"\u2728",'sparkling_heart':"\uD83D\uDC96",'speak_no_evil':"\uD83D\uDE4A",'speaker':"\uD83D\uDD08",'speaking_head':"\uD83D\uDDE3\uFE0F",'speech_balloon':"\uD83D\uDCAC",'speedboat':"\uD83D\uDEA4",'spider':"\uD83D\uDD77\uFE0F",'spider_web':"\uD83D\uDD78\uFE0F",'spiral_calendar':"\uD83D\uDDD3\uFE0F",'spiral_notepad':"\uD83D\uDDD2\uFE0F",'sponge':"\uD83E\uDDFD",'spoon':"\uD83E\uDD44",'squid':"\uD83E\uDD91",'sri_lanka':"\uD83C\uDDF1\uD83C\uDDF0",'st_barthelemy':"\uD83C\uDDE7\uD83C\uDDF1",'st_helena':"\uD83C\uDDF8\uD83C\uDDED",'st_kitts_nevis':"\uD83C\uDDF0\uD83C\uDDF3",'st_lucia':"\uD83C\uDDF1\uD83C\uDDE8",'st_martin':"\uD83C\uDDF2\uD83C\uDDEB",'st_pierre_miquelon':"\uD83C\uDDF5\uD83C\uDDF2",'st_vincent_grenadines':"\uD83C\uDDFB\uD83C\uDDE8",'stadium':"\uD83C\uDFDF\uFE0F",'standing_man':"\uD83E\uDDCD\u200D\u2642\uFE0F",'standing_person':"\uD83E\uDDCD",'standing_woman':"\uD83E\uDDCD\u200D\u2640\uFE0F",'star':"\u2B50",'star2':"\uD83C\uDF1F",'star_and_crescent':"\u262A\uFE0F",'star_of_david':"\u2721\uFE0F",'star_struck':"\uD83E\uDD29",'stars':"\uD83C\uDF20",'station':"\uD83D\uDE89",'statue_of_liberty':"\uD83D\uDDFD",'steam_locomotive':"\uD83D\uDE82",'stethoscope':"\uD83E\uDE7A",'stew':"\uD83C\uDF72",'stop_button':"\u23F9\uFE0F",'stop_sign':"\uD83D\uDED1",'stopwatch':"\u23F1\uFE0F",'straight_ruler':"\uD83D\uDCCF",'strawberry':"\uD83C\uDF53",'stuck_out_tongue':"\uD83D\uDE1B",'stuck_out_tongue_closed_eyes':"\uD83D\uDE1D",'stuck_out_tongue_winking_eye':"\uD83D\uDE1C",'student':"\uD83E\uDDD1\u200D\uD83C\uDF93",'studio_microphone':"\uD83C\uDF99\uFE0F",'stuffed_flatbread':"\uD83E\uDD59",'sudan':"\uD83C\uDDF8\uD83C\uDDE9",'sun_behind_large_cloud':"\uD83C\uDF25\uFE0F",'sun_behind_rain_cloud':"\uD83C\uDF26\uFE0F",'sun_behind_small_cloud':"\uD83C\uDF24\uFE0F",'sun_with_face':"\uD83C\uDF1E",'sunflower':"\uD83C\uDF3B",'sunglasses':"\uD83D\uDE0E",'sunny':"\u2600\uFE0F",'sunrise':"\uD83C\uDF05",'sunrise_over_mountains':"\uD83C\uDF04",'superhero':"\uD83E\uDDB8",'superhero_man':"\uD83E\uDDB8\u200D\u2642\uFE0F",'superhero_woman':"\uD83E\uDDB8\u200D\u2640\uFE0F",'supervillain':"\uD83E\uDDB9",'supervillain_man':"\uD83E\uDDB9\u200D\u2642\uFE0F",'supervillain_woman':"\uD83E\uDDB9\u200D\u2640\uFE0F",'surfer':"\uD83C\uDFC4",'surfing_man':"\uD83C\uDFC4\u200D\u2642\uFE0F",'surfing_woman':"\uD83C\uDFC4\u200D\u2640\uFE0F",'suriname':"\uD83C\uDDF8\uD83C\uDDF7",'sushi':"\uD83C\uDF63",'suspension_railway':"\uD83D\uDE9F",'svalbard_jan_mayen':"\uD83C\uDDF8\uD83C\uDDEF",'swan':"\uD83E\uDDA2",'swaziland':"\uD83C\uDDF8\uD83C\uDDFF",'sweat':"\uD83D\uDE13",'sweat_drops':"\uD83D\uDCA6",'sweat_smile':"\uD83D\uDE05",'sweden':"\uD83C\uDDF8\uD83C\uDDEA",'sweet_potato':"\uD83C\uDF60",'swim_brief':"\uD83E\uDE72",'swimmer':"\uD83C\uDFCA",'swimming_man':"\uD83C\uDFCA\u200D\u2642\uFE0F",'swimming_woman':"\uD83C\uDFCA\u200D\u2640\uFE0F",'switzerland':"\uD83C\uDDE8\uD83C\uDDED",'symbols':"\uD83D\uDD23",'synagogue':"\uD83D\uDD4D",'syria':"\uD83C\uDDF8\uD83C\uDDFE",'syringe':"\uD83D\uDC89",'t-rex':"\uD83E\uDD96",'taco':"\uD83C\uDF2E",'tada':"\uD83C\uDF89",'taiwan':"\uD83C\uDDF9\uD83C\uDDFC",'tajikistan':"\uD83C\uDDF9\uD83C\uDDEF",'takeout_box':"\uD83E\uDD61",'tanabata_tree':"\uD83C\uDF8B",'tangerine':"\uD83C\uDF4A",'tanzania':"\uD83C\uDDF9\uD83C\uDDFF",'taurus':"\u2649",'taxi':"\uD83D\uDE95",'tea':"\uD83C\uDF75",'teacher':"\uD83E\uDDD1\u200D\uD83C\uDFEB",'technologist':"\uD83E\uDDD1\u200D\uD83D\uDCBB",'teddy_bear':"\uD83E\uDDF8",'telephone':"\u260E\uFE0F",'telephone_receiver':"\uD83D\uDCDE",'telescope':"\uD83D\uDD2D",'tennis':"\uD83C\uDFBE",'tent':"\u26FA",'test_tube':"\uD83E\uDDEA",'thailand':"\uD83C\uDDF9\uD83C\uDDED",'thermometer':"\uD83C\uDF21\uFE0F",'thinking':"\uD83E\uDD14",'thought_balloon':"\uD83D\uDCAD",'thread':"\uD83E\uDDF5",'three':"3\uFE0F\u20E3",'thumbsdown':"\uD83D\uDC4E",'thumbsup':"\uD83D\uDC4D",'ticket':"\uD83C\uDFAB",'tickets':"\uD83C\uDF9F\uFE0F",'tiger':"\uD83D\uDC2F",'tiger2':"\uD83D\uDC05",'timer_clock':"\u23F2\uFE0F",'timor_leste':"\uD83C\uDDF9\uD83C\uDDF1",'tipping_hand_man':"\uD83D\uDC81\u200D\u2642\uFE0F",'tipping_hand_person':"\uD83D\uDC81",'tipping_hand_woman':"\uD83D\uDC81\u200D\u2640\uFE0F",'tired_face':"\uD83D\uDE2B",'tm':"\u2122\uFE0F",'togo':"\uD83C\uDDF9\uD83C\uDDEC",'toilet':"\uD83D\uDEBD",'tokelau':"\uD83C\uDDF9\uD83C\uDDF0",'tokyo_tower':"\uD83D\uDDFC",'tomato':"\uD83C\uDF45",'tonga':"\uD83C\uDDF9\uD83C\uDDF4",'tongue':"\uD83D\uDC45",'toolbox':"\uD83E\uDDF0",'tooth':"\uD83E\uDDB7",'top':"\uD83D\uDD1D",'tophat':"\uD83C\uDFA9",'tornado':"\uD83C\uDF2A\uFE0F",'tr':"\uD83C\uDDF9\uD83C\uDDF7",'trackball':"\uD83D\uDDB2\uFE0F",'tractor':"\uD83D\uDE9C",'traffic_light':"\uD83D\uDEA5",'train':"\uD83D\uDE8B",'train2':"\uD83D\uDE86",'tram':"\uD83D\uDE8A",'triangular_flag_on_post':"\uD83D\uDEA9",'triangular_ruler':"\uD83D\uDCD0",'trident':"\uD83D\uDD31",'trinidad_tobago':"\uD83C\uDDF9\uD83C\uDDF9",'tristan_da_cunha':"\uD83C\uDDF9\uD83C\uDDE6",'triumph':"\uD83D\uDE24",'trolleybus':"\uD83D\uDE8E",'trophy':"\uD83C\uDFC6",'tropical_drink':"\uD83C\uDF79",'tropical_fish':"\uD83D\uDC20",'truck':"\uD83D\uDE9A",'trumpet':"\uD83C\uDFBA",'tshirt':"\uD83D\uDC55",'tulip':"\uD83C\uDF37",'tumbler_glass':"\uD83E\uDD43",'tunisia':"\uD83C\uDDF9\uD83C\uDDF3",'turkey':"\uD83E\uDD83",'turkmenistan':"\uD83C\uDDF9\uD83C\uDDF2",'turks_caicos_islands':"\uD83C\uDDF9\uD83C\uDDE8",'turtle':"\uD83D\uDC22",'tuvalu':"\uD83C\uDDF9\uD83C\uDDFB",'tv':"\uD83D\uDCFA",'twisted_rightwards_arrows':"\uD83D\uDD00",'two':"2\uFE0F\u20E3",'two_hearts':"\uD83D\uDC95",'two_men_holding_hands':"\uD83D\uDC6C",'two_women_holding_hands':"\uD83D\uDC6D",'u5272':"\uD83C\uDE39",'u5408':"\uD83C\uDE34",'u55b6':"\uD83C\uDE3A",'u6307':"\uD83C\uDE2F",'u6708':"\uD83C\uDE37\uFE0F",'u6709':"\uD83C\uDE36",'u6e80':"\uD83C\uDE35",'u7121':"\uD83C\uDE1A",'u7533':"\uD83C\uDE38",'u7981':"\uD83C\uDE32",'u7a7a':"\uD83C\uDE33",'uganda':"\uD83C\uDDFA\uD83C\uDDEC",'uk':"\uD83C\uDDEC\uD83C\uDDE7",'ukraine':"\uD83C\uDDFA\uD83C\uDDE6",'umbrella':"\u2614",'unamused':"\uD83D\uDE12",'underage':"\uD83D\uDD1E",'unicorn':"\uD83E\uDD84",'united_arab_emirates':"\uD83C\uDDE6\uD83C\uDDEA",'united_nations':"\uD83C\uDDFA\uD83C\uDDF3",'unlock':"\uD83D\uDD13",'up':"\uD83C\uDD99",'upside_down_face':"\uD83D\uDE43",'uruguay':"\uD83C\uDDFA\uD83C\uDDFE",'us':"\uD83C\uDDFA\uD83C\uDDF8",'us_outlying_islands':"\uD83C\uDDFA\uD83C\uDDF2",'us_virgin_islands':"\uD83C\uDDFB\uD83C\uDDEE",'uzbekistan':"\uD83C\uDDFA\uD83C\uDDFF",'v':"\u270C\uFE0F",'vampire':"\uD83E\uDDDB",'vampire_man':"\uD83E\uDDDB\u200D\u2642\uFE0F",'vampire_woman':"\uD83E\uDDDB\u200D\u2640\uFE0F",'vanuatu':"\uD83C\uDDFB\uD83C\uDDFA",'vatican_city':"\uD83C\uDDFB\uD83C\uDDE6",'venezuela':"\uD83C\uDDFB\uD83C\uDDEA",'vertical_traffic_light':"\uD83D\uDEA6",'vhs':"\uD83D\uDCFC",'vibration_mode':"\uD83D\uDCF3",'video_camera':"\uD83D\uDCF9",'video_game':"\uD83C\uDFAE",'vietnam':"\uD83C\uDDFB\uD83C\uDDF3",'violin':"\uD83C\uDFBB",'virgo':"\u264D",'volcano':"\uD83C\uDF0B",'volleyball':"\uD83C\uDFD0",'vomiting_face':"\uD83E\uDD2E",'vs':"\uD83C\uDD9A",'vulcan_salute':"\uD83D\uDD96",'waffle':"\uD83E\uDDC7",'wales':"\uD83C\uDFF4\uDB40\uDC67\uDB40\uDC62\uDB40\uDC77\uDB40\uDC6C\uDB40\uDC73\uDB40\uDC7F",'walking':"\uD83D\uDEB6",'walking_man':"\uD83D\uDEB6\u200D\u2642\uFE0F",'walking_woman':"\uD83D\uDEB6\u200D\u2640\uFE0F",'wallis_futuna':"\uD83C\uDDFC\uD83C\uDDEB",'waning_crescent_moon':"\uD83C\uDF18",'waning_gibbous_moon':"\uD83C\uDF16",'warning':"\u26A0\uFE0F",'wastebasket':"\uD83D\uDDD1\uFE0F",'watch':"\u231A",'water_buffalo':"\uD83D\uDC03",'water_polo':"\uD83E\uDD3D",'watermelon':"\uD83C\uDF49",'wave':"\uD83D\uDC4B",'wavy_dash':"\u3030\uFE0F",'waxing_crescent_moon':"\uD83C\uDF12",'waxing_gibbous_moon':"\uD83C\uDF14",'wc':"\uD83D\uDEBE",'weary':"\uD83D\uDE29",'wedding':"\uD83D\uDC92",'weight_lifting':"\uD83C\uDFCB\uFE0F",'weight_lifting_man':"\uD83C\uDFCB\uFE0F\u200D\u2642\uFE0F",'weight_lifting_woman':"\uD83C\uDFCB\uFE0F\u200D\u2640\uFE0F",'western_sahara':"\uD83C\uDDEA\uD83C\uDDED",'whale':"\uD83D\uDC33",'whale2':"\uD83D\uDC0B",'wheel_of_dharma':"\u2638\uFE0F",'wheelchair':"\u267F",'white_check_mark':"\u2705",'white_circle':"\u26AA",'white_flag':"\uD83C\uDFF3\uFE0F",'white_flower':"\uD83D\uDCAE",'white_haired_man':"\uD83D\uDC68\u200D\uD83E\uDDB3",'white_haired_woman':"\uD83D\uDC69\u200D\uD83E\uDDB3",'white_heart':"\uD83E\uDD0D",'white_large_square':"\u2B1C",'white_medium_small_square':"\u25FD",'white_medium_square':"\u25FB\uFE0F",'white_small_square':"\u25AB\uFE0F",'white_square_button':"\uD83D\uDD33",'wilted_flower':"\uD83E\uDD40",'wind_chime':"\uD83C\uDF90",'wind_face':"\uD83C\uDF2C\uFE0F",'wine_glass':"\uD83C\uDF77",'wink':"\uD83D\uDE09",'wolf':"\uD83D\uDC3A",'woman':"\uD83D\uDC69",'woman_artist':"\uD83D\uDC69\u200D\uD83C\uDFA8",'woman_astronaut':"\uD83D\uDC69\u200D\uD83D\uDE80",'woman_cartwheeling':"\uD83E\uDD38\u200D\u2640\uFE0F",'woman_cook':"\uD83D\uDC69\u200D\uD83C\uDF73",'woman_dancing':"\uD83D\uDC83",'woman_facepalming':"\uD83E\uDD26\u200D\u2640\uFE0F",'woman_factory_worker':"\uD83D\uDC69\u200D\uD83C\uDFED",'woman_farmer':"\uD83D\uDC69\u200D\uD83C\uDF3E",'woman_firefighter':"\uD83D\uDC69\u200D\uD83D\uDE92",'woman_health_worker':"\uD83D\uDC69\u200D\u2695\uFE0F",'woman_in_manual_wheelchair':"\uD83D\uDC69\u200D\uD83E\uDDBD",'woman_in_motorized_wheelchair':"\uD83D\uDC69\u200D\uD83E\uDDBC",'woman_judge':"\uD83D\uDC69\u200D\u2696\uFE0F",'woman_juggling':"\uD83E\uDD39\u200D\u2640\uFE0F",'woman_mechanic':"\uD83D\uDC69\u200D\uD83D\uDD27",'woman_office_worker':"\uD83D\uDC69\u200D\uD83D\uDCBC",'woman_pilot':"\uD83D\uDC69\u200D\u2708\uFE0F",'woman_playing_handball':"\uD83E\uDD3E\u200D\u2640\uFE0F",'woman_playing_water_polo':"\uD83E\uDD3D\u200D\u2640\uFE0F",'woman_scientist':"\uD83D\uDC69\u200D\uD83D\uDD2C",'woman_shrugging':"\uD83E\uDD37\u200D\u2640\uFE0F",'woman_singer':"\uD83D\uDC69\u200D\uD83C\uDFA4",'woman_student':"\uD83D\uDC69\u200D\uD83C\uDF93",'woman_teacher':"\uD83D\uDC69\u200D\uD83C\uDFEB",'woman_technologist':"\uD83D\uDC69\u200D\uD83D\uDCBB",'woman_with_headscarf':"\uD83E\uDDD5",'woman_with_probing_cane':"\uD83D\uDC69\u200D\uD83E\uDDAF",'woman_with_turban':"\uD83D\uDC73\u200D\u2640\uFE0F",'womans_clothes':"\uD83D\uDC5A",'womans_hat':"\uD83D\uDC52",'women_wrestling':"\uD83E\uDD3C\u200D\u2640\uFE0F",'womens':"\uD83D\uDEBA",'woozy_face':"\uD83E\uDD74",'world_map':"\uD83D\uDDFA\uFE0F",'worried':"\uD83D\uDE1F",'wrench':"\uD83D\uDD27",'wrestling':"\uD83E\uDD3C",'writing_hand':"\u270D\uFE0F",'x':"\u274C",'yarn':"\uD83E\uDDF6",'yawning_face':"\uD83E\uDD71",'yellow_circle':"\uD83D\uDFE1",'yellow_heart':"\uD83D\uDC9B",'yellow_square':"\uD83D\uDFE8",'yemen':"\uD83C\uDDFE\uD83C\uDDEA",'yen':"\uD83D\uDCB4",'yin_yang':"\u262F\uFE0F",'yo_yo':"\uD83E\uDE80",'yum':"\uD83D\uDE0B",'zambia':"\uD83C\uDDFF\uD83C\uDDF2",'zany_face':"\uD83E\uDD2A",'zap':"\u26A1",'zebra':"\uD83E\uDD93",'zero':"0\uFE0F\u20E3",'zimbabwe':"\uD83C\uDDFF\uD83C\uDDFC",'zipper_mouth_face':"\uD83E\uDD10",'zombie':"\uD83E\uDDDF",'zombie_man':"\uD83E\uDDDF\u200D\u2642\uFE0F",'zombie_woman':"\uD83E\uDDDF\u200D\u2640\uFE0F",'zzz':"\uD83D\uDCA4",/* special emojis :P */'atom':'<img alt="atom" width="20" height="20" align="absmiddle" src="https://github.githubassets.com/images/icons/emoji/atom.png?v8">','basecamp':'<img alt="basecamp" width="20" height="20" align="absmiddle" src="https://github.githubassets.com/images/icons/emoji/basecamp.png?v8">','basecampy':'<img alt="basecampy" width="20" height="20" align="absmiddle" src="https://github.githubassets.com/images/icons/emoji/basecampy.png?v8">','bowtie':'<img alt="bowtie" width="20" height="20" align="absmiddle" src="https://github.githubassets.com/images/icons/emoji/bowtie.png?v8">','electron':'<img alt="electron" width="20" height="20" align="absmiddle" src="https://github.githubassets.com/images/icons/emoji/electron.png?v8">','feelsgood':'<img alt="feelsgood" width="20" height="20" align="absmiddle" src="https://github.githubassets.com/images/icons/emoji/feelsgood.png?v8">','finnadie':'<img alt="finnadie" width="20" height="20" align="absmiddle" src="https://github.githubassets.com/images/icons/emoji/finnadie.png?v8">','goberserk':'<img alt="goberserk" width="20" height="20" align="absmiddle" src="https://github.githubassets.com/images/icons/emoji/goberserk.png?v8">','godmode':'<img alt="godmode" width="20" height="20" align="absmiddle" src="https://github.githubassets.com/images/icons/emoji/godmode.png?v8">','hurtrealbad':'<img alt="hurtrealbad" width="20" height="20" align="absmiddle" src="https://github.githubassets.com/images/icons/emoji/hurtrealbad.png?v8">','neckbeard':'<img alt="neckbeard" width="20" height="20" align="absmiddle" src="https://github.githubassets.com/images/icons/emoji/neckbeard.png?v8">','octocat':'<img alt="octocat" width="20" height="20" align="absmiddle" src="https://github.githubassets.com/images/icons/emoji/octocat.png?v8">','rage1':'<img alt="rage1" width="20" height="20" align="absmiddle" src="https://github.githubassets.com/images/icons/emoji/rage1.png?v8">','rage2':'<img alt="rage2" width="20" height="20" align="absmiddle" src="https://github.githubassets.com/images/icons/emoji/rage2.png?v8">','rage3':'<img alt="rage3" width="20" height="20" align="absmiddle" src="https://github.githubassets.com/images/icons/emoji/rage3.png?v8">','rage4':'<img alt="rage4" width="20" height="20" align="absmiddle" src="https://github.githubassets.com/images/icons/emoji/rage4.png?v8">','shipit':'<img alt="shipit" width="20" height="20" align="absmiddle" src="https://github.githubassets.com/images/icons/emoji/shipit.png?v8">','suspect':'<img alt="suspect" width="20" height="20" align="absmiddle" src="https://github.githubassets.com/images/icons/emoji/suspect.png?v8">','trollface':'<img alt="trollface" width="20" height="20" align="absmiddle" src="https://github.githubassets.com/images/icons/emoji/trollface.png?v8">','showdown':'<img alt="showdown" width="20" height="20" align="absmiddle" src="data:image/png;base64,">'};/**
 * Reverse lookup tables for emoji, used by the HTML->Markdown converters to turn
 * showdown-generated emoji back into `:code:` form. Built lazily from showdown.helper.emojis
 * and cached, since the table is large and never changes at runtime.
 *
 * Returns an object with:
 *  - images:  { <img src> : <code> }  for the "special" image-based emoji
 *  - unicode: { <unicode value> : <code> } for normal emoji (first code wins on duplicates)
 *  - regex:   a single global RegExp matching any unicode emoji value, alternatives sorted
 *             longest-first so multi-codepoint / ZWJ sequences win over their prefixes
 * @returns {{images: Object, unicode: Object, regex: RegExp}}
 */showdown.helper.emojiReverse=function(){'use strict';var cache=null;return function(){if(cache){return cache;}var images={},unicode={},values=[];for(var code in showdown.helper.emojis){if(!Object.prototype.hasOwnProperty.call(showdown.helper.emojis,code)){continue;}var val=showdown.helper.emojis[code];if(/^\s*<img/.test(val)){var srcMatch=val.match(/src="([^"]*)"/);if(srcMatch&&!Object.prototype.hasOwnProperty.call(images,srcMatch[1])){images[srcMatch[1]]=code;}}else if(!Object.prototype.hasOwnProperty.call(unicode,val)){unicode[val]=code;values.push(val);}}// longest first so e.g. `zombie_woman` (🧟‍♀️) matches before `zombie` (🧟)
values.sort(function(a,b){return b.length-a.length;});var escaped=values.map(function(v){return v.replace(/[.*+?^${}()|[\]\\]/g,'\\$&');});cache={images:images,unicode:unicode,regex:new RegExp('('+escaped.join('|')+')','g')};return cache;};}();/**
 * Created by Estevao on 31-05-2015.
 */showdown.Event=/*#__PURE__*/function(){/**
   * Creates a new showdown Event object
   * @param {string} name
   * @param {string} input
   * @param {{}} [params]
   * @param {string} params.output
   * @param {RegExp} params.regexp
   * @param {{}} params.matches
   * @param {{}} params.attributes
   * @param {{}} params.globals
   * @param {{}} params.options
   * @param {showdown.Converter} params.converter
   */function _class(name,input,params){_classCallCheck(this,_class);params=params||{};var _params=params,output=_params.output,regexp=_params.regexp,matches=_params.matches,attributes=_params.attributes,globals=_params.globals,options=_params.options,converter=_params.converter;if(!showdown.helper.isString(name)){if(!showdown.helper.isString(name)){throw new TypeError('Event.name must be a string but '+_typeof(name)+' given');}}this._name=name.toLowerCase();this.input=input;this.output=output||input;this.regexp=regexp||null;this.matches=matches||{};this.attributes=attributes||{};this._globals=globals||{};this._options=showdown.helper.cloneObject(options,true)||{};this._converter=converter||undefined;}/** @returns {string} */return _createClass(_class,[{key:"name",get:function get(){return this._name;}/** @returns {string} */},{key:"input",get:function get(){return this._input;}/** @param {string} value */,set:function set(value){if(!showdown.helper.isString(value)){throw new TypeError('Event.input must be a string but '+_typeof(value)+' given');}this._input=value;}/** @returns {string} */},{key:"output",get:function get(){return this._output;}/** @param {string|null} value */,set:function set(value){if(!showdown.helper.isString(value)&&value!==null){throw new TypeError('Event.output must be a string but '+_typeof(value)+' given');}this._output=value;}/** @returns {null|RegExp} */},{key:"regexp",get:function get(){return this._regexp;}/** @param {null|RegExp} value */,set:function set(value){if(!(value instanceof RegExp)&&value!==null){throw new TypeError('Event.regexp must be a RegExp object (or null) but '+_typeof(value)+' given');}this._regexp=value;}/** @returns {{}} */},{key:"matches",get:function get(){return this._matches;}/** @param {{}}value */,set:function set(value){if(_typeof(value)!=='object'){throw new TypeError('Event.matches must be an object (or null) but '+_typeof(value)+' given');}this._matches={};for(var prop in value){if(Object.prototype.hasOwnProperty.call(value,prop)){var descriptor=void 0;if(/^_(.+)/.test(prop)){descriptor={enumerable:true,configurable:false,writable:false,value:value[prop]};}else{descriptor={enumerable:true,configurable:false,writable:true,value:value[prop]};}Object.defineProperty(this._matches,prop,descriptor);}}}/** @returns {{}} */},{key:"attributes",get:function get(){return this._attributes;}/** @param {{}} value */,set:function set(value){if(_typeof(value)!=='object'){throw new TypeError('Event.attributes must be an object (or null) but '+_typeof(value)+' given');}this._attributes=value;}/** @param {showdown.Converter} converter */},{key:"converter",get:/** @returns {showdown.Converter} */function get(){return this._converter;},set:function set(converter){this._converter=converter;}},{key:"options",get:function get(){return this._options;}},{key:"globals",get:function get(){return this._globals;}// FLUID INTERFACE
/**
   *
   * @param {string} value
   * @returns {showdown.Event}
   */},{key:"setInput",value:function setInput(value){this.input=value;return this;}/**
   *
   * @param {string|null} value
   * @returns {showdown.Event}
   */},{key:"setOutput",value:function setOutput(value){this.output=value;return this;}/**
   *
   * @param {RegExp} value
   * @returns {showdown.Event}
   */},{key:"setRegexp",value:function setRegexp(value){this.regexp=value;return this;}/**
   *
   * @param {{}}value
   * @returns {showdown.Event}
   */},{key:"setMatches",value:function setMatches(value){this.matches=value;return this;}/**
   *
   * @param {{}}value
   * @returns {showdown.Event}
   */},{key:"setAttributes",value:function setAttributes(value){this.attributes=value;return this;}},{key:"_setOptions",value:function _setOptions(value){this._options=value;return this;}},{key:"_setGlobals",value:function _setGlobals(value){this._globals=value;return this;}},{key:"_setConverter",value:function _setConverter(value){this.converter=value;return this;}/**
   * Legacy: Return the output text
   * @returns {string}
   */},{key:"getText",value:function getText(){return this.output;}},{key:"getMatches",value:function getMatches(){return this.matches;}}]);}();////
// makehtml/blockGamut.js
// Copyright (c) 2018 ShowdownJS
//
// These are all the transformations that form block-level
// tags like paragraphs, headers, and list items.
//
// ***Author:***
// - Estêvão Soares dos Santos (Tivie) <https://github.com/tivie>
////
showdown.subParser('makehtml.blockGamut',function(text,options,globals,skip){'use strict';skip=skip||false;var startEvent=new showdown.Event('makehtml.blockGamut.onStart',text);startEvent.setOutput(text)._setGlobals(globals)._setOptions(options);startEvent=globals.converter.dispatch(startEvent);text=startEvent.output;if(skip!=='makehtml.heading.setext'){text=showdown.subParser('makehtml.heading.setext')(text,options,globals);}if(skip!=='makehtml.heading.atx'){text=showdown.subParser('makehtml.heading.atx')(text,options,globals);}// Do Horizontal Rules:
if(skip!=='makehtml.horizontalRule'){text=showdown.subParser('makehtml.horizontalRule')(text,options,globals);}text=showdown.subParser('makehtml.list')(text,options,globals);// In CommonMark container mode the converter-level githubCodeBlock pass only claims
// indent-0 fences; list items own their indented fences (renderItem already ran
// githubCodeBlock on the de-indented content). This pass picks up genuinely top-level
// indented (1-3) fences here, after the list parser has claimed item content and before
// codeBlock could mistake their content lines for indented code.
if(options.cmSpec){text=showdown.subParser('makehtml.githubCodeBlock')(text,options,globals);}text=showdown.subParser('makehtml.codeBlock')(text,options,globals);text=showdown.subParser('makehtml.table')(text,options,globals);text=showdown.subParser('makehtml.blockquote')(text,options,globals);// The block quote container can reveal top-level content that the earlier codeBlock pass
// never saw (e.g. an indented-code line that followed a `>` line and so was not yet at the
// start of a block). Re-run codeBlock in container mode so that revealed indented code is
// recognized instead of falling through to a paragraph.
if(options.cmSpec){text=showdown.subParser('makehtml.codeBlock')(text,options,globals);}// We already ran _HashHTMLBlocks() before, in Markdown(), but that
// was to escape raw HTML in the original Markdown source. This time,
// we're escaping the markup we've just created, so that we don't wrap
// <p> tags around block-level tags.
text=showdown.subParser('makehtml.hashHTMLBlocks')(text,options,globals);var afterEvent=new showdown.Event('makehtml.blockGamut.onEnd',text);afterEvent.setOutput(text)._setGlobals(globals)._setOptions(options);afterEvent=globals.converter.dispatch(afterEvent);return afterEvent.output;});////
// makehtml/blockquote.js
// Copyright (c) 2018 ShowdownJS
//
// Transforms MD blockquotes into `<blockquote>` html entities
//
// Markdown uses email-style > characters for blockquoting.
// Markdown allows you to be lazy and only put the > before the first line of a hard-wrapped paragraph but
// it looks best if the text is hard wrapped with a > before every line.
//
// ***Author:***
// - Estêvão Soares dos Santos (Tivie) <https://github.com/tivie>
////
showdown.subParser('makehtml.blockquote',function(text,options,globals){'use strict';// Pathologically nested block quotes (e.g. `> > > …` thousands deep) recurse once per
// level through blockGamut, which exhausts the call stack in the default path and blows
// up super-linearly under cmSpec. Refuse to descend past a sane nesting depth; beyond it
// the surplus `>` markers simply render as literal text. The deepest nesting in the whole
// test corpus is 3, so this bound never affects real documents.
var maxNestingDepth=25;if((globals.blockquoteDepth||0)>=maxNestingDepth){return text;}var startEvent=new showdown.Event('makehtml.blockquote.onStart',text);startEvent.setOutput(text)._setGlobals(globals)._setOptions(options);startEvent=globals.converter.dispatch(startEvent);text=startEvent.output;if(options.cmSpec){text=parseCmBlockquotes(text);}else{var pattern=/(^ {0,3}>[ \t]?.+\n(.+\n)*\n*)+/gm;if(options.splitAdjacentBlockquotes){pattern=/^ {0,3}>[\s\S]*?\n\n/gm;}text=text.replace(pattern,function(bq){var wholeMatch=bq;bq=bq.replace(/^[ \t]*>[ \t]?/gm,'');// trim one level of quoting
bq=bq.replace(/^[ \t]+$/gm,'');// trim whitespace-only lines
return renderBlockquote(bq,wholeMatch,pattern);});}var afterEvent=new showdown.Event('makehtml.blockquote.onEnd',text);afterEvent.setOutput(text)._setGlobals(globals)._setOptions(options);afterEvent=globals.converter.dispatch(afterEvent);return afterEvent.output;/**
   * Render one block quote's already-stripped content into a hashed
   * `<blockquote>` element, dispatching the capture/hash lifecycle events.
   * @param {string} bq the block-quote content with markers already removed
   * @param {string} wholeMatch the original matched text (for the capture event)
   * @param {RegExp|null} pattern the matching pattern (event metadata only)
   * @returns {string}
   */function renderBlockquote(bq,wholeMatch,pattern){var otp,attributes;var captureStartEvent=new showdown.Event('makehtml.blockquote.onCapture',bq);captureStartEvent.setOutput(null)._setGlobals(globals)._setOptions(options).setRegexp(pattern||null).setMatches({_wholeMatch:wholeMatch,blockquote:bq}).setAttributes({});captureStartEvent=globals.converter.dispatch(captureStartEvent);// if something was passed as output, it takes precedence
// and will be used as output
if(captureStartEvent.output&&captureStartEvent.output!==''){otp=captureStartEvent.output;}else{bq=captureStartEvent.matches.blockquote;bq=showdown.subParser('makehtml.githubCodeBlock')(bq,options,globals);// CommonMark container mode: run the source-level leaf-block passes on the quote's
// own (marker-stripped) content, mirroring the converter pipeline, so an HTML block
// or a link reference definition nested inside the block quote is recognized in the
// quote's context instead of only at the top level.
if(options.cmSpec){bq=showdown.subParser('makehtml.hashHTMLBlocks')(bq,options,globals,true);bq=showdown.subParser('makehtml.stripLinkDefinitions')(bq,options,globals);}globals.blockquoteDepth=(globals.blockquoteDepth||0)+1;bq=showdown.subParser('makehtml.blockGamut')(bq,options,globals);// recurse
bq=showdown.subParser('makehtml.paragraphs')(bq,options,globals);globals.blockquoteDepth--;bq=bq.replace(/(^|\n)/g,'$1  ');// These leading spaces screw with <pre> content, so we need to fix that:
bq=bq.replace(/(\s*<pre>[^\r]+?<\/pre>)/gm,function(wm,m1){return m1.replace(/^ {2}/mg,'');});attributes=captureStartEvent.attributes;otp='<blockquote'+showdown.helper._populateAttributes(attributes)+'>\n'+bq+'\n</blockquote>';}var beforeHashEvent=new showdown.Event('makehtml.blockquote.onHash',otp);beforeHashEvent.setOutput(otp)._setGlobals(globals)._setOptions(options);beforeHashEvent=globals.converter.dispatch(beforeHashEvent);otp=beforeHashEvent.output;return showdown.subParser('makehtml.hashBlock')(otp,options,globals);}/**
   * CommonMark block-quote container parsing (spec section 5.1). Scans line by
   * line: a block quote consumes consecutive lines that carry the `>` marker
   * (an optional single space after `>` is stripped), plus lazy paragraph
   * continuation lines (markerless non-blank text that follows a paragraph line).
   * A blank line, or a markerless line that cannot lazily continue a paragraph,
   * ends the block quote; two block quotes separated by a blank line are distinct.
   * @param {string} str
   * @returns {string}
   */function parseCmBlockquotes(str){var marker=/^ {0,3}>/,lines=str.split('\n'),out=[],i=0,n=lines.length;// a markerless line that can lazily continue a paragraph: non-blank text that
// does not itself begin a block which would interrupt the paragraph
function isLazyParagraph(line){if(line.trim()===''){return false;}return!(marker.test(line)||// block quote
/^ {0,3}#{1,6}(?:[ \t]|$)/.test(line)||// ATX heading
/^ {0,3}(?:```|~~~)/.test(line)||// fenced code
/^ {0,3}([-*_])[ \t]*(?:\1[ \t]*){2,}$/.test(line)||// thematic break
/^ {0,3}(?:[-+*]|\d{1,9}[.)])[ \t]/.test(line)// list item
);}// true if, after peeling nested block-quote/list markers, the innermost block
// on this line is an open paragraph - so a following markerless line may lazily
// continue it (laziness propagates through container nesting)
function isParagraphLine(stripped){var s=stripped,indentedCode=/^ {4}/.test(stripped),m;while(m=s.match(/^ {0,3}>[ \t]?/)||s.match(/^ {0,3}(?:[-+*]|\d{1,9}[.)])[ \t]/)){s=s.slice(m[0].length);indentedCode=false;// leading spaces after a marker are container indent
}if(s.trim()===''||indentedCode){return false;}return!(/^ {0,3}(?:```|~~~)/.test(s)||/^ {0,3}#{1,6}(?:[ \t]|$)/.test(s)||/^ {0,3}([-*_])[ \t]*(?:\1[ \t]*){2,}$/.test(s));}while(i<n){if(!marker.test(lines[i])){out.push(lines[i]);i++;continue;}var bqLines=[],prevParagraph=false;while(i<n){var line=lines[i];if(marker.test(line)){var stripped=line.replace(/^ {0,3}>[ \t]?/,'');bqLines.push(stripped);prevParagraph=isParagraphLine(stripped);i++;}else if(prevParagraph&&isLazyParagraph(line)){// CommonMark: a setext underline may not be a lazy continuation line. Escape the
// leading marker so the recursive setext parser can't claim it as an underline;
// encodeBackslashEscapes restores the literal `=`/`-` downstream.
if(/^ {0,3}(?:=+|-+)[ \t]*$/.test(line)){line=line.replace(/^( {0,3})([=-])/,'$1\\$2');}bqLines.push(line);prevParagraph=isParagraphLine(line);i++;}else{break;// blank line or non-continuing line ends the block quote
}}// trailing newline mirrors the original regex match (which ended in \n) so the
// recursive block parsers see a complete final line
var content=bqLines.join('\n')+'\n';out.push(renderBlockquote(content,content,null));}return out.join('\n');}});////
// makehtml/cmInline.js
// Copyright (c) 2024 ShowdownJS
//
// Unified CommonMark inline parser (spec section 6).
//
// Invoked only when the `commonmarkInline` option is enabled. It resolves code
// spans, backslash escapes, character references, autolinks, raw HTML, links,
// images and emphasis together on a single delimiter stack, so the cross-construct
// precedence rules CommonMark requires (a link cannot contain a link; code spans /
// autolinks / raw HTML bind before links; emphasis interleaves with link brackets)
// are expressible - which the sequential per-construct passes could not.
//
// It is built on the same doubly-linked node list + delimiter stack + processEmphasis
// design used by makehtml.emphasisAndStrong's CommonMark path.
//
// ***Author:***
// - Estêvão Soares dos Santos (Tivie) <https://github.com/tivie>
////
/* jshint esnext: false, esversion: 9 */showdown.subParser('makehtml.cmInline',function(text,options,globals){'use strict';// ASCII punctuation, used by the flanking rules and backslash-escape check.
// Declared before parseCmInline runs (const is in the temporal dead zone until here).
var asciiPunct=/[!-/:-@[-`{-~]/;// Sticky regexes anchored at the scan cursor (lastIndex) so the recognizers never
// slice the tail of the string - keeps the tokenizer linear on `<`/`&`-heavy input.
// Reused across calls; each recognizer sets lastIndex before exec and the parse is
// not re-entrant within a single string.
var reEntity=new RegExp("&(?:#[0-9]{1,7}|#[xX][0-9a-fA-F]{1,6}|[a-zA-Z][a-zA-Z0-9]*);","y");// eslint-disable-next-line no-control-regex -- CommonMark autolinks exclude control chars (\x00-\x20) per spec
var reAutoUri=new RegExp("<[A-Za-z][A-Za-z0-9+.-]{1,31}:[^<>\\x00-\\x20]*>","y");// Showdown extension: <www...> angle autolinks (cmark-gfm does not autolink these, but the
// explicit <> is unambiguous user intent). Single variable class → linear / ReDoS-safe.
// eslint-disable-next-line no-control-regex -- same control-char exclusion as reAutoUri
var reAutoWww=new RegExp("<www\\.[^<>\\x00-\\x20]+>","y");var reAutoEmail=new RegExp("<[a-zA-Z0-9.!#$%&'*+/=?^_`{|}~-]+@[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?(?:\\.[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?)*>","y");var reRawHtml=new RegExp('(?:'+showdown.helper.regexes.cmHTMLTagSource+')','y');var startEvent=new showdown.Event('makehtml.cmInline.onStart',text);startEvent.setOutput(text)._setGlobals(globals)._setOptions(options);startEvent=globals.converter.dispatch(startEvent);text=startEvent.output;text=parseCmInline(text);// ghMentions and simplifiedAutoLink (GFM extensions) are applied to the serialized
// inline output and to emphasis inner content (see parseCmInline) so URLs and mentions
// inside emphasis are linked too. Real links, images and code spans are hashed by
// parseCmInline, so they are protected from these regexes. Copied from link.js;
// the writeAnchorTag / parseMail helpers below are too. TODO: share with link.js.
text=applyGfmInlineLinks(text);function applyGfmInlineLinks(text){// 5. Handle GithubMentions (if option is enabled)
if(options.ghMentions){var ghMentionsRegex=/(^|\s)(\\)?(@([a-z\d]+(?:[a-z\d._-]+?[a-z\d]+)*))/gi;text=text.replace(ghMentionsRegex,function(wholeMatch,st,escape,mentions,username){// bail if the mentions was escaped
if(escape==='\\'){return st+mentions;}// check if options.ghMentionsLink is a string
// TODO Validation should be done at initialization not at runtime
if(!showdown.helper.isString(options.ghMentionsLink)){throw new Error('ghMentionsLink option must be a string');}var url=options.ghMentionsLink.replace(/\{u}/g,username);return st+writeAnchorTag('reference',ghMentionsRegex,wholeMatch,mentions,null,url);});}// 8. Handle naked links (if option is enabled)
if(options.simplifiedAutoLink){// 8.1. Check for naked URLs
// we also include leading markdown magic chars [_*~] for cases like __https://www.google.com/foobar__
// An explicit scheme (http/https/ftp) does not require the host to contain a dot;
// a `www.` shortcut does (and is domain-validated below).
var nakedUrlRegex=/([_*~]*?)((?:(?:https?|ftp):\/\/[^\s<>"'`´]+|www\.[^\s<>"'`´.-][^\s<>"'`´]*?\.[a-z\d.]+[^\s<>"']*))\1/gi;text=text.replace(nakedUrlRegex,function(wholeMatch,leadingMDChars,url,offset,fullText){var isWww=/^www\./i.test(url);// GFM boundary rule: a "www." autolink (unlike a scheme URL) is not recognized when
// preceded by "<". By this pass "<" has been escaped to "&lt;", so a www match sitting
// right after it (e.g. the interior of a malformed <www.x.com foo> the angle recognizer
// could not consume) is left untouched — matching cmark-gfm, which links <https://x bim>
// but not <www.x bim>.
var urlStart=offset+leadingMDChars.length;if(isWww&&fullText.substring(urlStart-4,urlStart)==='&lt;'){return wholeMatch;}// we now will start traversing the url from the front to back, looking for punctuation chars [_*~,;:.!?\)\]]
var len=url.length;var suffix='';for(var i=len-1;i>=0;--i){var char=url.charAt(i);if(/[_*~,;:.!?]/.test(char)){// it's a punctuation char so we remove it from the url
url=url.slice(0,-1);// and prepend it to the suffix
suffix=char+suffix;}else if(/[)\]]/.test(char)){// it's a parenthesis so we need to check for "balance" (kinda)
var opPar=void 0,clPar=void 0;if(/\)/.test(char)){// it's a curved parenthesis
opPar=url.match(/\(/g)||[];clPar=url.match(/\)/g);}else{// it's a squared parenthesis
opPar=url.match(/\[/g)||[];clPar=url.match(/]/g);}if(opPar.length<clPar.length){// there are more closing Parenthesis than opening so chop it!!!!!
url=url.slice(0,-1);// and prepend it to the suffix
suffix=char+suffix;}else{// it's (kinda) balanced so our work is done
break;}}else{// it's not a punctuation or a parenthesis so our work is done
break;}}// GFM: a trailing ";" that completes an entity-reference-like "&name" is excluded
// from the link, so move the whole "&name;" into the suffix.
if(suffix.charAt(0)===';'){var entity=url.match(/&(?:amp;)?[a-z\d]+$/i);if(entity){url=url.slice(0,-entity[0].length);suffix=entity[0]+suffix;}}// GFM: "<" terminates the link. By this pass it has already been escaped to "&lt;"
// (and ">" to "&gt;"), so split there and keep the remainder as plain text.
var ltMatch=url.match(/&(?:lt|gt);/);if(ltMatch){var at=url.indexOf(ltMatch[0]);suffix=url.slice(at)+suffix;url=url.slice(0,at);}// GFM: the last two labels of the host may not contain "_"; otherwise it is not a
// valid autolink.
if(!validAutolinkHost(url,isWww)){return wholeMatch;}// we copy the treated url to the text variable
var txt=url;// finally, if it's a www shortcut, we prepend http(s)
// noinspection HttpUrlsUsage
url=isWww?(options.httpsAutoLinks?'https://':'http://')+url:url;// GFM: percent-encode non-ASCII characters in the href (the display text keeps the
// literal characters)
// eslint-disable-next-line no-control-regex -- \x00-\x7F is the ASCII range
url=url.replace(/[^\x00-\x7F]+/g,function(s){return encodeURI(s);});// url part is done so let's take care of text now
// we need to escape the text (because of links such as www.example.com/foo__bar__baz)
txt=txt.replace(showdown.helper.regexes.asteriskDashTildeAndColon,showdown.helper.escapeCharactersCallback);// and return the link tag, with the leadingMDChars and  suffix. The leadingMDChars are added at the end too because
// we consumed those characters in the regexp
return leadingMDChars+writeAnchorTag('autoLink',nakedUrlRegex,wholeMatch,txt,null,url)+suffix+leadingMDChars;});// 8.2. Check for naked mail (GFM extended email autolink).
var localPart='[A-Za-z\\d._+-]+',domainPart='[A-Za-z\\d_-]+(?:\\.[A-Za-z\\d_-]+)*';// A scheme is only recognised when it is not part of a preceding word (so `mmmmailto:`
// does not count) — any non-alphanumeric character (including `/`) is a valid boundary.
var schemeBoundary='(^|[^A-Za-z\\d])';// 8.2.1. `xmpp:` addresses keep their scheme and an optional `/resource`.
var xmppMailRegex=new RegExp(schemeBoundary+'(xmpp:)('+localPart+'@'+domainPart+')(\\/[A-Za-z\\d._-]*)?','gi');text=text.replace(xmppMailRegex,function(wholeMatch,lead,scheme,addr,resource){resource=resource||'';var trail='',body=resource||addr;while(/[.,;:!?]$/.test(body)){trail=body.slice(-1)+trail;body=body.slice(0,-1);}if(resource){resource=body;}else{addr=body;}if(!validMailAddr(addr)){return wholeMatch;}var target='xmpp:'+addr+resource;return lead+writeAnchorTag('autoLink',xmppMailRegex,wholeMatch,target,null,target)+trail;});// 8.2.2. `mailto:` addresses keep their scheme but never carry a path.
var mailtoRegex=new RegExp(schemeBoundary+'(mailto:)('+localPart+'@'+domainPart+')','gi');text=text.replace(mailtoRegex,function(wholeMatch,lead,scheme,addr){var trail='';while(/[.,;:!?]$/.test(addr)){trail=addr.slice(-1)+trail;addr=addr.slice(0,-1);}if(!validMailAddr(addr)){return wholeMatch;}var target='mailto:'+addr;return lead+writeAnchorTag('autoLink',mailtoRegex,wholeMatch,target,null,target)+trail;});// 8.2.3. Bare addresses become mailto: links. The address must be preceded by the
// string start or a character that cannot be part of the local-part (this also keeps
// us out of hash placeholders like the `¨E43E` produced for an escaped char).
var nakedMailRegex=new RegExp("(^|[^A-Za-z\\d._+\\-\\u00a8])("+localPart+'@'+domainPart+')','g');text=text.replace(nakedMailRegex,function(wholeMatch,lead,addr){if(!validMailAddr(addr)){return wholeMatch;}var m=parseMail(addr);return lead+writeAnchorTag('autoLink',nakedMailRegex,wholeMatch,m.mail,null,m.url);});}return text;}var afterEvent=new showdown.Event('makehtml.cmInline.onEnd',text);afterEvent.setOutput(text)._setGlobals(globals)._setOptions(options);afterEvent=globals.converter.dispatch(afterEvent);return afterEvent.output;// ---- shared character helpers (flanking rules) -------------------------------
function isPunct(ch){return ch!==undefined&&(asciiPunct.test(ch)||/(?:[!-\/:-@\[-`\{-~\xA1-\xA9\xAB\xAC\xAE-\xB1\xB4\xB6-\xB8\xBB\xBF\xD7\xF7\u02C2-\u02C5\u02D2-\u02DF\u02E5-\u02EB\u02ED\u02EF-\u02FF\u0375\u037E\u0384\u0385\u0387\u03F6\u0482\u055A-\u055F\u0589\u058A\u058D-\u058F\u05BE\u05C0\u05C3\u05C6\u05F3\u05F4\u0606-\u060F\u061B\u061D-\u061F\u066A-\u066D\u06D4\u06DE\u06E9\u06FD\u06FE\u0700-\u070D\u07F6-\u07F9\u07FE\u07FF\u0830-\u083E\u085E\u0888\u0964\u0965\u0970\u09F2\u09F3\u09FA\u09FB\u09FD\u0A76\u0AF0\u0AF1\u0B70\u0BF3-\u0BFA\u0C77\u0C7F\u0C84\u0D4F\u0D79\u0DF4\u0E3F\u0E4F\u0E5A\u0E5B\u0F01-\u0F17\u0F1A-\

\uFBD2\uFD3E-\uFD4F\uFD90\uFD91\uFDC8-\uFDCF\uFDFC-\uFDFF\uFE10-\uFE19\uFE30-\uFE52\uFE54-\uFE66\uFE68-\uFE6B\uFF01-\uFF0F

}function isWhitespace(ch){return ch===undefined||/\s/.test(ch)||/[ \xA0\u1680\u2000-\u200A\u2028\u2029\u202F\u205F\u3000]/.test(ch);}function isEscapable(ch){return ch!==undefined&&asciiPunct.test(ch);}// HTML-escape a run of plain text (entities already resolved to characters)
function escapeText(str){return str.replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;');}function hashSpan(html){return showdown.helper._hashHTMLSpan(html,globals);}// resolve backslash escapes of ASCII punctuation to the literal character
function resolveBackslash(str){return str.replace(/\\([!-/:-@[-`{-~])/g,'$1');}// ---- main parse --------------------------------------------------------------
function parseCmInline(str){var head=null,tail=null,delimiters=null,// top of emphasis delimiter stack
brackets=null,// top of bracket (link/image) stack
// backtick runs of these lengths have no closer in the rest of the string;
// future opens of the same length fail immediately (keeps backticks linear)
backtickNoCloser={};function appendNode(node){node.prev=tail;node.next=null;node.raw=node.raw||false;if(tail){tail.next=node;}else{head=node;}tail=node;return node;}function appendText(literal){return appendNode({type:'text',literal:literal});}// append already-final HTML that must not be escaped at render time
function appendRaw(html){return appendNode({type:'text',literal:html,raw:true});}var len=str.length;var i=0;while(i<len){var ch=str.charAt(i);if(ch==='\n'){// hard line break: two+ trailing spaces or a backslash before the newline.
// The <br /> is hashed so the later encodeAmpsAndAngles pass leaves it intact.
var n=tail;if(n&&n.type==='text'&&!n.raw&&/ {2,}$/.test(n.literal)){n.literal=n.literal.replace(/ +$/,'');appendRaw(hashSpan('<br />')+'\n');}else if(n&&n.type==='text'&&!n.raw&&/\\$/.test(n.literal)){n.literal=n.literal.slice(0,-1);appendRaw(hashSpan('<br />')+'\n');}else{if(n&&n.type==='text'&&!n.raw){n.literal=n.literal.replace(/ +$/,'');}appendText('\n');}i++;continue;}if(ch==='\\'){var next=str.charAt(i+1);if(next==='\n'){appendRaw(hashSpan('<br />')+'\n');i+=2;}else if(next==='¨'&&str.charAt(i+2)==='D'){// escaped `$` (the converter hashes `$` to the `¨D` placeholder early)
appendText('¨D');i+=3;}else if(isEscapable(next)){// Emit ordinary escaped punctuation as a Showdown escape placeholder (¨E<code>E)
// so the later passes (ghMentions, simplifiedAutoLink, emoji, strikethrough,
// ellipsis, ...) don't treat the char as markup - e.g. `\@user` must not become a
// mention. unescapeSpecialChars restores the literal char at the end of the
// pipeline. HTML-special chars stay literal so escapeText turns them into entities
// (&amp; &lt; &gt; &quot;); placeholders would otherwise round-trip to raw `<`/`&`.
if(next==='&'||next==='<'||next==='>'||next==='"'){appendText(next);}else{appendText('¨E'+next.charCodeAt(0)+'E');}i+=2;}else{appendText('\\');i++;}continue;}if(ch==='`'){var res=parseBacktick(str,i,backtickNoCloser);if(res){appendRaw(res.html);i=res.end;}else{var e=skipRun(str,i,'`');appendText(str.slice(i,e));i=e;}continue;}if(ch==='<'){var _res=parseAutolink(str,i)||parseRawHTML(str,i);if(_res){appendRaw(_res.html);i=_res.end;continue;}appendText('<');i++;continue;}if(ch==='&'){reEntity.lastIndex=i;var m=reEntity.exec(str);if(m){var decoded=showdown.helper.cmDecodeEntities(m[0]);if(decoded!==m[0]){appendText(decoded);i+=m[0].length;continue;}}appendText('&');i++;continue;}if(ch==='!'&&str.charAt(i+1)==='['){var node=appendText('![');pushBracket(node,true,i+2);i+=2;continue;}if(ch==='['){var _node=appendText('[');pushBracket(_node,false,i+1);i++;continue;}if(ch===']'){i=parseCloseBracket(str,i);continue;}if(ch==='*'||ch==='_'){var _start=i;while(i<len&&str.charAt(i)===ch){++i;}var run=str.slice(_start,i),before=_start===0?undefined:str.charAt(_start-1),after=i>=len?undefined:str.charAt(i);// `$` and `¨` are swapped for the two-char placeholders `¨D`/`¨T` before inline
// parsing (see converter.js) and only restored at the end. Resolve them here so
// flanking sees the real adjacent character rather than the placeholder's
// trailing/leading letter (e.g. the `D` of `¨D` would otherwise read as a letter).
if((before==='D'||before==='T')&&str.charAt(_start-2)==='¨'){before=before==='D'?'$':'¨';}if(after==='¨'&&(str.charAt(i+1)==='D'||str.charAt(i+1)==='T')){after=str.charAt(i+1)==='D'?'$':'¨';}var beforeWs=isWhitespace(before),afterWs=isWhitespace(after),beforePt=isPunct(before),afterPt=isPunct(after),leftFlanking=!afterWs&&(!afterPt||beforeWs||beforePt),rightFlanking=!beforeWs&&(!beforePt||afterWs||afterPt),canOpen=void 0,canClose=void 0;if(ch==='_'){canOpen=leftFlanking&&(!rightFlanking||beforePt);canClose=rightFlanking&&(!leftFlanking||afterPt);}else{canOpen=leftFlanking;canClose=rightFlanking;}var _node2=appendNode({type:'delim',cc:ch,literal:run,numdelims:run.length,origdelims:run.length,canOpen:canOpen,canClose:canClose});_node2.delimPrev=delimiters;_node2.delimNext=null;if(delimiters){delimiters.delimNext=_node2;}delimiters=_node2;continue;}// plain text run up to the next significant char
var start=i;while(i<len&&'\n\\`<&![]*_'.indexOf(str.charAt(i))===-1){++i;}if(i===start){appendText(str.charAt(i));i++;}else{appendText(str.slice(start,i));}}processEmphasis(null);// render the node list
var out='';for(var _n=head;_n!==null;_n=_n.next){out+=_n.raw?_n.literal:escapeText(_n.literal);}return out;// ---- bracket stack ---------------------------------------------------------
function pushBracket(node,image,sourceStart){brackets={node:node,prev:brackets,prevDelim:delimiters,image:image,active:true,sourceStart:sourceStart// index in `str` where the label text begins
};}function parseCloseBracket(s,idx){var opener=brackets;if(opener===null){appendText(']');return idx+1;}if(!opener.active){brackets=opener.prev;appendText(']');return idx+1;}// try to parse the destination/title or a reference that follows the `]`
var dest=null,title=null,width=null,height=null,matched=false,endIdx=idx+1;if(s.charAt(idx+1)==='('){var j=idx+2,n2=s.length,isWs=function isWs(c){return c===' '||c==='\t'||c==='\n';};while(j<n2&&isWs(s.charAt(j))){j++;}var d=showdown.helper.cmScanDestination(s,j);if(d){j=d.end;// parseImgDimensions (Showdown extension, not CommonMark): an optional
// ` =WxH` between destination and title. The `=WxH` is always consumed here so
// it never leaks into the output; buildImage only renders it when the option is
// on. Regex fragment copied from the inline image regex in image.js.
var dimStart=j;while(dimStart<n2&&isWs(s.charAt(dimStart))){dimStart++;}if(dimStart>j&&s.charAt(dimStart)==='='){var dim=/^=([*\d]+[A-Za-z%]{0,4})x([*\d]+[A-Za-z%]{0,4})/.exec(s.slice(dimStart));if(dim){width=dim[1];height=dim[2];j=dimStart+dim[0].length;}}var hadWs=false;while(j<n2&&isWs(s.charAt(j))){hadWs=true;j++;}var tc=s.charAt(j),t=null;if(hadWs&&(tc==='"'||tc==='\''||tc==='(')){t=showdown.helper.cmScanTitle(s,j);if(t){j=t.end;}}while(j<n2&&isWs(s.charAt(j))){j++;}if(s.charAt(j)===')'){dest=d.url;title=t?t.title:null;matched=true;endIdx=j+1;}}}if(!matched){// reference: full [label], collapsed [] or shortcut. Use the RAW source label
// (backslash escapes intact) - CommonMark matches labels by case-fold +
// whitespace only, so `[foo\!]` must not match a `[foo!]` definition.
var labelText=s.slice(opener.sourceStart,idx),refKey=null;if(s.charAt(idx+1)==='['){var close=findRefClose(s,idx+2);if(close!==-1){var inner=s.slice(idx+2,close);refKey=inner.trim()===''?labelText:inner;endIdx=close+1;}}else{refKey=labelText;// shortcut
endIdx=idx+1;}if(refKey!==null){var key=showdown.helper.cmNormalizeLabel(refKey);if(key!==''&&!showdown.helper.isUndefined(globals.gUrls[key])){dest=globals.gUrls[key];title=globals.gTitles[key];// parseImgDimensions: reference-style dimensions stored by stripLinkDefinitions
if(globals.gDimensions[key]){width=globals.gDimensions[key].width;height=globals.gDimensions[key].height;}matched=true;}}}if(!matched){brackets=opener.prev;appendText(']');return idx+1;}// process emphasis on the delimiters inside the brackets
processEmphasis(opener.prevDelim);// collect and render the inner nodes
var innerHTML=renderNodes(opener.node.next,null);var otpHTML;if(opener.image){otpHTML=buildImage(innerHTML,dest,title,width,height);}else{otpHTML=buildLink(innerHTML,dest,title);}// drop the opener node and everything after it, append the built span
removeFrom(opener.node);appendRaw(otpHTML);// remove any emphasis delimiters that belonged to the consumed range
pruneDelimiters(opener.prevDelim);if(!opener.image){// a link cannot be nested in another link
for(var b=opener.prev;b!==null;b=b.prev){if(!b.image){b.active=false;}}}brackets=opener.prev;return endIdx;}// ---- rendering helpers -----------------------------------------------------
// concatenate the rendered HTML of nodes [from .. to)
function renderNodes(from,to){var out='';for(var _n2=from;_n2!==null&&_n2!==to;_n2=_n2.next){out+=_n2.raw?_n2.literal:escapeText(_n2.literal);}return out;}function removeFrom(node){tail=node.prev;if(tail){tail.next=null;}else{head=null;}}function normalizeDest(dest){dest=resolveBackslash(dest);dest=showdown.helper.applyBaseUrl(options.relativePathBaseUrl,dest);return showdown.helper.cmNormalizeURL(dest);}function buildTitleAttr(title){if(title===null||title===undefined){return'';}return' title="'+showdown.helper.cmEscapeTitle(resolveBackslash(title))+'"';}function buildLink(innerHTML,dest,title){// safeMode: neutralize dangerous URL schemes (javascript:, vbscript:, data:, ...)
var href=options.safeMode&&!showdown.helper.isSafeUrl(dest)?'':normalizeDest(dest);var attrs=' href="'+href+'"'+buildTitleAttr(title);innerHTML=showdown.subParser('makehtml.hardLineBreaks')(innerHTML,options,globals);return hashSpan('<a'+attrs+'>'+innerHTML+'</a>');}function buildImage(innerHTML,dest,title,width,height){// alt text is the plain-text rendering of the label (markup stripped); the inner
// spans are hashed, so restore them before flattening
var alt=showdown.subParser('makehtml.unhashHTMLSpans')(innerHTML,options,globals).replace(/<img\b[^>]*?\salt="([^"]*)"[^>]*?\/?>/g,'$1').replace(/<[^>]*>/g,'');// safeMode: neutralize dangerous URL schemes; data:image/* stays allowed
var src=options.safeMode&&!showdown.helper.isSafeUrl(dest,{allowDataImage:true})?'':normalizeDest(dest);var attrs=' src="'+src+'" alt="'+alt+'"'+buildTitleAttr(title);// width/height gating copied from writeImageTag in image.js (parseImgDimensions)
if(options.parseImgDimensions){if(width){attrs+=' width="'+(width==='*'?'auto':width)+'"';}if(height){attrs+=' height="'+(height==='*'?'auto':height)+'"';}}return hashSpan('<img'+attrs+' />');}// ---- emphasis (CommonMark reference algorithm) -----------------------------
function removeDelimiter(d){if(d.delimPrev){d.delimPrev.delimNext=d.delimNext;}if(d.delimNext){d.delimNext.delimPrev=d.delimPrev;}else{delimiters=d.delimPrev;}}function insertAfter(node,newNode){newNode.prev=node;newNode.next=node.next;if(node.next){node.next.prev=newNode;}else{tail=newNode;}node.next=newNode;}// drop every delimiter at or above `bottom` from the stack (after a bracket span
// has consumed the nodes they pointed at)
function pruneDelimiters(bottom){var d=delimiters;while(d!==null&&d!==bottom){var p=d.delimPrev;removeDelimiter(d);d=p;}}function processEmphasis(stackBottom){var openersBottom={'_':[stackBottom,stackBottom,stackBottom],'*':[stackBottom,stackBottom,stackBottom]};var closer=delimiters;while(closer!==null&&closer.delimPrev!==null&&closer.delimPrev!==stackBottom){closer=closer.delimPrev;}if(closer===stackBottom){closer=stackBottom===null?closer:stackBottom.delimNext;}while(closer!==null){if(!closer.canClose){closer=closer.delimNext;continue;}var opener=closer.delimPrev,openerFound=false,oddMatch=void 0;while(opener!==null&&opener!==stackBottom&&opener!==openersBottom[closer.cc][closer.origdelims%3]){oddMatch=(closer.canOpen||opener.canClose)&&closer.origdelims%3!==0&&(opener.origdelims+closer.origdelims)%3===0;if(opener.cc===closer.cc&&opener.canOpen&&!oddMatch){openerFound=true;break;}opener=opener.delimPrev;}var oldCloser=closer;if(openerFound){var use=opener.numdelims>=2&&closer.numdelims>=2?2:1,tagOpen=use===2?'<strong>':'<em>',tagClose=use===2?'</strong>':'</em>';opener.literal=opener.literal.slice(0,opener.literal.length-use);opener.numdelims-=use;closer.literal=closer.literal.slice(use);closer.numdelims-=use;var inner=renderNodes(opener.next,closer);inner=applyGfmInlineLinks(inner);inner=showdown.subParser('makehtml.hardLineBreaks')(inner,options,globals);var wrapped=hashSpan(tagOpen+inner+tagClose);var n2=opener.next;while(n2!==null&&n2!==closer){var nx=n2.next;if(n2.type==='delim'){removeDelimiter(n2);}n2=nx;}var wrapNode={type:'text',literal:wrapped,raw:true};insertAfter(opener,wrapNode);wrapNode.next=closer;closer.prev=wrapNode;if(opener.numdelims===0){opener.literal='';removeDelimiter(opener);}if(closer.numdelims===0){closer.literal='';var tmp=closer.delimNext;removeDelimiter(closer);closer=tmp;}}else{openersBottom[oldCloser.cc][oldCloser.origdelims%3]=oldCloser.delimPrev;if(!oldCloser.canOpen){removeDelimiter(oldCloser);}closer=oldCloser.delimNext;}}}}// ---- token recognizers (return {html, end} or null) ----------------------------
function skipRun(str,i,ch){var j=i;while(j<str.length&&str.charAt(j)===ch){j++;}return j;}function parseBacktick(str,i,noCloser){var openEnd=skipRun(str,i,'`'),runLen=openEnd-i,n=str.length,j=openEnd;if(noCloser[runLen]){return null;}// find a closing run of backticks of exactly runLen (not part of a longer run)
while(j<n){if(str.charAt(j)==='`'){var runStart=j,runEnd=skipRun(str,j,'`');if(runEnd-runStart===runLen){var content=str.slice(openEnd,runStart).replace(/\n/g,' ');// strip exactly one leading and trailing space if the content is not all spaces
if(content.length>=2&&content.charAt(0)===' '&&content.charAt(content.length-1)===' '&&/[^ ]/.test(content)){content=content.slice(1,-1);}var encoded=showdown.subParser('makehtml.encodeCode')(content,options,globals);return{html:showdown.helper._hashHTMLSpan('<code>'+encoded+'</code>',globals),end:runEnd};}j=runEnd;}else{j++;}}noCloser[runLen]=true;// no closer of this length anywhere after here
return null;}function parseAutolink(str,i){if(!options.cmSpec){return null;}reAutoUri.lastIndex=i;var uri=reAutoUri.exec(str);if(uri){var raw=uri[0].slice(1,-1),href=showdown.helper.cmEncodeURI(raw).replace(/&/g,'&amp;');// safeMode: neutralize dangerous autolink schemes but keep the visible text
if(options.safeMode&&!showdown.helper.isSafeUrl(raw)){href='';}return{html:showdown.helper._hashHTMLSpan('<a href="'+href+'">'+escapeAngles(raw)+'</a>',globals),end:i+uri[0].length};}reAutoEmail.lastIndex=i;var email=reAutoEmail.exec(str);if(email){var _raw=email[0].slice(1,-1),_href,txt;// encodeEmails: entity-encode the address (href and text) when the option is on,
// so it works under cmSpec too. Mirrors parseMail in link.js (copied behavior).
if(options.encodeEmails){_href=showdown.helper.encodeEmailAddress('mailto:'+_raw);txt=showdown.helper.encodeEmailAddress(_raw);}else{_href='mailto:'+escapeAngles(_raw);txt=escapeAngles(_raw);}return{html:showdown.helper._hashHTMLSpan('<a href="'+_href+'">'+txt+'</a>',globals),end:i+email[0].length};}// Showdown extension: <www...> angle autolink. Applies the GFM www rule (http(s)://
// prepend + domain validation) to the explicitly delimited form. Active whenever
// cmSpec is on (the early `!options.cmSpec` guard above already scopes this).
reAutoWww.lastIndex=i;var www=reAutoWww.exec(str);if(www){var _raw2=www[0].slice(1,-1),host=_raw2.split(/[/?#]/)[0];// GFM www rule: the domain after "www." must contain a period, plus the shared host
// validation (>= 2 labels, last two labels have no "_").
if(host.slice(4).indexOf('.')!==-1&&validAutolinkHost(_raw2,true)){var full=(options.httpsAutoLinks?'https://':'http://')+_raw2,_href2=showdown.helper.cmEncodeURI(full).replace(/&/g,'&amp;');// safeMode: neutralize dangerous schemes but keep the visible text
if(options.safeMode&&!showdown.helper.isSafeUrl(full)){_href2='';}return{html:showdown.helper._hashHTMLSpan('<a href="'+_href2+'">'+escapeAngles(_raw2)+'</a>',globals),end:i+www[0].length};}}return null;}function escapeAngles(s){return s.replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;');}// ==== writeAnchorTag and parseMail copied verbatim from link.js ====
// Used by the copied ghMentions / simplifiedAutoLink post-passes above so they emit
// the exact same anchors as the legacy path. TODO: refactor to share with link.js.
function writeAnchorTag(subEvtName,pattern,wholeMatch,text,linkId,url,title,emptyCase){var matches={_wholeMatch:wholeMatch,_linkId:linkId,_url:url,_title:title,text:text},otp,attributes={};title=title||null;url=url||null;if(linkId){linkId=options.cmSpec?showdown.helper.cmNormalizeLabel(linkId):showdown.helper.caseFold(linkId);}else{linkId=null;}emptyCase=!!emptyCase;if(emptyCase){url='';}else if(!url){if(!linkId){// lower-case and turn embedded newlines into spaces
linkId=options.cmSpec?showdown.helper.cmNormalizeLabel(text):showdown.helper.caseFold(text).replace(/ ?\n/g,' ');}if(!showdown.helper.isUndefined(globals.gUrls[linkId])){url=globals.gUrls[linkId];if(!showdown.helper.isUndefined(globals.gTitles[linkId])){title=globals.gTitles[linkId];}}else{return wholeMatch;}}url=showdown.helper.applyBaseUrl(options.relativePathBaseUrl,url);// NOTE: deliberately diverges from the link.js original - we do NOT call
// cmNormalizeURL here even though cmSpec is on. These ghMentions/simplifiedAutoLink
// anchors are legacy GFM constructs; cmNormalizeURL would decode the entity-encoded
// mailto: href produced by encodeEmails, making the href/text inconsistent. Skipping
// it makes the output identical to the legacy (non-cmSpec) path.
url=url.replace(showdown.helper.regexes.asteriskDashTildeAndColon,showdown.helper.escapeCharactersCallback);attributes.href=url;if(title&&showdown.helper.isString(title)){if(options.cmSpec){title=showdown.helper.cmEscapeTitle(title);}else{title=title.replace(/"/g,'&quot;');}title=title.replace(showdown.helper.regexes.asteriskDashTildeAndColon,showdown.helper.escapeCharactersCallback);attributes.title=title;}var captureStartEvent=new showdown.Event('makehtml.link.'+subEvtName+'.onCapture',wholeMatch);captureStartEvent.setOutput(null)._setGlobals(globals)._setOptions(options).setRegexp(pattern).setMatches(matches).setAttributes(attributes);captureStartEvent=globals.converter.dispatch(captureStartEvent);// if something was passed as output, it takes precedence
// and will be used as output
if(captureStartEvent.output&&captureStartEvent.output!==''){otp=captureStartEvent.output;}else{attributes=captureStartEvent.attributes;text=captureStartEvent.matches.text||'';// Text can be a markdown element, so we run through the appropriate parsers
text=showdown.subParser('makehtml.codeSpan')(text,options,globals);text=showdown.subParser('makehtml.emoji')(text,options,globals);text=showdown.subParser('makehtml.underline')(text,options,globals);text=showdown.subParser('makehtml.emphasisAndStrong')(text,options,globals);text=showdown.subParser('makehtml.strikethrough')(text,options,globals);text=showdown.subParser('makehtml.ellipsis')(text,options,globals);text=showdown.subParser('makehtml.hashHTMLSpans')(text,options,globals);otp='<a'+showdown.helper._populateAttributes(attributes)+'>'+text+'</a>';}var beforeHashEvent=new showdown.Event('makehtml.link.'+subEvtName+'.onHash',otp);beforeHashEvent.setOutput(otp)._setGlobals(globals)._setOptions(options);beforeHashEvent=globals.converter.dispatch(beforeHashEvent);otp=beforeHashEvent.output;return showdown.subParser('makehtml.hashHTMLSpans')(otp,options,globals);}function parseMail(mail){var url='mailto:';mail=showdown.subParser('makehtml.unescapeSpecialChars')(mail,options,globals);if(options.encodeEmails){url=showdown.helper.encodeEmailAddress(url+mail);mail=showdown.helper.encodeEmailAddress(mail);}else{url=url+mail;}return{mail:mail,url:url};}// ==== end copied region ====
// GFM extended www autolink: the host must have at least two labels and the last two
// must not contain "_". Explicit-scheme (http/https/ftp) urls are not domain-validated.
function validAutolinkHost(url,isWww){if(!isWww){return true;}var host=url.split(/[/?#]/)[0],labels=host.split('.');if(labels.length<2){return false;}return!/_/.test(labels.slice(-2).join('.'));}// GFM extended email autolink: the domain must have at least two labels, must not end in
// "-" or "_", and its last two labels must not contain "_".
function validMailAddr(addr){var at=addr.lastIndexOf('@');if(at<1){return false;}var domain=addr.slice(at+1),labels=domain.split('.');if(labels.length<2){return false;}if(/[-_]$/.test(domain)){return false;}return!/_/.test(labels.slice(-2).join('.'));}function parseRawHTML(str,i){if(!options.cmSpec){return null;}reRawHtml.lastIndex=i;var m=reRawHtml.exec(str);if(!m){return null;}return{html:showdown.helper._hashHTMLSpan(m[0],globals),end:i+m[0].length};}// find the closing `]` of a reference label, honoring backslash escapes
function findRefClose(str,j){var n=str.length;while(j<n){var c=str.charAt(j);if(c==='\\'&&j+1<n){j+=2;continue;}if(c===']'){return j;}if(c==='['){return-1;}j++;}return-1;}});////
// makehtml/cmList.js
// Copyright (c) 2024 ShowdownJS
//
// CommonMark list container-block parser (spec section 5.2/5.3).
//
// This is a separate, line-based parser invoked only when the `commonmarkLists`
// option is enabled (it is part of the `commonmark` flavor). Showdown's default
// regex list parser (`makehtml.list`) is left untouched; `makehtml.list` simply
// delegates here when the flag is on.
//
// It differs from the regex parser in the ways CommonMark requires: a change of
// bullet character or ordered delimiter starts a new list, ordered lists keep
// their start number and the `)` delimiter, loose/tight is decided per list (not
// per item), and items nest by content indentation.
//
// ***Author:***
// - Estêvão Soares dos Santos (Tivie) <https://github.com/tivie>
////
showdown.subParser('makehtml.cmList',function(text,options,globals){'use strict';var startEvent=new showdown.Event('makehtml.list.onStart',text);startEvent.setOutput(text)._setGlobals(globals)._setOptions(options);startEvent=globals.converter.dispatch(startEvent);text=startEvent.output;text=parseCmList(text);var afterEvent=new showdown.Event('makehtml.list.onEnd',text);afterEvent.setOutput(text)._setGlobals(globals)._setOptions(options);afterEvent=globals.converter.dispatch(afterEvent);return afterEvent.output;/** @param {string} line @returns {number} count of leading spaces */function leadingSpaces(line){var m=line.match(/^ */);return m[0].length;}/** @param {string} line @returns {boolean} */function isBlank(line){return /^[ \t]*$/.test(line);}// a markerless line that can lazily continue an open paragraph: non-blank text
// that does not itself begin an interrupting block
function isLazyParagraph(line){if(isBlank(line)){return false;}return!(/^ {0,3}>/.test(line)||// block quote
/^ {0,3}#{1,6}(?:[ \t]|$)/.test(line)||// ATX heading
/^ {0,3}(?:```|~~~)/.test(line)||// fenced code
/^ {0,3}([-*_])[ \t]*(?:\1[ \t]*){2,}$/.test(line)||// thematic break
/^ {0,3}(?:[-+*]|\d{1,9}[.)])(?:[ \t]|$)/.test(line)// list item
);}/**
   * Match a list-item marker at the start of a line.
   * @param {string} line
   * @returns {{type:string, bullet:(string|null), delim:(string|null),
   *   num:(number|null), markerIndent:number, contentIndent:number,
   *   firstContent:string}|null}
   */function matchMarker(line){var m=line.match(/^( {0,3})([-+*]|\d{1,9}[.)])( *)([\s\S]*)$/);if(!m){return null;}var indent=m[1].length,markerTxt=m[2],spaces=m[3].length,rest=m[4];// the marker must be followed by at least one space (or end the line); `-one`
// is a paragraph, not a list
if(rest!==''&&spaces===0){return null;}var k=markerTxt.length,type=/[-+*]/.test(markerTxt)?'ul':'ol',contentIndent,firstContent;if(rest===''){contentIndent=indent+k+1;firstContent='';}else if(spaces>=5){// 5+ spaces: one space is the marker gap, the rest is indented code
contentIndent=indent+k+1;firstContent=new Array(spaces).join(' ')+rest;}else{contentIndent=indent+k+spaces;firstContent=rest;}return{type:type,bullet:type==='ul'?markerTxt:null,delim:type==='ol'?markerTxt.slice(-1):null,num:type==='ol'?parseInt(markerTxt,10):null,markerIndent:indent,contentIndent:contentIndent,firstContent:firstContent};}/**
   * Gather one list item starting at line index `i`. Consumes the marker line and
   * subsequent lines indented to the item's content column, plus lazy paragraph
   * continuations. Blank lines that are followed by more item content are kept
   * (internal); trailing blank lines are left for the caller.
   * @returns {{content:string[], end:number, blanksFollow:number, internalBlank:boolean}}
   */function gatherItem(lines,i,m){var n=lines.length,w=m.contentIndent,content=[m.firstContent],internalBlank=false;i++;// a list item may begin with at most one blank line: an empty marker followed
// by a blank line is an empty item; later indented content is not part of it
if(m.firstContent===''&&i<n&&isBlank(lines[i])){var j=i;while(j<n&&isBlank(lines[j])){j++;}return{content:[''],end:i,blanksFollow:j-i,internalBlank:false};}while(i<n){var line=lines[i];if(isBlank(line)){// look ahead: blanks are internal only if the item continues afterwards
var _j=i;while(_j<n&&isBlank(lines[_j])){_j++;}if(_j<n&&leadingSpaces(lines[_j])>=w&&!isBlank(lines[_j])){for(var b=i;b<_j;++b){content.push('');}if(content.some(function(c){return c.trim()!=='';})){internalBlank=true;}i=_j;continue;}return{content:content,end:i,blanksFollow:_j-i,internalBlank:internalBlank};}if(leadingSpaces(line)>=w){content.push(line.slice(w));i++;}else if(isLazyParagraph(line)&&lastIsParagraph(content)){content.push(line);i++;}else{return{content:content,end:i,blanksFollow:0,internalBlank:internalBlank};}}return{content:content,end:i,blanksFollow:0,internalBlank:internalBlank};}// Whether an item's content has a blank line separating two of its *top-level*
// blocks (which makes the list loose). A blank line inside a nested list/block
// quote does not count - it makes the nested list loose, not this one. We track
// the content-indent of the deepest open nested container and ignore lines (and
// blanks) inside it.
function itemLoose(content){var containerIndent=-1,// content indent of the open nested container (-1 = none)
topBlockSeen=false,blank=false;for(var li=0;li<content.length;++li){var line=content[li];if(line.trim()===''){blank=true;continue;}var ind=leadingSpaces(line);if(containerIndent>=0&&ind>=containerIndent){continue;// inside the nested container - not a top-level line of this item
}if(blank&&topBlockSeen){return true;}// blank between two top-level blocks
blank=false;topBlockSeen=true;var mk=matchMarker(line);if(mk){containerIndent=mk.contentIndent;}else if(/^ {0,3}>/.test(line)){containerIndent=ind+1;}else{containerIndent=-1;// a plain top-level block (paragraph / indented code)
// a fenced code block is a single block: skip its interior (including blank
// lines) so blanks inside the fence are not counted as separators between two
// top-level blocks (which would wrongly make the list loose)
var fence=line.match(/^ {0,3}(```+|~~~+)/);if(fence){var closeRe=new RegExp('^ {0,3}['+fence[1][0]+']{'+fence[1].length+',}[ \\t]*$');while(li+1<content.length&&!closeRe.test(content[li+1])){li++;}if(li+1<content.length){li++;}// consume the closing fence line
}}}return false;}// whether the last content line is an open paragraph (so a lazy continuation line
// may be appended). Nested block-quote / list markers are peeled first, so lazy
// continuation propagates through container nesting (e.g. `1. > Blockquote`
// followed by a markerless line continues the inner block quote's paragraph).
function lastIsParagraph(content){var last=content[content.length-1];if(last===undefined){return false;}var s=last,indentedCode=/^ {4}/.test(last),m;while(m=s.match(/^ {0,3}>[ \t]?/)||s.match(/^ {0,3}(?:[-+*]|\d{1,9}[.)])[ \t]/)){s=s.slice(m[0].length);indentedCode=false;}if(s.trim()===''||indentedCode){return false;}return!(/^ {0,3}(?:```|~~~)/.test(s)||/^ {0,3}#{1,6}(?:[ \t]|$)/.test(s));}/**
   * Render one list item's content into `<li>…</li>`. In a tight list the item's
   * direct paragraphs are not wrapped in `<p>`; in a loose list they are. Nested
   * blocks (sub-lists, block quotes, code) are produced by blockGamut.
   */function renderItem(content,loose){// trailing newline so the recursive block parsers (esp. indented code, which
// requires each line to end in \n) see a complete final line
var str=content.join('\n')+'\n';// drop the empty leading line that an empty marker (`-` with content on the next
// line) contributes, so an indented-code first block sees a clean block start
// (codeBlock keys off `^`/a preceding blank line) instead of a lone leading newline
str=str.replace(/^\n+/,'');// The item's raw source — the payload for the per-item capture event below (matches
// makehtml.list, which emits the item's raw markdown).
var rawItem=content.join('\n');// GFM task detection on the (leading-newline-stripped) raw line: it selects the event
// name and the <li> attributes. The marker must be followed by whitespace (see
// makehtml.list.taskListItem.checkbox); a bare `[ ]` is not a task.
var taskMatch=options.tasklists?/^[ \t]*\[([xX ])](?=[ \t])/.exec(str):null,checked=!!taskMatch&&taskMatch[1].trim()!=='',eventName=taskMatch?'makehtml.list.taskListItem':'makehtml.list.listItem',attributes={},matches={_wholeMatch:rawItem,listItem:rawItem};if(taskMatch){matches._taskListButton='['+taskMatch[1]+']';matches._taskListButtonChecked=taskMatch[1];// Bare `<li>` per the GFM spec; the legacy bullet styling/classes are only added
// when `moreStyling` is enabled.
if(options.moreStyling){attributes.classes=['task-list-item'];attributes.style='list-style-type: none;';if(checked){attributes.classes.push('task-list-item-complete');}}}var captureStartEvent=new showdown.Event(eventName+'.onCapture',rawItem);captureStartEvent.setOutput(null)._setGlobals(globals)._setOptions(options).setMatches(matches).setAttributes(attributes);captureStartEvent=globals.converter.dispatch(captureStartEvent);var rendered;// A listener may pass output, which takes precedence and is used verbatim as the item
// markup (mirrors makehtml.list); otherwise we render the (possibly edited) item.
if(captureStartEvent.output&&captureStartEvent.output!==''){rendered=captureStartEvent.output;}else{attributes=captureStartEvent.attributes;var liAttrs=showdown.helper._populateAttributes(attributes);// honor a listener that rewrote the item's raw markdown via matches.listItem
if(captureStartEvent.matches.listItem!==rawItem){str=(captureStartEvent.matches.listItem+'\n').replace(/^\n+/,'');}// Render the checkbox on the raw line, before any block/span parsing, so the
// injected <input> flows through blockGamut/spanGamut just like any other inline HTML.
if(taskMatch){str=showdown.subParser('makehtml.list.taskListItem.checkbox')(str,options,globals);}str=showdown.subParser('makehtml.githubCodeBlock')(str,options,globals);str=showdown.subParser('makehtml.blockGamut')(str,options,globals);str=str.replace(/^\n+/,'').replace(/\n+$/,'');var grafs=str.split(/\n{2,}/g),out=[];for(var gi=0;gi<grafs.length;++gi){var g=grafs[gi];if(/¨([KG])(\d+)\1/.test(g)){out.push(g);}else if(/\S/.test(g)){g=showdown.subParser('makehtml.spanGamut')(g,options,globals);if(loose){g=g.replace(/^[ \t]*/,'<p>')+'</p>';}out.push(g);}}var body=out.join('\n');// an empty item is always `<li></li>`, regardless of loose/tight
if(body.trim()===''){rendered='<li'+liAttrs+'></li>\n';}else{// CommonMark serialization: a loose item opens/closes on its own lines; a tight
// item opens on a new line only when its content begins with a block child, and
// closes on a new line only when its content ends with a block child (so trailing
// inline text hugs `</li>`).
var open=loose||/^¨[KG]\d+[KG]/.test(body)?'<li'+liAttrs+'>\n':'<li'+liAttrs+'>',close=loose||/¨[KG]\d+[KG]\s*$/.test(body)?'\n</li>\n':'</li>\n';rendered=open+body+close;}}var beforeHashEvent=new showdown.Event(eventName+'.onHash',rendered);beforeHashEvent.setOutput(rendered)._setGlobals(globals)._setOptions(options);beforeHashEvent=globals.converter.dispatch(beforeHashEvent);return beforeHashEvent.output;}// a line that is open paragraph text (so a following list marker would have to
// "interrupt a paragraph" - which empty items and non-1 ordered lists may not do)
function isParaText(line){if(isBlank(line)||/¨[A-Za-z]?\d+[A-Za-z]/.test(line)){return false;}return isLazyParagraph(line)||matchMarker(line)!==null;}/**
   * CommonMark list parsing. Scans line by line, leaving non-list lines untouched
   * and converting each run of list items of the same type into a hashed list.
   * @param {string} str
   * @returns {string}
   */function parseCmList(str){var lines=str.split('\n'),out=[],i=0,n=lines.length,prevParagraph=false;while(i<n){var m=matchMarker(lines[i]);if(!m){out.push(lines[i]);prevParagraph=isParaText(lines[i]);i++;continue;}// a list item may interrupt a paragraph only if it is non-empty and (for
// ordered lists) starts with 1; otherwise the marker line is paragraph text
if(prevParagraph&&(m.firstContent.trim()===''||m.type==='ol'&&m.num!==1)){out.push(lines[i]);prevParagraph=true;i++;continue;}// collect a run of items that form a single list
var type=m.type,bullet=m.bullet,delim=m.delim,startNum=m.num,listStart=i,items=[],loose=false;while(i<n){var mm=matchMarker(lines[i]);if(!mm||mm.type!==type){break;}if(type==='ul'&&mm.bullet!==bullet){break;}if(type==='ol'&&mm.delim!==delim){break;}var item=gatherItem(lines,i,mm);if(itemLoose(item.content)){loose=true;}items.push(item.content);i=item.end;if(item.blanksFollow>0){var j=i+item.blanksFollow,next=j<n?matchMarker(lines[j]):null,continues=next&&next.type===type&&(type==='ul'?next.bullet===bullet:next.delim===delim);if(continues){loose=true;// blank line between items
i=j;continue;}break;// blanks end the list (left in place for the outer loop)
}}// the raw source of this whole list block — payload for the list-level capture event
var rawList=lines.slice(listStart,i).join('\n');var listCapture=new showdown.Event('makehtml.list.onCapture',rawList);listCapture.setOutput(null)._setGlobals(globals)._setOptions(options).setMatches({_wholeMatch:rawList,list:rawList}).setAttributes({});listCapture=globals.converter.dispatch(listCapture);var otp=void 0;// a listener may pass output, which takes precedence over rendering the items
if(listCapture.output&&listCapture.output!==''){otp=listCapture.output;}else{var attrs=listCapture.attributes||{};if(type==='ol'&&startNum!==1){attrs.start=startNum;}var body='';for(var k=0;k<items.length;++k){body+=renderItem(items[k],loose);}otp='\n\n<'+type+showdown.helper._populateAttributes(attrs)+'>\n'+body+'</'+type+'>\n';}var listHash=new showdown.Event('makehtml.list.onHash',otp);listHash.setOutput(otp)._setGlobals(globals)._setOptions(options);listHash=globals.converter.dispatch(listHash);otp=listHash.output;out.push(showdown.subParser('makehtml.hashBlock')(otp,options,globals));prevParagraph=false;}return out.join('\n');}});////
// makehtml/codeBlock.js
// Copyright (c) 2022 ShowdownJS
//
// Process Markdown `<pre><code>` blocks.
//
// ***Author:***
// - Estêvão Soares dos Santos (Tivie) <https://github.com/tivie>
////
showdown.subParser('makehtml.codeBlock',function(text,options,globals){'use strict';var startEvent=new showdown.Event('makehtml.codeBlock.onStart',text);startEvent.setOutput(text)._setGlobals(globals)._setOptions(options);startEvent=globals.converter.dispatch(startEvent);text=startEvent.output;// sentinel workarounds for lack of \A and \Z, safari\khtml bug
text+='¨0';var pattern=/(?:\n\n|^)((?:(?: {4}|\t).*\n+)+)(\n* {0,3}[^ \t\n]|(?=¨0))/g;text=text.replace(pattern,function(wholeMatch,m1,m2){var codeblock=m1,nextChar=m2,end='\n',otp,attributes={pre:{},code:{}};var captureStartEvent=new showdown.Event('makehtml.codeBlock.onCapture',codeblock);captureStartEvent.setOutput(null)._setGlobals(globals)._setOptions(options).setRegexp(pattern).setMatches({_wholeMatch:wholeMatch,codeblock:codeblock}).setAttributes(attributes);captureStartEvent=globals.converter.dispatch(captureStartEvent);// if something was passed as output, it takes precedence
// and will be used as output
if(captureStartEvent.output&&captureStartEvent.output!==''){otp=captureStartEvent.output;}else{codeblock=captureStartEvent.matches.codeblock;codeblock=showdown.helper.outdent(codeblock);codeblock=showdown.subParser('makehtml.encodeCode')(codeblock,options,globals);//codeblock = showdown.subParser('makehtml.detab')(codeblock, options, globals);
codeblock=codeblock.replace(/^\n+/g,'');// trim leading newlines
codeblock=codeblock.replace(/\n+$/g,'');// trim trailing newlines
attributes=captureStartEvent.attributes;otp='<pre><code>';if(!showdown.helper.isUndefined(attributes)){otp='<pre'+showdown.helper._populateAttributes(attributes.pre)+'>';otp+='<code'+showdown.helper._populateAttributes(attributes.code)+'>';}if(options.omitExtraWLInCodeBlocks){end='';}otp+=codeblock+end+'</code></pre>';}var beforeHashEvent=new showdown.Event('makehtml.codeBlock.onHash',otp);beforeHashEvent.setOutput(otp)._setGlobals(globals)._setOptions(options);beforeHashEvent=globals.converter.dispatch(beforeHashEvent);otp=beforeHashEvent.output;return showdown.subParser('makehtml.hashBlock')(otp,options,globals)+nextChar;});// strip sentinel
text=text.replace(/¨0/,'');var afterEvent=new showdown.Event('makehtml.codeBlock.onEnd',text);afterEvent.setOutput(text)._setGlobals(globals)._setOptions(options);afterEvent=globals.converter.dispatch(afterEvent);return afterEvent.output;});////
// makehtml/codeSpan.js
// Copyright (c) 2018 ShowdownJS
//
// Transforms MD code spans into `<code>` html entities
//
// Backtick quotes are used for <code></code> spans.
//
// You can use multiple backticks as the delimiters if you want to
// include literal backticks in the code span. So, this input:
//
// Just type ``foo `bar` baz`` at the prompt.
//
// Will translate to:
//
// <p>Just type <code>foo `bar` baz</code> at the prompt.</p>
//
// There's no arbitrary limit to the number of backticks you
// can use as delimters. If you need three consecutive backticks
// in your code, use four for delimiters, etc.
//
// You can use spaces to get literal backticks at the edges:
// ... type `` `bar` `` ...
//
// Turns to:
// ... type <code>`bar`</code> ...
//
// ***Author:***
// - Estêvão Soares dos Santos (Tivie) <https://github.com/tivie>
////
showdown.subParser('makehtml.codeSpan',function(text,options,globals){'use strict';var startEvent=new showdown.Event('makehtml.codeSpan.onStart',text);startEvent.setOutput(text)._setGlobals(globals)._setOptions(options);startEvent=globals.converter.dispatch(startEvent);text=startEvent.output;if(showdown.helper.isUndefined(text)){text='';}var pattern=/(^|[^\\])(`+)([^\r]*?[^`])\2(?!`)/gm;text=text.replace(pattern,function(wholeMatch,m1,m2,c){var otp,attributes={};c=c.replace(/^([ \t]*)/g,'');// leading whitespace
c=c.replace(/[ \t]*$/g,'');// trailing whitespace
// remove newlines
c=c.replace(/\n/g,' ');var captureStartEvent=new showdown.Event('makehtml.codeSpan.onCapture',c);captureStartEvent.setOutput(null)._setGlobals(globals)._setOptions(options).setRegexp(pattern).setMatches({_wholeMatch:wholeMatch,code:c}).setAttributes({});captureStartEvent=globals.converter.dispatch(captureStartEvent);// if something was passed as output, it takes precedence
// and will be used as output
if(captureStartEvent.output&&captureStartEvent.output!==''){otp=m1+captureStartEvent.output;}else{c=captureStartEvent.matches.code;c=showdown.subParser('makehtml.encodeCode')(c,options,globals);otp=m1+'<code'+showdown.helper._populateAttributes(attributes)+'>'+c+'</code>';}var beforeHashEvent=new showdown.Event('makehtml.codeSpan.onHash',otp);beforeHashEvent.setOutput(otp)._setGlobals(globals)._setOptions(options);beforeHashEvent=globals.converter.dispatch(beforeHashEvent);otp=beforeHashEvent.output;return showdown.subParser('makehtml.hashHTMLSpans')(otp,options,globals);});var afterEvent=new showdown.Event('makehtml.codeSpan.onEnd',text);afterEvent.setOutput(text)._setGlobals(globals)._setOptions(options);afterEvent=globals.converter.dispatch(afterEvent);return afterEvent.output;});////
// makehtml/completeHTMLDocument.js
// Copyright (c) 2018 ShowdownJS
//
// Create a full HTML document from the processed markdown
//
// ***Author:***
// - Estêvão Soares dos Santos (Tivie) <https://github.com/tivie>
////
// noinspection HtmlRequiredLangAttribute
showdown.subParser('makehtml.completeHTMLDocument',function(text,options,globals){'use strict';if(!options.completeHTMLDocument){return text;}var startEvent=new showdown.Event('makehtml.completeHTMLDocument.onStart',text);startEvent.setOutput(text)._setGlobals(globals)._setOptions(options);startEvent=globals.converter.dispatch(startEvent);text=startEvent.output;var doctype='html',doctypeParsed='<!DOCTYPE HTML>\n',title='',charset='<meta charset="utf-8">\n',lang='',metadata='';if(typeof globals.metadata.parsed.doctype!=='undefined'){doctypeParsed='<!DOCTYPE '+globals.metadata.parsed.doctype+'>\n';doctype=globals.metadata.parsed.doctype.toString().toLowerCase();if(doctype==='html'||doctype==='html5'){charset='<meta charset="utf-8">';}}for(var meta in globals.metadata.parsed){if(Object.prototype.hasOwnProperty.call(globals.metadata.parsed,meta)){switch(meta.toLowerCase()){case'doctype':break;case'title':title='<title>'+globals.metadata.parsed.title+'</title>\n';break;case'charset':if(doctype==='html'||doctype==='html5'){charset='<meta charset="'+globals.metadata.parsed.charset+'">\n';}else{charset='<meta name="charset" content="'+globals.metadata.parsed.charset+'">\n';}break;case'language':case'lang':lang=' lang="'+globals.metadata.parsed[meta]+'"';metadata+='<meta name="'+meta+'" content="'+globals.metadata.parsed[meta]+'">\n';break;default:metadata+='<meta name="'+meta+'" content="'+globals.metadata.parsed[meta]+'">\n';}}}text=doctypeParsed+'<html'+lang+'>\n<head>\n'+title+charset+metadata+'</head>\n<body>\n'+text.trim()+'\n</body>\n</html>';var afterEvent=new showdown.Event('makehtml.completeHTMLDocument.onEnd',text);afterEvent.setOutput(text)._setGlobals(globals)._setOptions(options);afterEvent=globals.converter.dispatch(afterEvent);return afterEvent.output;});////
// makehtml/decodeEntities.js
// Copyright (c) 2024 ShowdownJS
//
// Resolves HTML5 named (`&copy;`), decimal (`&#35;`) and hexadecimal (`&#xcab;`)
// character references to their corresponding characters, per the CommonMark spec.
//
// This diverges from Showdown's default behavior (which preserves entities verbatim),
// so it is gated behind the `decodeEntities` option (enabled by the `commonmark` flavor).
//
// It runs late in the pipeline - after inline parsing and while code spans/blocks are
// still hashed - so that:
//   - a decoded markdown character (e.g. `&#42;` -> `*`) is NOT re-parsed as emphasis;
//   - entities inside code remain escaped (the code is hashed, hence untouched);
//   - decoded newlines stay literal inside their block.
//
// ***Author:***
// - Estêvão Soares dos Santos (Tivie) <https://github.com/tivie>
////
showdown.subParser('makehtml.decodeEntities',function(text,options,globals){'use strict';if(!options.decodeEntities){return text;}var startEvent=new showdown.Event('makehtml.decodeEntities.onStart',text);startEvent.setOutput(text)._setGlobals(globals)._setOptions(options);startEvent=globals.converter.dispatch(startEvent);text=startEvent.output;var entities=showdown.helper.htmlEntities;// Re-escape HTML-significant characters so a decoded char (e.g. `&lt;` -> `<`) stays
// a literal in the HTML output rather than being read as markup.
function escapeOutput(str){return str.replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;');}// Map a code point to its character, replacing disallowed values with U+FFFD.
function fromCodePoint(cp){if(cp===0||cp>0x10FFFF||cp>=0xD800&&cp<=0xDFFF){return'�';}try{return String.fromCodePoint(cp);}catch(_unused2){return'�';}}// Match any `&…;` candidate that survived encodeAmpsAndAngles (which already escaped
// bare ampersands and `&name`/`&name?;` non-references). We classify each candidate here.
text=text.replace(/&([#0-9a-zA-Z]+);/g,function(wholeMatch,body){var m;// decimal numeric reference: 1-7 digits
if(m=/^#([0-9]{1,7})$/.exec(body)){return escapeOutput(fromCodePoint(parseInt(m[1],10)));}// hexadecimal numeric reference: 1-6 hex digits
if(m=/^#[xX]([0-9a-fA-F]{1,6})$/.exec(body)){return escapeOutput(fromCodePoint(parseInt(m[1],16)));}// named reference (must be a known HTML5 entity name)
if(/^[a-zA-Z][a-zA-Z0-9]*$/.test(body)&&Object.prototype.hasOwnProperty.call(entities,body)){return escapeOutput(entities[body]);}// not a valid reference - escape the ampersand and leave the rest intact
return'&amp;'+body+';';});var afterEvent=new showdown.Event('makehtml.decodeEntities.onEnd',text);afterEvent.setOutput(text)._setGlobals(globals)._setOptions(options);afterEvent=globals.converter.dispatch(afterEvent);return afterEvent.output;});////
// makehtml/disallowedHtmlTags.js
// Copyright (c) 2018 ShowdownJS
//
// GFM "disallowed raw HTML" extension (tagfilter): a small blacklist of HTML tags
// is neutralized in the output by escaping their leading `<` to `&lt;`. These tags
// are singled out because they change how the surrounding markup is interpreted
// (script/style/iframe/etc.). See https://github.github.com/gfm/#disallowed-raw-html-extension-
//
// When the `safeMode` option is on, this stage also neutralizes a broader set of
// dangerous raw-HTML tags Showdown never generates and strips inline event-handler
// attributes (on*=...), so that embedded `<script>`, `<img onerror>`, `<svg onload>`
// and similar cannot execute. This is defense-in-depth, not a full HTML sanitizer.
//
// ***Author:***
// - Estêvão Soares dos Santos (Tivie) <https://github.com/tivie>
////
showdown.subParser('makehtml.disallowedHtmlTags',function(text,options,globals){'use strict';if(!options.disallowRawHTML&&!options.safeMode){return text;}var startEvent=new showdown.Event('makehtml.disallowedHtmlTags.onStart',text);startEvent.setOutput(text)._setGlobals(globals)._setOptions(options);startEvent=globals.converter.dispatch(startEvent);text=startEvent.output;// Run over the (near final) output: Showdown never generates these tags from Markdown
// and code blocks/spans already entity-escape `<`, so only genuine raw-HTML passthrough
// is touched. Both opening and closing forms are filtered, case-insensitively.
text=text.replace(/<(\/?(?:title|textarea|style|xmp|iframe|noembed|noframes|script|plaintext))/gi,'&lt;$1');if(options.safeMode){// Additional dangerous tags Showdown never emits from Markdown (svg/math/form/
// media/embedding/document-metadata). Escaping the leading `<` renders them inert.
text=text.replace(/<(\/?(?:svg|math|form|object|embed|base|link|meta|applet|frame|frameset|button|select|option|audio|video|source|track|template|portal))(?=[\s/>])/gi,'&lt;$1');// Strip inline event-handler attributes (onclick, onerror, onload, ...) from any
// surviving tag. Showdown's own generated attributes are never `on*`, so this only
// affects raw-HTML passthrough (e.g. `<img src=x onerror=alert(1)>`). The separator
// before the handler may be whitespace, `/`, or the closing quote of the previous
// attribute — browsers accept all of these (`<img/onerror>`, `<img\tonerror>`,
// `<a href="x"onmouseover=y>`). A whitespace/`/` separator is dropped; a quote
// separator is preserved so the previous attribute stays terminated.
text=text.replace(/(["'\s/])on[a-z]+\s*=\s*(?:"[^"]*"|'[^']*'|[^\s"'>]+)/gi,function(whole,sep){return sep==='"'||sep==='\''?sep:'';});// Neutralize dangerous URL schemes (javascript:, vbscript:, data:text/html, ...) in the
// URL-bearing attributes of raw-HTML tags — Showdown's own scheme allowlist only covers the
// links/images it generates, not raw HTML the author embedded. This runs over EVERY surviving
// tag (not just `<a>`/`<area>`) so it also catches e.g. `<input formaction="javascript:...">`
// (`<input>` is intentionally not escaped, to keep tasklist checkboxes). Only real tags are
// matched (code blocks have their `<` entity-escaped, so `&lt;a ...` is skipped), and Showdown's
// generated tags carry only safe/relative URLs so re-checking them is a harmless no-op.
//   - The tag body is `(?:"[^"]*"|'[^']*'|[^>])*` (quote-aware) rather than `[^>]*` so a `>`
//     inside a quoted attribute value does not truncate the tag early.
//   - The attribute separator is `["'\s/]` (not just `\s`) because browsers accept `/` and
//     quote-adjacent attributes (`<a/href=…>`, `<a id="x"href=…>`); the separator is preserved.
text=text.replace(/<[a-zA-Z][a-zA-Z0-9:-]*\b(?:"[^"]*"|'[^']*'|[^>])*>/g,function(tag){return tag.replace(/(["'\s/](?:href|xlink:href|formaction|action)\s*=\s*)("[^"]*"|'[^']*'|[^\s"'>]+)/gi,function(whole,pre,val){var unquoted=val.replace(/^(["'])([\s\S]*)\1$/,'$2');return showdown.helper.isSafeUrl(unquoted)?whole:pre+'""';});});}var afterEvent=new showdown.Event('makehtml.disallowedHtmlTags.onEnd',text);afterEvent.setOutput(text)._setGlobals(globals)._setOptions(options);afterEvent=globals.converter.dispatch(afterEvent);return afterEvent.output;});////
// makehtml/ellipsis.js
// Copyright (c) 2018 ShowdownJS
//
// transform three dots (...) into ellipsis (…)
//
// ***Author:***
// - Estêvão Soares dos Santos (Tivie) <https://github.com/tivie>
////
showdown.subParser('makehtml.ellipsis',function(text,options,globals){'use strict';if(!options.ellipsis){return text;}var startEvent=new showdown.Event('makehtml.ellipsis.onStart',text);startEvent.setOutput(text)._setGlobals(globals)._setOptions(options);startEvent=globals.converter.dispatch(startEvent);text=startEvent.output;var ellipsisRegex=/\.\.\./g;text=text.replace(ellipsisRegex,function(wholeMatch){var otp;var captureStartEvent=new showdown.Event('makehtml.ellipsis.onCapture',wholeMatch);captureStartEvent.setOutput(null)._setGlobals(globals)._setOptions(options).setRegexp(ellipsisRegex).setMatches({_wholeMatch:wholeMatch,ellipsis:wholeMatch}).setAttributes({});captureStartEvent=globals.converter.dispatch(captureStartEvent);// if something was passed as output, it takes precedence
// and will be used as output
if(captureStartEvent.output&&captureStartEvent.output!==''){otp=captureStartEvent.output;}else{otp='…';}var beforeHashEvent=new showdown.Event('makehtml.ellipsis.onHash',otp);beforeHashEvent.setOutput(otp)._setGlobals(globals)._setOptions(options);beforeHashEvent=globals.converter.dispatch(beforeHashEvent);return beforeHashEvent.output;});var afterEvent=new showdown.Event('makehtml.ellipsis.onEnd',text);afterEvent.setOutput(text)._setGlobals(globals)._setOptions(options);afterEvent=globals.converter.dispatch(afterEvent);return afterEvent.output;});////
// makehtml/emoji.js
// Copyright (c) 2018 ShowdownJS
//
// Turn emoji codes into emojis
// List of supported emojis: https://github.com/showdownjs/showdown/wiki/Emojis
//
// ***Author:***
// - Estêvão Soares dos Santos (Tivie) <https://github.com/tivie>
////
showdown.subParser('makehtml.emoji',function(text,options,globals){'use strict';if(!options.emoji){return text;}var startEvent=new showdown.Event('makehtml.emoji.onStart',text);startEvent.setOutput(text)._setGlobals(globals)._setOptions(options);startEvent=globals.converter.dispatch(startEvent);text=startEvent.output;var pattern=/:(\S+?):/g;text=text.replace(pattern,function(wholeMatch,emojiCode){if(!Object.prototype.hasOwnProperty.call(showdown.helper.emojis,emojiCode)){return wholeMatch;}var otp;var captureStartEvent=new showdown.Event('makehtml.emoji.onCapture',emojiCode);captureStartEvent.setOutput(null)._setGlobals(globals)._setOptions(options).setRegexp(pattern).setMatches({_wholeMatch:wholeMatch,emojiCode:emojiCode}).setAttributes({});captureStartEvent=globals.converter.dispatch(captureStartEvent);// if something was passed as output, it takes precedence and will be used as output
if(captureStartEvent.output&&captureStartEvent.output!==''){otp=captureStartEvent.output;}else{otp=showdown.helper.emojis[emojiCode];}var beforeHashEvent=new showdown.Event('makehtml.emoji.onHash',otp);beforeHashEvent.setOutput(otp)._setGlobals(globals)._setOptions(options);beforeHashEvent=globals.converter.dispatch(beforeHashEvent);otp=beforeHashEvent.output;// Image-based emoji (e.g. :octocat:) render as an <img> tag. Hash it so later
// HTML-escaping passes don't turn its `<`/`>` into entities - under cmSpec the
// generic span hashing skips void tags (it is meant to escape malformed raw HTML),
// so the generated markup must protect itself here. Unicode emoji are returned as-is.
if(/^\s*</.test(otp)){otp=showdown.helper._hashHTMLSpan(otp,globals);}return otp;});var afterEvent=new showdown.Event('makehtml.emoji.onEnd',text);afterEvent.setOutput(text)._setGlobals(globals)._setOptions(options);afterEvent=globals.converter.dispatch(afterEvent);return afterEvent.output;});////
// makehtml/emphasisAndStrong.js
// Copyright (c) 2022 ShowdownJS
//
// Transforms MD emphasis and strong into `<em>` and `<strong>` html entities
//
// Markdown treats asterisks (*) and underscores (_) as indicators of emphasis.
// Text wrapped with one * or _ will be wrapped with an HTML <em> tag;
// double *’s or _’s will be wrapped with an HTML <strong> tag
//
// ***Author:***
// - Estêvão Soares dos Santos (Tivie) <https://github.com/tivie>
////
/* jshint esnext: false, esversion: 9 */// (esversion 9 enables the \p{...} Unicode property escapes used for CommonMark flanking rules)
showdown.subParser('makehtml.emphasisAndStrong',function(text,options,globals){'use strict';var startEvent=new showdown.Event('makehtml.emphasisAndStrong.onStart',text);startEvent.setOutput(text)._setGlobals(globals)._setOptions(options);startEvent=globals.converter.dispatch(startEvent);text=startEvent.output;/**
   * @param {string} txt
   * @param {string} tags
   * @param {string} wholeMatch
   * @param {RegExp} pattern
   * @returns {string}
   */function parseInside(txt,tags,wholeMatch,pattern){var otp='ERROR',attributes,subEventName;switch(tags){case'<em>':attributes={em:{}};subEventName='emphasis';break;case'<strong>':attributes={strong:{}};subEventName='strong';break;case'<strong><em>':attributes={em:{},strong:{}};subEventName='emphasisAndStrong';break;default:attributes={};subEventName='emphasisAndStrong';break;}var captureStartEvent=new showdown.Event('makehtml.emphasisAndStrong.'+subEventName+'.onCapture',txt);captureStartEvent.setOutput(null)._setGlobals(globals)._setOptions(options).setRegexp(pattern).setMatches({_wholeMatch:wholeMatch,text:txt}).setAttributes(attributes);captureStartEvent=globals.converter.dispatch(captureStartEvent);// if something was passed as output, it takes precedence
// and will be used as output
if(captureStartEvent.output&&captureStartEvent.output!==''){otp=captureStartEvent.output;}else{attributes=captureStartEvent.attributes;if(showdown.helper.isUndefined(attributes.em)){attributes.em={};}if(showdown.helper.isUndefined(attributes.strong)){attributes.strong={};}switch(tags){case'<em>':otp='<em'+showdown.helper._populateAttributes(attributes.em)+'>'+showdown.subParser('makehtml.hardLineBreaks')(txt,options,globals)+'</em>';break;case'<strong>':otp='<strong'+showdown.helper._populateAttributes(attributes.strong)+'>'+showdown.subParser('makehtml.hardLineBreaks')(txt,options,globals)+'</strong>';break;case'<strong><em>':otp='<strong'+showdown.helper._populateAttributes(attributes.strong)+'>'+'<em'+showdown.helper._populateAttributes(attributes.em)+'>'+showdown.subParser('makehtml.hardLineBreaks')(txt,options,globals)+'</em>'+'</strong>';break;}}var beforeHashEvent=new showdown.Event('makehtml.emphasisAndStrong.'+subEventName+'.onHash',otp);beforeHashEvent.setOutput(otp)._setGlobals(globals)._setOptions(options);beforeHashEvent=globals.converter.dispatch(beforeHashEvent);otp=beforeHashEvent.output;otp=showdown.subParser('makehtml.hashHTMLSpans')(otp,options,globals);return otp;}// CommonMark-compliant emphasis/strong parsing (delimiter-run algorithm).
// Gated behind the `commonmarkEmphasis` option (enabled by the commonmark flavor) because
// it diverges from Showdown's default behavior (e.g. intraword underscores, flanking rules).
if(options.cmSpec){text=parseCommonmarkEmphasis(text);var cmAfterEvent=new showdown.Event('makehtml.emphasisAndStrong.onEnd',text);cmAfterEvent.setOutput(text)._setGlobals(globals)._setOptions(options);cmAfterEvent=globals.converter.dispatch(cmAfterEvent);return cmAfterEvent.output;}/**
   * Parse emphasis and strong emphasis using the CommonMark delimiter-run algorithm.
   * @param {string} str
   * @returns {string}
   */function parseCommonmarkEmphasis(str){// CommonMark punctuation = ASCII punctuation + Unicode P and S categories
var asciiPunct=/[!-/:-@[-`{-~]/;function isPunct(ch){return ch!==undefined&&(asciiPunct.test(ch)||/(?:[!-\/:-@\[-`\{-~\xA1-\xA9\xAB\xAC\xAE-\xB1\xB4\xB6-\xB8\xBB\xBF\xD7\xF7\u02C2-\u02C5\u02D2-\u02DF\u02E5-\u02EB\u02ED\u02EF-\u02FF\u0375\u037E\u0384\u0385\u0387\u03F6\u0482\u055A-\u055F\u0589\u058A\u058D-\u058F\u05BE\u05C0\u05C3\u05C6\u05F3\u05F4\u0606-\u060F\u061B\u061D-\u061F\u066A-\u066D\u06D4\u06DE\u06E9\u06FD\u06FE\u0700-\u070D\u07F6-\u07F9\u07FE\u07FF\u0830-\u083E\u085E\u0888\u0964\u0965\u0970\u09F2\u09F3\u09FA\u09FB\u09FD\u0A76\u0AF0\u0AF1\u0B70\u0BF3-\u0BFA\u0C77\u0C7F\u0C84\u0D4F\u0D79\u0DF4\u0E3F\u0E4F\u0E5A\u0E5B\u0F01-\u0F17\u0F1A-\

\uFBD2\uFD3E-\uFD4F\uFD90\uFD91\uFDC8-\uFDCF\uFDFC-\uFDFF\uFE10-\uFE19\uFE30-\uFE52\uFE54-\uFE66\uFE68-\uFE6B\uFF01-\uFF0F

}function isWhitespace(ch){return ch===undefined||/\s/.test(ch)||/[ \xA0\u1680\u2000-\u200A\u2028\u2029\u202F\u205F\u3000]/.test(ch);}// 1. Tokenize into a doubly-linked list of nodes; collect a delimiter stack.
var head=null,tail=null,delimiters=null;// tail of delimiter stack
function appendNode(node){node.prev=tail;node.next=null;if(tail){tail.next=node;}else{head=node;}tail=node;return node;}var len=str.length;var i=0;while(i<len){var ch=str.charAt(i);if(ch==='*'||ch==='_'){var start=i;while(i<len&&str.charAt(i)===ch){++i;}var run=str.slice(start,i),before=start===0?undefined:str.charAt(start-1),after=i>=len?undefined:str.charAt(i),beforeWs=isWhitespace(before),afterWs=isWhitespace(after),beforePt=isPunct(before),afterPt=isPunct(after),leftFlanking=!afterWs&&(!afterPt||beforeWs||beforePt),rightFlanking=!beforeWs&&(!beforePt||afterWs||afterPt),canOpen=void 0,canClose=void 0;if(ch==='_'){canOpen=leftFlanking&&(!rightFlanking||beforePt);canClose=rightFlanking&&(!leftFlanking||afterPt);}else{canOpen=leftFlanking;canClose=rightFlanking;}var node=appendNode({type:'delim',cc:ch,literal:run,numdelims:run.length,origdelims:run.length,canOpen:canOpen,canClose:canClose});node.delimPrev=delimiters;node.delimNext=null;if(delimiters){delimiters.delimNext=node;}delimiters=node;}else{// accumulate a literal text run up to the next delimiter
var _start2=i;while(i<len&&str.charAt(i)!=='*'&&str.charAt(i)!=='_'){++i;}appendNode({type:'text',literal:str.slice(_start2,i)});}}// 2. Process emphasis (CommonMark reference algorithm).
processEmphasis();// 3. Render remaining nodes back to a string.
var out='';for(var n=head;n!==null;n=n.next){out+=n.literal;}return out;function removeDelimiter(d){if(d.delimPrev){d.delimPrev.delimNext=d.delimNext;}if(d.delimNext){d.delimNext.delimPrev=d.delimPrev;}else{delimiters=d.delimPrev;// top of stack
}}function insertAfter(node,newNode){newNode.prev=node;newNode.next=node.next;if(node.next){node.next.prev=newNode;}else{tail=newNode;}node.next=newNode;}function processEmphasis(){// one lower-bound per delimiter char, bucketed by (origdelims % 3) per the spec
var openersBottom={'_':[null,null,null],'*':[null,null,null]};// start from the bottom-most delimiter on the stack
var closer=delimiters;while(closer!==null&&closer.delimPrev!==null){closer=closer.delimPrev;}while(closer!==null){if(!closer.canClose){closer=closer.delimNext;continue;}// look back for a matching opener
var opener=closer.delimPrev,openerFound=false,oddMatch=void 0;while(opener!==null&&opener!==openersBottom[closer.cc][closer.origdelims%3]){oddMatch=(closer.canOpen||opener.canClose)&&closer.origdelims%3!==0&&(opener.origdelims+closer.origdelims)%3===0;if(opener.cc===closer.cc&&opener.canOpen&&!oddMatch){openerFound=true;break;}opener=opener.delimPrev;}var oldCloser=closer;if(openerFound){var use=opener.numdelims>=2&&closer.numdelims>=2?2:1,tagOpen=use===2?'<strong>':'<em>',tagClose=use===2?'</strong>':'</em>';// trim consumed delimiters from the opener (end) and closer (start) literals
opener.literal=opener.literal.slice(0,opener.literal.length-use);opener.numdelims-=use;closer.literal=closer.literal.slice(use);closer.numdelims-=use;// collect inner nodes (between opener and closer), render and hash them
var inner='';for(var _n3=opener.next;_n3!==null&&_n3!==closer;_n3=_n3.next){inner+=_n3.literal;}// emphasis content is hashed here, before the rest of the span gamut runs, so apply
// hard line breaks and encode double quotes now (CommonMark renders quotes as &quot;)
inner=showdown.subParser('makehtml.hardLineBreaks')(inner,options,globals);inner=inner.replace(/"/g,'&quot;');var wrapped=showdown.subParser('makehtml.hashHTMLSpans')(tagOpen+inner+tagClose,options,globals);// remove inner nodes and their delimiters from the lists
var n2=opener.next;while(n2!==null&&n2!==closer){var nx=n2.next;if(n2.type==='delim'){removeDelimiter(n2);}n2=nx;}// splice in a single text node holding the wrapped (hashed) result
var wrapNode={type:'text',literal:wrapped};insertAfter(opener,wrapNode);// relink opener.next directly to closer (drop the removed inner nodes)
wrapNode.next=closer;closer.prev=wrapNode;// drop fully-consumed delimiters
if(opener.numdelims===0){opener.literal='';removeDelimiter(opener);}if(closer.numdelims===0){closer.literal='';var tmp=closer.delimNext;removeDelimiter(closer);closer=tmp;}}else{openersBottom[oldCloser.cc][oldCloser.origdelims%3]=oldCloser.delimPrev;if(!oldCloser.canOpen){removeDelimiter(oldCloser);}closer=oldCloser.delimNext;}}}}// it's faster to have separate regexes for each case than have just one
// because of backtracking, in some cases, it could lead to an exponential effect
// called "catastrophic backtrace". Ominous!
var lmwuStrongEmRegex=/\b___(\S[\s\S]*?)___\b/g,lmwuStrongRegex=/\b__(\S[\s\S]*?)__\b/g,lmwuEmRegex=/\b_(\S[\s\S]*?)_\b/g,underscoreStrongEmRegex=/___(\S[\s\S]*?)___/g,unserscoreStrongRegex=/__(\S[\s\S]*?)__/g,unserscoreEmRegex=/_([^\s_][\s\S]*?)_/g,asteriskStrongEm=/\*\*\*(\S[\s\S]*?)\*\*\*/g,asteriskStrong=/\*\*(\S[\s\S]*?)\*\*/g,asteriskEm=/\*([^\s*][\s\S]*?)\*/g;// Parse underscores
if(options.literalMidWordUnderscores){text=text.replace(lmwuStrongEmRegex,function(wm,txt){return parseInside(txt,'<strong><em>',wm,lmwuStrongEmRegex);});text=text.replace(lmwuStrongRegex,function(wm,txt){return parseInside(txt,'<strong>',wm,lmwuStrongRegex);});text=text.replace(lmwuEmRegex,function(wm,txt){return parseInside(txt,'<em>',wm,lmwuEmRegex);});}else{text=text.replace(underscoreStrongEmRegex,function(wm,m){return /\S$/.test(m)?parseInside(m,'<strong><em>',wm,underscoreStrongEmRegex):wm;});text=text.replace(unserscoreStrongRegex,function(wm,m){return /\S$/.test(m)?parseInside(m,'<strong>',wm,unserscoreStrongRegex):wm;});text=text.replace(unserscoreEmRegex,function(wm,m){// !/^_[^_]/.test(m) - test if it doesn't start with __ (since it seems redundant, we removed it)
return /\S$/.test(m)?parseInside(m,'<em>',wm,unserscoreEmRegex):wm;});}// Now parse asterisks
text=text.replace(asteriskStrongEm,function(wm,m){return /\S$/.test(m)?parseInside(m,'<strong><em>',wm,asteriskStrongEm):wm;});text=text.replace(asteriskStrong,function(wm,m){return /\S$/.test(m)?parseInside(m,'<strong>',wm,asteriskStrong):wm;});text=text.replace(asteriskEm,function(wm,m){// !/^\*[^*]/.test(m) - test if it doesn't start with ** (since it seems redundant, we removed it)
return /\S$/.test(m)?parseInside(m,'<em>',wm,asteriskEm):wm;});//}
var afterEvent=new showdown.Event('makehtml.emphasisAndStrong.onEnd',text);afterEvent.setOutput(text)._setGlobals(globals)._setOptions(options);afterEvent=globals.converter.dispatch(afterEvent);return afterEvent.output;});////
// makehtml/encodeAmpsAndAngles.js
// Copyright (c) 2018 ShowdownJS
//
// Smart processing for ampersands and angle brackets that need to be encoded.
//
// ***Author:***
// - Estêvão Soares dos Santos (Tivie) <https://github.com/tivie>
////
showdown.subParser('makehtml.encodeAmpsAndAngles',function(text,options,globals){'use strict';var startEvent=new showdown.Event('makehtml.encodeAmpsAndAngles.onStart',text);startEvent.setOutput(text)._setGlobals(globals)._setOptions(options);startEvent=globals.converter.dispatch(startEvent);text=startEvent.output;// Ampersand-encoding based entirely on Nat Irons's Amputator MT plugin:
// http://bumppo.net/projects/amputator/
text=text.replace(/&(?!#?[xX]?(?:[\da-fA-F]+|\w+);)/g,'&amp;');// Encode naked <'s
text=text.replace(/<(?![a-z/?$!])/gi,'&lt;');// Encode <
text=text.replace(/</g,'&lt;');// Encode >
text=text.replace(/>/g,'&gt;');// encode "
text=text.replace(/"/g,'&quot;');var afterEvent=new showdown.Event('makehtml.encodeAmpsAndAngles.onEnd',text);afterEvent.setOutput(text)._setGlobals(globals)._setOptions(options);afterEvent=globals.converter.dispatch(afterEvent);return afterEvent.output;});////
// makehtml/encodeBackslashEscapes.js
// Copyright (c) 2018 ShowdownJS
//
// Returns the string, with after processing the following backslash escape sequences.
//
// The polite way to do this is with the new escapeCharacters() function:
//
// text = escapeCharacters(text,"\\",true);
// text = escapeCharacters(text,"`*_{}[]()>#+-.!",true);
//
// ...but we're sidestepping its use of the (slow) RegExp constructor
// as an optimization for Firefox.  This function gets called a LOT.
//
// ***Author:***
// - attacklab
// - Estêvão Soares dos Santos (Tivie) <https://github.com/tivie>
////
showdown.subParser('makehtml.encodeBackslashEscapes',function(text,options,globals){'use strict';var startEvent=new showdown.Event('makehtml.encodeBackslashEscapes.onStart',text);startEvent.setOutput(text)._setGlobals(globals)._setOptions(options);startEvent=globals.converter.dispatch(startEvent);text=startEvent.output;text=text.replace(/\\(\\)/g,showdown.helper.escapeCharactersCallback).replace(/\\([!#%'()*+,\-./:;=?@[\]\\^_`{|}~])/g,showdown.helper.escapeCharactersCallback).replace(/\\¨D/g,'¨D')// escape $ (which was already escaped as ¨D) (charcode is 36)
.replace(/\\&/g,'&amp;')// escape &
.replace(/\\"/g,'&quot;')// escaping "
.replace(/\\</g,'&lt;')// escaping <
.replace(/\\>/g,'&gt;');// escaping >
var afterEvent=new showdown.Event('makehtml.encodeBackslashEscapes.onEnd',text);afterEvent.setOutput(text)._setGlobals(globals)._setOptions(options);afterEvent=globals.converter.dispatch(afterEvent);return afterEvent.output;});////
// makehtml/emoji.js
// Copyright (c) 2018 ShowdownJS
//
// Encode/escape certain characters inside Markdown code runs.
// The point is that in code, these characters are literals,
// and lose their special Markdown meanings.
//
// ***Author:***
// - Estêvão Soares dos Santos (Tivie) <https://github.com/tivie>
////
showdown.subParser('makehtml.encodeCode',function(text,options,globals){'use strict';var startEvent=new showdown.Event('makehtml.encodeCode.onStart',text);startEvent.setOutput(text)._setGlobals(globals)._setOptions(options);startEvent=globals.converter.dispatch(startEvent);text=startEvent.output;// Encode all ampersands; HTML entities are not
// entities within a Markdown code span.
text=text.replace(/&/g,'&amp;')// Do the angle bracket song and dance:
.replace(/</g,'&lt;').replace(/>/g,'&gt;')// encode "
.replace(/"/g,'&quot;')// Now, escape characters that are magic in Markdown:
.replace(/([*_{}[\]\\=~-])/g,showdown.helper.escapeCharactersCallback);var afterEvent=new showdown.Event('makehtml.encodeCode.onEnd',text);afterEvent.setOutput(text)._setGlobals(globals)._setOptions(options);afterEvent=globals.converter.dispatch(afterEvent);return afterEvent.output;});////
// makehtml/escapeSpecialCharsWithinTagAttributes.js
// Copyright (c) 2018 ShowdownJS
//
// Within tags -- meaning between < and > -- encode [\ ` * _ ~ =] so they
// don't conflict with their use in Markdown for code, italics and strong.
//
// ***Author:***
// - Estêvão Soares dos Santos (Tivie) <https://github.com/tivie>
////
showdown.subParser('makehtml.escapeSpecialCharsWithinTagAttributes',function(text,options,globals){'use strict';var startEvent=new showdown.Event('makehtml.escapeSpecialCharsWithinTagAttributes.onStart',text);startEvent.setOutput(text)._setGlobals(globals)._setOptions(options);startEvent=globals.converter.dispatch(startEvent);text=startEvent.output;// In CommonMark raw-HTML mode this escaping is skipped: inline raw HTML is instead
// recognized with the strict grammar and hashed in spanGamut (makehtml.hashCmRawHTML),
// after backslash escapes and link/image destinations have been resolved. Escaping
// `=`/`_`/etc. here would corrupt that later strict tag recognition.
if(!options.cmSpec){// Build a regex to find HTML tags.
var tags=/<\/?[a-z\d_:-]+(?:\s+[\s\S]+?)?>/gi,comments=/<!(--(([^>-]|-[^>])([^-]|-[^-])*)--)>/gi;text=text.replace(tags,function(wholeMatch){return wholeMatch.replace(/(.)<\/?code>(?=.)/g,'$1`').replace(/([\\`*_~=|])/g,showdown.helper.escapeCharactersCallback);});text=text.replace(comments,function(wholeMatch){return wholeMatch.replace(/([\\`*_~=|])/g,showdown.helper.escapeCharactersCallback);});}var afterEvent=new showdown.Event('makehtml.escapeSpecialCharsWithinTagAttributes.onEnd',text);afterEvent.setOutput(text)._setGlobals(globals)._setOptions(options);afterEvent=globals.converter.dispatch(afterEvent);return afterEvent.output;});/**
 * GFM footnotes (cmark-gfm footnotes extension).
 *
 * Runs in two phases (selected by the `phase` argument):
 *   'strip' - before stripLinkDefinitions: collect `[^label]: body` definitions
 *             into globals and replace `[^label]` references that have a definition
 *             with a hashed `<sup>` span (numbered by order of first reference).
 *   'build' - after paragraphs: render the referenced footnotes into a
 *             `<section class="footnotes">` and append it to the document.
 */showdown.subParser('makehtml.footnotes',function(text,options,globals,phase){'use strict';if(!options.footnotes){return text;}if(phase==='strip'){text=collectDefinitions(text);text=replaceReferences(text);return text;}if(phase==='build'){return buildSection(text);}return text;// ---- phase 1a: collect (and remove) `[^label]: body` definitions ----------
function collectDefinitions(str){globals.gFootnotes=globals.gFootnotes||{};str+='¨0';var n=str.length,out='',i=0;while(i<n){// `i` is always at a line start here; a footnote definition may begin at any line
// (it can interrupt a paragraph), so we try one at every line.
var def=tryParseDefinition(str,i);if(def){var norm=showdown.helper.cmNormalizeLabel(def.rawLabel);if(norm!==''&&showdown.helper.isUndefined(globals.gFootnotes[norm])){globals.gFootnotes[norm]={bodyRaw:def.body,rawLabel:def.rawLabel};}i=def.end;continue;}var nl=str.indexOf('\n',i);if(nl===-1){out+=str.slice(i);break;}out+=str.slice(i,nl+1);i=nl+1;}return out.replace(/¨0/,'');}/**
   * Try to parse a single footnote definition starting at `start` (a line start).
   * The body is the same-line remainder plus continuation lines indented by at
   * least 4 spaces (with that 4-space base removed), up to a less-indented line.
   * @returns {{rawLabel: string, body: string, end: number}|null}
   */function tryParseDefinition(str,start){var n=str.length,j=start,sp=0;while(j<n&&str.charAt(j)===' '&&sp<3){j++;sp++;}if(str.charAt(j)!=='['||str.charAt(j+1)!=='^'){return null;}j+=2;var label='';while(j<n){var c=str.charAt(j);if(c==='\\'&&j+1<n){label+=c+str.charAt(j+1);j+=2;continue;}if(c===']'){break;}// a footnote label may not contain whitespace or a nested '['
if(c==='['||/\s/.test(c)){return null;}label+=c;j++;}if(str.charAt(j)!==']'||label===''||str.charAt(j+1)!==':'){return null;}j+=2;var nl=str.indexOf('\n',j),firstEnd=nl===-1?n:nl,firstLine=str.slice(j,firstEnd).replace(/^[ \t]+/,'').replace(/¨0$/,''),bodyLines=[];if(firstLine!==''){bodyLines.push(firstLine);}var k=nl===-1?n:nl+1;while(k<n){var lineNl=str.indexOf('\n',k),lineEnd=lineNl===-1?n:lineNl,line=str.slice(k,lineEnd);if(line.slice(0,2)==='¨0'){break;}if(/^[ \t]*$/.test(line)){bodyLines.push('');k=lineNl===-1?n:lineNl+1;continue;}if(/^ {4}/.test(line)){bodyLines.push(line.slice(4));k=lineNl===-1?n:lineNl+1;}else{break;}}while(bodyLines.length&&bodyLines[bodyLines.length-1]===''){bodyLines.pop();}return{rawLabel:label,body:bodyLines.join('\n'),end:k};}// ---- phase 1b: replace `[^label]` references with hashed <sup> spans -------
// Replace inline code spans with `¨FN<n>¨` placeholders so a `[^id]` inside them stays
// literal. Linear scan (ReDoS-safe): a code span is a back-tick run closed by a run of the
// same length. Unbalanced runs are left as text. Worst case is sub-quadratic because runs
// that stay unmatched must have distinct lengths (equal-length runs pair up).
function hashInlineCodeSpans(str,store){var pieces=[],textStart=0,i=0,n=str.length;while(i<n){if(str.charAt(i)!=='`'){i++;continue;}var j=i;while(j<n&&str.charAt(j)==='`'){j++;}var len=j-i,k=j,close=-1;while(k<n){if(str.charAt(k)==='`'){var m=k;while(m<n&&str.charAt(m)==='`'){m++;}if(m-k===len){close=m;break;}k=m;}else{k++;}}if(close!==-1){pieces.push(str.substring(textStart,i));store.push(str.substring(i,close));pieces.push('¨FN'+(store.length-1)+'¨');textStart=close;i=close;}else{// opening run with no matching closer: leave it as literal text
i=j;}}pieces.push(str.substring(textStart));return pieces.join('');}function replaceReferences(str,raw){globals.gFootnoteOrder=globals.gFootnoteOrder||[];globals.gFootnoteRefs=globals.gFootnoteRefs||{};// Protect inline code spans so a `[^id]` inside back-ticks stays literal. A linear scan
// (not a backtracking regex) keeps this safe on long back-tick runs in attacker input.
// Fenced/indented code is already hashed before this pass.
var spans=[];str=hashInlineCodeSpans(str,spans);// a footnote label may not contain whitespace, so `[^a b]` is not a reference.
// `[` is excluded from the label too: it can never appear in a valid label, and
// excluding it stops the scan from running past the label start of the *next*
// `[^...` (which would otherwise backtrack quadratically on inputs like `[^`.repeat(n)).
str=str.replace(/\[\^([^\s\][]+)]/g,function(whole,rawLabel,offset,full){// an escaped reference (`\[^id]`, an odd number of leading back-slashes) is literal
var bs=0,p=offset-1;while(p>=0&&full.charAt(p)==='\\'){bs++;p--;}if(bs%2===1){return whole;}var norm=showdown.helper.cmNormalizeLabel(rawLabel),fn=globals.gFootnotes[norm];if(showdown.helper.isUndefined(fn)){return whole;}var ref=globals.gFootnoteRefs[norm];if(showdown.helper.isUndefined(ref)){globals.gFootnoteOrder.push(norm);ref=globals.gFootnoteRefs[norm]={number:globals.gFootnoteOrder.length,encId:showdown.helper.cmEncodeURI(fn.rawLabel),occurrences:0};}ref.occurrences++;var idSuffix=ref.occurrences===1?'':'-'+ref.occurrences,sup='<sup class="footnote-ref"><a href="#fn-'+ref.encId+'" id="fnref-'+ref.encId+idSuffix+'" data-footnote-ref>'+ref.number+'</a></sup>';// `raw` (footnote bodies): emit the markup inline so the nested conversion keeps it;
// otherwise (main text) hash it so the inline parser leaves it alone.
return raw?sup:showdown.helper._hashHTMLSpan(sup,globals);});// restore the protected code spans
str=str.replace(/¨FN(\d+)¨/g,function(whole,n){return spans[n];});return str;}// ---- phase 2: render the referenced footnotes into a <section> ------------
function buildSection(str){var order=globals.gFootnoteOrder;if(!order||order.length===0){return str;}var items='';for(var idx=0;idx<order.length;idx++){var norm=order[idx],fn=globals.gFootnotes[norm],ref=globals.gFootnoteRefs[norm],body=renderBody(fn.bodyRaw);body=insertBackrefs(body,buildBackrefs(ref));items+='<li id="fn-'+ref.encId+'">\n'+body+'\n</li>\n';}return str+'\n<section class="footnotes" data-footnotes>\n<ol>\n'+items+'</ol>\n</section>\n';}function renderBody(bodyRaw){// Resolve any footnote references nested inside this body, continuing the document's
// numbering (this may append further footnotes to the order, which buildSection's
// loop then renders). They are emitted as raw <sup> markup so the nested conversion
// preserves them.
var body=replaceReferences(bodyRaw,true);// Render the (multi-block) body to complete HTML via a nested conversion. This runs
// the full block pipeline (indented-code framing, fenced code, block quotes,
// paragraphs) and leaves no hash placeholders behind, which matters because the
// section is appended after the main paragraphs pass.
return globals.converter.makeHtml(body).replace(/\n+$/,'');}function buildBackrefs(ref){var parts=[];for(var j=1;j<=ref.occurrences;j++){var id='fnref-'+ref.encId+(j===1?'':'-'+j),idx=ref.number+(j===1?'':'-'+j),inner=j===1?'↩':'↩<sup class="footnote-ref">'+j+'</sup>';parts.push('<a href="#'+id+'" class="footnote-backref" data-footnote-backref data-footnote-backref-idx="'+idx+'" aria-label="Back to reference '+idx+'">'+inner+'</a>');}return parts.join(' ');}function insertBackrefs(body,backrefs){if(/<\/p>\s*$/.test(body)){return body.replace(/<\/p>\s*$/,' '+backrefs+'</p>');}return body+'\n'+backrefs;}});////
// makehtml/githubCodeBlock.js
// Copyright (c) 2018 ShowdownJS
//
// Handle github codeblocks prior to running HashHTML so that
// HTML contained within the codeblock gets escaped properly
// Example:
// ```ruby
// def hello_world(x)
//     puts "Hello, #{x}"
// end
// ```
//
// ***Author:***
// - Estêvão Soares dos Santos (Tivie) <https://github.com/tivie>
////
showdown.subParser('makehtml.githubCodeBlock',function(text,options,globals,topLevelOnly){'use strict';// early exit if option is not enabled
if(!options.ghCodeBlocks){return text;}var startEvent=new showdown.Event('makehtml.githubCodeBlock.onStart',text);startEvent.setOutput(text)._setGlobals(globals)._setOptions(options);startEvent=globals.converter.dispatch(startEvent);text=startEvent.output+'¨0';// In CommonMark container mode the converter-level pass restricts the *opening* fence to
// indent 0; indent 1-3 opening fences are owned by the container parsers (a list item's /
// block quote's own fence) or handled by a later blockGamut pass for true top-level ones.
// This stops an item's indented opening/closing fence from being mistaken for a new
// top-level opening fence. The closing fence keeps its 0-3 indent allowance either way.
var open=topLevelOnly?'':' {0,3}';//const accentRegex = /(?:^|\n) {0,3}(```+|~~~+) *([^\n\t`~]*)\n([\s\S]*?)(?:(\n {0,3}\1[`~]*)|¨0)/g;
// The info string is the rest of the opening line. CommonMark forbids backticks only in a
// *backtick* fence's info string (a tilde fence may contain backticks and tildes); that
// restriction is enforced in parse() since it needs to know the fence type. The `(?!`)` /
// `(?!~)` lookaheads keep the opening fence maximal: without them the now-permissive info
// class lets the delimiter backtrack (e.g. `~~~~~~` splitting into a `~~~` fence + `~~~`
// info), inventing a spurious shorter fence.
var fence='(```+(?!`)|~~~+(?!~))';var closedBlockRegex=new RegExp('^'+open+fence+' *([^\\n]*)\\n([\\s\\S]*?)\\n {0,3}\\1[`~]*','gm');var unclosedBlockRegex=new RegExp('^'+open+fence+' *([^\\n]*)\\n([\\s\\S]*?)¨0','gm');var emptyBlockRegex=new RegExp('^'+open+fence+' *([^\\n]*)\\n {0,3}\\1[`~]*','gm');text=text.replace(closedBlockRegex,function(wholeMatch,delim,language,codeblock){return parse(closedBlockRegex,wholeMatch,delim,language,codeblock);});text=text.replace(emptyBlockRegex,function(wholeMatch,delim,language){return parse(emptyBlockRegex,wholeMatch,delim,language,'');});// In topLevelOnly mode, skip the unclosed (run-to-EOF) pass: a genuine indent-0 opener
// pairs via the closed/empty passes above, whereas a lone indent-0 *closing* fence of an
// indent 1-3 top-level block would otherwise be mistaken for an opener and swallow the
// rest of the document. Such an indented top-level fence (and any truly unclosed one) is
// handled by the 0-3 blockGamut pass that runs after the container parsers.
if(!topLevelOnly){text=text.replace(unclosedBlockRegex,function(wholeMatch,delim,language,codeblock){return parse(unclosedBlockRegex,wholeMatch,delim,language,codeblock);});}// attacklab: strip sentinel
text=text.replace(/¨0/,'');var afterEvent=new showdown.Event('makehtml.githubCodeBlock.onEnd',text);afterEvent.setOutput(text)._setGlobals(globals)._setOptions(options);afterEvent=globals.converter.dispatch(afterEvent);return afterEvent.output;function parse(pattern,wholeMatch,delim,language,codeblock){// CommonMark: a backtick info string may not contain backticks (tilde fences may). When it
// does, this is not a code block, so leave the text for inline/paragraph parsing.
if(delim.charAt(0)==='`'&&language.indexOf('`')!==-1){return wholeMatch;}var end=options.omitExtraWLInCodeBlocks?'':'\n',otp,attributes={pre:{},code:{}};var captureStartEvent=new showdown.Event('makehtml.githubCodeBlock.onCapture',codeblock);captureStartEvent.setOutput(null)._setGlobals(globals)._setOptions(options).setRegexp(pattern).setMatches({_whoteMatch:wholeMatch,codeblock:codeblock,infostring:language}).setAttributes(attributes);captureStartEvent=globals.converter.dispatch(captureStartEvent);// if something was passed as output, it takes precedence
// and will be used as output
if(captureStartEvent.output&&captureStartEvent.output!==''){otp=captureStartEvent.output;}else{// First parse the github code block
var infostring=captureStartEvent.matches.infostring;var lang=infostring.trim().split(' ')[0];// CommonMark resolves backslash escapes of ASCII punctuation in the info string
if(options.decodeEntities){lang=lang.replace(/\\([!-/:-@[-`{-~])/g,'$1');}// The language ends up inside the code element's `class` attribute, so escape the
// characters that could break out of it. Otherwise an info string such as
// `js"onmouseover="alert(1)` would close the attribute and inject an event handler (XSS).
// `&` is intentionally left alone: the later decodeEntities pass resolves real entities
// (e.g. `&ouml;`) for the class and re-escapes any that decode to `"`/`<`/`>`, so these
// replacements survive it while a literal quote is neutralized in both modes.
lang=lang.replace(/"/g,'&quot;').replace(/</g,'&lt;').replace(/>/g,'&gt;');codeblock=captureStartEvent.matches.codeblock;codeblock=showdown.subParser('makehtml.encodeCode')(codeblock,options,globals);//codeblock = showdown.subParser('makehtml.detab')(codeblock, options, globals);
codeblock=codeblock.replace(/^\n+/g,'')// trim leading newlines
.replace(/\n+$/g,'');// trim trailing whitespace
attributes=captureStartEvent.attributes;otp='<pre><code>';if(!showdown.helper.isUndefined(attributes)){otp='<pre'+showdown.helper._populateAttributes(attributes.pre)+'>';// if the language has spaces followed by some other chars, according to the spec we should just ignore everything
// after the first space
if(infostring){if(!attributes.code){attributes.code={};}if(!attributes.code.classes){attributes.code.classes=[];}if(attributes.code.classes){if(showdown.helper.isString(attributes.code.classes)){attributes.code.classes+=' '+lang+' language-'+lang;}else if(showdown.helper.isArray(attributes.code.classes)){attributes.code.classes.push(lang);attributes.code.classes.push('language-'+lang);}}}otp+='<code'+showdown.helper._populateAttributes(attributes.code)+'>';}if(options.omitExtraWLInCodeBlocks){end='';}otp+=codeblock+end+'</code></pre>';}var beforeHashEvent=new showdown.Event('makehtml.githubCodeBlock.onHash',otp);beforeHashEvent.setOutput(otp)._setGlobals(globals)._setOptions(options);beforeHashEvent=globals.converter.dispatch(beforeHashEvent);otp=beforeHashEvent.output;otp=showdown.subParser('makehtml.hashBlock')(otp,options,globals);// Since GHCodeblocks can be false positives, we need to
// store the primitive text and the parsed text in a global var,
// and then return a token
return'\n\n¨G'+(globals.ghCodeBlocks.push({text:wholeMatch,codeblock:otp})-1)+'G\n\n';}});////
// makehtml/hardLineBreaks.js
// Copyright (c) 2022 ShowdownJS
//
// Transforms hard line breaks (trailing spaces / backslash) into `<br />` tags.
//
// ***Author:***
// - Estêvão Soares dos Santos (Tivie) <https://github.com/tivie>
////
showdown.subParser('makehtml.hardLineBreaks',function(text,options,globals){'use strict';var startEvent=new showdown.Event('makehtml.hardLineBreaks.onStart',text);startEvent.setOutput(text)._setGlobals(globals)._setOptions(options);startEvent=globals.converter.dispatch(startEvent);text=startEvent.output;// Do hard breaks
if(options.simpleLineBreaks){// GFM style hard breaks
// only add line breaks if the text does not contain a block (special case for lists)
if(!/\n\n¨K/.test(text)){text=text.replace(/\n+/gm,'<br />\n');}}else{// Vanilla hard breaks
text=text.replace(/  +\n/g,'<br />\n');}text=text.replace(/\\\n/g,'<br />\n');var afterEvent=new showdown.Event('makehtml.hardLineBreaks.onEnd',text);afterEvent.setOutput(text)._setGlobals(globals)._setOptions(options);afterEvent=globals.converter.dispatch(afterEvent);return afterEvent.output;});////
// makehtml/hashBlock.js
// Copyright (c) 2018 ShowdownJS
//
//
// ***Author:***
// - Estêvão Soares dos Santos (Tivie) <https://github.com/tivie>
////
showdown.subParser('makehtml.hashBlock',function(text,options,globals){'use strict';var startEvent=new showdown.Event('makehtml.hashBlock.onStart',text);startEvent.setOutput(text)._setGlobals(globals)._setOptions(options);startEvent=globals.converter.dispatch(startEvent);text=startEvent.output;text=text.replace(/(^\n+|\n+$)/g,'');text='\n\n¨K'+(globals.gHtmlBlocks.push(text)-1)+'K\n\n';var afterEvent=new showdown.Event('makehtml.hashBlock.onEnd',text);afterEvent.setOutput(text)._setGlobals(globals)._setOptions(options);afterEvent=globals.converter.dispatch(afterEvent);return afterEvent.output;});////
// makehtml/hashCodeTags.js
// Copyright (c) 2018 ShowdownJS
//
// Hash and escape <code> elements that should not be parsed as markdown
//
// ***Author:***
// - Estêvão Soares dos Santos (Tivie) <https://github.com/tivie>
////
showdown.subParser('makehtml.hashCodeTags',function(text,options,globals){'use strict';var startEvent=new showdown.Event('makehtml.hashCodeTags.onStart',text);startEvent.setOutput(text)._setGlobals(globals)._setOptions(options);startEvent=globals.converter.dispatch(startEvent);text=startEvent.output;var repFunc=function repFunc(wholeMatch,match,left,right){var codeblock=left+showdown.subParser('makehtml.encodeCode')(match,options,globals)+right;return'¨C'+(globals.gHtmlSpans.push(codeblock)-1)+'C';};// Hash naked <code>
text=showdown.helper.replaceRecursiveRegExp(text,repFunc,'<code\\b[^>]*>','</code>','gim');var afterEvent=new showdown.Event('makehtml.hashCodeTags.onEnd',text);afterEvent.setOutput(text)._setGlobals(globals)._setOptions(options);afterEvent=globals.converter.dispatch(afterEvent);return afterEvent.output;});showdown.subParser('makehtml.hashElement',function(text,options,globals){'use strict';return function(wholeMatch,m1){var blockText=m1;// Undo double lines
blockText=blockText.replace(/\n\n/g,'\n');blockText=blockText.replace(/^\n/,'');// strip trailing blank lines
blockText=blockText.replace(/\n+$/g,'');// Replace the element text with a marker ("¨KxK" where x is its key)
blockText='\n\n¨K'+(globals.gHtmlBlocks.push(blockText)-1)+'K\n\n';return blockText;};});////
// makehtml/hashHTMLBlocks.js
// Copyright (c) 2018 ShowdownJS
//
// Hash HTML blocks
//
// ***Author:***
// - Estêvão Soares dos Santos (Tivie) <https://github.com/tivie>
////
showdown.subParser('makehtml.hashHTMLBlocks',function(text,options,globals,sourceMode){'use strict';var startEvent=new showdown.Event('makehtml.hashHTMLBlocks.onStart',text);startEvent.setOutput(text)._setGlobals(globals)._setOptions(options);startEvent=globals.converter.dispatch(startEvent);text=startEvent.output;var blockTags=['pre','div','h1','h2','h3','h4','h5','h6','blockquote','table','dl','ol','ul','script','noscript','form','fieldset','iframe','math','style','section','header','footer','nav','article','aside','address','audio','canvas','figure','hgroup','output','video','details','p'],repFunc=function repFunc(wholeMatch,match,left,right){var txt=wholeMatch;// check if this html element is marked as markdown
// if so, it's contents should be parsed as markdown
if(left.search(/\bmarkdown\b/)!==-1){txt=left+globals.converter.makeHtml(match)+right;}return'\n\n¨K'+(globals.gHtmlBlocks.push(txt)-1)+'K\n\n';};if(options.backslashEscapesHTMLTags){// encode backslash escaped HTML tags
text=text.replace(/\\<(\/?[^>]+?)>/g,function(wm,inside){return'&lt;'+inside+'&gt;';});}// The CommonMark block scanner only applies to the original Markdown source
// (the converter-level pass). blockGamut re-invokes this subparser on the markup
// it has just generated to prevent <p>-wrapping; there the existing balanced-tag
// hashing is correct (and must not over-consume generated block tags).
if(options.cmSpec&&sourceMode){text=parseCmHTMLBlocks(text);var cmAfterEvent=new showdown.Event('makehtml.hashHTMLBlocks.onEnd',text);cmAfterEvent.setOutput(text)._setGlobals(globals)._setOptions(options);cmAfterEvent=globals.converter.dispatch(cmAfterEvent);return cmAfterEvent.output;}// hash HTML Blocks
for(var i=0;i<blockTags.length;++i){var opTagPos=void 0,rgx1=new RegExp('^ {0,3}(<'+blockTags[i]+'\\b[^>]*>)','im'),patLeft='<'+blockTags[i]+'\\b[^>]*>',patRight='</'+blockTags[i]+'>';// 1. Look for the first position of the first opening HTML tag in the text
while((opTagPos=showdown.helper.regexIndexOf(text,rgx1))!==-1){// if the HTML tag is \ escaped, we need to escape it and break
//2. Split the text in that position
var subTexts=showdown.helper.splitAtIndex(text,opTagPos),//3. Match recursively
newSubText1=showdown.helper.replaceRecursiveRegExp(subTexts[1],repFunc,patLeft,patRight,'im');// prevent an infinite loop
if(newSubText1===subTexts[1]){break;}text=subTexts[0].concat(newSubText1);}}// HR SPECIAL CASE
text=text.replace(/(\n {0,3}(<(hr)\b([^<>])*?\/?>)[ \t]*(?=\n{2,}))/g,showdown.subParser('makehtml.hashElement')(text,options,globals));// Special case for standalone HTML comments
// A comment is terminated by either `-->` or `--!>` (the HTML "comment end
// bang" state). Matching only `-->` lets content an author believes is
// commented-out leak through to the browser as live HTML (js/bad-tag-filter).
text=showdown.helper.replaceRecursiveRegExp(text,function(txt){return'\n\n¨K'+(globals.gHtmlBlocks.push(txt)-1)+'K\n\n';},'^ {0,3}<!--','--!?>','gm');// PHP and ASP-style processor instructions (<?...?> and <%...%>)
text=text.replace(/\n\n( {0,3}<([?%])[^\r]*?\2>[ \t]*(?=\n{2,}))/g,showdown.subParser('makehtml.hashElement')(text,options,globals));var afterEvent=new showdown.Event('makehtml.hashHTMLBlocks.onEnd',text);afterEvent.setOutput(text)._setGlobals(globals)._setOptions(options);afterEvent=globals.converter.dispatch(afterEvent);return afterEvent.output;/**
   * CommonMark HTML blocks (spec section 4.6): a line-based scanner implementing the
   * 7 block types, each with its own start and end condition. Block content is hashed
   * verbatim (not parsed as Markdown). Types 1-6 may interrupt a paragraph; type 7
   * may only start at a block boundary (document start or after a blank line).
   * @param {string} str
   * @returns {string}
   */function parseCmHTMLBlocks(str){var blockNames='address|article|aside|base|basefont|blockquote|body|caption|'+'center|col|colgroup|dd|details|dialog|dir|div|dl|dt|fieldset|figcaption|figure|'+'footer|form|frame|frameset|h1|h2|h3|h4|h5|h6|head|header|hr|html|iframe|legend|'+'li|link|main|menu|menuitem|nav|noframes|ol|optgroup|option|p|param|section|'+'summary|table|tbody|td|tfoot|th|thead|title|tr|track|ul';var type1Start=/^ {0,3}<(?:script|pre|style|textarea)(?:[ \t>]|$)/i,type1End=/<\/(?:script|pre|style|textarea)>/i,type2Start=/^ {0,3}<!--/,type2End=/-->/,type3Start=/^ {0,3}<\?/,type3End=/\?>/,type4Start=/^ {0,3}<![A-Za-z]/,type4End=/>/,type5Start=/^ {0,3}<!\[CDATA\[/,type5End=/]]>/,type6Start=new RegExp('^ {0,3}</?(?:'+blockNames+')(?:[ \\t/>]|$)','i'),type7Start=new RegExp('^ {0,3}(?:'+showdown.helper.regexes.cmOpenTagSource+'|'+showdown.helper.regexes.cmCloseTagSource+')[ \\t]*$'),isBlank=/^[ \t]*$/;function startType(line,prevBlank){if(type1Start.test(line)){return 1;}if(type2Start.test(line)){return 2;}if(type3Start.test(line)){return 3;}if(type5Start.test(line)){return 5;}if(type4Start.test(line)){return 4;}if(type6Start.test(line)){return 6;}// type 7 cannot interrupt a paragraph
if(prevBlank&&type7Start.test(line)){return 7;}return 0;}// a fenced-code opener (mirrors githubCodeBlock: an info string carries no backtick,
// tilde or tab). A fenced code block takes precedence over an HTML block start, so its
// interior lines must not be scanned for HTML block starts.
var fenceOpen=/^ {0,3}(```+|~~~+) *[^`~\t]*$/;var lines=str.split('\n'),out=[],i=0,prevBlank=true;// document start behaves like "after a blank line"
while(i<lines.length){var type=startType(lines[i],prevBlank);if(type===0){var fence=lines[i].match(fenceOpen);if(fence){// consume the whole fenced region verbatim (closing fence inclusive, or EOF) so
// an HTML-block-like line inside the fence cannot open an HTML block. The fence
// itself is hashed later by githubCodeBlock.
var closeRe=new RegExp('^ {0,3}['+fence[1][0]+']{'+fence[1].length+',}[ \\t]*$');out.push(lines[i]);i++;while(i<lines.length){out.push(lines[i]);if(closeRe.test(lines[i])){i++;break;}i++;}prevBlank=false;continue;}out.push(lines[i]);prevBlank=isBlank.test(lines[i]);i++;continue;}var blockLines=[];if(type>=1&&type<=5){// ends on (and includes) the line matching the end condition; may be the
// start line itself
var endRe=[null,type1End,type2End,type3End,type4End,type5End][type];while(i<lines.length){var l=lines[i];blockLines.push(l);i++;if(endRe.test(l)){break;}}}else{// types 6 and 7 end before the first following blank line (or at EOF)
while(i<lines.length&&!isBlank.test(lines[i])){blockLines.push(lines[i]);i++;}}// wrap the placeholder in blank lines so later block parsing treats it as its
// own block. A CommonMark HTML block is raw verbatim source: its content (including
// any entities) must NOT be decoded, so it gets a distinct `¨R` marker and a separate
// store, and is restored late - after decodeEntities - rather than being unhashed in
// paragraphs like generated `¨K`/`¨G` blocks (which legitimately decode).
out.push('\n¨R'+(globals.gHtmlRawBlocks.push(blockLines.join('\n'))-1)+'R\n');prevBlank=false;}return out.join('\n');}});////
// makehtml/hashHTMLSpans.js
// Copyright (c) 2018 ShowdownJS
//
// Hash span elements that should not be parsed as markdown
//
// ***Author:***
// - Estêvão Soares dos Santos (Tivie) <https://github.com/tivie>
////
showdown.subParser('makehtml.hashHTMLSpans',function(text,options,globals){'use strict';var startEvent=new showdown.Event('makehtml.hashHTMLSpans.onStart',text);startEvent.setOutput(text)._setGlobals(globals)._setOptions(options);startEvent=globals.converter.dispatch(startEvent);text=startEvent.output;// NOTE: the self-closing character classes exclude `<` (`[^<>]` rather than `[^>]`) so a scan
// for a tag's `>` can never run across the start of the *next* `<`. This keeps matching linear
// on pathological input (`'<'.repeat(n)`) without changing results — a real tag has no bare `<`.
// Hash Self Closing tags
if(!options.cmSpec){text=text.replace(/<[^<>]+?\/>/gi,function(wm){return showdown.helper._hashHTMLSpan(wm,globals);});}// Hash whole `<tag …>…</tag>` spans. This is done with a forward cursor + an "absent close
// tag" cache rather than a `<([^<>]+?)>[\s\S]*?<\/\1>` regex: that regex re-scans to EOF for a
// matching `</tag>` at *every* `<` when none follows, which is O(n^2) on inputs like
// `'<a>'.repeat(n)` or `'<a>'.repeat(n) + '</z>'`. The cursor scans each character once and, the
// first time a given `</name>` is found to be absent ahead, never searches for it again. Two
// passes preserve the old ordering/semantics: first tags whose *entire* `<…>` content is the
// closing name (no attributes), then tags whose first token is the closing name (with attributes).
text=hashPairedTags(text,'full');text=hashPairedTags(text,'name');// Hash self closing tags without />. In CommonMark raw-HTML mode the valid inline
// raw HTML has already been hashed earlier (spanGamut); any `<…>` left here is
// malformed and must NOT be hashed (it is escaped by encodeAmpsAndAngles instead).
if(!options.cmSpec){text=text.replace(/<[^<>]+?>/gi,function(wm){return showdown.helper._hashHTMLSpan(wm,globals);});}/**
   * Hash `<open>…</close>` spans in a single linear pass.
   * @param {string} str
   * @param {'full'|'name'} mode `full`: the close tag name is the whole `<…>` content (tags
   *   without attributes); `name`: the content must contain whitespace and the close tag name
   *   is its first token (tags with attributes).
   * @returns {string}
   */function hashPairedTags(str,mode){var out='',i=0,len=str.length,absent=Object.create(null);while(i<len){if(str.charAt(i)!=='<'){out+=str.charAt(i);i++;continue;}var gt=str.indexOf('>',i+1);// a real open tag has a `>` and no `<` between `<` and `>`
if(gt===-1){out+=str.charAt(i);i++;continue;}var inner=str.slice(i+1,gt);if(inner.indexOf('<')!==-1){out+=str.charAt(i);i++;continue;}var closeName=void 0;if(mode==='full'){if(inner.length===0){out+=str.charAt(i);i++;continue;}closeName=inner;}else{var sp=inner.search(/\s/);if(sp<=0){out+=str.charAt(i);i++;continue;}closeName=inner.slice(0,sp);}var closeStr='</'+closeName+'>';if(absent[closeStr]){out+=str.charAt(i);i++;continue;}var ci=str.indexOf(closeStr,gt+1);if(ci===-1){absent[closeStr]=true;out+=str.charAt(i);i++;continue;}out+=showdown.helper._hashHTMLSpan(str.slice(i,ci+closeStr.length),globals);i=ci+closeStr.length;}return out;}var afterEvent=new showdown.Event('makehtml.hashHTMLSpans.onEnd',text);afterEvent.setOutput(text)._setGlobals(globals)._setOptions(options);afterEvent=globals.converter.dispatch(afterEvent);return afterEvent.output;});////
// makehtml/githubCodeBlock.js
// Copyright (c) 2018 ShowdownJS
//
// Hash and escape <pre><code> elements that should not be parsed as markdown
//
// ***Author:***
// - Estêvão Soares dos Santos (Tivie) <https://github.com/tivie>
////
showdown.subParser('makehtml.hashPreCodeTags',function(text,options,globals){'use strict';var startEvent=new showdown.Event('makehtml.hashPreCodeTags.onStart',text);startEvent.setOutput(text)._setGlobals(globals)._setOptions(options);startEvent=globals.converter.dispatch(startEvent);text=startEvent.output;var repFunc=function repFunc(wholeMatch,match,left,right){// encode html entities
var codeblock=left+showdown.subParser('makehtml.encodeCode')(match,options,globals)+right;return'\n\n¨G'+(globals.ghCodeBlocks.push({text:wholeMatch,codeblock:codeblock})-1)+'G\n\n';};// Hash <pre><code>
text=showdown.helper.replaceRecursiveRegExp(text,repFunc,'^ {0,3}<pre\\b[^>]*>\\s*<code\\b[^>]*>','^ {0,3}</code>\\s*</pre>','gim');var afterEvent=new showdown.Event('makehtml.hashPreCodeTags.onEnd',text);afterEvent.setOutput(text)._setGlobals(globals)._setOptions(options);afterEvent=globals.converter.dispatch(afterEvent);return afterEvent.output;});////
// makehtml/blockquote.js
// Copyright (c) 2018 ShowdownJS
//
// Transforms MD headings into `<h#>` html entities
//
// Setext-style headers:
//	Header 1
//	========
//
//	Header 2
//	--------
//
// atx-style headers:
//  # Header 1
//  ## Header 2
//  ## Header 2 with closing hashes ##
//  ...
//  ###### Header 6
//
// ***Author:***
// - Estêvão Soares dos Santos (Tivie) <https://github.com/tivie>
////
(function(){/**
   *
   * @param {string} subEvtName Heading style ('atx' or 'setext'), used to namespace the dispatched events
   * @param {RegExp} pattern
   * @param {string} wholeMatch
   * @param {string} headingText
   * @param {string} headingLevel
   * @param {string} headingId
   * @param {{}} options
   * @param {{}} globals
   * @returns {string}
   */function parseHeader(subEvtName,pattern,wholeMatch,headingText,headingLevel,headingId,options,globals){var captureStartEvent=new showdown.Event('makehtml.heading.'+subEvtName+'.onCapture',headingText),otp;captureStartEvent.setOutput(null)._setGlobals(globals)._setOptions(options).setRegexp(pattern).setMatches({_wholeMatch:wholeMatch,heading:headingText}).setAttributes({id:headingId});captureStartEvent=globals.converter.dispatch(captureStartEvent);// if something was passed as output, it takes precedence
// and will be used as output
if(captureStartEvent.output&&captureStartEvent.output!==''){otp=captureStartEvent.output;}else{headingText=captureStartEvent.matches.heading;var spanGamut=showdown.subParser('makehtml.spanGamut')(headingText,options,globals),attributes=captureStartEvent.attributes;otp='<h'+headingLevel+showdown.helper._populateAttributes(attributes)+'>'+spanGamut+'</h'+headingLevel+'>';}var beforeHashEvent=new showdown.Event('makehtml.heading.'+subEvtName+'.onHash',otp);beforeHashEvent.setOutput(otp)._setGlobals(globals)._setOptions(options);beforeHashEvent=globals.converter.dispatch(beforeHashEvent);otp=beforeHashEvent.output;return showdown.subParser('makehtml.hashBlock')(otp,options,globals);}showdown.subParser('makehtml.heading',function(text,options,globals){'use strict';var startEvent=new showdown.Event('makehtml.heading.onStart',text);startEvent.setOutput(text)._setGlobals(globals)._setOptions(options);startEvent=globals.converter.dispatch(startEvent);text=startEvent.output;text=showdown.subParser('makehtml.heading.setext')(text,options,globals);text=showdown.subParser('makehtml.heading.atx')(text,options,globals);var afterEvent=new showdown.Event('makehtml.heading.onEnd',text);afterEvent.setOutput(text)._setGlobals(globals)._setOptions(options);afterEvent=globals.converter.dispatch(afterEvent);return afterEvent.output;});// Normalize the `headerIds` option into {enabled, prefix, raw}. Accepts `false`
// (no ids), an object `{prefix, raw}`, or `true`/undefined (github ids, no prefix).
function resolveHeaderIds(opt){if(opt===false){return{enabled:false,prefix:'',raw:false};}if(showdown.helper.isObject(opt)){return{enabled:true,prefix:showdown.helper.isString(opt.prefix)?opt.prefix:'',raw:!!opt.raw};}// true / null / undefined / anything else: default github ids, no prefix
return{enabled:true,prefix:'',raw:false};}showdown.subParser('makehtml.heading.id',function(m,options,globals){var title;var cfg=resolveHeaderIds(options.headerIds);// when ids are disabled the caller omits the attribute entirely
if(!cfg.enabled){return null;}// Prefix id to prevent causing inadvertent pre-existing style matches.
title=cfg.prefix+m;if(cfg.raw){// minimal sanitization: only spaces, ', ", > and < become dashes (the prefix is
// included, so it gets the same treatment). WARNING: may produce malformed ids.
title=title.replace(/ /g,'-')// replace previously escaped chars (&, ¨ and $)
.replace(/&amp;/g,'&').replace(/¨T/g,'¨').replace(/¨D/g,'$')// replace ", ', > and <
.replace(/["'><]/g,'-').toLowerCase();}else{// default: GitHub-compatible ids (spaces become dashes, non-alphanumeric chars
// are stripped). Borrowed from github's redcarpet so results are similar.
title=title.replace(/ /g,'-')// replace previously escaped chars (&, ¨ and $)
.replace(/&amp;/g,'').replace(/¨T/g,'').replace(/¨D/g,'')// replace rest of the chars (&~$ are repeated as they might have been escaped)
.replace(/[&+$,/:;=?@"#{}|^¨¿？：~[\]`、゠＝…‥『』〝〟「」\\*()｛｝（）［］【】%.。，¡!！'<>]/g,'').toLowerCase();}if(globals.hashLinkCounts[title]){title=title+'-'+globals.hashLinkCounts[title]++;}else{globals.hashLinkCounts[title]=1;}return title;});showdown.subParser('makehtml.heading.setext',function(text,options,globals){var startEvent=new showdown.Event('makehtml.heading.setext.onStart',text);startEvent.setOutput(text)._setGlobals(globals)._setOptions(options);startEvent=globals.converter.dispatch(startEvent);text=startEvent.output;// NOTE: the first line is `[^ \t\n].*` (single leading non-space + rest) rather
// than `[^ \t\n]+.*`. The latter has two overlapping greedy quantifiers that can
// consume the same characters, causing O(n^2) backtracking on a long line with no
// trailing underline (a DoS via any long whitespace-free input). The two forms
// match the same set of lines and capture the same substring.
var setextRegexH1=/^( {0,3}([^ \t\n].*\n)(.+\n)?(.+\n)?)( {0,3}=+[ \t]*)$/gm,setextRegexH2=/^( {0,3}([^ \t\n].*\n)(.+\n)?(.+\n)?)( {0,3}(-+)[ \t]*)$/gm;// CommonMark: a setext heading's text is a paragraph, so the line(s) before the
// underline may not begin another kind of block. Since setext runs before the
// list/block-quote parsers in blockGamut, a `-` underline would otherwise steal
// list-item lines (e.g. `- foo\n-`). When commonmarkLists is enabled, skip the
// match if the heading text starts a list item or block quote; the container
// parsers then handle those lines (and any genuine underline inside an item).
function cmSkipSetext(headingText){var first=headingText.split('\n')[0];return /^ {0,3}(?:[-+*]|\d{1,9}[.)])(?:[ \t]|$)/.test(first)||/^ {0,3}>/.test(first)||// a fenced-code opener is a leaf block, not setext paragraph text. In container
// mode the converter-level fence pass leaves indent 1-3 fences for the block
// parsers, so they can reach setext here; let the fence (and any container
// marker below it) be claimed by the block/container parsers instead.
/^ {0,3}(?:```|~~~)/.test(first);}text=text.replace(setextRegexH1,function(wholeMatch,headingText,line1,line2,line3,line4){if(options.cmSpec&&cmSkipSetext(headingText)){return wholeMatch;}return parseSetextHeading(setextRegexH2,options.headerLevelStart,wholeMatch,headingText,line1,line2,line3,line4);});text=text.replace(setextRegexH2,function(wholeMatch,headingText,line1,line2,line3,line4){if(options.cmSpec&&cmSkipSetext(headingText)){return wholeMatch;}return parseSetextHeading(setextRegexH2,options.headerLevelStart+1,wholeMatch,headingText,line1,line2,line3,line4);});var afterEvent=new showdown.Event('makehtml.heading.setext.onEnd',text);afterEvent.setOutput(text)._setGlobals(globals)._setOptions(options);afterEvent=globals.converter.dispatch(afterEvent);return showdown.subParser('makehtml.hashHTMLBlocks')(afterEvent.output,options,globals);function parseSetextHeading(pattern,headingLevel,wholeMatch,headingText,line1,line2,line3,line4){// count lines
var count=headingText.trim().split('\n').length;var prepend='';var nPrepend;var hrCheckRgx=/^ {0,3}[-_*]([-_*] ?){2,}$/;// one liner edge cases
if(count===1){// hr
// let's find the hr edge case first
if(showdown.helper.trimEnd(line1).match(hrCheckRgx)){// it's the edge case, so it's a false positive
prepend=showdown.subParser('makehtml.horizontalRule')(line1,options,globals);if(prepend!==line1){// it's an oneliner list
return prepend.trim()+'\n'+line4;}}// now check if it's an unordered list
if(line1.match(/^ {0,3}[-*+][ \t]/)){if(line4.trim().match(/^=+/)){line1+=line4;}prepend=showdown.subParser('makehtml.list')(line1,options,globals);if(prepend!==line1){// it's an oneliner list
return prepend.trim()+'\n'+line4;}}// check if it's a blockquote
if(line1.match(/^ {0,3}>[ \t]?[^ \t]/)){if(line4.trim().match(/^=+/)){line1+=line4;}prepend=showdown.subParser('makehtml.blockquote')(line1,options,globals);if(prepend!==line1){// it's an oneliner blockquote
return prepend.trim()+'\n'+line4;}}// no edge case let's proceed as usual
}else{var multilineText;// multiline is a bit trickier
// first we must take care of the edge cases of:
// case1: |  case2:
// ---    |  ---
// foo    |  foo
// ---    |  bar
//        |  ---
//
if(showdown.helper.trimEnd(line1).match(hrCheckRgx)){nPrepend=showdown.subParser('makehtml.horizontalRule')(line1,options,globals);if(nPrepend!==line1){line1='';// we add the parsed block to prepend
prepend=nPrepend.trim();// and remove the line from the headingText, so it doesn't appear repeated
headingText=line2+(line3?line3:'');}}// now we take care of these cases:
// case1: |  case2:
// foo    |  foo
// ***    |  ***
// ---    |  bar
//        |  ---
//
if(showdown.helper.trimEnd(line2).match(hrCheckRgx)){// This case sucks, because the first line could be anything!!!
// first let's make sure it's a hr
nPrepend=showdown.subParser('makehtml.horizontalRule')(line2,options,globals);if(nPrepend!==line2){line2=nPrepend;// it is, so now we must parse line1 also
if(line1){line1=showdown.subParser('makehtml.blockGamut')(line1,options,globals);line1=showdown.subParser('makehtml.paragraphs')(line1,options,globals);line1=line1.trim()+'\n';prepend=line1;// and clear line1
line1='';}// we add the parsed blocks to prepend
prepend+=line2.trim()+'\n';line2='';// and remove the lines from the headingText, so it doesn't appear repeated
headingText=line3?line3:'';}}// all edge cases should be treated now
multilineText=line1+line2+(line3?line3:'');//if (line4.trim().match(/^=+/)) {
//  multilineText += line4;
//}
nPrepend=showdown.subParser('makehtml.blockGamut')(multilineText,options,globals,'makehtml.heading.setext');if(nPrepend!==multilineText){// we found one or more blocks, so we need to reparse (blocks should take precendence though)
nPrepend=showdown.helper.trimEnd(nPrepend);// let's check if the last line is a parsed block
var newLines=nPrepend.trim().split('\n');var nLastLine=newLines.pop().toString();if(/^¨K\d+K$/.test(nLastLine)||/^\s*$/gm.test(nLastLine)){// everything before --- or === is a block or empty line, so it's a false positive
prepend+=nPrepend+'\n\n';headingText='';}else{// the last line is something else... so let's look at the line before that
var toHeading=nLastLine;nLastLine=newLines.pop().toString();if(/^¨K\d+K$/.test(nLastLine)===false&&/^\s*$/gm.test(nLastLine)===false){toHeading=nLastLine+'\n'+toHeading;}headingText=toHeading;prepend=newLines.join('\n').trim();}}}// trim stuff
headingText=headingText.trim();// let's check if heading is empty
// after looking for blocks, heading text might be empty which is a false positive
if(!headingText){return prepend+line4;}// after this, we're pretty sure it's a heading so let's proceed
// (the id subparser returns null when the headerIds option disables ids)
var id=showdown.subParser('makehtml.heading.id')(headingText,options,globals);return prepend+parseHeader('setext',pattern,wholeMatch,headingText,headingLevel,id,options,globals);}});// Strip a trailing ATX closing sequence — `[ \t]*#*[ \t]*` — from a heading's text, computed
// right-to-left in linear time (a regex `/[ \t]*#*[ \t]*$/` would itself backtrack quadratically
// while searching for the match position). If the whole text is strippable (e.g. `# ###`), the
// legacy lazy regex kept the first character, so we do the same.
function stripAtxClosingSequence(str){var end=str.length;while(end>0&&(str.charAt(end-1)===' '||str.charAt(end-1)==='\t')){end--;}while(end>0&&str.charAt(end-1)==='#'){end--;}while(end>0&&(str.charAt(end-1)===' '||str.charAt(end-1)==='\t')){end--;}var stripped=str.slice(0,end);return stripped===''?str.charAt(0):stripped;}showdown.subParser('makehtml.heading.atx',function(text,options,globals){var startEvent=new showdown.Event('makehtml.heading.atx.onStart',text);startEvent.setOutput(text)._setGlobals(globals)._setOptions(options);startEvent=globals.converter.dispatch(startEvent);text=startEvent.output;// The default variant captures the heading text greedily (`(.+)$`) and strips the optional
// trailing closing-hash sequence (of ANY length, per CommonMark) in code below. The old
// `(.+?)[ \t]*#*[ \t]*$` form backtracked quadratically on a line of many `#` followed by
// text (e.g. `'#'.repeat(n) + ' h'`), because the lazy text capture re-scanned the `#` run
// at every position. The requireSpace variant already can't backtrack that way (its `[ \t]+`
// after the opening hashes fails fast on a pure/`#`-prefixed line), so it is left unchanged.
var atxRegex=options.requireSpaceBeforeHeadingText?/^ {0,3}(#{1,6})[ \t]+(.+?)(?:[ \t]+#+)?[ \t]*$/gm:/^ {0,3}(#{1,6})[ \t]*(.+)$/gm;var stripClosing=!options.requireSpaceBeforeHeadingText;text=text.replace(atxRegex,function(wholeMatch,m1,m2){var headingText=stripClosing?stripAtxClosingSequence(m2):m2,headingLevel=options.headerLevelStart-1+m1.length,// the id subparser returns null when the headerIds option disables ids
id=showdown.subParser('makehtml.heading.id')(headingText,options,globals);return parseHeader('atx',atxRegex,wholeMatch,headingText,headingLevel,id,options,globals);});var afterEvent=new showdown.Event('makehtml.heading.atx.onEnd',text);afterEvent.setOutput(text)._setGlobals(globals)._setOptions(options);afterEvent=globals.converter.dispatch(afterEvent);return showdown.subParser('makehtml.hashHTMLBlocks')(afterEvent.output,options,globals);});})();////
// makehtml/blockquote.js
// Copyright (c) 2018 ShowdownJS
//
// Turn Markdown horizontal rule shortcuts into <hr /> tags.
//
// Any 3 or more unindented consecutive hyphens, asterisks or underscores with or without a space beetween them
// in a single line is considered a horizontal rule
//
// ***Author:***
// - Estêvão Soares dos Santos (Tivie) <https://github.com/tivie>
////
showdown.subParser('makehtml.horizontalRule',function(text,options,globals){'use strict';var startEvent=new showdown.Event('makehtml.horizontalRule.onStart',text);startEvent.setOutput(text)._setGlobals(globals)._setOptions(options);startEvent=globals.converter.dispatch(startEvent);text=startEvent.output;// CommonMark: an *indented* line of consecutive dashes that directly follows a
// non-blank line is a setext underline inside a list item (e.g. `- Bar\n  ---`),
// not a thematic break. When commonmarkLists is on, leave it for the list/setext
// parsers. An unindented `---` after a list item is still a real thematic break
// (it ends the list), and a spaced form like `- - -` is never an underline.
function isSetextUnderlineDash(wholeMatch,args){if(!options.cmSpec||!/^ +-+[ \t]*$/.test(wholeMatch)){return false;}return precededByNonBlank(args[args.length-1],args[args.length-2]);}// parses: --- and - - -
var rgx1=/^ {0,2}( ?-){3,}[ \t]*$/gm;text=text.replace(rgx1,function(wholeMatch){if(isSetextUnderlineDash(wholeMatch,arguments)){return wholeMatch;}return parse(rgx1,wholeMatch);});// parses: -\t-\t-
var rgx2=/^ {0,3}-(\t?-){2,}[ \t]*$/gm;text=text.replace(rgx2,function(wholeMatch){if(isSetextUnderlineDash(wholeMatch,arguments)){return wholeMatch;}return parse(rgx2,wholeMatch);});var rgx3=/^ {0,2}( ?\*){3,}[ \t]*$/gm;text=text.replace(rgx3,function(wholeMatch){return parse(rgx3,wholeMatch);});var rgx4=/^ {0,3}\*(\t?\*){2,}[ \t]*$/gm;text=text.replace(rgx4,function(wholeMatch){return parse(rgx4,wholeMatch);});var rgx5=/^ {0,2}( ?_){3,}[ \t]*$/gm;text=text.replace(rgx5,function(wholeMatch){return parse(rgx5,wholeMatch);});var rgx6=/^ {0,3}_(\t?_){2,}[ \t]*$/gm;text=text.replace(rgx6,function(wholeMatch){return parse(rgx6,wholeMatch);});// super weird horizontal rule
var rgx7=/^ {0,3}(- *){2,}-[ \t]*$/gm;text=text.replace(rgx7,function(wholeMatch){if(isSetextUnderlineDash(wholeMatch,arguments)){return wholeMatch;}return parse(rgx7,wholeMatch);});var rgx8=/^ {0,3}(\* *){2,}\*[ \t]*$/gm;text=text.replace(rgx8,function(wholeMatch){return parse(rgx8,wholeMatch);});var rgx9=/^ {0,3}(_ *){2,}_[ \t]*$/gm;text=text.replace(rgx9,function(wholeMatch){return parse(rgx9,wholeMatch);});var afterEvent=new showdown.Event('makehtml.horizontalRule.onEnd',text);afterEvent.setOutput(text)._setGlobals(globals)._setOptions(options);afterEvent=globals.converter.dispatch(afterEvent);return afterEvent.output;/**
   * Whether the line preceding the match at `offset` is non-blank.
   * @param {string} str
   * @param {number} offset start index of the matched line (a line start)
   * @returns {boolean}
   */function precededByNonBlank(str,offset){if(offset===0){return false;}var end=offset-1,// index of the newline ending the previous line
start=str.lastIndexOf('\n',end-1)+1;return str.slice(start,end).trim()!=='';}/**
   *
   * @param {RegExp} pattern
   * @param {string} wholeMatch
   * @returns {string}
   */function parse(pattern,wholeMatch){var otp;var captureStartEvent=new showdown.Event('makehtml.horizontalRule.onCapture',wholeMatch);captureStartEvent.setOutput(null)._setGlobals(globals)._setOptions(options).setRegexp(pattern).setMatches({_whoteMatch:wholeMatch}).setAttributes({});captureStartEvent=globals.converter.dispatch(captureStartEvent);// if something was passed as output, it takes precedence
// and will be used as output
if(captureStartEvent.output&&captureStartEvent.output!==''){otp=captureStartEvent.output;}else{otp='<hr'+showdown.helper._populateAttributes(captureStartEvent.attributes)+' />';}var beforeHashEvent=new showdown.Event('makehtml.horizontalRule.onHash',otp);beforeHashEvent.setOutput(otp)._setGlobals(globals)._setOptions(options);beforeHashEvent=globals.converter.dispatch(beforeHashEvent);otp=beforeHashEvent.output;otp=showdown.subParser('makehtml.hashBlock')(otp,options,globals);return otp;}});////
// makehtml/htmlEntities.js
// Copyright (c) 2024 ShowdownJS
//
// HTML5 named character reference table (name -> replacement string).
// Generated from the WHATWG HTML5 named character reference set.
// Used by makehtml.decodeEntities to resolve &name; references (CommonMark).
////
showdown.helper.htmlEntities={'AElig':'Æ','AMP':'&','Aacute':'Á','Abreve':'Ă','Acirc':'Â','Acy':'А','Afr':'𝔄','Agrave':'À','Alpha':'Α','Amacr':'Ā','And':'⩓','Aogon':'Ą','Aopf':'𝔸','ApplyFunction':'⁡','Aring':'Å','Ascr':'𝒜','Assign':'≔','Atilde':'Ã','Auml':'Ä','Backslash':'∖','Barv':'⫧','Barwed':'⌆','Bcy':'Б','Because':'∵','Bernoullis':'ℬ','Beta':'Β','Bfr':'𝔅','Bopf':'𝔹','Breve':'˘','Bscr':'ℬ','Bumpeq':'≎','CHcy':'Ч','COPY':'©','Cacute':'Ć','Cap':'⋒','CapitalDifferentialD':'ⅅ','Cayleys':'ℭ','Ccaron':'Č','Ccedil':'Ç','Ccirc':'Ĉ','Cconint':'∰','Cdot':'Ċ','Cedilla':'¸','CenterDot':'·','Cfr':'ℭ','Chi':'Χ','CircleDot':'⊙','CircleMinus':'⊖','CirclePlus':'⊕','CircleTimes':'⊗','ClockwiseContourIntegral':'∲','CloseCurlyDoubleQuote':'”','CloseCurlyQuote':'’','Colon':'∷','Colone':'⩴','Congruent':'≡','Conint':'∯','ContourIntegral':'∮','Copf':'ℂ','Coproduct':'∐','CounterClockwiseContourIntegral':'∳','Cross':'⨯','Cscr':'𝒞','Cup':'⋓','CupCap':'≍','DD':'ⅅ','DDotrahd':'⤑','DJcy':'Ђ','DScy':'Ѕ','DZcy':'Џ','Dagger':'‡','Darr':'↡','Dashv':'⫤','Dcaron':'Ď','Dcy':'Д','Del':'∇','Delta':'Δ','Dfr':'𝔇','DiacriticalAcute':'´','DiacriticalDot':'˙','DiacriticalDoubleAcute':'˝','DiacriticalGrave':'`','DiacriticalTilde':'˜','Diamond':'⋄','DifferentialD':'ⅆ','Dopf':'𝔻','Dot':'¨','DotDot':'⃜','DotEqual':'≐','DoubleContourIntegral':'∯','DoubleDot':'¨','DoubleDownArrow':'⇓','DoubleLeftArrow':'⇐','DoubleLeftRightArrow':'⇔','DoubleLeftTee':'⫤','DoubleLongLeftArrow':'⟸','DoubleLongLeftRightArrow':'⟺','DoubleLongRightArrow':'⟹','DoubleRightArrow':'⇒','DoubleRightTee':'⊨','DoubleUpArrow':'⇑','DoubleUpDownArrow':'⇕','DoubleVerticalBar':'∥','DownArrow':'↓','DownArrowBar':'⤓','DownArrowUpArrow':'⇵','DownBreve':'̑','DownLeftRightVector':'⥐','DownLeftTeeVector':'⥞','DownLeftVector':'↽','DownLeftVectorBar':'⥖','DownRightTeeVector':'⥟','DownRightVector':'⇁','DownRightVectorBar':'⥗','DownTee':'⊤','DownTeeArrow':'↧','Downarrow':'⇓','Dscr':'𝒟','Dstrok':'Đ','ENG':'Ŋ','ETH':'Ð','Eacute':'É','Ecaron':'Ě','Ecirc':'Ê','Ecy':'Э','Edot':'Ė','Efr':'𝔈','Egrave':'È','Element':'∈','Emacr':'Ē','EmptySmallSquare':'◻','EmptyVerySmallSquare':'▫','Eogon':'Ę','Eopf':'𝔼','Epsilon':'Ε','Equal':'⩵','EqualTilde':'≂','Equilibrium':'⇌','Escr':'ℰ','Esim':'⩳','Eta':'Η','Euml':'Ë','Exists':'∃','ExponentialE':'ⅇ','Fcy':'Ф','Ffr':'𝔉','FilledSmallSquare':'◼','FilledVerySmallSquare':'▪','Fopf':'𝔽','ForAll':'∀','Fouriertrf':'ℱ','Fscr':'ℱ','GJcy':'Ѓ','GT':'>','Gamma':'Γ','Gammad':'Ϝ','Gbreve':'Ğ','Gcedil':'Ģ','Gcirc':'Ĝ','Gcy':'Г','Gdot':'Ġ','Gfr':'𝔊','Gg':'⋙','Gopf':'𝔾','GreaterEqual':'≥','GreaterEqualLess':'⋛','GreaterFullEqual':'≧','GreaterGreater':'⪢','GreaterLess':'≷','GreaterSlantEqual':'⩾','GreaterTilde':'≳','Gscr':'𝒢','Gt':'≫','HARDcy':'Ъ','Hacek':'ˇ','Hat':'^','Hcirc':'Ĥ','Hfr':'ℌ','HilbertSpace':'ℋ','Hopf':'ℍ','HorizontalLine':'─','Hscr':'ℋ','Hstrok':'Ħ','HumpDownHump':'≎','HumpEqual':'≏','IEcy':'Е','IJlig':'Ĳ','IOcy':'Ё','Iacute':'Í','Icirc':'Î','Icy':'И','Idot':'İ','Ifr':'ℑ','Igrave':'Ì','Im':'ℑ','Imacr':'Ī','ImaginaryI':'ⅈ','Implies':'⇒','Int':'∬','Integral':'∫','Intersection':'⋂','InvisibleComma':'⁣','InvisibleTimes':'⁢','Iogon':'Į','Iopf':'𝕀','Iota':'Ι','Iscr':'ℐ','Itilde':'Ĩ','Iukcy':'І','Iuml':'Ï','Jcirc':'Ĵ','Jcy':'Й','Jfr':'𝔍','Jopf':'𝕁','Jscr':'𝒥','Jsercy':'Ј','Jukcy':'Є','KHcy':'Х','KJcy':'Ќ','Kappa':'Κ','Kcedil':'Ķ','Kcy':'К','Kfr':'𝔎','Kopf':'𝕂','Kscr':'𝒦','LJcy':'Љ','LT':'<','Lacute':'Ĺ','Lambda':'Λ','Lang':'⟪','Laplacetrf':'ℒ','Larr':'↞','Lcaron':'Ľ','Lcedil':'Ļ','Lcy':'Л','LeftAngleBracket':'⟨','LeftArrow':'←','LeftArrowBar':'⇤','LeftArrowRightArrow':'⇆','LeftCeiling':'⌈','LeftDoubleBracket':'⟦','LeftDownTeeVector':'⥡','LeftDownVector':'⇃','LeftDownVectorBar':'⥙','LeftFloor':'⌊','LeftRightArrow':'↔','LeftRightVector':'⥎','LeftTee':'⊣','LeftTeeArrow':'↤','LeftTeeVector':'⥚','LeftTriangle':'⊲','LeftTriangleBar':'⧏','LeftTriangleEqual':'⊴','LeftUpDownVector':'⥑','LeftUpTeeVector':'⥠','LeftUpVector':'↿','LeftUpVectorBar':'⥘','LeftVector':'↼','LeftVectorBar':'⥒','Leftarrow':'⇐','Leftrightarrow':'⇔','LessEqualGreater':'⋚','LessFullEqual':'≦','LessGreater':'≶','LessLess':'⪡','LessSlantEqual':'⩽','LessTilde':'≲','Lfr':'𝔏','Ll':'⋘','Lleftarrow':'⇚','Lmidot':'Ŀ','LongLeftArrow':'⟵','LongLeftRightArrow':'⟷','LongRightArrow':'⟶','Longleftarrow':'⟸','Longleftrightarrow':'⟺','Longrightarrow':'⟹','Lopf':'𝕃','LowerLeftArrow':'↙','LowerRightArrow':'↘','Lscr':'ℒ','Lsh':'↰','Lstrok':'Ł','Lt':'≪','Map':'⤅','Mcy':'М','MediumSpace':' ','Mellintrf':'ℳ','Mfr':'𝔐','MinusPlus':'∓','Mopf':'𝕄','Mscr':'ℳ','Mu':'Μ','NJcy':'Њ','Nacute':'Ń','Ncaron':'Ň','Ncedil':'Ņ','Ncy':'Н','NegativeMediumSpace':'​','NegativeThickSpace':'​','NegativeThinSpace':'​','NegativeVeryThinSpace':'​','NestedGreaterGreater':'≫','NestedLessLess':'≪','NewLine':'\n','Nfr':'𝔑','NoBreak':'⁠','NonBreakingSpace':' ','Nopf':'ℕ','Not':'⫬','NotCongruent':'≢','NotCupCap':'≭','NotDoubleVerticalBar':'∦','NotElement':'∉','NotEqual':'≠','NotEqualTilde':'≂̸','NotExists':'∄','NotGreater':'≯','NotGreaterEqual':'≱','NotGreaterFullEqual':'≧̸','NotGreaterGreater':'≫̸','NotGreaterLess':'≹','NotGreaterSlantEqual':'⩾̸','NotGreaterTilde':'≵','NotHumpDownHump':'≎̸','NotHumpEqual':'≏̸','NotLeftTriangle':'⋪','NotLeftTriangleBar':'⧏̸','NotLeftTriangleEqual':'⋬','NotLess':'≮','NotLessEqual':'≰','NotLessGreater':'≸','NotLessLess':'≪̸','NotLessSlantEqual':'⩽̸','NotLessTilde':'≴','NotNestedGreaterGreater':'⪢̸','NotNestedLessLess':'⪡̸','NotPrecedes':'⊀','NotPrecedesEqual':'⪯̸','NotPrecedesSlantEqual':'⋠','NotReverseElement':'∌','NotRightTriangle':'⋫','NotRightTriangleBar':'⧐̸','NotRightTriangleEqual':'⋭','NotSquareSubset':'⊏̸','NotSquareSubsetEqual':'⋢','NotSquareSuperset':'⊐̸','NotSquareSupersetEqual':'⋣','NotSubset':'⊂⃒','NotSubsetEqual':'⊈','NotSucceeds':'⊁','NotSucceedsEqual':'⪰̸','NotSucceedsSlantEqual':'⋡','NotSucceedsTilde':'≿̸','NotSuperset':'⊃⃒','NotSupersetEqual':'⊉','NotTilde':'≁','NotTildeEqual':'≄','NotTildeFullEqual':'≇','NotTildeTilde':'≉','NotVerticalBar':'∤','Nscr':'𝒩','Ntilde':'Ñ','Nu':'Ν','OElig':'Œ','Oacute':'Ó','Ocirc':'Ô','Ocy':'О','Odblac':'Ő','Ofr':'𝔒','Ograve':'Ò','Omacr':'Ō','Omega':'Ω','Omicron':'Ο','Oopf':'𝕆','OpenCurlyDoubleQuote':'“','OpenCurlyQuote':'‘','Or':'⩔','Oscr':'𝒪','Oslash':'Ø','Otilde':'Õ','Otimes':'⨷','Ouml':'Ö','OverBar':'‾','OverBrace':'⏞','OverBracket':'⎴','OverParenthesis':'⏜','PartialD':'∂','Pcy':'П','Pfr':'𝔓','Phi':'Φ','Pi':'Π','PlusMinus':'±','Poincareplane':'ℌ','Popf':'ℙ','Pr':'⪻','Precedes':'≺','PrecedesEqual':'⪯','PrecedesSlantEqual':'≼','PrecedesTilde':'≾','Prime':'″','Product':'∏','Proportion':'∷','Proportional':'∝','Pscr':'𝒫','Psi':'Ψ','QUOT':'"','Qfr':'𝔔','Qopf':'ℚ','Qscr':'𝒬','RBarr':'⤐','REG':'®','Racute':'Ŕ','Rang':'⟫','Rarr':'↠','Rarrtl':'⤖','Rcaron':'Ř','Rcedil':'Ŗ','Rcy':'Р','Re':'ℜ','ReverseElement':'∋','ReverseEquilibrium':'⇋','ReverseUpEquilibrium':'⥯','Rfr':'ℜ','Rho':'Ρ','RightAngleBracket':'⟩','RightArrow':'→','RightArrowBar':'⇥','RightArrowLeftArrow':'⇄','RightCeiling':'⌉','RightDoubleBracket':'⟧','RightDownTeeVector':'⥝','RightDownVector':'⇂','RightDownVectorBar':'⥕','RightFloor':'⌋','RightTee':'⊢','RightTeeArrow':'↦','RightTeeVector':'⥛','RightTriangle':'⊳','RightTriangleBar':'⧐','RightTriangleEqual':'⊵','RightUpDownVector':'⥏','RightUpTeeVector':'⥜','RightUpVector':'↾','RightUpVectorBar':'⥔','RightVector':'⇀','RightVectorBar':'⥓','Rightarrow':'⇒','Ropf':'ℝ','RoundImplies':'⥰','Rrightarrow':'⇛','Rscr':'ℛ','Rsh':'↱','RuleDelayed':'⧴','SHCHcy':'Щ','SHcy':'Ш','SOFTcy':'Ь','Sacute':'Ś','Sc':'⪼','Scaron':'Š','Scedil':'Ş','Scirc':'Ŝ','Scy':'С','Sfr':'𝔖','ShortDownArrow':'↓','ShortLeftArrow':'←','ShortRightArrow':'→','ShortUpArrow':'↑','Sigma':'Σ','SmallCircle':'∘','Sopf':'𝕊','Sqrt':'√','Square':'□','SquareIntersection':'⊓','SquareSubset':'⊏','SquareSubsetEqual':'⊑','SquareSuperset':'⊐','SquareSupersetEqual':'⊒','SquareUnion':'⊔','Sscr':'𝒮','Star':'⋆','Sub':'⋐','Subset':'⋐','SubsetEqual':'⊆','Succeeds':'≻','SucceedsEqual':'⪰','SucceedsSlantEqual':'≽','SucceedsTilde':'≿','SuchThat':'∋','Sum':'∑','Sup':'⋑','Superset':'⊃','SupersetEqual':'⊇','Supset':'⋑','THORN':'Þ','TRADE':'™','TSHcy':'Ћ','TScy':'Ц','Tab':'\t','Tau':'Τ','Tcaron':'Ť','Tcedil':'Ţ','Tcy':'Т','Tfr':'𝔗','Therefore':'∴','Theta':'Θ','ThickSpace':'  ','ThinSpace':' ','Tilde':'∼','TildeEqual':'≃','TildeFullEqual':'≅','TildeTilde':'≈','Topf':'𝕋','TripleDot':'⃛','Tscr':'𝒯','Tstrok':'Ŧ','Uacute':'Ú','Uarr':'↟','Uarrocir':'⥉','Ubrcy':'Ў','Ubreve':'Ŭ','Ucirc':'Û','Ucy':'У','Udblac':'Ű','Ufr':'𝔘','Ugrave':'Ù','Umacr':'Ū','UnderBar':'_','UnderBrace':'⏟','UnderBracket':'⎵','UnderParenthesis':'⏝','Union':'⋃','UnionPlus':'⊎','Uogon':'Ų','Uopf':'𝕌','UpArrow':'↑','UpArrowBar':'⤒','UpArrowDownArrow':'⇅','UpDownArrow':'↕','UpEquilibrium':'⥮','UpTee':'⊥','UpTeeArrow':'↥','Uparrow':'⇑','Updownarrow':'⇕','UpperLeftArrow':'↖','UpperRightArrow':'↗','Upsi':'ϒ','Upsilon':'Υ','Uring':'Ů','Uscr':'𝒰','Utilde':'Ũ','Uuml':'Ü','VDash':'⊫','Vbar':'⫫','Vcy':'В','Vdash':'⊩','Vdashl':'⫦','Vee':'⋁','Verbar':'‖','Vert':'‖','VerticalBar':'∣','VerticalLine':'|','VerticalSeparator':'❘','VerticalTilde':'≀','VeryThinSpace':' ','Vfr':'𝔙','Vopf':'𝕍','Vscr':'𝒱','Vvdash':'⊪','Wcirc':'Ŵ','Wedge':'⋀','Wfr':'𝔚','Wopf':'𝕎','Wscr':'𝒲','Xfr':'𝔛','Xi':'Ξ','Xopf':'𝕏','Xscr':'𝒳','YAcy':'Я','YIcy':'Ї','YUcy':'Ю','Yacute':'Ý','Ycirc':'Ŷ','Ycy':'Ы','Yfr':'𝔜','Yopf':'𝕐','Yscr':'𝒴','Yuml':'Ÿ','ZHcy':'Ж','Zacute':'Ź','Zcaron':'Ž','Zcy':'З','Zdot':'Ż','ZeroWidthSpace':'​','Zeta':'Ζ','Zfr':'ℨ','Zopf':'ℤ','Zscr':'𝒵','aacute':'á','abreve':'ă','ac':'∾','acE':'∾̳','acd':'∿','acirc':'â','acute':'´','acy':'а','aelig':'æ','af':'⁡','afr':'𝔞','agrave':'à','alefsym':'ℵ','aleph':'ℵ','alpha':'α','amacr':'ā','amalg':'⨿','amp':'&','and':'∧','andand':'⩕','andd':'⩜','andslope':'⩘','andv':'⩚','ang':'∠','ange':'⦤','angle':'∠','angmsd':'∡','angmsdaa':'⦨','angmsdab':'⦩','angmsdac':'⦪','angmsdad':'⦫','angmsdae':'⦬','angmsdaf':'⦭','angmsdag':'⦮','angmsdah':'⦯','angrt':'∟','angrtvb':'⊾','angrtvbd':'⦝','angsph':'∢','angst':'Å','angzarr':'⍼','aogon':'ą','aopf':'𝕒','ap':'≈','apE':'⩰','apacir':'⩯','ape':'≊','apid':'≋','apos':'\'','approx':'≈','approxeq':'≊','aring':'å','ascr':'𝒶','ast':'*','asymp':'≈','asympeq':'≍','atilde':'ã','auml':'ä','awconint':'∳','awint':'⨑','bNot':'⫭','backcong':'≌','backepsilon':'϶','backprime':'‵','backsim':'∽','backsimeq':'⋍','barvee':'⊽','barwed':'⌅','barwedge':'⌅','bbrk':'⎵','bbrktbrk':'⎶','bcong':'≌','bcy':'б','bdquo':'„','becaus':'∵','because':'∵','bemptyv':'⦰','bepsi':'϶','bernou':'ℬ','beta':'β','beth':'ℶ','between':'≬','bfr':'𝔟','bigcap':'⋂','bigcirc':'◯','bigcup':'⋃','bigodot':'⨀','bigoplus':'⨁','bigotimes':'⨂','bigsqcup':'⨆','bigstar':'★','bigtriangledown':'▽','bigtriangleup':'△','biguplus':'⨄','bigvee':'⋁','bigwedge':'⋀','bkarow':'⤍','blacklozenge':'⧫','blacksquare':'▪','blacktriangle':'▴','blacktriangledown':'▾','blacktriangleleft':'◂','blacktriangleright':'▸','blank':'␣','blk12':'▒','blk14':'░','blk34':'▓','block':'█','bne':'=⃥','bnequiv':'≡⃥','bnot':'⌐','bopf':'𝕓','bot':'⊥','bottom':'⊥','bowtie':'⋈','boxDL':'╗','boxDR':'╔','boxDl':'╖','boxDr':'╓','boxH':'═','boxHD':'╦','boxHU':'╩','boxHd':'╤','boxHu':'╧','boxUL':'╝','boxUR':'╚','boxUl':'╜','boxUr':'╙','boxV':'║','boxVH':'╬','boxVL':'╣','boxVR':'╠','boxVh':'╫','boxVl':'╢','boxVr':'╟','boxbox':'⧉','boxdL':'╕','boxdR':'╒','boxdl':'┐','boxdr':'┌','boxh':'─','boxhD':'╥','boxhU':'╨','boxhd':'┬','boxhu':'┴','boxminus':'⊟','boxplus':'⊞','boxtimes':'⊠','boxuL':'╛','boxuR':'╘','boxul':'┘','boxur':'└','boxv':'│','boxvH':'╪','boxvL':'╡','boxvR':'╞','boxvh':'┼','boxvl':'┤','boxvr':'├','bprime':'‵','breve':'˘','brvbar':'¦','bscr':'𝒷','bsemi':'⁏','bsim':'∽','bsime':'⋍','bsol':'\\','bsolb':'⧅','bsolhsub':'⟈','bull':'•','bullet':'•','bump':'≎','bumpE':'⪮','bumpe':'≏','bumpeq':'≏','cacute':'ć','cap':'∩','capand':'⩄','capbrcup':'⩉','capcap':'⩋','capcup':'⩇','capdot':'⩀','caps':'∩︀','caret':'⁁','caron':'ˇ','ccaps':'⩍','ccaron':'č','ccedil':'ç','ccirc':'ĉ','ccups':'⩌','ccupssm':'⩐','cdot':'ċ','cedil':'¸','cemptyv':'⦲','cent':'¢','centerdot':'·','cfr':'𝔠','chcy':'ч','check':'✓','checkmark':'✓','chi':'χ','cir':'○','cirE':'⧃','circ':'ˆ','circeq':'≗','circlearrowleft':'↺','circlearrowright':'↻','circledR':'®','circledS':'Ⓢ','circledast':'⊛','circledcirc':'⊚','circleddash':'⊝','cire':'≗','cirfnint':'⨐','cirmid':'⫯','cirscir':'⧂','clubs':'♣','clubsuit':'♣','colon':':','colone':'≔','coloneq':'≔','comma':',','commat':'@','comp':'∁','compfn':'∘','complement':'∁','complexes':'ℂ','cong':'≅','congdot':'⩭','conint':'∮','copf':'𝕔','coprod':'∐','copy':'©','copysr':'℗','crarr':'↵','cross':'✗','cscr':'𝒸','csub':'⫏','csube':'⫑','csup':'⫐','csupe':'⫒','ctdot':'⋯','cudarrl':'⤸','cudarrr':'⤵','cuepr':'⋞','cuesc':'⋟','cularr':'↶','cularrp':'⤽','cup':'∪','cupbrcap':'⩈','cupcap':'⩆','cupcup':'⩊','cupdot':'⊍','cupor':'⩅','cups':'∪︀','curarr':'↷','curarrm':'⤼','curlyeqprec':'⋞','curlyeqsucc':'⋟','curlyvee':'⋎','curlywedge':'⋏','curren':'¤','curvearrowleft':'↶','curvearrowright':'↷','cuvee':'⋎','cuwed':'⋏','cwconint':'∲','cwint':'∱','cylcty':'⌭','dArr':'⇓','dHar':'⥥','dagger':'†','daleth':'ℸ','darr':'↓','dash':'‐','dashv':'⊣','dbkarow':'⤏','dblac':'˝','dcaron':'ď','dcy':'д','dd':'ⅆ','ddagger':'‡','ddarr':'⇊','ddotseq':'⩷','deg':'°','delta':'δ','demptyv':'⦱','dfisht':'⥿','dfr':'𝔡','dharl':'⇃','dharr':'⇂','diam':'⋄','diamond':'⋄','diamondsuit':'♦','diams':'♦','die':'¨','digamma':'ϝ','disin':'⋲','div':'÷','divide':'÷','divideontimes':'⋇','divonx':'⋇','djcy':'ђ','dlcorn':'⌞','dlcrop':'⌍','dollar':'$','dopf':'𝕕','dot':'˙','doteq':'≐','doteqdot':'≑','dotminus':'∸','dotplus':'∔','dotsquare':'⊡','doublebarwedge':'⌆','downarrow':'↓','downdownarrows':'⇊','downharpoonleft':'⇃','downharpoonright':'⇂','drbkarow':'⤐','drcorn':'⌟','drcrop':'⌌','dscr':'𝒹','dscy':'ѕ','dsol':'⧶','dstrok':'đ','dtdot':'⋱','dtri':'▿','dtrif':'▾','duarr':'⇵','duhar':'⥯','dwangle':'⦦','dzcy':'џ','dzigrarr':'⟿','eDDot':'⩷','eDot':'≑','eacute':'é','easter':'⩮','ecaron':'ě','ecir':'≖','ecirc':'ê','ecolon':'≕','ecy':'э','edot':'ė','ee':'ⅇ','efDot':'≒','efr':'𝔢','eg':'⪚','egrave':'è','egs':'⪖','egsdot':'⪘','el':'⪙','elinters':'⏧','ell':'ℓ','els':'⪕','elsdot':'⪗','emacr':'ē','empty':'∅','emptyset':'∅','emptyv':'∅','emsp':' ','emsp13':' ','emsp14':' ','eng':'ŋ','ensp':' ','eogon':'ę','eopf':'𝕖','epar':'⋕','eparsl':'⧣','eplus':'⩱','epsi':'ε','epsilon':'ε','epsiv':'ϵ','eqcirc':'≖','eqcolon':'≕','eqsim':'≂','eqslantgtr':'⪖','eqslantless':'⪕','equals':'=','equest':'≟','equiv':'≡','equivDD':'⩸','eqvparsl':'⧥','erDot':'≓','erarr':'⥱','escr':'ℯ','esdot':'≐','esim':'≂','eta':'η','eth':'ð','euml':'ë','euro':'€','excl':'!','exist':'∃','expectation':'ℰ','exponentiale':'ⅇ','fallingdotseq':'≒','fcy':'ф','female':'♀','ffilig':'ﬃ','fflig':'ﬀ','ffllig':'ﬄ','ffr':'𝔣','filig':'ﬁ','fjlig':'fj','flat':'♭','fllig':'ﬂ','fltns':'▱','fnof':'ƒ','fopf':'𝕗','forall':'∀','fork':'⋔','forkv':'⫙','fpartint':'⨍','frac12':'½','frac13':'⅓','frac14':'¼','frac15':'⅕','frac16':'⅙','frac18':'⅛','frac23':'⅔','frac25':'⅖','frac34':'¾','frac35':'⅗','frac38':'⅜','frac45':'⅘','frac56':'⅚','frac58':'⅝','frac78':'⅞','frasl':'⁄','frown':'⌢','fscr':'𝒻','gE':'≧','gEl':'⪌','gacute':'ǵ','gamma':'γ','gammad':'ϝ','gap':'⪆','gbreve':'ğ','gcirc':'ĝ','gcy':'г','gdot':'ġ','ge':'≥','gel':'⋛','geq':'≥','geqq':'≧','geqslant':'⩾','ges':'⩾','gescc':'⪩','gesdot':'⪀','gesdoto':'⪂','gesdotol':'⪄','gesl':'⋛︀','gesles':'⪔','gfr':'𝔤','gg':'≫','ggg':'⋙','gimel':'ℷ','gjcy':'ѓ','gl':'≷','glE':'⪒','gla':'⪥','glj':'⪤','gnE':'≩','gnap':'⪊','gnapprox':'⪊','gne':'⪈','gneq':'⪈','gneqq':'≩','gnsim':'⋧','gopf':'𝕘','grave':'`','gscr':'ℊ','gsim':'≳','gsime':'⪎','gsiml':'⪐','gt':'>','gtcc':'⪧','gtcir':'⩺','gtdot':'⋗','gtlPar':'⦕','gtquest':'⩼','gtrapprox':'⪆','gtrarr':'⥸','gtrdot':'⋗','gtreqless':'⋛','gtreqqless':'⪌','gtrless':'≷','gtrsim':'≳','gvertneqq':'≩︀','gvnE':'≩︀','hArr':'⇔','hairsp':' ','half':'½','hamilt':'ℋ','hardcy':'ъ','harr':'↔','harrcir':'⥈','harrw':'↭','hbar':'ℏ','hcirc':'ĥ','hearts':'♥','heartsuit':'♥','hellip':'…','hercon':'⊹','hfr':'𝔥','hksearow':'⤥','hkswarow':'⤦','hoarr':'⇿','homtht':'∻','hookleftarrow':'↩','hookrightarrow':'↪','hopf':'𝕙','horbar':'―','hscr':'𝒽','hslash':'ℏ','hstrok':'ħ','hybull':'⁃','hyphen':'‐','iacute':'í','ic':'⁣','icirc':'î','icy':'и','iecy':'е','iexcl':'¡','iff':'⇔','ifr':'𝔦','igrave':'ì','ii':'ⅈ','iiiint':'⨌','iiint':'∭','iinfin':'⧜','iiota':'℩','ijlig':'ĳ','imacr':'ī','image':'ℑ','imagline':'ℐ','imagpart':'ℑ','imath':'ı','imof':'⊷','imped':'Ƶ','in':'∈','incare':'℅','infin':'∞','infintie':'⧝','inodot':'ı','int':'∫','intcal':'⊺','integers':'ℤ','intercal':'⊺','intlarhk':'⨗','intprod':'⨼','iocy':'ё','iogon':'į','iopf':'𝕚','iota':'ι','iprod':'⨼','iquest':'¿','iscr':'𝒾','isin':'∈','isinE':'⋹','isindot':'⋵','isins':'⋴','isinsv':'⋳','isinv':'∈','it':'⁢','itilde':'ĩ','iukcy':'і','iuml':'ï','jcirc':'ĵ','jcy':'й','jfr':'𝔧','jmath':'ȷ','jopf':'𝕛','jscr':'𝒿','jsercy':'ј','jukcy':'є','kappa':'κ','kappav':'ϰ','kcedil':'ķ','kcy':'к','kfr':'𝔨','kgreen':'ĸ','khcy':'х','kjcy':'ќ','kopf':'𝕜','kscr':'𝓀','lAarr':'⇚','lArr':'⇐','lAtail':'⤛','lBarr':'⤎','lE':'≦','lEg':'⪋','lHar':'⥢','lacute':'ĺ','laemptyv':'⦴','lagran':'ℒ','lambda':'λ','lang':'⟨','langd':'⦑','langle':'⟨','lap':'⪅','laquo':'«','larr':'←','larrb':'⇤','larrbfs':'⤟','larrfs':'⤝','larrhk':'↩','larrlp':'↫','larrpl':'⤹','larrsim':'⥳','larrtl':'↢','lat':'⪫','latail':'⤙','late':'⪭','lates':'⪭︀','lbarr':'⤌','lbbrk':'❲','lbrace':'{','lbrack':'[','lbrke':'⦋','lbrksld':'⦏','lbrkslu':'⦍','lcaron':'ľ','lcedil':'ļ','lceil':'⌈','lcub':'{','lcy':'л','ldca':'⤶','ldquo':'“','ldquor':'„','ldrdhar':'⥧','ldrushar':'⥋','ldsh':'↲','le':'≤','leftarrow':'←','leftarrowtail':'↢','leftharpoondown':'↽','leftharpoonup':'↼','leftleftarrows':'⇇','leftrightarrow':'↔','leftrightarrows':'⇆','leftrightharpoons':'⇋','leftrightsquigarrow':'↭','leftthreetimes':'⋋','leg':'⋚','leq':'≤','leqq':'≦','leqslant':'⩽','les':'⩽','lescc':'⪨','lesdot':'⩿','lesdoto':'⪁','lesdotor':'⪃','lesg':'⋚︀','lesges':'⪓','lessapprox':'⪅','lessdot':'⋖','lesseqgtr':'⋚','lesseqqgtr':'⪋','lessgtr':'≶','lesssim':'≲','lfisht':'⥼','lfloor':'⌊','lfr':'𝔩','lg':'≶','lgE':'⪑','lhard':'↽','lharu':'↼','lharul':'⥪','lhblk':'▄','ljcy':'љ','ll':'≪','llarr':'⇇','llcorner':'⌞','llhard':'⥫','lltri':'◺','lmidot':'ŀ','lmoust':'⎰','lmoustache':'⎰','lnE':'≨','lnap':'⪉','lnapprox':'⪉','lne':'⪇','lneq':'⪇','lneqq':'≨','lnsim':'⋦','loang':'⟬','loarr':'⇽','lobrk':'⟦','longleftarrow':'⟵','longleftrightarrow':'⟷','longmapsto':'⟼','longrightarrow':'⟶','looparrowleft':'↫','looparrowright':'↬','lopar':'⦅','lopf':'𝕝','loplus':'⨭','lotimes':'⨴','lowast':'∗','lowbar':'_','loz':'◊','lozenge':'◊','lozf':'⧫','lpar':'(','lparlt':'⦓','lrarr':'⇆','lrcorner':'⌟','lrhar':'⇋','lrhard':'⥭','lrm':'‎','lrtri':'⊿','lsaquo':'‹','lscr':'𝓁','lsh':'↰','lsim':'≲','lsime':'⪍','lsimg':'⪏','lsqb':'[','lsquo':'‘','lsquor':'‚','lstrok':'ł','lt':'<','ltcc':'⪦','ltcir':'⩹','ltdot':'⋖','lthree':'⋋','ltimes':'⋉','ltlarr':'⥶','ltquest':'⩻','ltrPar':'⦖','ltri':'◃','ltrie':'⊴','ltrif':'◂','lurdshar':'⥊','luruhar':'⥦','lvertneqq':'≨︀','lvnE':'≨︀','mDDot':'∺','macr':'¯','male':'♂','malt':'✠','maltese':'✠','map':'↦','mapsto':'↦','mapstodown':'↧','mapstoleft':'↤','mapstoup':'↥','marker':'▮','mcomma':'⨩','mcy':'м','mdash':'—','measuredangle':'∡','mfr':'𝔪','mho':'℧','micro':'µ','mid':'∣','midast':'*','midcir':'⫰','middot':'·','minus':'−','minusb':'⊟','minusd':'∸','minusdu':'⨪','mlcp':'⫛','mldr':'…','mnplus':'∓','models':'⊧','mopf':'𝕞','mp':'∓','mscr':'𝓂','mstpos':'∾','mu':'μ','multimap':'⊸','mumap':'⊸','nGg':'⋙̸','nGt':'≫⃒','nGtv':'≫̸','nLeftarrow':'⇍','nLeftrightarrow':'⇎','nLl':'⋘̸','nLt':'≪⃒','nLtv':'≪̸','nRightarrow':'⇏','nVDash':'⊯','nVdash':'⊮','nabla':'∇','nacute':'ń','nang':'∠⃒','nap':'≉','napE':'⩰̸','napid':'≋̸','napos':'ŉ','napprox':'≉','natur':'♮','natural':'♮','naturals':'ℕ','nbsp':' ','nbump':'≎̸','nbumpe':'≏̸','ncap':'⩃','ncaron':'ň','ncedil':'ņ','ncong':'≇','ncongdot':'⩭̸','ncup':'⩂','ncy':'н','ndash':'–','ne':'≠','neArr':'⇗','nearhk':'⤤','nearr':'↗','nearrow':'↗','nedot':'≐̸','nequiv':'≢','nesear':'⤨','nesim':'≂̸','nexist':'∄','nexists':'∄','nfr':'𝔫','ngE':'≧̸','nge':'≱','ngeq':'≱','ngeqq':'≧̸','ngeqslant':'⩾̸','nges':'⩾̸','ngsim':'≵','ngt':'≯','ngtr':'≯','nhArr':'⇎','nharr':'↮','nhpar':'⫲','ni':'∋','nis':'⋼','nisd':'⋺','niv':'∋','njcy':'њ','nlArr':'⇍','nlE':'≦̸','nlarr':'↚','nldr':'‥','nle':'≰','nleftarrow':'↚','nleftrightarrow':'↮','nleq':'≰','nleqq':'≦̸','nleqslant':'⩽̸','nles':'⩽̸','nless':'≮','nlsim':'≴','nlt':'≮','nltri':'⋪','nltrie':'⋬','nmid':'∤','nopf':'𝕟','not':'¬','notin':'∉','notinE':'⋹̸','notindot':'⋵̸','notinva':'∉','notinvb':'⋷','notinvc':'⋶','notni':'∌','notniva':'∌','notnivb':'⋾','notnivc':'⋽','npar':'∦','nparallel':'∦','nparsl':'⫽⃥','npart':'∂̸','npolint':'⨔','npr':'⊀','nprcue':'⋠','npre':'⪯̸','nprec':'⊀','npreceq':'⪯̸','nrArr':'⇏','nrarr':'↛','nrarrc':'⤳̸','nrarrw':'↝̸','nrightarrow':'↛','nrtri':'⋫','nrtrie':'⋭','nsc':'⊁','nsccue':'⋡','nsce':'⪰̸','nscr':'𝓃','nshortmid':'∤','nshortparallel':'∦','nsim':'≁','nsime':'≄','nsimeq':'≄','nsmid':'∤','nspar':'∦','nsqsube':'⋢','nsqsupe':'⋣','nsub':'⊄','nsubE':'⫅̸','nsube':'⊈','nsubset':'⊂⃒','nsubseteq':'⊈','nsubseteqq':'⫅̸','nsucc':'⊁','nsucceq':'⪰̸','nsup':'⊅','nsupE':'⫆̸','nsupe':'⊉','nsupset':'⊃⃒','nsupseteq':'⊉','nsupseteqq':'⫆̸','ntgl':'≹','ntilde':'ñ','ntlg':'≸','ntriangleleft':'⋪','ntrianglelefteq':'⋬','ntriangleright':'⋫','ntrianglerighteq':'⋭','nu':'ν','num':'#','numero':'№','numsp':' ','nvDash':'⊭','nvHarr':'⤄','nvap':'≍⃒','nvdash':'⊬','nvge':'≥⃒','nvgt':'>⃒','nvinfin':'⧞','nvlArr':'⤂','nvle':'≤⃒','nvlt':'<⃒','nvltrie':'⊴⃒','nvrArr':'⤃','nvrtrie':'⊵⃒','nvsim':'∼⃒','nwArr':'⇖','nwarhk':'⤣','nwarr':'↖','nwarrow':'↖','nwnear':'⤧','oS':'Ⓢ','oacute':'ó','oast':'⊛','ocir':'⊚','ocirc':'ô','ocy':'о','odash':'⊝','odblac':'ő','odiv':'⨸','odot':'⊙','odsold':'⦼','oelig':'œ','ofcir':'⦿','ofr':'𝔬','ogon':'˛','ograve':'ò','ogt':'⧁','ohbar':'⦵','ohm':'Ω','oint':'∮','olarr':'↺','olcir':'⦾','olcross':'⦻','oline':'‾','olt':'⧀','omacr':'ō','omega':'ω','omicron':'ο','omid':'⦶','ominus':'⊖','oopf':'𝕠','opar':'⦷','operp':'⦹','oplus':'⊕','or':'∨','orarr':'↻','ord':'⩝','order':'ℴ','orderof':'ℴ','ordf':'ª','ordm':'º','origof':'⊶','oror':'⩖','orslope':'⩗','orv':'⩛','oscr':'ℴ','oslash':'ø','osol':'⊘','otilde':'õ','otimes':'⊗','otimesas':'⨶','ouml':'ö','ovbar':'⌽','par':'∥','para':'¶','parallel':'∥','parsim':'⫳','parsl':'⫽','part':'∂','pcy':'п','percnt':'%','period':'.','permil':'‰','perp':'⊥','pertenk':'‱','pfr':'𝔭','phi':'φ','phiv':'ϕ','phmmat':'ℳ','phone':'☎','pi':'π','pitchfork':'⋔','piv':'ϖ','planck':'ℏ','planckh':'ℎ','plankv':'ℏ','plus':'+','plusacir':'⨣','plusb':'⊞','pluscir':'⨢','plusdo':'∔','plusdu':'⨥','pluse':'⩲','plusmn':'±','plussim':'⨦','plustwo':'⨧','pm':'±','pointint':'⨕','popf':'𝕡','pound':'£','pr':'≺','prE':'⪳','prap':'⪷','prcue':'≼','pre':'⪯','prec':'≺','precapprox':'⪷','preccurlyeq':'≼','preceq':'⪯','precnapprox':'⪹','precneqq':'⪵','precnsim':'⋨','precsim':'≾','prime':'′','primes':'ℙ','prnE':'⪵','prnap':'⪹','prnsim':'⋨','prod':'∏','profalar':'⌮','profline':'⌒','profsurf':'⌓','prop':'∝','propto':'∝','prsim':'≾','prurel':'⊰','pscr':'𝓅','psi':'ψ','puncsp':' ','qfr':'𝔮','qint':'⨌','qopf':'𝕢','qprime':'⁗','qscr':'𝓆','quaternions':'ℍ','quatint':'⨖','quest':'?','questeq':'≟','quot':'"','rAarr':'⇛','rArr':'⇒','rAtail':'⤜','rBarr':'⤏','rHar':'⥤','race':'∽̱','racute':'ŕ','radic':'√','raemptyv':'⦳','rang':'⟩','rangd':'⦒','range':'⦥','rangle':'⟩','raquo':'»','rarr':'→','rarrap':'⥵','rarrb':'⇥','rarrbfs':'⤠','rarrc':'⤳','rarrfs':'⤞','rarrhk':'↪','rarrlp':'↬','rarrpl':'⥅','rarrsim':'⥴','rarrtl':'↣','rarrw':'↝','ratail':'⤚','ratio':'∶','rationals':'ℚ','rbarr':'⤍','rbbrk':'❳','rbrace':'}','rbrack':']','rbrke':'⦌','rbrksld':'⦎','rbrkslu':'⦐','rcaron':'ř','rcedil':'ŗ','rceil':'⌉','rcub':'}','rcy':'р','rdca':'⤷','rdldhar':'⥩','rdquo':'”','rdquor':'”','rdsh':'↳','real':'ℜ','realine':'ℛ','realpart':'ℜ','reals':'ℝ','rect':'▭','reg':'®','rfisht':'⥽','rfloor':'⌋','rfr':'𝔯','rhard':'⇁','rharu':'⇀','rharul':'⥬','rho':'ρ','rhov':'ϱ','rightarrow':'→','rightarrowtail':'↣','rightharpoondown':'⇁','rightharpoonup':'⇀','rightleftarrows':'⇄','rightleftharpoons':'⇌','rightrightarrows':'⇉','rightsquigarrow':'↝','rightthreetimes':'⋌','ring':'˚','risingdotseq':'≓','rlarr':'⇄','rlhar':'⇌','rlm':'‏','rmoust':'⎱','rmoustache':'⎱','rnmid':'⫮','roang':'⟭','roarr':'⇾','robrk':'⟧','ropar':'⦆','ropf':'𝕣','roplus':'⨮','rotimes':'⨵','rpar':')','rpargt':'⦔','rppolint':'⨒','rrarr':'⇉','rsaquo':'›','rscr':'𝓇','rsh':'↱','rsqb':']','rsquo':'’','rsquor':'’','rthree':'⋌','rtimes':'⋊','rtri':'▹','rtrie':'⊵','rtrif':'▸','rtriltri':'⧎','ruluhar':'⥨','rx':'℞','sacute':'ś','sbquo':'‚','sc':'≻','scE':'⪴','scap':'⪸','scaron':'š','sccue':'≽','sce':'⪰','scedil':'ş','scirc':'ŝ','scnE':'⪶','scnap':'⪺','scnsim':'⋩','scpolint':'⨓','scsim':'≿','scy':'с','sdot':'⋅','sdotb':'⊡','sdote':'⩦','seArr':'⇘','searhk':'⤥','searr':'↘','searrow':'↘','sect':'§','semi':';','seswar':'⤩','setminus':'∖','setmn':'∖','sext':'✶','sfr':'𝔰','sfrown':'⌢','sharp':'♯','shchcy':'щ','shcy':'ш','shortmid':'∣','shortparallel':'∥','shy':'­','sigma':'σ','sigmaf':'ς','sigmav':'ς','sim':'∼','simdot':'⩪','sime':'≃','simeq':'≃','simg':'⪞','simgE':'⪠','siml':'⪝','simlE':'⪟','simne':'≆','simplus':'⨤','simrarr':'⥲','slarr':'←','smallsetminus':'∖','smashp':'⨳','smeparsl':'⧤','smid':'∣','smile':'⌣','smt':'⪪','smte':'⪬','smtes':'⪬︀','softcy':'ь','sol':'/','solb':'⧄','solbar':'⌿','sopf':'𝕤','spades':'♠','spadesuit':'♠','spar':'∥','sqcap':'⊓','sqcaps':'⊓︀','sqcup':'⊔','sqcups':'⊔︀','sqsub':'⊏','sqsube':'⊑','sqsubset':'⊏','sqsubseteq':'⊑','sqsup':'⊐','sqsupe':'⊒','sqsupset':'⊐','sqsupseteq':'⊒','squ':'□','square':'□','squarf':'▪','squf':'▪','srarr':'→','sscr':'𝓈','ssetmn':'∖','ssmile':'⌣','sstarf':'⋆','star':'☆','starf':'★','straightepsilon':'ϵ','straightphi':'ϕ','strns':'¯','sub':'⊂','subE':'⫅','subdot':'⪽','sube':'⊆','subedot':'⫃','submult':'⫁','subnE':'⫋','subne':'⊊','subplus':'⪿','subrarr':'⥹','subset':'⊂','subseteq':'⊆','subseteqq':'⫅','subsetneq':'⊊','subsetneqq':'⫋','subsim':'⫇','subsub':'⫕','subsup':'⫓','succ':'≻','succapprox':'⪸','succcurlyeq':'≽','succeq':'⪰','succnapprox':'⪺','succneqq':'⪶','succnsim':'⋩','succsim':'≿','sum':'∑','sung':'♪','sup':'⊃','sup1':'¹','sup2':'²','sup3':'³','supE':'⫆','supdot':'⪾','supdsub':'⫘','supe':'⊇','supedot':'⫄','suphsol':'⟉','suphsub':'⫗','suplarr':'⥻','supmult':'⫂','supnE':'⫌','supne':'⊋','supplus':'⫀','supset':'⊃','supseteq':'⊇','supseteqq':'⫆','supsetneq':'⊋','supsetneqq':'⫌','supsim':'⫈','supsub':'⫔','supsup':'⫖','swArr':'⇙','swarhk':'⤦','swarr':'↙','swarrow':'↙','swnwar':'⤪','szlig':'ß','target':'⌖','tau':'τ','tbrk':'⎴','tcaron':'ť','tcedil':'ţ','tcy':'т','tdot':'⃛','telrec':'⌕','tfr':'𝔱','there4':'∴','therefore':'∴','theta':'θ','thetasym':'ϑ','thetav':'ϑ','thickapprox':'≈','thicksim':'∼','thinsp':' ','thkap':'≈','thksim':'∼','thorn':'þ','tilde':'˜','times':'×','timesb':'⊠','timesbar':'⨱','timesd':'⨰','tint':'∭','toea':'⤨','top':'⊤','topbot':'⌶','topcir':'⫱','topf':'𝕥','topfork':'⫚','tosa':'⤩','tprime':'‴','trade':'™','triangle':'▵','triangledown':'▿','triangleleft':'◃','trianglelefteq':'⊴','triangleq':'≜','triangleright':'▹','trianglerighteq':'⊵','tridot':'◬','trie':'≜','triminus':'⨺','triplus':'⨹','trisb':'⧍','tritime':'⨻','trpezium':'⏢','tscr':'𝓉','tscy':'ц','tshcy':'ћ','tstrok':'ŧ','twixt':'≬','twoheadleftarrow':'↞','twoheadrightarrow':'↠','uArr':'⇑','uHar':'⥣','uacute':'ú','uarr':'↑','ubrcy':'ў','ubreve':'ŭ','ucirc':'û','ucy':'у','udarr':'⇅','udblac':'ű','udhar':'⥮','ufisht':'⥾','ufr':'𝔲','ugrave':'ù','uharl':'↿','uharr':'↾','uhblk':'▀','ulcorn':'⌜','ulcorner':'⌜','ulcrop':'⌏','ultri':'◸','umacr':'ū','uml':'¨','uogon':'ų','uopf':'𝕦','uparrow':'↑','updownarrow':'↕','upharpoonleft':'↿','upharpoonright':'↾','uplus':'⊎','upsi':'υ','upsih':'ϒ','upsilon':'υ','upuparrows':'⇈','urcorn':'⌝','urcorner':'⌝','urcrop':'⌎','uring':'ů','urtri':'◹','uscr':'𝓊','utdot':'⋰','utilde':'ũ','utri':'▵','utrif':'▴','uuarr':'⇈','uuml':'ü','uwangle':'⦧','vArr':'⇕','vBar':'⫨','vBarv':'⫩','vDash':'⊨','vangrt':'⦜','varepsilon':'ϵ','varkappa':'ϰ','varnothing':'∅','varphi':'ϕ','varpi':'ϖ','varpropto':'∝','varr':'↕','varrho':'ϱ','varsigma':'ς','varsubsetneq':'⊊︀','varsubsetneqq':'⫋︀','varsupsetneq':'⊋︀','varsupsetneqq':'⫌︀','vartheta':'ϑ','vartriangleleft':'⊲','vartriangleright':'⊳','vcy':'в','vdash':'⊢','vee':'∨','veebar':'⊻','veeeq':'≚','vellip':'⋮','verbar':'|','vert':'|','vfr':'𝔳','vltri':'⊲','vnsub':'⊂⃒','vnsup':'⊃⃒','vopf':'𝕧','vprop':'∝','vrtri':'⊳','vscr':'𝓋','vsubnE':'⫋︀','vsubne':'⊊︀','vsupnE':'⫌︀','vsupne':'⊋︀','vzigzag':'⦚','wcirc':'ŵ','wedbar':'⩟','wedge':'∧','wedgeq':'≙','weierp':'℘','wfr':'𝔴','wopf':'𝕨','wp':'℘','wr':'≀','wreath':'≀','wscr':'𝓌','xcap':'⋂','xcirc':'◯','xcup':'⋃','xdtri':'▽','xfr':'𝔵','xhArr':'⟺','xharr':'⟷','xi':'ξ','xlArr':'⟸','xlarr':'⟵','xmap':'⟼','xnis':'⋻','xodot':'⨀','xopf':'𝕩','xoplus':'⨁','xotime':'⨂','xrArr':'⟹','xrarr':'⟶','xscr':'𝓍','xsqcup':'⨆','xuplus':'⨄','xutri':'△','xvee':'⋁','xwedge':'⋀','yacute':'ý','yacy':'я','ycirc':'ŷ','ycy':'ы','yen':'¥','yfr':'𝔶','yicy':'ї','yopf':'𝕪','yscr':'𝓎','yucy':'ю','yuml':'ÿ','zacute':'ź','zcaron':'ž','zcy':'з','zdot':'ż','zeetrf':'ℨ','zeta':'ζ','zfr':'𝔷','zhcy':'ж','zigrarr':'⇝','zopf':'𝕫','zscr':'𝓏','zwj':'‍','zwnj':'‌'};////
// makehtml/blockquote.js
// Copyright (c) 2018 ShowdownJS
//
// Turn Markdown image into <img> tags.
//
// ***Author:***
// - Estêvão Soares dos Santos (Tivie) <https://github.com/tivie>
////
showdown.subParser('makehtml.image',function(text,options,globals){'use strict';var startEvent=new showdown.Event('makehtml.image.onStart',text);startEvent.setOutput(text)._setGlobals(globals)._setOptions(options);startEvent=globals.converter.dispatch(startEvent);text=startEvent.output;var inlineRegExp=/!\[([^\]]*?)][ \t]*\([ \t]?<?(\S+?(?:\(\S{0,200}?\)\S{0,200}?)?)>?(?: =([*\d]+[A-Za-z%]{0,4})x([*\d]+[A-Za-z%]{0,4}))?[ \t]*(?:(["'])([^"]*?)\5)?[ \t]?\)/g,crazyRegExp=/!\[([^\]]*?)][ \t]*\([ \t]?<([^>]*)>(?: =([*\d]+[A-Za-z%]{0,4})x([*\d]+[A-Za-z%]{0,4}))?[ \t]*(?:(["'])([^"]*?)\5)?[ \t]?\)/g,base64RegExp=/!\[([^\]]*?)][ \t]*\([ \t]?<?(data:.+?\/.+?;base64,[A-Za-z\d+/=\n]+?)>?(?: =([*\d]+[A-Za-z%]{0,4})x([*\d]+[A-Za-z%]{0,4}))?[ \t]*(?:(["'])([^"]*?)\6)?[ \t]?\)/g,referenceRegExp=/!\[([^\]]*?)] ?(?:\n *)?\[([\s\S]*?)]/g,refShortcutRegExp=/!\[([^[\]]+)]/g;// Every markdown image syntax requires a closing ']'. When there is none there is nothing to
// match, so skip the (backtracking-prone) passes. This also neutralizes pathological inputs
// such as '!['.repeat(n), whose alt-text scan would otherwise cost O(n^2) looking for a ']'.
if(text.indexOf(']')!==-1){// First, handle reference-style labeled images: ![alt text][id]
text=text.replace(referenceRegExp,function(wholeMatch,altText,linkId){return writeImageTag('reference',referenceRegExp,wholeMatch,altText,null,linkId);});// Next, handle inline images:  ![alt text](url =<width>x<height> "optional title")
if(options.cmSpec){// CommonMark inline-image parsing, symmetric to the link scanner: balanced-paren
// and `<...>` destinations, titles, backslash escapes, arbitrary label nesting.
text=parseCmInlineImages(text);}else if(text.indexOf(')')!==-1){// Every legacy inline-image syntax ends in ')'. Without one there is nothing to match, so
// skip these passes — this neutralizes inputs like '![a](' + 'a('.repeat(n) whose
// destination scan would otherwise backtrack quadratically looking for a ')'.
// base64 encoded images
text=text.replace(base64RegExp,function(wholeMatch,altText,url,width,height,m5,title){url=url.replace(/\s/g,'');return writeImageTag('inline',base64RegExp,wholeMatch,altText,url,null,width,height,title);});// cases with crazy urls like ./image/cat1).png
text=text.replace(crazyRegExp,function(wholeMatch,altText,url,width,height,m5,title){url=showdown.helper.applyBaseUrl(options.relativePathBaseUrl,url);return writeImageTag('inline',crazyRegExp,wholeMatch,altText,url,null,width,height,title);});// normal cases
text=text.replace(inlineRegExp,function(wholeMatch,altText,url,width,height,m5,title){url=showdown.helper.applyBaseUrl(options.relativePathBaseUrl,url);return writeImageTag('inline',inlineRegExp,wholeMatch,altText,url,null,width,height,title);});}// handle reference-style shortcuts: ![img text]
text=text.replace(refShortcutRegExp,function(wholeMatch,altText){return writeImageTag('reference',refShortcutRegExp,wholeMatch,altText);});}var afterEvent=new showdown.Event('makehtml.image.onEnd',text);afterEvent.setOutput(text)._setGlobals(globals)._setOptions(options);afterEvent=globals.converter.dispatch(afterEvent);return afterEvent.output;/**
   * @param {string} subEvtName
   * @param {RegExp} pattern
   * @param {string} wholeMatch
   * @param {string} altText
   * @param {string|null} [url]
   * @param {string|null} [linkId]
   * @param {string|null} [width]
   * @param {string|null} [height]
   * @param {string|null} [title]
   * @returns {string}
   */function writeImageTag(subEvtName,pattern,wholeMatch,altText,url,linkId,width,height,title){var gUrls=globals.gUrls,gTitles=globals.gTitles,gDims=globals.gDimensions,matches={_wholeMatch:wholeMatch,_altText:altText,_linkId:linkId,_url:url,_width:width,_height:height,_title:title},otp,attributes;if(linkId){linkId=options.cmSpec?showdown.helper.cmNormalizeLabel(linkId):showdown.helper.caseFold(linkId);}else{linkId=null;}if(!title){title=null;}// Special case for explicit empty url
if(wholeMatch.search(/\(<?\s*>? ?(['"].*['"])?\)$/m)>-1){url='';}else if(showdown.helper.isUndefined(url)||url===''||url===null){if(linkId===''||linkId===null){// lower-case and turn embedded newlines into spaces
linkId=options.cmSpec?showdown.helper.cmNormalizeLabel(altText):showdown.helper.caseFold(altText).replace(/ ?\n/g,' ');}if(!showdown.helper.isUndefined(gUrls[linkId])){url=gUrls[linkId];if(!showdown.helper.isUndefined(gTitles[linkId])){title=gTitles[linkId];}if(!showdown.helper.isUndefined(gDims[linkId])){width=gDims[linkId].width;height=gDims[linkId].height;}}else{return wholeMatch;}}// safeMode: neutralize dangerous URL schemes; data:image/* stays allowed so
// inline base64 images keep working
if(options.safeMode&&!showdown.helper.isSafeUrl(url,{allowDataImage:true})){url='';}if(options.cmSpec){// CommonMark: the alt text is the plain-text rendering of the label, with
// inline markup stripped (`![foo *bar*]` -> alt="foo bar").
altText=flattenAltText(altText);}altText=altText.replace(/"/g,'&quot;')//altText = showdown.helper.escapeCharacters(altText, '*_', false);
.replace(showdown.helper.regexes.asteriskDashTildeAndColon,showdown.helper.escapeCharactersCallback);//url = showdown.helper.escapeCharacters(url, '*_', false);
if(options.cmSpec){url=showdown.helper.cmNormalizeURL(url);}url=url.replace(showdown.helper.regexes.asteriskDashTildeAndColon,showdown.helper.escapeCharactersCallback);// escape characters that would otherwise break out of the quoted src attribute
// (a `"` in the URL is an attribute-injection vector). cmSpec flavors already
// percent-encode the URL above, so this is a no-op there.
url=url.replace(/"/g,'&quot;').replace(/</g,'&lt;').replace(/>/g,'&gt;');if(title&&showdown.helper.isString(title)){if(options.cmSpec){title=showdown.helper.cmEscapeTitle(title);}else{title=title.replace(/"/g,'&quot;');}title=title.replace(showdown.helper.regexes.asteriskDashTildeAndColon,showdown.helper.escapeCharactersCallback);}// Image dimensions are a non-standard extension: only honor them when
// parseImgDimensions is enabled. The `=WxH` syntax is still consumed by the
// regex (so it never leaks into the output), but the width/height attributes
// are dropped when the option is off - matching the reference-definition path,
// which only stores dimensions in gDimensions when the option is on.
if(options.parseImgDimensions&&width){width=width==='*'?'auto':width;}else{width=null;}if(options.parseImgDimensions&&height){height=height==='*'?'auto':height;}else{height=null;}var captureStartEvent=new showdown.Event('makehtml.image.'+subEvtName+'.onCapture',wholeMatch);captureStartEvent.setOutput(null)._setGlobals(globals)._setOptions(options).setRegexp(pattern).setMatches(matches).setAttributes({src:url,alt:altText,title:title,width:width,height:height});captureStartEvent=globals.converter.dispatch(captureStartEvent);// if something was passed as output, it takes precedence
// and will be used as output
if(captureStartEvent.output&&captureStartEvent.output!==''){otp=captureStartEvent.output;}else{attributes=captureStartEvent.attributes;otp='<img'+showdown.helper._populateAttributes(attributes)+' />';}var beforeHashEvent=new showdown.Event('makehtml.image.'+subEvtName+'.onHash',otp);beforeHashEvent.setOutput(otp)._setGlobals(globals)._setOptions(options);beforeHashEvent=globals.converter.dispatch(beforeHashEvent);otp=beforeHashEvent.output;return otp;}/**
   * CommonMark inline-image scanner. Finds `![label](destination "title")` spans,
   * matching the label by counting nested brackets and parsing the destination and
   * title with the shared cursor-based helpers.
   * @param {string} str
   * @returns {string}
   */function parseCmInlineImages(str){var inlineImgRegexp=/!\[[\s\S]*?]\([\s\S]*?\)/,// representative pattern (event metadata only)
n=str.length,out='',last=0,i=0;while(i<n){if(str.charAt(i)!=='!'||str.charAt(i+1)!=='['){i++;continue;}var depth=1,k=i+2,labelEnd=-1;while(k<n){var c=str.charAt(k);if(c==='\\'&&k+1<n){k+=2;continue;}if(c==='['){depth++;}else if(c===']'){depth--;if(depth===0){labelEnd=k;break;}}k++;}if(labelEnd!==-1&&str.charAt(labelEnd+1)==='('){var parsed=parseCmImgDestTitle(str,labelEnd+2);if(parsed){var label=str.slice(i+2,labelEnd),url=showdown.helper.applyBaseUrl(options.relativePathBaseUrl,parsed.url);out+=str.slice(last,i);out+=writeImageTag('inline',inlineImgRegexp,str.slice(i,parsed.end+1),label,url,null,null,null,parsed.title);i=parsed.end+1;last=i;continue;}}i++;}out+=str.slice(last);return out;}/**
   * Parse an image destination and optional title starting just after the opening
   * `(`. Returns `{url, title, end}` (end = index of the closing `)`) or `null`.
   * @param {string} str
   * @param {number} j
   * @returns {{url: string, title: (string|null), end: number}|null}
   */function parseCmImgDestTitle(str,j){var n=str.length,isWs=function isWs(c){return c===' '||c==='\t'||c==='\n';};while(j<n&&isWs(str.charAt(j))){j++;}var dest=showdown.helper.cmScanDestination(str,j);if(!dest){return null;}j=dest.end;var hadWs=false;while(j<n&&isWs(str.charAt(j))){hadWs=true;j++;}var title=null,tc=str.charAt(j);if(j<n&&(tc==='"'||tc==='\''||tc==='(')){if(!hadWs){return null;}var t=showdown.helper.cmScanTitle(str,j);if(!t){return null;}title=t.title;j=t.end;}while(j<n&&isWs(str.charAt(j))){j++;}if(j>=n||str.charAt(j)!==')'){return null;}return{url:dest.url,title:title,end:j};}/**
   * Produce the plain-text rendering of an image label for the `alt` attribute,
   * per CommonMark: inline markup is processed and then flattened to text. Nested
   * images contribute their own alt text; links and emphasis contribute their
   * text content.
   * @param {string} t
   * @returns {string}
   */function flattenAltText(t){t=showdown.subParser('makehtml.codeSpan')(t,options,globals);t=showdown.subParser('makehtml.image')(t,options,globals);t=showdown.subParser('makehtml.link')(t,options,globals);t=showdown.subParser('makehtml.emoji')(t,options,globals);t=showdown.subParser('makehtml.underline')(t,options,globals);t=showdown.subParser('makehtml.emphasisAndStrong')(t,options,globals);t=showdown.subParser('makehtml.strikethrough')(t,options,globals);t=showdown.subParser('makehtml.ellipsis')(t,options,globals);// restore hashed spans (e.g. produced by nested images/links) so we can read them
t=showdown.subParser('makehtml.unhashHTMLSpans')(t,options,globals);// a nested image contributes its alt text; every other tag contributes its
// text content, so replace images by their alt then strip the remaining tags
t=t.replace(/<img\b[^>]*?\salt="([^"]*)"[^>]*?\/?>/g,'$1');t=t.replace(/<[^>]*>/g,'');return t;}});////
// makehtml/links.js
// Copyright (c) 2018 ShowdownJS
//
// Transforms MD links into `<a>` html anchors
//
// A link contains link text (the visible text), a link destination (the URI that is the link destination), and
// optionally a link title. There are two basic kinds of links in Markdown.
// In inline links the destination and title are given immediately after the link text.
// In reference links the destination and title are defined elsewhere in the document.
//
// ***Author:***
// - Estevão Soares dos Santos (Tivie) <https://github.com/tivie>
////
showdown.subParser('makehtml.link',function(text,options,globals){//
// Parser starts here
//
var startEvent=new showdown.Event('makehtml.link.onStart',text);startEvent.setOutput(text)._setGlobals(globals)._setOptions(options);startEvent=globals.converter.dispatch(startEvent);text=startEvent.output;// Every markdown link/reference syntax requires a closing ']'. When there is none there is
// nothing for the (backtracking-prone) reference/inline passes to match, so skip them. This
// also neutralizes pathological inputs such as '['.repeat(n), which would otherwise cost
// O(n^2) as each pass scans forward for a ']' that never appears. Autolinks (< >) below do
// not need ']', so they stay outside this guard.
if(text.indexOf(']')!==-1){// 1. Handle reference-style links: [link text] [id]
// The label sub-pattern excludes `[` from the inner negated class (`[^\][]` not `[^\]]`) so a
// scan for the label's closing `]` cannot run across the `[` that starts the *next* bracket
// group. This keeps matching linear on inputs like `'[^'.repeat(n) + ' ]'` (which contain a
// stray `]` that defeats the earlier "no `]` at all" fast-path) without changing results.
var referenceRegex=/\[((?:\[[^\][]*]|[^[\]])*)] ?(?:\n *)?\[(.*?)]/g;text=text.replace(referenceRegex,function(wholeMatch,text,linkId){// bail if we find 2 newlines somewhere
if(/\n\n/.test(wholeMatch)){return wholeMatch;}return writeAnchorTag('reference',referenceRegex,wholeMatch,text,linkId);});// 2. Handle inline-style links: [link text](url "optional title")
if(options.cmSpec){// CommonMark inline-link parsing: a manual scanner that handles balanced-paren
// and `<...>` destinations, titles in "...", '...' or (...), and backslash escapes.
text=parseCmInlineLinks(text);}else if(text.indexOf(')')!==-1){// Every legacy inline-link syntax ends in ')'. Without one there is nothing to match, so
// skip these passes — this neutralizes pathological inputs like '[a](' + 'a('.repeat(n),
// whose destination scan would otherwise backtrack quadratically looking for a ')'.
// 2.1. Look for empty cases: []() and [empty]() and []("title")
// The link text and title captures exclude their own delimiters (`[^\]]*?` / `[^"']*`)
// rather than the unbounded `.*?` / `.*`. Run as the first inline pass over raw text, the
// old form re-scanned O(n) chars from every `[` on balanced nested input like
// `'[x]('.repeat(n) + 'u' + ')'.repeat(n)`, giving O(n^2). This mirrors the bounding already
// applied to the sibling inline/image regexes (the image parser likewise disallows `]` in alt).
var inlineEmptyRegex=/\[([^\]]*?)]\(<? ?>? ?(["']([^"']*)["'])?\)/g;text=text.replace(inlineEmptyRegex,function(wholeMatch,text,m1,title){return writeAnchorTag('inline',inlineEmptyRegex,wholeMatch,text,null,null,title,true);});// 2.2. Look for cases with crazy urls like ./image/cat1).png
// the url mus be enclosed in <>
var inlineCrazyRegex=/\[((?:\[[^\][]*]|[^[\]])*)]\s?\([ \t]?<([^>]*)>(?:[ \t]*((["'])([^"]*?)\4))?[ \t]?\)/g;text=text.replace(inlineCrazyRegex,function(wholeMatch,text,url,m1,m2,title){return writeAnchorTag('inline',inlineCrazyRegex,wholeMatch,text,null,url,title);});// 2.3. inline links with no title or titles wrapped in ' or ":
// [text](url.com) || [text](<url.com>) || [text](url.com "title") || [text](<url.com> "title")
var inlineNormalRegex1=/\[([\S ]*?)]\s?\( *<?([^\s'"]*?(?:\(\S{0,200}?\)\S{0,200}?)?)>?\s*(?:(['"])(.*?)\3)? *\)/g;text=text.replace(inlineNormalRegex1,function(wholeMatch,text,url,m1,title){return writeAnchorTag('inline',inlineNormalRegex1,wholeMatch,text,null,url,title);});// 2.4. inline links with titles wrapped in (): [foo](bar.com (title))
var inlineNormalRegex2=/\[([\S ]*?)]\s?\( *<?([^\s'"]*?(?:\(\S{0,200}?\)\S{0,200}?)?)>?\s+\((.*?)\) *\)/g;text=text.replace(inlineNormalRegex2,function(wholeMatch,text,url,title){return writeAnchorTag('inline',inlineNormalRegex2,wholeMatch,text,null,url,title);});}// 3. Handle reference-style shortcuts: [link text]
// These must come last in case there's a [link text][1] or [link text](/foo)
var referenceShortcutRegex=/\[([^[\]]+)]/g;text=text.replace(referenceShortcutRegex,function(wholeMatch,text){return writeAnchorTag('reference',referenceShortcutRegex,wholeMatch,text);});}// 4. Handle angle brackets links -> `<http://example.com/>`
// Must come after links, because you can use < and > delimiters in inline links like [this](<url>).
if(options.cmSpec){// CommonMark autolinks: any scheme (2-32 chars) URI, and emails, with no entity encoding.
// 4.1. URI autolinks: <scheme:rest>
// eslint-disable-next-line no-control-regex -- CommonMark autolinks exclude control chars (\x00-\x20) per spec
var cmUriAutolinkRegex=/<([A-Za-z][A-Za-z0-9+.-]{1,31}:[^<>\x00-\x20]*)>/g;text=text.replace(cmUriAutolinkRegex,function(wholeMatch,uri){// backslash escapes do not work inside autolinks, so restore them to literal backslash + char
var raw=showdown.subParser('makehtml.unescapeSpecialChars')(uri.replace(/(¨E\d+E)/g,'\\$1'),options,globals);// safeMode: neutralize dangerous autolink schemes but keep the visible text
var href=options.safeMode&&!showdown.helper.isSafeUrl(raw)?'':cmEscapeHref(showdown.helper.cmEncodeURI(raw));var otp='<a href="'+href+'">'+cmEscapeText(raw)+'</a>';return showdown.subParser('makehtml.hashHTMLSpans')(otp,options,globals);});// 4.2. Email autolinks: <foo@bar.example.com>
var cmEmailAutolinkRegex=/<([a-zA-Z0-9.!#$%&'*+/=?^_`{|}~-]+@[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?(?:\.[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?)*)>/g;text=text.replace(cmEmailAutolinkRegex,function(wholeMatch,email){var raw=showdown.subParser('makehtml.unescapeSpecialChars')(email.replace(/(¨E\d+E)/g,'\\$1'),options,globals);var otp='<a href="'+cmEscapeHref('mailto:'+raw)+'">'+cmEscapeText(raw)+'</a>';return showdown.subParser('makehtml.hashHTMLSpans')(otp,options,globals);});}else{// 4.1. Handle links first
var angleBracketsLinksRegex=/<(((?:https?|ftp):\/\/|www\.)[^'">\s]+)>/gi;text=text.replace(angleBracketsLinksRegex,function(wholeMatch,url,urlStart){// backslash escaped characters do not work inside autolinks (according to commonmark spec... sure)
// so let's unescape them (and add a backslash html entity before)
url=url.replace(/(¨E\d+E)/g,'\\$1');url=showdown.subParser('makehtml.unescapeSpecialChars')(url,options,globals);var text=url;// now let's replace some entities which should be properly url encoded
url=showdown.helper.urlASCIIEncoding(url);// noinspection HttpUrlsUsage
url=urlStart==='www.'?(options.httpsAutoLinks?'https://':'http://')+url:url;return writeAnchorTag('angleBrackets',angleBracketsLinksRegex,wholeMatch,text,null,url);});// 4.2. Then mail adresses
var angleBracketsMailRegex=/<(?:mailto:)?([-.\w]+@[-a-z\d]+(\.[-a-z\d]+)*\.[a-z]+)>/gi;text=text.replace(angleBracketsMailRegex,function(wholeMatch,mail){var m=parseMail(mail);return writeAnchorTag('angleBrackets',angleBracketsMailRegex,wholeMatch,m.mail,null,m.url);});}// 5. Handle GithubMentions (if option is enabled)
if(options.ghMentions){var ghMentionsRegex=/(^|\s)(\\)?(@([a-z\d]+(?:[a-z\d._-]+?[a-z\d]+)*))/gi;text=text.replace(ghMentionsRegex,function(wholeMatch,st,escape,mentions,username){// bail if the mentions was escaped
if(escape==='\\'){return st+mentions;}// check if options.ghMentionsLink is a string
// TODO Validation should be done at initialization not at runtime
if(!showdown.helper.isString(options.ghMentionsLink)){throw new Error('ghMentionsLink option must be a string');}var url=options.ghMentionsLink.replace(/\{u}/g,username);return st+writeAnchorTag('reference',ghMentionsRegex,wholeMatch,mentions,null,url);});}// 6 and 7 have to come here to prevent naked links to catch html
// 6. Handle <a> tags
text=text.replace(/<a\s[^>]*>[\s\S]*<\/a>/g,function(wholeMatch){return showdown.helper._hashHTMLSpan(wholeMatch,globals);});// 7. Handle <img> tags
text=text.replace(/<img\s[^>]*\/?>/g,function(wholeMatch){return showdown.helper._hashHTMLSpan(wholeMatch,globals);});// 8. Handle naked links (if option is enabled)
if(options.simplifiedAutoLink){// 8.1. Check for naked URLs
// we also include leading markdown magic chars [_*~] for cases like __https://www.google.com/foobar__
var nakedUrlRegex=/([_*~]*?)(((?:https?|ftp):\/\/|www\.)[^\s<>"'`´.-][^\s<>"'`´]*?\.[a-z\d.]+[^\s<>"']*)\1/gi;text=text.replace(nakedUrlRegex,function(wholeMatch,leadingMDChars,url,urlPrefix){// we now will start traversing the url from the front to back, looking for punctuation chars [_*~,;:.!?\)\]]
var len=url.length;var suffix='';for(var i=len-1;i>=0;--i){var char=url.charAt(i);if(/[_*~,;:.!?]/.test(char)){// it's a punctuation char so we remove it from the url
url=url.slice(0,-1);// and prepend it to the suffix
suffix=char+suffix;}else if(/[)\]]/.test(char)){// it's a parenthesis so we need to check for "balance" (kinda)
var opPar=void 0,clPar=void 0;if(/\)/.test(char)){// it's a curved parenthesis
opPar=url.match(/\(/g)||[];clPar=url.match(/\)/g);}else{// it's a squared parenthesis
opPar=url.match(/\[/g)||[];clPar=url.match(/]/g);}if(opPar.length<clPar.length){// there are more closing Parenthesis than opening so chop it!!!!!
url=url.slice(0,-1);// and prepend it to the suffix
suffix=char+suffix;}else{// it's (kinda) balanced so our work is done
break;}}else{// it's not a punctuation or a parenthesis so our work is done
break;}}// we copy the treated url to the text variable
var txt=url;// finally, if it's a www shortcut, we prepend http(s)
// noinspection HttpUrlsUsage
url=urlPrefix==='www.'?(options.httpsAutoLinks?'https://':'http://')+url:url;// url part is done so let's take care of text now
// we need to escape the text (because of links such as www.example.com/foo__bar__baz)
txt=txt.replace(showdown.helper.regexes.asteriskDashTildeAndColon,showdown.helper.escapeCharactersCallback);// and return the link tag, with the leadingMDChars and  suffix. The leadingMDChars are added at the end too because
// we consumed those characters in the regexp
return leadingMDChars+writeAnchorTag('autoLink',nakedUrlRegex,wholeMatch,txt,null,url)+suffix+leadingMDChars;});// 8.2. Now check for naked mail
var nakedMailRegex=/(^|\s)(?:mailto:)?([A-Za-z\d!#$%&'*+-/=?^_`{|}~.]+@[-a-z\d]+(\.[-a-z\d]+)*\.[a-z]+)(?=$|\s)/gmi;text=text.replace(nakedMailRegex,function(wholeMatch,leadingChar,mail){var m=parseMail(mail);return leadingChar+writeAnchorTag('autoLink',nakedMailRegex,wholeMatch,m.mail,null,m.url);});}var afterEvent=new showdown.Event('makehtml.link.onEnd',text);afterEvent.setOutput(text)._setGlobals(globals)._setOptions(options);afterEvent=globals.converter.dispatch(afterEvent);return afterEvent.output;/**
   * CommonMark inline-link scanner. Finds `[label](destination "title")` spans,
   * parsing the destination and title with a hand-written cursor so that balanced
   * parentheses, `<...>` destinations, the three title delimiters and backslash
   * escapes are handled per the spec. Anything that does not parse as a valid
   * inline link is left untouched (to be handled by the reference/shortcut passes).
   * @param {string} str
   * @returns {string}
   */function parseCmInlineLinks(str){var inlineLinkRegexp=/\[[\s\S]*?]\([\s\S]*?\)/,// representative pattern (for event metadata only)
n=str.length,out='',last=0,i=0;while(i<n){if(str.charAt(i)!=='['){i++;continue;}// find the matching `]`, counting nested brackets and honoring backslash escapes
var depth=1,k=i+1,labelEnd=-1;while(k<n){var c=str.charAt(k);if(c==='\\'&&k+1<n){k+=2;continue;}if(c==='['){depth++;}else if(c===']'){depth--;if(depth===0){labelEnd=k;break;}}k++;}if(labelEnd!==-1&&str.charAt(labelEnd+1)==='('){var parsed=parseCmDestTitle(str,labelEnd+2);if(parsed){var label=str.slice(i+1,labelEnd);out+=str.slice(last,i);out+=writeAnchorTag('inline',inlineLinkRegexp,str.slice(i,parsed.end+1),label,null,parsed.url,parsed.title,parsed.emptyCase);i=parsed.end+1;last=i;continue;}}// not a valid inline link here; advance past this `[` so a nested `[...]( )`
// still gets a chance to match
i++;}out+=str.slice(last);return out;}/**
   * Parse a CommonMark link destination and optional title starting just after the
   * opening `(`. Returns `{url, title, emptyCase, end}` where `end` is the index of
   * the closing `)`, or `null` if the span is not a valid destination/title.
   * @param {string} str
   * @param {number} j index right after the opening `(`
   * @returns {{url: string, title: (string|null), emptyCase: boolean, end: number}|null}
   */function parseCmDestTitle(str,j){var n=str.length,isWs=function isWs(c){return c===' '||c==='\t'||c==='\n';},url,emptyCase=false;// optional leading whitespace
while(j<n&&isWs(str.charAt(j))){j++;}if(str.charAt(j)==='<'){// angle-bracket destination: up to an unescaped `>`, no raw newline or `<`
j++;var buf='';while(j<n&&str.charAt(j)!=='>'){var c=str.charAt(j);if(c==='\n'||c==='<'){return null;}if(c==='\\'&&j+1<n){buf+=c+str.charAt(j+1);j+=2;continue;}buf+=c;j++;}if(j>=n||str.charAt(j)!=='>'){return null;}j++;// consume `>`
url=buf;if(url===''){emptyCase=true;}}else{// bare destination: balanced parentheses, ends at whitespace or an unbalanced `)`
var depth=0,_buf2='';while(j<n){var _c2=str.charAt(j);if(_c2==='\\'&&j+1<n){_buf2+=_c2+str.charAt(j+1);j+=2;continue;}if(isWs(_c2)){break;}if(_c2==='('){depth++;_buf2+=_c2;j++;continue;}if(_c2===')'){if(depth===0){break;}depth--;_buf2+=_c2;j++;continue;}_buf2+=_c2;j++;}if(depth!==0){return null;}// unbalanced parens -> not a link
url=_buf2;if(url===''){emptyCase=true;}}// optional whitespace separating destination and title
var hadWs=false;while(j<n&&isWs(str.charAt(j))){hadWs=true;j++;}var title=null,tc=str.charAt(j);if(j<n&&(tc==='"'||tc==='\''||tc==='(')){// a title must be separated from the destination by whitespace
if(!hadWs){return null;}var close=tc==='('?')':tc,_buf3='';j++;var closed=false;while(j<n){var _c3=str.charAt(j);if(_c3==='\\'&&j+1<n){_buf3+=_c3+str.charAt(j+1);j+=2;continue;}if(tc==='('&&_c3==='('){return null;}// unescaped `(` invalid in (...) title
if(_c3===close){closed=true;j++;break;}_buf3+=_c3;j++;}if(!closed){return null;}title=_buf3;}// optional trailing whitespace, then the required closing `)`
while(j<n&&isWs(str.charAt(j))){j++;}if(j>=n||str.charAt(j)!==')'){return null;}return{url:url,title:title,emptyCase:emptyCase,end:j};}/**
   *
   * @param {string} subEvtName
   * @param {RegExp} pattern
   * @param {string} wholeMatch
   * @param {string} text
   * @param {string|null} [linkId]
   * @param {string|null} [url]
   * @param {string|null} [title]
   * @param {boolean} [emptyCase]
   * @returns {string}
   */function writeAnchorTag(subEvtName,pattern,wholeMatch,text,linkId,url,title,emptyCase){var matches={_wholeMatch:wholeMatch,_linkId:linkId,_url:url,_title:title,text:text},otp,attributes={};title=title||null;url=url||null;if(linkId){linkId=options.cmSpec?showdown.helper.cmNormalizeLabel(linkId):showdown.helper.caseFold(linkId);}else{linkId=null;}emptyCase=!!emptyCase;if(emptyCase){url='';}else if(!url){if(!linkId){// lower-case and turn embedded newlines into spaces
linkId=options.cmSpec?showdown.helper.cmNormalizeLabel(text):showdown.helper.caseFold(text).replace(/ ?\n/g,' ');}if(!showdown.helper.isUndefined(globals.gUrls[linkId])){url=globals.gUrls[linkId];if(!showdown.helper.isUndefined(globals.gTitles[linkId])){title=globals.gTitles[linkId];}}else{return wholeMatch;}}url=showdown.helper.applyBaseUrl(options.relativePathBaseUrl,url);// safeMode: neutralize dangerous URL schemes (javascript:, vbscript:, data:, ...)
if(options.safeMode&&!showdown.helper.isSafeUrl(url)){url='';}if(options.cmSpec){url=showdown.helper.cmNormalizeURL(url);}url=url.replace(showdown.helper.regexes.asteriskDashTildeAndColon,showdown.helper.escapeCharactersCallback);// escape characters that would otherwise break out of the quoted href attribute
// (a `"` in the destination is an attribute-injection vector). cmSpec flavors already
// percent-encode the URL above, so this is a no-op there.
url=url.replace(/"/g,'&quot;').replace(/</g,'&lt;').replace(/>/g,'&gt;');attributes.href=url;if(title&&showdown.helper.isString(title)){if(options.cmSpec){title=showdown.helper.cmEscapeTitle(title);}else{title=title.replace(/"/g,'&quot;');}title=title.replace(showdown.helper.regexes.asteriskDashTildeAndColon,showdown.helper.escapeCharactersCallback);attributes.title=title;}var captureStartEvent=new showdown.Event('makehtml.link.'+subEvtName+'.onCapture',wholeMatch);captureStartEvent.setOutput(null)._setGlobals(globals)._setOptions(options).setRegexp(pattern).setMatches(matches).setAttributes(attributes);captureStartEvent=globals.converter.dispatch(captureStartEvent);// if something was passed as output, it takes precedence
// and will be used as output
if(captureStartEvent.output&&captureStartEvent.output!==''){otp=captureStartEvent.output;}else{attributes=captureStartEvent.attributes;text=captureStartEvent.matches.text||'';// Text can be a markdown element, so we run through the appropriate parsers
text=showdown.subParser('makehtml.codeSpan')(text,options,globals);text=showdown.subParser('makehtml.emoji')(text,options,globals);text=showdown.subParser('makehtml.underline')(text,options,globals);text=showdown.subParser('makehtml.emphasisAndStrong')(text,options,globals);text=showdown.subParser('makehtml.strikethrough')(text,options,globals);text=showdown.subParser('makehtml.ellipsis')(text,options,globals);text=showdown.subParser('makehtml.hashHTMLSpans')(text,options,globals);otp='<a'+showdown.helper._populateAttributes(attributes)+'>'+text+'</a>';}var beforeHashEvent=new showdown.Event('makehtml.link.'+subEvtName+'.onHash',otp);beforeHashEvent.setOutput(otp)._setGlobals(globals)._setOptions(options);beforeHashEvent=globals.converter.dispatch(beforeHashEvent);otp=beforeHashEvent.output;return showdown.subParser('makehtml.hashHTMLSpans')(otp,options,globals);}/**
   * @param {string} mail
   * @returns {{mail: string, url: string}}
   */function parseMail(mail){var url='mailto:';mail=showdown.subParser('makehtml.unescapeSpecialChars')(mail,options,globals);if(options.encodeEmails){url=showdown.helper.encodeEmailAddress(url+mail);mail=showdown.helper.encodeEmailAddress(mail);}else{url=url+mail;}return{mail:mail,url:url};}// HTML-escape an autolink href (the URL is already percent-encoded)
function cmEscapeHref(url){return url.replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;');}// HTML-escape the visible autolink text (no percent-encoding)
function cmEscapeText(txt){return txt.replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;');}});////
// makehtml/list.js
// Copyright (c) 2022 ShowdownJS
//
// Transforms MD lists into `<ul>` or `<ol>` html list
//
// Markdown supports ordered (numbered) and unordered (bulleted) lists.
// Unordered lists use asterisks, pluses, and hyphens - interchangably - as list markers
// Ordered lists use numbers followed by periods.
//
// ***Author:***
// - Estevão Soares dos Santos (Tivie) <https://github.com/tivie>
////
showdown.subParser('makehtml.list',function(text,options,globals){'use strict';// Start of list parsing
var subListRgx=/^(( {0,3}([*+-]|\d+[.])[ \t]+)[^\r]+?(¨0|\n{2,}(?=\S)(?![ \t]*(?:[*+-]|\d+[.])[ \t]+)))/gm;var mainListRgx=/(\n\n|^\n?)(( {0,3}([*+-]|\d+[.])[ \t]+)[^\r]+?(¨0|\n{2,}(?=\S)(?![ \t]*(?:[*+-]|\d+[.])[ \t]+)))/gm;var listTypeRgx=/[*+-]/g;// CommonMark list parsing is a separate container-block parser, invoked only when
// the commonmark flavor selects it. The regex parser below stays the default path.
if(options.cmSpec){return showdown.subParser('makehtml.cmList')(text,options,globals);}var startEvent=new showdown.Event('makehtml.list.onStart',text);startEvent.setOutput(text)._setGlobals(globals)._setOptions(options);startEvent=globals.converter.dispatch(startEvent);text=startEvent.output;// add sentinel to hack around khtml/safari bug:
// http://bugs.webkit.org/show_bug.cgi?id=11231
text+='¨0';if(globals.gListLevel){text=text.replace(subListRgx,function(wholeMatch,list,m2){return parseConsecutiveLists(subListRgx,list,m2.search(listTypeRgx)>-1?'ul':'ol',true);});}else{text=text.replace(mainListRgx,function(wholeMatch,m1,list,m3){return parseConsecutiveLists(mainListRgx,list,m3.search(listTypeRgx)>-1?'ul':'ol',false);});}// strip sentinel
text=text.replace(/¨0/,'');var afterEvent=new showdown.Event('makehtml.list.onEnd',text);afterEvent.setOutput(text)._setGlobals(globals)._setOptions(options);afterEvent=globals.converter.dispatch(afterEvent);return afterEvent.output;/**
   * Process the contents of a single ordered or unordered list, splitting it
   * into individual list items.
   * @param {string} listStr
   * @param {boolean} trimTrailing
   * @returns {string}
   */function processListItems(listStr,trimTrailing){// The $g_list_level global keeps track of when we're inside a list.
// Each time we enter a list, we increment it; when we leave a list,
// we decrement. If it's zero, we're not in a list anymore.
//
// We do this because when we're not inside a list, we want to treat
// something like this:
//
//    I recommend upgrading to version
//    8. Oops, now this line is treated
//    as a sub-list.
//
// As a single paragraph, despite the fact that the second line starts
// with a digit-period-space sequence.
//
// Whereas when we're inside a list (or sub-list), that line will be
// treated as the start of a sub-list. What a kludge, huh? This is
// an aspect of Markdown's syntax that's hard to parse perfectly
// without resorting to mind-reading. Perhaps the solution is to
// change the syntax rules such that sub-lists must start with a
// starting cardinal number; e.g. "1." or "a.".
globals.gListLevel++;// trim trailing blank lines:
listStr=listStr.replace(/\n{2,}$/,'\n');// attacklab: add sentinel to emulate \z
listStr+='¨0';var rgx=/(\n)?(^ {0,3})([*+-]|\d+[.])[ \t]+((\[([xX ])])?[ \t]*[^\r]+?(\n{1,2}))(?=\n*(¨0| {0,3}([*+-]|\d+[.])[ \t]+))/gm,isParagraphed=/\n[ \t]*\n(?!¨0)/.test(listStr);// Since version 1.5, nesting sublists requires 4 spaces (or 1 tab) indentation,
// which is a syntax breaking change
// activating this option reverts to old behavior
// This will be removed in version 2.0
if(options.disableForced4SpacesIndentedSublists){rgx=/(\n)?(^ {0,3})([*+-]|\d+[.])[ \t]+((\[([xX ])])?[ \t]*[^\r]+?(\n{1,2}))(?=\n*(¨0|\2([*+-]|\d+[.])[ \t]+))/gm;}listStr=listStr.replace(rgx,function(wholeMatch,m1,m2,m3,m4,taskbtn,checkedRaw){var item=showdown.helper.outdent(m4),attributes={},checked=checkedRaw&&checkedRaw.trim()!=='',eventName='makehtml.list.listItem',captureStartEvent,matches={_wholeMatch:wholeMatch,listItem:item};// Support for github tasklists. GFM requires whitespace after the marker (see
// makehtml.list.taskListItem.checkbox), so a bare `[ ]`/`[ ]x` is not a task and is left literal.
if(taskbtn&&options.tasklists&&/^[ \t]*\[[xX ]][ \t]/.test(item)){// it's a github tasklist and tasklists are enabled
// Bare `<li>` per the GFM spec; the legacy bullet styling/classes are only added
// when `moreStyling` is enabled.
if(options.moreStyling){attributes.classes=['task-list-item'];attributes.style='list-style-type: none;';if(checked){attributes.classes.push('task-list-item-complete');}}eventName='makehtml.list.taskListItem';matches._taskListButton=taskbtn;matches._taskListButtonChecked=checkedRaw;}captureStartEvent=new showdown.Event(eventName+'.onCapture',item);captureStartEvent.setOutput(null)._setGlobals(globals)._setOptions(options).setRegexp(rgx).setMatches(matches).setAttributes(attributes);captureStartEvent=globals.converter.dispatch(captureStartEvent);// if something was passed as output, it takes precedence
// and will be used as output
if(captureStartEvent.output&&captureStartEvent.output!==''){item=captureStartEvent.output;}else{attributes=captureStartEvent.attributes;item=captureStartEvent.matches.listItem;// even if user there's no tasklist, it's fine because the tasklist handler will bail without raising the event
if(options.tasklists){item=showdown.subParser('makehtml.list.taskListItem.checkbox')(item,options,globals);}// ISSUE #312
// This input: - - - a
// causes trouble to the parser, since it interprets it as:
// <ul><li><li><li>a</li></li></li></ul>
// instead of:
// <ul><li>- - a</li></ul>
// So, to prevent it, we will put a marker (¨A)in the beginning of the line
// Kind of hackish/monkey patching, but seems more effective than overcomplicating the list parser
item=item.replace(/^([-*+]|\d\.)[ \t]+[\S\n ]*/g,function(wm2){return'¨A'+wm2;});// SPECIAL CASE: a heading followed by a paragraph of text that is not separated by a double newline
// or/nor indented. ex:
//
// - # foo
// bar is great
//
// While this does now follow the spec per se, not allowing for this might cause confusion since
// header blocks don't need double-newlines after
if(/^#+.+\n.+/.test(item)){item=item.replace(/^(#+.+)$/m,'$1\n');}// m1 - Leading line or
// Has a double return (multi paragraph)
if(m1||item.search(/\n{2,}/)>-1){item=showdown.subParser('makehtml.githubCodeBlock')(item,options,globals);item=showdown.subParser('makehtml.blockquote')(item,options,globals);item=showdown.subParser('makehtml.heading')(item,options,globals);item=showdown.subParser('makehtml.list')(item,options,globals);item=showdown.subParser('makehtml.codeBlock')(item,options,globals);item=showdown.subParser('makehtml.table')(item,options,globals);item=showdown.subParser('makehtml.hashHTMLBlocks')(item,options,globals);//item = showdown.subParser('makehtml.paragraphs')(item, options, globals);
// TODO: This is a copy of the paragraph parser
// This is a provisory fix for issue #494
// For a permanent fix we need to rewrite the paragraph parser, passing the unhashify logic outside
// so that we can call the paragraph parser without accidently unashifying previously parsed blocks
// Strip leading and trailing lines:
item=item.replace(/^\n+/g,'');item=item.replace(/\n+$/g,'');var grafs=item.split(/\n{2,}/g),grafsOut=[],end=grafs.length;// Wrap <p> tags
for(var i=0;i<end;i++){var str=grafs[i];// if this is an HTML marker, copy it
if(str.search(/¨([KG])(\d+)\1/g)>=0){grafsOut.push(str);// test for presence of characters to prevent empty lines being parsed
// as paragraphs (resulting in undesired extra empty paragraphs)
}else if(str.search(/\S/)>=0){str=showdown.subParser('makehtml.spanGamut')(str,options,globals);str=str.replace(/^([ \t]*)/g,'<p>');str+='</p>';grafsOut.push(str);}}item=grafsOut.join('\n');// Strip leading and trailing lines:
item=item.replace(/^\n+/g,'');item=item.replace(/\n+$/g,'');}else{// Recursion for sub-lists:
item=showdown.subParser('makehtml.list')(item,options,globals);item=item.replace(/\n$/,'');// chomp(item)
item=showdown.subParser('makehtml.hashHTMLBlocks')(item,options,globals);// Colapse double linebreaks
item=item.replace(/\n\n+/g,'\n\n');if(isParagraphed){item=showdown.subParser('makehtml.paragraphs')(item,options,globals);}else{item=showdown.subParser('makehtml.spanGamut')(item,options,globals);}}// now we need to remove the marker (¨A)
item=item.replace('¨A','');// we can finally wrap the line in list item tags
item='<li'+showdown.helper._populateAttributes(attributes)+'>'+item+'</li>\n';}var beforeHashEvent=new showdown.Event(eventName+'.onHash',item);beforeHashEvent.setOutput(item)._setGlobals(globals)._setOptions(options);beforeHashEvent=globals.converter.dispatch(beforeHashEvent);return beforeHashEvent.output;});// attacklab: strip sentinel
listStr=listStr.replace(/¨0/g,'');globals.gListLevel--;if(trimTrailing){listStr=listStr.replace(/\s+$/,'');}return listStr;}/**
   *
   * @param {string} list
   * @param {string} listType
   * @returns {string|null}
   */function styleStartNumber(list,listType){// check if ol and starts by a number different than 1
if(listType==='ol'){var res=list.match(/^ *(\d+)\./);if(res&&res[1]!=='1'){return res[1];}}return null;}/**
   * Check and parse consecutive lists (better fix for issue #142)
   * @param {RegExp} pattern
   * @param {string} list
   * @param {string} listType
   * @param {boolean} trimTrailing
   * @returns {string}
   */function parseConsecutiveLists(pattern,list,listType,trimTrailing){var otp='';var captureStartEvent=new showdown.Event('makehtml.list.onCapture',list);captureStartEvent.setOutput(null)._setGlobals(globals)._setOptions(options).setRegexp(pattern).setMatches({_wholeMatch:list,list:list}).setAttributes({});captureStartEvent=globals.converter.dispatch(captureStartEvent);var attributes=captureStartEvent.attributes;// if something was passed as output, it takes precedence
// and will be used as output
if(captureStartEvent.output&&captureStartEvent.output!==''){otp=captureStartEvent.output;}else{// check if we caught 2 or more consecutive lists by mistake
// we use the counterRgx, meaning if listType is UL we look for OL and vice versa
var olRgx=options.disableForced4SpacesIndentedSublists?/^ ?\d+\.[ \t]/gm:/^ {0,3}\d+\.[ \t]/gm;var ulRgx=options.disableForced4SpacesIndentedSublists?/^ ?[*+-][ \t]/gm:/^ {0,3}[*+-][ \t]/gm;var counterRxg=listType==='ul'?olRgx:ulRgx;var attrs=showdown.helper.cloneObject(attributes);if(list.search(counterRxg)!==-1){(function parseCL(txt){var pos=txt.search(counterRxg);attrs.start=styleStartNumber(list,listType);if(pos!==-1){// slice
otp+='\n\n<'+listType+showdown.helper._populateAttributes(attrs)+'>\n'+processListItems(txt.slice(0,pos),!!trimTrailing)+'</'+listType+'>\n';// invert counterType and listType
listType=listType==='ul'?'ol':'ul';counterRxg=listType==='ul'?olRgx:ulRgx;//recurse
parseCL(txt.slice(pos),attrs);}else{otp+='\n\n<'+listType+showdown.helper._populateAttributes(attrs)+'>\n'+processListItems(txt,!!trimTrailing)+'</'+listType+'>\n';}})(list,attributes);}else{attrs.start=styleStartNumber(list,listType);otp='\n\n<'+listType+showdown.helper._populateAttributes(attrs)+'>\n'+processListItems(list,!!trimTrailing)+'</'+listType+'>\n';}}var beforeHashEvent=new showdown.Event('makehtml.list.onHash',otp);beforeHashEvent.setOutput(otp)._setGlobals(globals)._setOptions(options);beforeHashEvent=globals.converter.dispatch(beforeHashEvent);return beforeHashEvent.output;}});/**
 * Parse metadata at the top of the document
 */showdown.subParser('makehtml.metadata',function(text,options,globals){'use strict';if(!options.metadata){return text;}var startEvent=new showdown.Event('makehtml.metadata.onStart',text);startEvent.setOutput(text)._setGlobals(globals)._setOptions(options);startEvent=globals.converter.dispatch(startEvent);text=startEvent.output;/**
   * @param {RegExp} pattern
   * @param {string} wholeMatch
   * @param {string} format
   * @param {string} content
   * @returns {string}
   */function parseMetadataContents(pattern,wholeMatch,format,content){var otp;var captureStartEvent=new showdown.Event('makehtml.metadata.onCapture',content);captureStartEvent.setOutput(null)._setGlobals(globals)._setOptions(options).setRegexp(pattern).setMatches({_wholeMatch:wholeMatch,format:format,content:content}).setAttributes({});captureStartEvent=globals.converter.dispatch(captureStartEvent);format=captureStartEvent.matches.format;content=captureStartEvent.matches.content;// if something was passed as output, it will be used as output
if(captureStartEvent.output&&captureStartEvent.output!==''){otp=captureStartEvent.output;}else{otp='¨M';}// raw is raw so it's not changed in any way
globals.metadata.raw=content;globals.metadata.format=format;// escape chars significant in html element and attribute contexts so metadata
// values/keys can't break out of <title>, <meta ...> or the doctype when
// completeHTMLDocument concatenates them into the document head
content=showdown.helper.escapeHTMLEntities(content)// Restore dollar signs and tremas
.replace(/¨D/g,'$$').replace(/¨T/g,'¨')// replace multiple spaces
.replace(/\n {4}/g,' ');content.replace(/^([\S ]+): +([\s\S]+?)$/gm,function(wm,key,value){globals.metadata.parsed[key]=value;return'';});var beforeHashEvent=new showdown.Event('makehtml.metadata.onHash',otp);beforeHashEvent.setOutput(otp)._setGlobals(globals)._setOptions(options);beforeHashEvent=globals.converter.dispatch(beforeHashEvent);otp=beforeHashEvent.output;if(!otp){otp='¨M';}return otp;}// 1. Metadata with «««»»» delimiters
var rgx1=/^\s*«««+\s*(\S*?)\n([\s\S]+?)\n»»»+\s*\n/;text=text.replace(rgx1,function(wholeMatch,format,content){return parseMetadataContents(rgx1,wholeMatch,format,content);});// 2. Metadata with YAML delimiters
var rgx2=/^\s*---+\s*(\S*?)\n([\s\S]+?)\n---+\s*\n/;text=text.replace(rgx2,function(wholeMatch,format,content){return parseMetadataContents(rgx1,wholeMatch,format,content);});text=text.replace(/¨M/g,'');var afterEvent=new showdown.Event('makehtml.metadata.onEnd',text);afterEvent.setOutput(text)._setGlobals(globals)._setOptions(options);afterEvent=globals.converter.dispatch(afterEvent);return afterEvent.output;});/**
 *
 */showdown.subParser('makehtml.paragraphs',function(text,options,globals){'use strict';var startEvent=new showdown.Event('makehtml.paragraphs.onStart',text);startEvent.setOutput(text)._setGlobals(globals)._setOptions(options);startEvent=globals.converter.dispatch(startEvent);text=startEvent.output;// Strip leading and trailing lines:
text=text.replace(/^\n+/g,'');text=text.replace(/\n+$/g,'');var grafs=text.split(/\n{2,}/g),grafsOut=[],end=grafs.length;// Wrap <p> tags
for(var i=0;i<end;i++){var str=grafs[i];// if this is an HTML marker, copy it (¨R = a raw CommonMark HTML block, deferred past
// decodeEntities and restored later - it must not be <p>-wrapped or unhashed here)
if(str.search(/¨([KGR])(\d+)\1/g)>=0){grafsOut.push(str);// test for presence of characters to prevent empty lines being parsed
// as paragraphs (resulting in undesired extra empty paragraphs)
}else if(str.search(/\S/)>=0){str=showdown.subParser('makehtml.spanGamut')(str,options,globals);str=str.replace(/^([ \t]*)/g,'<p>');str+='</p>';grafsOut.push(str);}}/** Unhashify HTML blocks */end=grafsOut.length;for(var _i=0;_i<end;_i++){var blockText=void 0,grafsOutIt=grafsOut[_i],codeFlag=false;// if this is a marker for an html block...
var blockMatch=void 0;while((blockMatch=/¨([KG])(\d+)\1/.exec(grafsOutIt))!==null){var delim=blockMatch[1],num=blockMatch[2];if(delim==='K'){blockText=globals.gHtmlBlocks[num];}else{// we need to check if ghBlock is a false positive
if(codeFlag){// use encoded version of all text
blockText=showdown.subParser('makehtml.encodeCode')(globals.ghCodeBlocks[num].text,options,globals);}else{blockText=globals.ghCodeBlocks[num].codeblock;}}blockText=blockText.replace(/\$/g,'$$$$');// Escape any dollar signs
grafsOutIt=grafsOutIt.replace(/(\n\n)?¨([KG])\d+\2(\n\n)?/,blockText);// Check if grafsOutIt is a pre->code
if(/^<pre\b[^>]*>\s*<code\b[^>]*>/.test(grafsOutIt)){codeFlag=true;}}grafsOut[_i]=grafsOutIt;}text=grafsOut.join('\n');// Strip leading and trailing lines:
text=text.replace(/^\n+/g,'');text=text.replace(/\n+$/g,'');var afterEvent=new showdown.Event('makehtml.paragraphs.onEnd',text);afterEvent.setOutput(text)._setGlobals(globals)._setOptions(options);afterEvent=globals.converter.dispatch(afterEvent);return afterEvent.output;});/**
 * Run extension
 */showdown.subParser('makehtml.runExtension',function(ext,text,options,globals){'use strict';if(ext.filter){text=ext.filter(text,globals.converter,options);}else if(ext.regex){// TODO remove this when old extension loading mechanism is deprecated
var re=ext.regex;if(!(re instanceof RegExp)){re=new RegExp(re,'g');}text=text.replace(re,ext.replace);}return text;});/**
 * These are all the transformations that occur *within* block-level
 * tags like paragraphs, headers, and list items.
 */showdown.subParser('makehtml.spanGamut',function(text,options,globals){'use strict';var startEvent=new showdown.Event('makehtml.spanGamut.onStart',text);startEvent.setOutput(text)._setGlobals(globals)._setOptions(options);startEvent=globals.converter.dispatch(startEvent);text=startEvent.output;if(options.cmSpec){// underline runs BEFORE cmInline (mirroring the legacy order, where underline runs
// before emphasisAndStrong): it claims `__`/`___` as `<u>` and escapes any remaining
// `_`, so cmInline doesn't consume those underscores as emphasis. cmInline then hashes
// the raw `<u>` tags as CommonMark raw HTML and leaves the escaped `_` placeholders be.
text=showdown.subParser('makehtml.underline')(text,options,globals);// Unified CommonMark inline parser: code spans, backslash escapes, entities,
// autolinks, raw HTML, links, images and emphasis resolved together on one
// delimiter stack (replaces the sequential codeSpan/link/image/emphasis passes
// and the raw-HTML hashing below). The remaining Showdown-only extras (emoji,
// strikethrough, ellipsis) and the final encodeAmpsAndAngles/hardLineBreaks run
// after, on the non-hashed text.
text=showdown.subParser('makehtml.cmInline')(text,options,globals);text=showdown.subParser('makehtml.emoji')(text,options,globals);text=showdown.subParser('makehtml.strikethrough')(text,options,globals);text=showdown.subParser('makehtml.ellipsis')(text,options,globals);// hash the raw HTML these extras produce (e.g. strikethrough's `<del>`, image-based
// emoji's `<img>`) before encodeAmpsAndAngles, otherwise their `<`/`>` get escaped to
// `&lt;`/`&gt;`. Mirrors the legacy branch, which hashes spans before encoding.
text=showdown.subParser('makehtml.hashHTMLSpans')(text,options,globals);text=showdown.subParser('makehtml.encodeAmpsAndAngles')(text,options,globals);text=showdown.subParser('makehtml.hardLineBreaks')(text,options,globals);var cmAfterEvent=new showdown.Event('makehtml.spanGamut.onEnd',text);cmAfterEvent.setOutput(text)._setGlobals(globals)._setOptions(options);cmAfterEvent=globals.converter.dispatch(cmAfterEvent);return cmAfterEvent.output;}text=showdown.subParser('makehtml.codeSpan')(text,options,globals);text=showdown.subParser('makehtml.escapeSpecialCharsWithinTagAttributes')(text,options,globals);text=showdown.subParser('makehtml.encodeBackslashEscapes')(text,options,globals);// Process link and image tags. Images must come first,
// because ![foo][f] looks like a link.
text=showdown.subParser('makehtml.image')(text,options,globals);text=showdown.subParser('makehtml.link')(text,options,globals);if(options.cmSpec){// CommonMark inline raw HTML: recognize well-formed tags/comments/PIs/
// declarations/CDATA and hash them now - after backslash escapes and link/image
// destinations are resolved, but before emphasis - so that markup characters
// inside a tag (e.g. `<a href="**">`) are not parsed as emphasis. Malformed
// `<…>` is left for encodeAmpsAndAngles to escape. Fresh regex per call (this
// subparser runs re-entrantly).
text=text.replace(new RegExp(showdown.helper.regexes.cmHTMLTagSource,'g'),function(wm){// Backslash escapes are not processed inside raw HTML, so restore any escape
// placeholders that encodeBackslashEscapes produced back to literal `\<char>`.
wm=wm.replace(/¨E(\d+)E/g,function(m,code){return'\\'+String.fromCharCode(parseInt(code,10));});return showdown.helper._hashHTMLSpan(wm,globals);});}text=showdown.subParser('makehtml.emoji')(text,options,globals);text=showdown.subParser('makehtml.underline')(text,options,globals);text=showdown.subParser('makehtml.emphasisAndStrong')(text,options,globals);text=showdown.subParser('makehtml.strikethrough')(text,options,globals);text=showdown.subParser('makehtml.ellipsis')(text,options,globals);// we need to hash HTML tags inside spans
text=showdown.subParser('makehtml.hashHTMLSpans')(text,options,globals);// now we encode amps and angles
text=showdown.subParser('makehtml.encodeAmpsAndAngles')(text,options,globals);text=showdown.subParser('makehtml.hardLineBreaks')(text,options,globals);var afterEvent=new showdown.Event('makehtml.spanGamut.onEnd',text);afterEvent.setOutput(text)._setGlobals(globals)._setOptions(options);afterEvent=globals.converter.dispatch(afterEvent);return afterEvent.output;});showdown.subParser('makehtml.strikethrough',function(text,options,globals){'use strict';if(!options.strikethrough){return text;}var startEvent=new showdown.Event('makehtml.strikethrough.onStart',text);startEvent.setOutput(text)._setGlobals(globals)._setOptions(options);startEvent=globals.converter.dispatch(startEvent);text=startEvent.output;// GFM strikethrough: a run of one or two tildes, matched in length, with flanking
// (the run must hug non-whitespace on the inside) and rejecting runs of three or more.
// Group 1 captures the character before the opening run (or the string start) so it can
// be restored; group 3 is the struck-through content.
var strikethroughRegex=/(^|[^~])(~{1,2})(?=[^\s~])([\s\S]*?[^\s~])\2(?!~)/g;text=text.replace(strikethroughRegex,function(wholeMatch,prefix,run,txt){var otp;var captureStartEvent=new showdown.Event('makehtml.strikethrough.onCapture',txt);captureStartEvent.setOutput(null)._setGlobals(globals)._setOptions(options).setRegexp(strikethroughRegex).setMatches({_wholeMatch:wholeMatch,strikethrough:txt}).setAttributes({});captureStartEvent=globals.converter.dispatch(captureStartEvent);// if something was passed as output, it takes precedence
// and will be used as output
if(captureStartEvent.output&&captureStartEvent.output!==''){otp=captureStartEvent.output;}else{otp='<del'+showdown.helper._populateAttributes(captureStartEvent.attributes)+'>'+showdown.subParser('makehtml.hardLineBreaks')(txt,options,globals)+'</del>';}var beforeHashEvent=new showdown.Event('makehtml.strikethrough.onHash',otp);beforeHashEvent.setOutput(otp)._setGlobals(globals)._setOptions(options);beforeHashEvent=globals.converter.dispatch(beforeHashEvent);// restore the character that preceded the opening run
return prefix+beforeHashEvent.output;});var afterEvent=new showdown.Event('makehtml.strikethrough.onEnd',text);afterEvent.setOutput(text)._setGlobals(globals)._setOptions(options);afterEvent=globals.converter.dispatch(afterEvent);return afterEvent.output;});/**
 * Strips link definitions from text, stores the URLs and titles in
 * hash references.
 * Link defs are in the form: ^[id]: url "optional title"
 */showdown.subParser('makehtml.stripLinkDefinitions',function(text,options,globals){'use strict';var regex=/^ {0,3}\[([^\]]+)]:[ \t]*\n?[ \t]*<?([^>\s]+)>?(?: =([*\d]+[A-Za-z%]{0,4})x([*\d]+[A-Za-z%]{0,4}))?[ \t]*\n?[ \t]*(?:(\n*)["|'(](.+?)["|')][ \t]*)?(?:\n+|(?=¨0))/gm,base64Regex=/^ {0,3}\[([^\]]+)]:[ \t]*\n?[ \t]*<?(data:.+?\/.+?;base64,[A-Za-z\d+/=\n]+?)>?(?: =([*\d]+[A-Za-z%]{0,4})x([*\d]+[A-Za-z%]{0,4}))?[ \t]*\n?[ \t]*(?:(\n*)["|'(](.+?)["|')][ \t]*)?(?:\n\n|(?=¨0)|(?=\n\[))/gm;var startEvent=new showdown.Event('makehtml.stripLinkDefinitions.onStart',text);startEvent.setOutput(text)._setGlobals(globals)._setOptions(options);startEvent=globals.converter.dispatch(startEvent);text=startEvent.output;// attacklab: sentinel workarounds for lack of \A and \Z, safari\khtml bug
text+='¨0';var replaceFunc=function replaceFunc(wholeMatch,linkId,url,width,height,blankLines,title){// if there aren't two instances of linkId it must not be a reference link so back out
linkId=showdown.helper.caseFold(linkId);if(showdown.helper.caseFold(text).split(linkId).length-1<2){return wholeMatch;}var captureStartEvent=new showdown.Event('makehtml.stripLinkDefinitions.onCapture',wholeMatch);captureStartEvent.setOutput(null)._setGlobals(globals)._setOptions(options).setRegexp(regex).setMatches({_wholeMatch:wholeMatch,linkId:linkId,url:url,width:width,height:height,blankLines:blankLines,title:title}).setAttributes({});captureStartEvent=globals.converter.dispatch(captureStartEvent);var otp;// if something was passed as output, it takes precedence and will be used as output
if(captureStartEvent.output&&captureStartEvent.output!==''){otp=captureStartEvent.output;}else{// a listener may have normalized the captured groups
linkId=captureStartEvent.matches.linkId;url=captureStartEvent.matches.url;width=captureStartEvent.matches.width;height=captureStartEvent.matches.height;blankLines=captureStartEvent.matches.blankLines;title=captureStartEvent.matches.title;if(url.match(/^data:.+?\/.+?;base64,/)){// remove newlines
globals.gUrls[linkId]=url.replace(/\s/g,'');}else{url=showdown.helper.applyBaseUrl(options.relativePathBaseUrl,url);globals.gUrls[linkId]=showdown.subParser('makehtml.encodeAmpsAndAngles')(url,options,globals);// Link IDs are case-insensitive
}if(blankLines){// Oops, found blank lines, so it's not a title.
// Put back the parenthetical statement we stole.
otp=blankLines+title;}else{if(title){globals.gTitles[linkId]=title.replace(/["']/g,'&quot;');}if(options.parseImgDimensions&&width&&height){globals.gDimensions[linkId]={width:width,height:height};}// Completely remove the definition from the text
otp='';}}var beforeHashEvent=new showdown.Event('makehtml.stripLinkDefinitions.onHash',otp);beforeHashEvent.setOutput(otp)._setGlobals(globals)._setOptions(options);beforeHashEvent=globals.converter.dispatch(beforeHashEvent);return beforeHashEvent.output;};if(options.cmSpec){text=parseCmLinkDefinitions(text);}else{// first we try to find base64 link references
text=text.replace(base64Regex,replaceFunc);text=text.replace(regex,replaceFunc);}// attacklab: strip sentinel
text=text.replace(/¨0/,'');var afterEvent=new showdown.Event('makehtml.stripLinkDefinitions.onEnd',text);afterEvent.setOutput(text)._setGlobals(globals)._setOptions(options);afterEvent=globals.converter.dispatch(afterEvent);return afterEvent.output;/**
   * CommonMark link reference definition parsing. Scans block by block: a
   * definition may appear at the start of the document, after a blank line, or
   * immediately after another definition. Supports multi-line definitions,
   * `<...>` (and empty `<>`) destinations, multi-line titles, backslash escapes
   * and first-definition-wins for duplicate labels.
   * @param {string} str text with the trailing `¨0` sentinel
   * @returns {string}
   */function parseCmLinkDefinitions(str){var n=str.length,out='',i=0,atBlockStart=true;while(i<n){if(atBlockStart){var def=tryParseCmDefinition(str,i);if(def){commitDefinition(def);i=def.end;atBlockStart=true;// consecutive definitions are allowed
continue;}}// consume one line verbatim
var nl=str.indexOf('\n',i);if(nl===-1){out+=str.slice(i);break;}var line=str.slice(i,nl+1);out+=line;i=nl+1;// A definition can begin after a blank line or after a self-contained leaf
// block such as an ATX heading or a thematic break, but it must not
// interrupt a paragraph.
atBlockStart=/^[ \t]*\n$/.test(line)||/^ {0,3}#{1,6}(?:[ \t]|\n)/.test(line)||/^ {0,3}([-*_])[ \t]*(?:\1[ \t]*){2,}\n$/.test(line);}return out;}/**
   * Store a parsed definition, honoring first-definition-wins, and dispatch the
   * capture event so listener extensions still observe link-definition captures.
   * @param {{linkId: string, url: string, title: (string|null), wholeMatch: string}} def
   */function commitDefinition(def){var captureStartEvent=new showdown.Event('makehtml.stripLinkDefinitions.onCapture',def.wholeMatch);captureStartEvent.setOutput(null)._setGlobals(globals)._setOptions(options).setRegexp(null).setMatches({_wholeMatch:def.wholeMatch,linkId:def.linkId,url:def.url,title:def.title}).setAttributes({});captureStartEvent=globals.converter.dispatch(captureStartEvent);var linkId=captureStartEvent.matches.linkId,url=captureStartEvent.matches.url,title=captureStartEvent.matches.title;// first definition wins
if(showdown.helper.isUndefined(globals.gUrls[linkId])){globals.gUrls[linkId]=url;if(title!==null&&!showdown.helper.isUndefined(title)){globals.gTitles[linkId]=title;}// parseImgDimensions: store reference-style dimensions (gating copied from the
// legacy stripLinkDefinitions replaceFunc); consumed by cmInline's image builder
if(options.parseImgDimensions&&def.width&&def.height){globals.gDimensions[linkId]={width:def.width,height:def.height};}}}/**
   * Attempt to parse a single CommonMark link reference definition starting at
   * `start` (a line start). Returns the parsed definition with `end` (index just
   * past the consumed text) or `null` if there is no definition here.
   * @param {string} str
   * @param {number} start
   * @returns {{linkId: string, url: string, title: (string|null), wholeMatch: string, end: number}|null}
   */function tryParseCmDefinition(str,start){var n=str.length,j=start,isEnd=function isEnd(p){return p>=n||str.charAt(p)==='\n'||str.slice(p,p+2)==='¨0';};// up to 3 leading spaces
var sp=0;while(j<n&&str.charAt(j)===' '&&sp<3){j++;sp++;}if(str.charAt(j)!=='['){return null;}j++;// label: up to the first unescaped ']', no nested '[' or blank line
var label='',sawNonWs=false;while(j<n){var c=str.charAt(j);if(c==='\\'&&j+1<n){label+=c+str.charAt(j+1);j+=2;sawNonWs=true;continue;}if(c===']'){break;}if(c==='['){return null;}if(!/\s/.test(c)){sawNonWs=true;}label+=c;j++;}if(j>=n||str.charAt(j)!==']'||!sawNonWs||/\n[ \t]*\n/.test(label)){return null;}j++;// consume ']'
if(str.charAt(j)!==':'){return null;}j++;// whitespace (incl at most one line ending) before the destination
var nlCount=0;while(j<n&&/[ \t\n]/.test(str.charAt(j))){if(str.charAt(j)==='\n'){nlCount++;if(nlCount>1){return null;}}j++;}var dest=showdown.helper.cmScanDestination(str,j);if(!dest||!dest.angle&&dest.url===''){return null;}var afterDest=dest.end,url=dest.url;// parseImgDimensions (Showdown extension, not CommonMark): an optional ` =WxH`
// between the destination and the title. Always consumed here; only stored as
// dimensions when the option is on (see commitDefinition). Regex fragment copied
// from the inline image regex in image.js.
var width=null,height=null;var dimPos=afterDest;while(dimPos<n&&/[ \t]/.test(str.charAt(dimPos))){dimPos++;}if(dimPos>afterDest&&str.charAt(dimPos)==='='){var dim=/^=([*\d]+[A-Za-z%]{0,4})x([*\d]+[A-Za-z%]{0,4})/.exec(str.slice(dimPos));if(dim){width=dim[1];height=dim[2];afterDest=dimPos+dim[0].length;}}// optional title, separated from the destination by whitespace (and at most
// one line ending, with no blank line)
var k=afterDest,sawSpace=false,sawNl=false;while(k<n&&/[ \t]/.test(str.charAt(k))){k++;sawSpace=true;}if(str.charAt(k)==='\n'){sawNl=true;k++;while(k<n&&/[ \t]/.test(str.charAt(k))){k++;}}var title=null,end;var tc=str.charAt(k);if((sawSpace||sawNl)&&(tc==='"'||tc==='\''||tc==='(')){var t=showdown.helper.cmScanTitle(str,k);if(t){var p=t.end;while(p<n&&/[ \t]/.test(str.charAt(p))){p++;}if(isEnd(p)){title=t.title;end=str.charAt(p)==='\n'?p+1:p;}}}if(title===null){// no (valid) title: the destination must be the last token on its line
var _p=afterDest;while(_p<n&&/[ \t]/.test(str.charAt(_p))){_p++;}if(!isEnd(_p)){return null;}end=str.charAt(_p)==='\n'?_p+1:_p;}var linkId=showdown.helper.cmNormalizeLabel(label);if(linkId===''){return null;}if(url.match(/^data:.+?\/.+?;base64,/)){url=url.replace(/\s/g,'');}else{url=showdown.helper.cmNormalizeURL(showdown.helper.applyBaseUrl(options.relativePathBaseUrl,url));}if(title!==null){title=showdown.helper.cmEscapeTitle(title);}return{linkId:linkId,url:url,title:title,width:width,height:height,wholeMatch:str.slice(start,end),end:end};}});showdown.subParser('makehtml.table',function(text,options,globals){'use strict';if(!options.tables){return text;}//
// parser starts here
//
var startEvent=new showdown.Event('makehtml.table.onStart',text);startEvent.setOutput(text)._setGlobals(globals)._setOptions(options);startEvent=globals.converter.dispatch(startEvent);text=startEvent.output;// GFM §4.10: a table is broken at the first line that begins another
// block-level construct (blockquote, ATX heading, fenced code, thematic
// break). Such a line is not a table row, so we split it — and everything
// after it — off the match. Because `table` runs immediately before
// `blockquote` (and the rest of blockGamut) in the pipeline, the returned
// tail is reprocessed and converted normally.
var blockStartRgx=/^ {0,3}(?:>|#{1,6}(?:[ \t]|$)|```|~~~|(?:\*[ \t]*){3,}$|(?:-[ \t]*){3,}$|(?:_[ \t]*){3,}$)/;// parse multi column tables.
// The header row is matched as `(?=[^\n]*\|)[^\n]+\n` (a whole line that contains at least one
// pipe) instead of `\|?.+\|.+\n`. The old form had two greedy `.+` around a `\|`, so when the
// following delimiter row failed to match it backtracked over every pipe position in the header
// — quadratic on a long pipe-heavy line like `'|a'.repeat(n)`. The lookahead + single greedy
// `[^\n]+` matches the same set of header lines without that backtracking.
var tableRgx=/^ {0,3}(?=[^\n]*\|)[^\n]+\n {0,3}\|?[ \t]*:?[ \t]*[-=]+[ \t]*:?[ \t]*\|[ \t]*:?[ \t]*[-=]+[\s\S]+?(?:\n\n|¨0)/gm;text=text.replace(tableRgx,function(wholeMatch){var split=breakOnBlock(wholeMatch);// Neutralize escaped pipes only within the actual table text (not the trailing block
// or any non-table content) so `\|` inside code spans elsewhere is left for the normal
// backslash-escape / code-span passes to handle.
var table=split.table.replace(/\\(\|)/g,showdown.helper.escapeCharactersCallback);return parse(tableRgx,table)+split.tail;});var singeColTblRgx=/^ {0,3}\|.+\|[ \t]*\n {0,3}\|?[ \t]*:?[ \t]*[-=]+[ \t]*:?[ \t]*\|[ \t]*\n( {0,3}\|.+\|[ \t]*\n)*(?:\n|¨0)/gm;text=text.replace(singeColTblRgx,function(wholeMatch){var table=wholeMatch.replace(/\\(\|)/g,showdown.helper.escapeCharactersCallback);return parse(singeColTblRgx,table);});var afterEvent=new showdown.Event('makehtml.table.onEnd',text);afterEvent.setOutput(text)._setGlobals(globals)._setOptions(options);afterEvent=globals.converter.dispatch(afterEvent);return afterEvent.output;/**
   * Split a greedily-matched table block at the first body line that begins
   * another block-level construct. The header (line 0) and delimiter (line 1)
   * are never terminators; scanning starts at the first body row.
   * @param {string} wholeMatch
   * @returns {{table: string, tail: string}}
   */function breakOnBlock(wholeMatch){var lines=wholeMatch.split('\n');for(var i=2;i<lines.length;++i){if(blockStartRgx.test(lines[i])){return{table:lines.slice(0,i).join('\n')+'\n',tail:lines.slice(i).join('\n')};}}return{table:wholeMatch,tail:''};}/**
   *
   * @param {RegExp} pattern
   * @param {string} wholeMatch
   * @returns {string}
   */function parse(pattern,wholeMatch){var tab=preParse(wholeMatch);// if parseTable returns null then it's a malformed table so we return what we caught
if(!tab){return wholeMatch;}// only now we consider it to be a markdown table and start doing stuff
var headers=tab.rawHeaders;var styles=tab.rawStyles;var cells=tab.rawCells;var otp;var captureStartEvent=new showdown.Event('makehtml.table.onCapture',wholeMatch);captureStartEvent.setOutput(null)._setGlobals(globals)._setOptions(options).setRegexp(pattern).setMatches({_wholeMatch:wholeMatch,table:wholeMatch}).setAttributes({});captureStartEvent=globals.converter.dispatch(captureStartEvent);if(captureStartEvent.output&&captureStartEvent.output!==''){// user provided an otp, so we use it
otp=captureStartEvent.output;}else{// user changed matches.table, so we need to generate headers, styles, and cells again
if(captureStartEvent.matches.table!==wholeMatch){tab=preParse(captureStartEvent.matches.table);// user passed a malformed table, so we bail
if(!tab){return wholeMatch;}headers=tab.rawHeaders;styles=tab.rawStyles;cells=tab.rawCells;}var parsedTab=parseTable(headers,styles,cells);var attributes=captureStartEvent.attributes;otp=buildTableOtp(parsedTab.headers,parsedTab.cells,attributes);}var beforeHashEvent=new showdown.Event('makehtml.table.onHash',otp);beforeHashEvent.setOutput(otp)._setGlobals(globals)._setOptions(options);beforeHashEvent=globals.converter.dispatch(beforeHashEvent);otp=beforeHashEvent.output;return otp;}/**
   *
   * @param {string[]} headers
   * @param {string[]} cells
   * @param {{}} attributes
   * @returns {string}
   */function buildTableOtp(headers,cells,attributes){var otp;var colCount=headers.length,rowCount=cells.length;otp='<table'+showdown.helper._populateAttributes(attributes)+'>\n<thead>\n<tr>\n';for(var i=0;i<colCount;++i){otp+=headers[i];}otp+='</tr>\n</thead>\n<tbody>\n';for(var _i2=0;_i2<rowCount;++_i2){otp+='<tr>\n';for(var ii=0;ii<colCount;++ii){otp+=cells[_i2][ii];}otp+='</tr>\n';}otp+='</tbody>\n</table>\n';return otp;}/**
   * @param {string} rawTable
   * @returns {{rawHeaders: string[], rawStyles: string[], rawCells: *[]}|null}
   */function preParse(rawTable){var tableLines=rawTable.split('\n');for(var i=0;i<tableLines.length;++i){// strip wrong first and last column if wrapped tables are used
if(/^ {0,3}\|/.test(tableLines[i])){tableLines[i]=tableLines[i].replace(/^ {0,3}\|/,'');}if(/\|[ \t]*$/.test(tableLines[i])){tableLines[i]=tableLines[i].replace(/\|[ \t]*$/,'');}// parse code spans first, but we only support one line code spans
tableLines[i]=showdown.subParser('makehtml.codeSpan')(tableLines[i],options,globals);}var rawHeaders=tableLines[0].split('|').map(function(s){return s.trim();}),rawStyles=tableLines[1].split('|').map(function(s){return s.trim();}),rawCells=[];tableLines.shift();tableLines.shift();for(var _i3=0;_i3<tableLines.length;++_i3){if(tableLines[_i3].trim()===''){continue;}rawCells.push(tableLines[_i3].split('|').map(function(s){return s.trim();}));}if(rawHeaders.length!==rawStyles.length){return null;}return{rawHeaders:rawHeaders,rawStyles:rawStyles,rawCells:rawCells};}/**
   *
   * @param {string[]} rawHeaders
   * @param {string[]} rawStyles
   * @param {string[]} rawCells
   * @returns {{headers: *, cells: *}}
   */function parseTable(rawHeaders,rawStyles,rawCells){var headers=[],cells=[],styles=[],colCount=rawHeaders.length,rowCount=rawCells.length;for(var i=0;i<colCount;++i){styles.push(parseStyle(rawStyles[i]));}for(var _i4=0;_i4<colCount;++_i4){var header=void 0;var captureStartEvent=new showdown.Event('makehtml.table.header.onCapture',rawHeaders[_i4]);captureStartEvent.setOutput(null)._setGlobals(globals)._setOptions(options).setRegexp(null).setMatches({_wholeMatch:rawHeaders[_i4],header:rawHeaders[_i4]}).setAttributes(styles[_i4]);captureStartEvent=globals.converter.dispatch(captureStartEvent);if(captureStartEvent.output&&captureStartEvent.output!==''){// user provided an otp, so we use it
header=captureStartEvent.output;}else{header=parseHeader(captureStartEvent.matches.header,styles[_i4]);}var beforeHashEvent=new showdown.Event('makehtml.table.header.onHash',header);beforeHashEvent.setOutput(header)._setGlobals(globals)._setOptions(options);beforeHashEvent=globals.converter.dispatch(beforeHashEvent);header=beforeHashEvent.output;headers.push(header);}for(var _i5=0;_i5<rowCount;++_i5){var row=[];for(var ii=0;ii<colCount;++ii){var cell=!showdown.helper.isUndefined(rawCells[_i5][ii])?rawCells[_i5][ii]:'',attributes=!showdown.helper.isUndefined(styles[ii])?styles[ii]:{};// since we're reusing attributes, we might need to remove the id that we previously set for headers
if(attributes.id){attributes.classes=[attributes.id]+'_col';delete attributes.id;}var _captureStartEvent=new showdown.Event('makehtml.table.cell.onCapture',cell);_captureStartEvent.setOutput(null)._setGlobals(globals)._setOptions(options).setRegexp(null).setMatches({_wholeMatch:cell,cell:cell}).setAttributes(attributes);_captureStartEvent=globals.converter.dispatch(_captureStartEvent);if(_captureStartEvent.output&&_captureStartEvent.output!==''){// user provided an otp, so we use it
cell=_captureStartEvent.output;}else{attributes=_captureStartEvent.attributes;cell=parseCell(_captureStartEvent.matches.cell,attributes);}var _beforeHashEvent=new showdown.Event('makehtml.table.cell.onHash',cell);_beforeHashEvent.setOutput(cell)._setGlobals(globals)._setOptions(options);_beforeHashEvent=globals.converter.dispatch(_beforeHashEvent);cell=_beforeHashEvent.output;row.push(cell);}cells.push(row);}return{headers:headers,cells:cells};}/**
   * @param {string} sLine
   * @returns {{}|{style: string}}
   */function parseStyle(sLine){if(/^:[ \t]*-+$/.test(sLine)){return{style:'text-align:left;'};}else if(/^-+[ \t]*:[ \t]*$/.test(sLine)){return{style:'text-align:right;'};}else if(/^:[ \t]*-+[ \t]*:$/.test(sLine)){return{style:'text-align:center;'};}else{return{};}}/**
   *
   * @param {string} headerText
   * @param {{id: string}} attributes
   * @returns {string}
   */function parseHeader(headerText,attributes){headerText=headerText.trim();headerText=showdown.subParser('makehtml.spanGamut')(headerText,options,globals);if(options.tablesHeaderId){attributes.id=headerText.replace(/ /g,'_').toLowerCase();}return'<th'+showdown.helper._populateAttributes(attributes)+'>'+headerText+'</th>\n';}/**
   *
   * @param {string} cellText
   * @param {{}} attributes
   * @returns {string}
   */function parseCell(cellText,attributes){cellText=showdown.subParser('makehtml.spanGamut')(cellText,options,globals);return'<td'+showdown.helper._populateAttributes(attributes)+'>'+cellText+'</td>\n';}});////
// makehtml/taskListItem.js
// Copyright (c) 2024 ShowdownJS
//
// GFM task-list item renderer, shared by both list parsers.
//
// Given the raw text of a single list item, it matches a leading `[ ]`/`[x]`
// marker plus the rest of that line and renders the marker as a disabled
// `<input type="checkbox">`, leaving the line's text in place for the caller to
// parse further. It runs at the same (raw, pre-parse) stage in both
// `makehtml.list` (the default regex parser) and `makehtml.cmList` (the
// commonmark container-block parser), so the events below always expose the
// task item's raw source line on either path.
//
// Events (nested under the `makehtml.list.taskListItem` namespace that the default
// list parser already uses for its item-level events):
//   makehtml.list.taskListItem.checkbox.onCapture - fired on a matched task line.
//     `input` and `_wholeMatch` are the full source line (`[ ] foo *bar*`);
//     `attributes` are the checkbox attributes. Returning output overrides the line.
//   makehtml.list.taskListItem.checkbox.onHash    - fired with the rendered line
//     (`<input ...> foo *bar*`) before it is handed back to the caller.
//
// ***Author:***
// - Estêvão Soares dos Santos (Tivie) <https://github.com/tivie>
////
showdown.subParser('makehtml.list.taskListItem.checkbox',function(text,options,globals){'use strict';if(!options.tasklists){return text;}// Match the marker and the remainder of its (first) line. The text is captured so
// the events expose the full line, not just the checkbox; everything after the line
// is left untouched for the caller's block/span parsing.
//
// Per GFM the marker must be followed by at least one space/tab to be a task: a bare
// `[ ]` (nothing after) or `[ ]x` (no whitespace) is left literal, matching cmark-gfm.
var taskItemRgx=/^([ \t]*)\[([xX ])](?=[ \t])([^\n]*)/;return text.replace(taskItemRgx,function(wm,prefix,checkedRaw,lineText){var checked=checkedRaw.trim()!=='';// GFM spec output is a bare `<input disabled type="checkbox">` (checked items add a
// leading `checked`). Only when `moreStyling` is enabled do we keep the legacy inline
// style that visually aligns the checkbox.
var attributes=options.moreStyling?{type:'checkbox',disabled:true,style:'margin: 0px 0.35em 0.25em -1.6em; vertical-align: middle;',checked:checked}:checked?{checked:true,disabled:true,type:'checkbox'}:{disabled:true,type:'checkbox'};var captureStartEvent=new showdown.Event('makehtml.list.taskListItem.checkbox.onCapture',wm);captureStartEvent.setOutput(null)._setGlobals(globals)._setOptions(options).setRegexp(taskItemRgx).setMatches({_wholeMatch:wm,_taskListButton:prefix+'['+checkedRaw+']',_taskListButtonChecked:checkedRaw,_taskListItemText:lineText}).setAttributes(attributes);captureStartEvent=globals.converter.dispatch(captureStartEvent);var otp;if(captureStartEvent.output&&captureStartEvent.output!==''){otp=captureStartEvent.output;}else{attributes=captureStartEvent.attributes;var txt=captureStartEvent.matches._taskListItemText;otp=prefix+'<input'+showdown.helper._populateAttributes(attributes)+'>'+txt;}var beforeHashEvent=new showdown.Event('makehtml.list.taskListItem.checkbox.onHash',otp);beforeHashEvent.setOutput(otp)._setGlobals(globals)._setOptions(options);beforeHashEvent=globals.converter.dispatch(beforeHashEvent);return beforeHashEvent.output;});});showdown.subParser('makehtml.underline',function(text,options,globals){'use strict';if(!options.underline){return text;}var startEvent=new showdown.Event('makehtml.underline.onStart',text);startEvent.setOutput(text)._setGlobals(globals)._setOptions(options);startEvent=globals.converter.dispatch(startEvent);text=startEvent.output;// Resolve an escaped underscore (`\_`) to the escape placeholder (¨E95E, charcode of
// `_`), consuming the backslash, so it is neither underlined nor left with a stray `\`.
// Needed because under cmSpec underline runs before backslash escapes are resolved; a
// no-op in the legacy path, where `\_` is already a placeholder before this runs.
text=text.replace(/\\_/g,'¨E95E');if(options.literalMidWordUnderscores){var rgx1=/\b___(\S[\s\S]*?)___\b/g;text=text.replace(rgx1,function(wm,txt){return parse(rgx1,wm,txt);});var rgx2=/\b__(\S[\s\S]*?)__\b/g;text=text.replace(rgx2,function(wm,txt){return parse(rgx2,wm,txt);});}else{var rgx3=/___(\S[\s\S]*?)___/g;text=text.replace(rgx3,function(wm,txt){if(!/\S$/.test(txt)){return wm;}return parse(rgx3,wm,txt);});var rgx4=/__(\S[\s\S]*?)__/g;text=text.replace(rgx4,function(wm,txt){if(!/\S$/.test(txt)){return wm;}return parse(rgx4,wm,txt);});}// escape remaining underscores to prevent them being parsed by italic and bold
text=text.replace(/(_)/g,showdown.helper.escapeCharactersCallback);var afterEvent=new showdown.Event('makehtml.underline.onEnd',text);afterEvent.setOutput(text)._setGlobals(globals)._setOptions(options);afterEvent=globals.converter.dispatch(afterEvent);return afterEvent.output;function parse(pattern,wholeMatch,txt){var otp;var captureStartEvent=new showdown.Event('makehtml.underline.onCapture',txt);captureStartEvent.setOutput(null)._setGlobals(globals)._setOptions(options).setRegexp(pattern).setMatches({_wholeMatch:wholeMatch,strikethrough:txt}).setAttributes({});captureStartEvent=globals.converter.dispatch(captureStartEvent);// if something was passed as output, it takes precedence
// and will be used as output
if(captureStartEvent.output&&captureStartEvent.output!==''){otp=captureStartEvent.output;}else{otp='<u'+showdown.helper._populateAttributes(captureStartEvent.attributes)+'>'+showdown.subParser('makehtml.hardLineBreaks')(txt,options,globals)+'</u>';}var beforeHashEvent=new showdown.Event('makehtml.underline.onHash',otp);beforeHashEvent.setOutput(otp)._setGlobals(globals)._setOptions(options);beforeHashEvent=globals.converter.dispatch(beforeHashEvent);return beforeHashEvent.output;}});/**
 * Swap back in all the special characters we've hidden.
 */showdown.subParser('makehtml.unescapeSpecialChars',function(text,options,globals){'use strict';var startEvent=new showdown.Event('makehtml.unescapeSpecialChars.onStart',text);startEvent.setOutput(text)._setGlobals(globals)._setOptions(options);startEvent=globals.converter.dispatch(startEvent);text=startEvent.output;text=text.replace(/¨E(\d+)E/g,function(wholeMatch,m1){var charCodeToReplace=parseInt(m1);return String.fromCharCode(charCodeToReplace);});var afterEvent=new showdown.Event('makehtml.unescapeSpecialChars.onEnd',text);afterEvent.setOutput(text)._setGlobals(globals)._setOptions(options);afterEvent=globals.converter.dispatch(afterEvent);return afterEvent.output;});////
// makehtml/unhashHTMLSpans.js
// Copyright (c) 2018 ShowdownJS
//
// Unhash HTML spans
//
// ***Author:***
// - Estêvão Soares dos Santos (Tivie) <https://github.com/tivie>
////
showdown.subParser('makehtml.unhashHTMLSpans',function(text,options,globals){'use strict';var startEvent=new showdown.Event('makehtml.unhashHTMLSpans.onStart',text);startEvent.setOutput(text)._setGlobals(globals)._setOptions(options);startEvent=globals.converter.dispatch(startEvent);text=startEvent.output;// Resolve one span placeholder to its stored HTML, expanding any nested placeholders it
// contains (bounded depth, mirrors the historical "assume 20 as limit for recurse").
function resolveSpan(num){var repText=globals.gHtmlSpans[num],limit=0;while(/¨C(\d+)C/.test(repText)){var n2=repText.match(/¨C(\d+)C/)[1];repText=repText.replace('¨C'+n2+'C',globals.gHtmlSpans[n2]);if(limit===10){console.error('maximum nesting of 20 spans reached!!!');break;}++limit;}return repText;}// Single pass over the document (was: one String.replace per span, i.e. O(spans × text) —
// quadratic when the input produces many spans, e.g. `'a_'.repeat(n)`).
text=text.replace(/¨C(\d+)C/g,function(whole,num){return resolveSpan(num);});var afterEvent=new showdown.Event('makehtml.unhashHTMLSpans.onEnd',text);afterEvent.setOutput(text)._setGlobals(globals)._setOptions(options);afterEvent=globals.converter.dispatch(afterEvent);return afterEvent.output;});showdown.subParser('makeMarkdown.blockquote',function(node,options,globals){'use strict';var startEvent=new showdown.Event('makeMarkdown.blockquote.onStart',node.outerHTML);startEvent.setOutput(null)._setGlobals(globals)._setOptions(options).setMatches({node:node});startEvent=globals.converter.dispatch(startEvent);var result;if(startEvent.output&&startEvent.output!==''){result=startEvent.output;}else{result=function(){var txt='';if(node.hasChildNodes()){var children=node.childNodes,childrenLength=children.length;for(var i=0;i<childrenLength;++i){var innerTxt=showdown.subParser('makeMarkdown.node')(children[i],options,globals);if(innerTxt===''){continue;}txt+=innerTxt;}}// cleanup
txt=txt.trim();txt='> '+txt.split('\n').join('\n> ');return txt;}();}var endEvent=new showdown.Event('makeMarkdown.blockquote.onEnd',result);endEvent.setOutput(result)._setGlobals(globals)._setOptions(options).setMatches({node:node});endEvent=globals.converter.dispatch(endEvent);return endEvent.output;});showdown.subParser('makeMarkdown.break',function(node,options,globals){'use strict';var startEvent=new showdown.Event('makeMarkdown.break.onStart',node.outerHTML);startEvent.setOutput(null)._setGlobals(globals)._setOptions(options).setMatches({node:node});startEvent=globals.converter.dispatch(startEvent);var result;if(startEvent.output&&startEvent.output!==''){result=startEvent.output;}else{result='  \n';}var endEvent=new showdown.Event('makeMarkdown.break.onEnd',result);endEvent.setOutput(result)._setGlobals(globals)._setOptions(options).setMatches({node:node});endEvent=globals.converter.dispatch(endEvent);return endEvent.output;});showdown.subParser('makeMarkdown.codeBlock',function(node,options,globals){'use strict';var startEvent=new showdown.Event('makeMarkdown.codeBlock.onStart',node.outerHTML);startEvent.setOutput(null)._setGlobals(globals)._setOptions(options).setMatches({node:node});startEvent=globals.converter.dispatch(startEvent);var result;if(startEvent.output&&startEvent.output!==''){result=startEvent.output;}else{var lang=node.getAttribute('language'),num=node.getAttribute('precodenum'),code=globals.preList[num];if(options.ghCodeBlocks){result='```'+lang+'\n'+code+'\n```';}else{// fenced code blocks disabled -> emit raw HTML (re-escape the special chars that
// substitutePreCodeTags decoded when it stashed the content)
var escaped=code.replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;'),langClass=lang?' class="'+lang+'"':'';result='<pre><code'+langClass+'>'+escaped+'</code></pre>';}}var endEvent=new showdown.Event('makeMarkdown.codeBlock.onEnd',result);endEvent.setOutput(result)._setGlobals(globals)._setOptions(options).setMatches({node:node});endEvent=globals.converter.dispatch(endEvent);return endEvent.output;});showdown.subParser('makeMarkdown.codeSpan',function(node,options,globals){'use strict';var startEvent=new showdown.Event('makeMarkdown.codeSpan.onStart',node.outerHTML);startEvent.setOutput(null)._setGlobals(globals)._setOptions(options).setMatches({node:node});startEvent=globals.converter.dispatch(startEvent);var result;if(startEvent.output&&startEvent.output!==''){result=startEvent.output;}else{var code=showdown.helper.unescapeHTMLEntities(node.innerHTML);// pick a backtick fence longer than the longest run of backticks inside the content
var backtickRuns=code.match(/`+/g),longestRun=0;if(backtickRuns){for(var b=0;b<backtickRuns.length;++b){if(backtickRuns[b].length>longestRun){longestRun=backtickRuns[b].length;}}}var fence=new Array(longestRun+2).join('`'),pad=longestRun>0?' ':'';result=fence+pad+code+pad+fence;}var endEvent=new showdown.Event('makeMarkdown.codeSpan.onEnd',result);endEvent.setOutput(result)._setGlobals(globals)._setOptions(options).setMatches({node:node});endEvent=globals.converter.dispatch(endEvent);return endEvent.output;});showdown.subParser('makeMarkdown.emphasis',function(node,options,globals){'use strict';var startEvent=new showdown.Event('makeMarkdown.emphasis.onStart',node.outerHTML);startEvent.setOutput(null)._setGlobals(globals)._setOptions(options).setMatches({node:node});startEvent=globals.converter.dispatch(startEvent);var result;if(startEvent.output&&startEvent.output!==''){result=startEvent.output;}else{result=function(){var txt='';if(node.hasChildNodes()){txt+='*';var children=node.childNodes,childrenLength=children.length;for(var i=0;i<childrenLength;++i){txt+=showdown.subParser('makeMarkdown.node')(children[i],options,globals);}txt+='*';}return txt;}();}var endEvent=new showdown.Event('makeMarkdown.emphasis.onEnd',result);endEvent.setOutput(result)._setGlobals(globals)._setOptions(options).setMatches({node:node});endEvent=globals.converter.dispatch(endEvent);return endEvent.output;});// Reverse direction (HTML -> Markdown) for GFM footnotes. Handles the two shapes the
// forward parser (and GitHub) produce:
//   <sup class="footnote-ref"><a href="#fn-ID" ...>N</a></sup>      -> [^ID]
//   <section class="footnotes"><ol><li id="fn-ID">...<a class="footnote-backref">...</a></li></ol></section>
//                                                                   -> [^ID]: ...body...
// Gated by the `footnotes` option in makeMarkdown.node, mirroring makeHtml.
showdown.subParser('makeMarkdown.footnotes',function(node,options,globals){'use strict';var startEvent=new showdown.Event('makeMarkdown.footnotes.onStart',node.outerHTML);startEvent.setOutput(null)._setGlobals(globals)._setOptions(options).setMatches({node:node});startEvent=globals.converter.dispatch(startEvent);var result;if(startEvent.output&&startEvent.output!==''){result=startEvent.output;}else{result=function(){var tagName=node.tagName.toLowerCase();if(tagName==='sup'){return renderReference(node);}if(tagName==='section'){return renderSection(node);}return'';}();}var endEvent=new showdown.Event('makeMarkdown.footnotes.onEnd',result);endEvent.setOutput(result)._setGlobals(globals)._setOptions(options).setMatches({node:node});endEvent=globals.converter.dispatch(endEvent);return endEvent.output;// the label lives in the `#fn-<label>` href (percent-encoded by makeHtml); recover it
function decodeLabel(s){try{return decodeURIComponent(s);}catch(_unused3){return s;}}function renderReference(sup){var a=sup.querySelector('a');if(!a){return'';}var label=decodeLabel((a.getAttribute('href')||'').replace(/^#fn-/,''));return'[^'+label+']';}function renderSection(section){var items=section.querySelectorAll('ol > li');if(!items.length){items=section.querySelectorAll('li');}var defs=[];for(var i=0;i<items.length;++i){var li=items[i],label=decodeLabel((li.getAttribute('id')||'').replace(/^fn-/,''));// drop the back-reference anchors (and the nested <sup> some carry)
var backrefs=li.querySelectorAll('a.footnote-backref');for(var b=0;b<backrefs.length;++b){backrefs[b].parentNode.removeChild(backrefs[b]);}// render the cleaned body
var body='';for(var c=0;c<li.childNodes.length;++c){body+=showdown.subParser('makeMarkdown.node')(li.childNodes[c],options,globals);}body=body.replace(/[ \t]+$/gm,'')// trailing space left by a removed backref
.replace(/\n+$/,'');// `[^label]: ` on the first line; continuation lines indented by 4 spaces
var lines=body.split('\n'),def='[^'+label+']: '+(lines[0]||'');for(var l=1;l<lines.length;++l){def+='\n'+(lines[l]===''?'':'    '+lines[l]);}defs.push(def);}return defs.join('\n\n');}});showdown.subParser('makeMarkdown.header',function(node,options,globals,headerLevel){'use strict';var startEvent=new showdown.Event('makeMarkdown.header.onStart',node.outerHTML);startEvent.setOutput(null)._setGlobals(globals)._setOptions(options).setMatches({node:node});startEvent=globals.converter.dispatch(startEvent);var result;if(startEvent.output&&startEvent.output!==''){result=startEvent.output;}else{result=function(){var headerMark=new Array(headerLevel+1).join('#'),txt='';if(node.hasChildNodes()){txt=headerMark+' ';var children=node.childNodes,childrenLength=children.length;for(var i=0;i<childrenLength;++i){txt+=showdown.subParser('makeMarkdown.node')(children[i],options,globals);}}return txt;}();}var endEvent=new showdown.Event('makeMarkdown.header.onEnd',result);endEvent.setOutput(result)._setGlobals(globals)._setOptions(options).setMatches({node:node});endEvent=globals.converter.dispatch(endEvent);return endEvent.output;});showdown.subParser('makeMarkdown.hr',function(node,options,globals){'use strict';var startEvent=new showdown.Event('makeMarkdown.hr.onStart',node.outerHTML);startEvent.setOutput(null)._setGlobals(globals)._setOptions(options).setMatches({node:node});startEvent=globals.converter.dispatch(startEvent);var result;if(startEvent.output&&startEvent.output!==''){result=startEvent.output;}else{result='---';}var endEvent=new showdown.Event('makeMarkdown.hr.onEnd',result);endEvent.setOutput(result)._setGlobals(globals)._setOptions(options).setMatches({node:node});endEvent=globals.converter.dispatch(endEvent);return endEvent.output;});showdown.subParser('makeMarkdown.image',function(node,options,globals){'use strict';var startEvent=new showdown.Event('makeMarkdown.image.onStart',node.outerHTML);startEvent.setOutput(null)._setGlobals(globals)._setOptions(options).setMatches({node:node});startEvent=globals.converter.dispatch(startEvent);var result;if(startEvent.output&&startEvent.output!==''){result=startEvent.output;}else{result=function(){var txt='';// reverse the `emoji` option: "special" emoji render as an <img> whose src is known.
// Map that back to its :code:. When emoji is off (or the src isn't an emoji) this falls
// through to normal image handling, so a disabled feature degrades to plain image markdown.
if(options.emoji&&node.hasAttribute('src')){var emojiImages=showdown.helper.emojiReverse().images,src=node.getAttribute('src');if(Object.prototype.hasOwnProperty.call(emojiImages,src)){return':'+emojiImages[src]+':';}}if(node.hasAttribute('src')&&node.getAttribute('src')!==''){var hasDimensions=node.hasAttribute('width')&&node.hasAttribute('height');// image dimensions are a showdown-specific syntax; when the option is disabled but the
// image carries dimensions, fall back to raw HTML so the size isn't silently lost
if(hasDimensions&&!options.parseImgDimensions){return node.outerHTML;}txt+='!['+showdown.helper.escapeMarkdownText(node.getAttribute('alt')||'')+'](';txt+='<'+showdown.helper.escapeMarkdownDestination(node.getAttribute('src'))+'>';if(hasDimensions){var width=node.getAttribute('width');var height=node.getAttribute('height');txt+=' ='+(width==='auto'?'*':width)+'x'+(height==='auto'?'*':height);}if(node.hasAttribute('title')){txt+=' "'+showdown.helper.escapeMarkdownTitle(node.getAttribute('title'))+'"';}txt+=')';}return txt;}();}var endEvent=new showdown.Event('makeMarkdown.image.onEnd',result);endEvent.setOutput(result)._setGlobals(globals)._setOptions(options).setMatches({node:node});endEvent=globals.converter.dispatch(endEvent);return endEvent.output;});showdown.subParser('makeMarkdown.input',function(node,options,globals){'use strict';var startEvent=new showdown.Event('makeMarkdown.input.onStart',node.outerHTML);startEvent.setOutput(null)._setGlobals(globals)._setOptions(options).setMatches({node:node});startEvent=globals.converter.dispatch(startEvent);var result;if(startEvent.output&&startEvent.output!==''){result=startEvent.output;}else{result=function(){// A checkbox input maps to a `[ ]`/`[x]` task marker only when the `tasklists` option
// is enabled, the checkbox lives inside a list item, AND it is followed by some content.
// The list-item context matters: in Markdown a `[ ]`/`[x]` marker is only a task when it
// leads a list item, so a naked checkbox (top level, or inside a `<p>`/`<div>`) would
// degrade into meaningless literal bracket text that no longer round-trips back to a
// checkbox. The following-content check matters for the same reason: a bare
// `<li><input type="checkbox"></li>` has no task text, and `- [ ]` on its own does not
// round-trip to a checkbox, so it is kept as the raw `<input>` instead. Detection
// deliberately keys on the `<li>` ancestry rather than the `task-list-item` class (which
// `makeHtml` only emits under `moreStyling`), so checkboxes in hand-written or unstyled
// list HTML are still recognised. Anything that fails these checks passes through as raw
// HTML. A `checked` attribute (any value, including empty) marks it done; its absence open.
if(node.getAttribute('type')!=='checkbox'||!options.tasklists||!isInsideListItem(node)||!hasFollowingContent(node)){return node.outerHTML;}return node.getAttribute('checked')!==null?'[x]':'[ ]';}();}var endEvent=new showdown.Event('makeMarkdown.input.onEnd',result);endEvent.setOutput(result)._setGlobals(globals)._setOptions(options).setMatches({node:node});endEvent=globals.converter.dispatch(endEvent);return endEvent.output;// Walks up the ancestor chain looking for an enclosing `<li>` (covers both tight
// `<li><input>` items and loose `<li><p><input>` ones).
function isInsideListItem(n){var parent=n.parentNode;while(parent&&typeof parent.tagName!=='undefined'){if(parent.tagName.toLowerCase()==='li'){return true;}parent=parent.parentNode;}return false;}// True when the checkbox is followed by actual task text — an element sibling, or a text
// node that is not just whitespace. A checkbox with nothing meaningful after it is not a
// task list item (`- [ ]` alone does not round-trip back to a checkbox).
function hasFollowingContent(n){var sibling=n.nextSibling;while(sibling){if(sibling.nodeType===1){return true;}if(sibling.nodeType===3&&sibling.nodeValue.trim()!==''){return true;}sibling=sibling.nextSibling;}return false;}});showdown.subParser('makeMarkdown.links',function(node,options,globals){'use strict';var startEvent=new showdown.Event('makeMarkdown.links.onStart',node.outerHTML);startEvent.setOutput(null)._setGlobals(globals)._setOptions(options).setMatches({node:node});startEvent=globals.converter.dispatch(startEvent);var result;if(startEvent.output&&startEvent.output!==''){result=startEvent.output;}else{result=function(){var txt='';if(!node.hasChildNodes()){return txt;}var children=node.childNodes,childrenLength=children.length;// anchors without an href (e.g. named anchors) lose their link semantics but keep their text
if(!node.hasAttribute('href')){for(var n=0;n<childrenLength;++n){txt+=showdown.subParser('makeMarkdown.node')(children[n],options,globals);}return txt;}// special case for mentions
// to simplify (and not make stuff really complicated) mentions will only work in this circumstance:
// <a class="user-mention" href="https://github.com/user">@user</a>
// that is, if there's a "user-mention" class and option ghMentions is true
// otherwise is ignored
var classes=node.getAttribute('class');if(options.ghMentions&&/(?:^| )user-mention\b/.test(classes)){for(var ii=0;ii<childrenLength;++ii){txt+=showdown.subParser('makeMarkdown.node')(children[ii],options,globals);}return txt;}var innerTxt='';for(var i=0;i<childrenLength;++i){innerTxt+=showdown.subParser('makeMarkdown.node')(children[i],options,globals);}var href=node.getAttribute('href');// autolink: when the link text is identical to the href and there's no title,
// emit the compact <href> form instead of [href](<href>)
if(!node.hasAttribute('title')&&innerTxt===href){return'<'+showdown.helper.escapeMarkdownDestination(href)+'>';}txt='['+innerTxt+'](<'+showdown.helper.escapeMarkdownDestination(href)+'>';if(node.hasAttribute('title')){txt+=' "'+showdown.helper.escapeMarkdownTitle(node.getAttribute('title'))+'"';}txt+=')';return txt;}();}var endEvent=new showdown.Event('makeMarkdown.links.onEnd',result);endEvent.setOutput(result)._setGlobals(globals)._setOptions(options).setMatches({node:node});endEvent=globals.converter.dispatch(endEvent);return endEvent.output;});showdown.subParser('makeMarkdown.list',function(node,options,globals,type){'use strict';var startEvent=new showdown.Event('makeMarkdown.list.onStart',node.outerHTML);startEvent.setOutput(null)._setGlobals(globals)._setOptions(options).setMatches({node:node});startEvent=globals.converter.dispatch(startEvent);var result;if(startEvent.output&&startEvent.output!==''){result=startEvent.output;}else{result=function(){var txt='';if(!node.hasChildNodes()){return'';}var listItems=node.childNodes,listItemsLenght=listItems.length,listNum=node.getAttribute('start')||1;for(var i=0;i<listItemsLenght;++i){if(typeof listItems[i].tagName==='undefined'||listItems[i].tagName.toLowerCase()!=='li'){continue;}// define the bullet to use in list
var bullet=void 0;if(type==='ol'){bullet=listNum.toString()+'. ';}else{bullet='- ';}// parse list item
txt+=bullet+showdown.subParser('makeMarkdown.listItem')(listItems[i],options,globals);++listNum;}return txt.trim();}();}var endEvent=new showdown.Event('makeMarkdown.list.onEnd',result);endEvent.setOutput(result)._setGlobals(globals)._setOptions(options).setMatches({node:node});endEvent=globals.converter.dispatch(endEvent);return endEvent.output;});showdown.subParser('makeMarkdown.listItem',function(node,options,globals){'use strict';var startEvent=new showdown.Event('makeMarkdown.listItem.onStart',node.outerHTML);startEvent.setOutput(null)._setGlobals(globals)._setOptions(options).setMatches({node:node});startEvent=globals.converter.dispatch(startEvent);var result;if(startEvent.output&&startEvent.output!==''){result=startEvent.output;}else{result=function(){var listItemTxt='';var children=node.childNodes,childrenLenght=children.length;for(var i=0;i<childrenLenght;++i){var child=children[i];// A nested list must begin on its own line, otherwise the indentation step below
// glues it to the preceding inline text (e.g. `a<ul>…` would render as `- a- b`
// instead of `- a` followed by an indented `- b`).
if(child.nodeType===1&&/^[ou]l$/i.test(child.tagName)&&listItemTxt!==''&&!/\n$/.test(listItemTxt)){listItemTxt+='\n';}listItemTxt+=showdown.subParser('makeMarkdown.node')(child,options,globals);}// if it's only one liner, we need to add a newline at the end
if(!/\n$/.test(listItemTxt)){listItemTxt+='\n';}else{// it's multiparagraph, so we need to indent
listItemTxt=listItemTxt.split('\n').join('\n    ').replace(/^ {4}$/gm,'').replace(/\n\n+/g,'\n\n');}return listItemTxt;}();}var endEvent=new showdown.Event('makeMarkdown.listItem.onEnd',result);endEvent.setOutput(result)._setGlobals(globals)._setOptions(options).setMatches({node:node});endEvent=globals.converter.dispatch(endEvent);return endEvent.output;});showdown.subParser('makeMarkdown.node',function(node,options,globals,spansOnly){'use strict';spansOnly=spansOnly||false;var startEvent=new showdown.Event('makeMarkdown.node.onStart',node.outerHTML||node.nodeValue||'');startEvent.setOutput(null)._setGlobals(globals)._setOptions(options).setMatches({node:node});startEvent=globals.converter.dispatch(startEvent);var result;if(startEvent.output&&startEvent.output!==''){result=startEvent.output;}else{result=function(){var txt='';// edge case of text without wrapper paragraph
if(node.nodeType===3){return showdown.subParser('makeMarkdown.txt')(node,options,globals);}// HTML comment
if(node.nodeType===8){return'<!--'+node.data+'-->\n\n';}// process only node elements
if(node.nodeType!==1){return'';}var tagName=node.tagName.toLowerCase();// Renders an element as raw HTML while still converting its children to markdown.
// Used for unknown wrapper elements and for feature-gated constructs (strikethrough,
// underline, tables, ...) when their option is disabled, so the reverse direction stays
// symmetric with makeHtml: a construct makeHtml wouldn't parse isn't emitted as markdown
// here either. Embedded/replaced-content and void/empty elements are emitted verbatim.
function renderRawElement(n,tag){var rawContentTags=['script','style','canvas','audio','video','iframe','object','svg','math','noscript','template','picture'];var kids=n.childNodes;if(kids&&kids.length>0&&rawContentTags.indexOf(tag)===-1){var inner='';for(var k=0;k<kids.length;++k){inner+=showdown.subParser('makeMarkdown.node')(kids[k],options,globals,spansOnly);}// a block child terminates itself with a blank line; trim that trailing separator so
// the wrapper's closing tag stays compact instead of dangling on its own line
inner=inner.replace(/\n+$/,'');// derive the opening tag from outerHTML so attribute serialization is preserved
// exactly (and is robust against ">" inside attribute values)
var outer=n.outerHTML,closeTag='</'+tag+'>',openTag=outer.substring(0,outer.length-n.innerHTML.length-closeTag.length);return openTag+inner+closeTag;}return n.outerHTML;}switch(tagName){//
// BLOCKS
//
case'h1':if(!spansOnly){txt=showdown.subParser('makeMarkdown.header')(node,options,globals,1)+'\n\n';}break;case'h2':if(!spansOnly){txt=showdown.subParser('makeMarkdown.header')(node,options,globals,2)+'\n\n';}break;case'h3':if(!spansOnly){txt=showdown.subParser('makeMarkdown.header')(node,options,globals,3)+'\n\n';}break;case'h4':if(!spansOnly){txt=showdown.subParser('makeMarkdown.header')(node,options,globals,4)+'\n\n';}break;case'h5':if(!spansOnly){txt=showdown.subParser('makeMarkdown.header')(node,options,globals,5)+'\n\n';}break;case'h6':if(!spansOnly){txt=showdown.subParser('makeMarkdown.header')(node,options,globals,6)+'\n\n';}break;case'p':if(!spansOnly){txt=showdown.subParser('makeMarkdown.paragraph')(node,options,globals)+'\n\n';}break;case'blockquote':if(!spansOnly){txt=showdown.subParser('makeMarkdown.blockquote')(node,options,globals)+'\n\n';}break;case'hr':if(!spansOnly){txt=showdown.subParser('makeMarkdown.hr')(node,options,globals)+'\n\n';}break;case'ol':if(!spansOnly){txt=showdown.subParser('makeMarkdown.list')(node,options,globals,'ol')+'\n\n';}break;case'ul':if(!spansOnly){txt=showdown.subParser('makeMarkdown.list')(node,options,globals,'ul')+'\n\n';}break;case'precode':if(!spansOnly){txt=showdown.subParser('makeMarkdown.codeBlock')(node,options,globals)+'\n\n';}break;case'pre':if(!spansOnly){txt=showdown.subParser('makeMarkdown.pre')(node,options,globals)+'\n\n';}break;case'table':if(!spansOnly){// when tables are disabled, emit the table verbatim: recursing into its block-level
// cells would inject blank lines that break the raw HTML block on re-parse
txt=(options.tables?showdown.subParser('makeMarkdown.table')(node,options,globals):node.outerHTML)+'\n\n';}break;//
// SPANS
//
case'code':txt=showdown.subParser('makeMarkdown.codeSpan')(node,options,globals);break;case'em':case'i':txt=showdown.subParser('makeMarkdown.emphasis')(node,options,globals);break;case'strong':case'b':txt=showdown.subParser('makeMarkdown.strong')(node,options,globals);break;case'del':case's':case'strike':txt=options.strikethrough?showdown.subParser('makeMarkdown.strikethrough')(node,options,globals):renderRawElement(node,tagName);break;case'u':txt=options.underline?showdown.subParser('makeMarkdown.underline')(node,options,globals):renderRawElement(node,tagName);break;case'a':txt=showdown.subParser('makeMarkdown.links')(node,options,globals);break;case'img':txt=showdown.subParser('makeMarkdown.image')(node,options,globals);break;case'br':txt=showdown.subParser('makeMarkdown.break')(node,options,globals);break;case'input':txt=showdown.subParser('makeMarkdown.input')(node,options,globals);break;case'sup':// GFM footnote reference; otherwise an ordinary inline <sup> kept as raw HTML
if(options.footnotes&&/(?:^| )footnote-ref(?: |$)/.test(node.getAttribute('class')||'')){txt=showdown.subParser('makeMarkdown.footnotes')(node,options,globals);}else{txt=renderRawElement(node,tagName);if(!spansOnly){txt+='\n\n';}}break;case'section':// GFM footnotes section; otherwise an ordinary <section> kept as raw HTML
if(!spansOnly&&options.footnotes&&/(?:^| )footnotes(?: |$)/.test(node.getAttribute('class')||'')){txt=showdown.subParser('makeMarkdown.footnotes')(node,options,globals)+'\n\n';}else{txt=renderRawElement(node,tagName);if(!spansOnly){txt+='\n\n';}}break;default:// unknown tag: keep the wrapper as raw HTML but still convert its children
txt=renderRawElement(node,tagName);if(!spansOnly){txt+='\n\n';}}// common normalization
// TODO eventually
return txt;}();}var endEvent=new showdown.Event('makeMarkdown.node.onEnd',result);endEvent.setOutput(result)._setGlobals(globals)._setOptions(options).setMatches({node:node});endEvent=globals.converter.dispatch(endEvent);return endEvent.output;});showdown.subParser('makeMarkdown.paragraph',function(node,options,globals){'use strict';var startEvent=new showdown.Event('makeMarkdown.paragraph.onStart',node.outerHTML);startEvent.setOutput(null)._setGlobals(globals)._setOptions(options).setMatches({node:node});startEvent=globals.converter.dispatch(startEvent);var result;if(startEvent.output&&startEvent.output!==''){result=startEvent.output;}else{result=function(){var txt='';if(node.hasChildNodes()){var children=node.childNodes,childrenLength=children.length;for(var i=0;i<childrenLength;++i){txt+=showdown.subParser('makeMarkdown.node')(children[i],options,globals);}}// some text normalization
txt=txt.trim();return txt;}();}var endEvent=new showdown.Event('makeMarkdown.paragraph.onEnd',result);endEvent.setOutput(result)._setGlobals(globals)._setOptions(options).setMatches({node:node});endEvent=globals.converter.dispatch(endEvent);return endEvent.output;});showdown.subParser('makeMarkdown.pre',function(node,options,globals){'use strict';var startEvent=new showdown.Event('makeMarkdown.pre.onStart',node.outerHTML);startEvent.setOutput(null)._setGlobals(globals)._setOptions(options).setMatches({node:node});startEvent=globals.converter.dispatch(startEvent);var result;if(startEvent.output&&startEvent.output!==''){result=startEvent.output;}else{var num=node.getAttribute('prenum');result='<pre>'+globals.preList[num]+'</pre>';}var endEvent=new showdown.Event('makeMarkdown.pre.onEnd',result);endEvent.setOutput(result)._setGlobals(globals)._setOptions(options).setMatches({node:node});endEvent=globals.converter.dispatch(endEvent);return endEvent.output;});showdown.subParser('makeMarkdown.strikethrough',function(node,options,globals){'use strict';var startEvent=new showdown.Event('makeMarkdown.strikethrough.onStart',node.outerHTML);startEvent.setOutput(null)._setGlobals(globals)._setOptions(options).setMatches({node:node});startEvent=globals.converter.dispatch(startEvent);var result;if(startEvent.output&&startEvent.output!==''){result=startEvent.output;}else{result=function(){var txt='';if(node.hasChildNodes()){txt+='~~';var children=node.childNodes,childrenLength=children.length;for(var i=0;i<childrenLength;++i){txt+=showdown.subParser('makeMarkdown.node')(children[i],options,globals);}txt+='~~';}return txt;}();}var endEvent=new showdown.Event('makeMarkdown.strikethrough.onEnd',result);endEvent.setOutput(result)._setGlobals(globals)._setOptions(options).setMatches({node:node});endEvent=globals.converter.dispatch(endEvent);return endEvent.output;});showdown.subParser('makeMarkdown.strong',function(node,options,globals){'use strict';var startEvent=new showdown.Event('makeMarkdown.strong.onStart',node.outerHTML);startEvent.setOutput(null)._setGlobals(globals)._setOptions(options).setMatches({node:node});startEvent=globals.converter.dispatch(startEvent);var result;if(startEvent.output&&startEvent.output!==''){result=startEvent.output;}else{result=function(){var txt='';if(node.hasChildNodes()){txt+='**';var children=node.childNodes,childrenLength=children.length;for(var i=0;i<childrenLength;++i){txt+=showdown.subParser('makeMarkdown.node')(children[i],options,globals);}txt+='**';}return txt;}();}var endEvent=new showdown.Event('makeMarkdown.strong.onEnd',result);endEvent.setOutput(result)._setGlobals(globals)._setOptions(options).setMatches({node:node});endEvent=globals.converter.dispatch(endEvent);return endEvent.output;});showdown.subParser('makeMarkdown.table',/**
   *
   * @param {DocumentFragment} node
   * @param {{}} options
   * @param {{}} globals
   * @returns {string}
   */function(node,options,globals){'use strict';var startEvent=new showdown.Event('makeMarkdown.table.onStart',node.outerHTML);startEvent.setOutput(null)._setGlobals(globals)._setOptions(options).setMatches({node:node});startEvent=globals.converter.dispatch(startEvent);var result;if(startEvent.output&&startEvent.output!==''){result=startEvent.output;}else{result=function(){var txt='',tableArray=[[],[]],headings,rows=[],colCount,i,ii;/**
         * @param {Element} tr
         */function iterateRow(tr){var children=tr.childNodes,cols=[];// we need to iterate by order, since td and th can be used interchangeably and in any order
// we will ignore malformed stuff, comments and floating text.
for(var _i6=0;_i6<children.length;++_i6){var childName=children[_i6].nodeName.toUpperCase();if(childName==='TD'||childName==='TH'){cols.push(children[_i6]);}}return cols;}// first lets look for <thead>
// we will ignore thead without <tr> children
// also, since markdown doesn't support tables with multiple heading rows, only the first one will be transformed
// the rest will count as regular rows
if(node.querySelectorAll(':scope>thead').length!==0&&node.querySelectorAll(':scope>thead>tr').length!==0){var thead=node.querySelectorAll(':scope>thead>tr');// thead>tr can have td and th children
for(i=0;i<thead.length;++i){rows.push(iterateRow(thead[i]));}}// now let's look for tbody
// we will ignore tbody without <tr> children
if(node.querySelectorAll(':scope>tbody').length!==0&&node.querySelectorAll(':scope>tbody>tr').length!==0){var tbody=node.querySelectorAll(':scope>tbody>tr');// tbody>tr can have td and th children, although th are not very screen reader friendly
for(i=0;i<tbody.length;++i){rows.push(iterateRow(tbody[i]));}}// now look for tfoot
if(node.querySelectorAll(':scope>tfoot').length!==0&&node.querySelectorAll(':scope>tfoot>tr').length!==0){var tfoot=node.querySelectorAll(':scope>tfoot>tr');// tfoot>tr can have td and th children, although th are not very screen reader friendly
for(i=0;i<tfoot.length;++i){rows.push(iterateRow(tfoot[i]));}}// lastly look for naked tr
if(node.querySelectorAll(':scope>tr').length!==0){var tr=node.querySelectorAll(':scope>tr');// tfoot>tr can have td and th children, although th are not very screen reader friendly
for(i=0;i<tr.length;++i){rows.push(iterateRow(tr[i]));}}// TODO: implement <caption> in tables https://developer.mozilla.org/pt-BR/docs/Web/HTML/Element/caption
// note: <colgroup> is ignored, since they are basically styling
// we need now to account for cases of completely empty tables, like <table></table> or equivalent
if(rows.length===0){// table is empty, return empty text
return txt;}// count the first row. We need it to trim the table (if table rows have inconsistent number of columns)
colCount=rows[0].length;// let's shift the first row as a heading
headings=rows.shift();for(i=0;i<headings.length;++i){var headContent=showdown.subParser('makeMarkdown.tableCell')(headings[i],options,globals),align='---';if(headings[i].hasAttribute('style')){var style=headings[i].getAttribute('style').toLowerCase().replace(/\s/g,'');switch(style){case'text-align:left;':align=':---';break;case'text-align:right;':align='---:';break;case'text-align:center;':align=':---:';break;}}// fall back to the deprecated `align` attribute when no inline text-align style matched
if(align==='---'&&headings[i].hasAttribute('align')){switch(headings[i].getAttribute('align').toLowerCase()){case'left':align=':---';break;case'right':align='---:';break;case'center':align=':---:';break;}}tableArray[0][i]=headContent.trim();tableArray[1][i]=align;}// now iterate through the rows and create the pseudo output (not pretty yet)
for(i=0;i<rows.length;++i){var r=tableArray.push([])-1;for(ii=0;ii<colCount;++ii){var cellContent=' ';if(typeof rows[i][ii]!=='undefined'){// Note: if rows[i][ii] is undefined, it means the row has fewer elements than the header,
// and empty content will be added
cellContent=showdown.subParser('makeMarkdown.tableCell')(rows[i][ii],options,globals);}tableArray[r].push(cellContent);}}// now tidy up the output, aligning cells and stuff
var cellSpacesCount=3;for(i=0;i<tableArray.length;++i){for(ii=0;ii<tableArray[i].length;++ii){var strLen=tableArray[i][ii].length;if(strLen>cellSpacesCount){cellSpacesCount=strLen;}}}for(i=0;i<tableArray.length;++i){for(ii=0;ii<tableArray[i].length;++ii){if(i===1){if(tableArray[i][ii].slice(-1)===':'){tableArray[i][ii]=showdown.helper.padEnd(tableArray[i][ii].slice(0,-1),cellSpacesCount-1,'-')+':';}else{tableArray[i][ii]=showdown.helper.padEnd(tableArray[i][ii],cellSpacesCount,'-');}}else{tableArray[i][ii]=showdown.helper.padEnd(tableArray[i][ii],cellSpacesCount);}}txt+='| '+tableArray[i].join(' | ')+' |\n';}return txt.trim();}();}var endEvent=new showdown.Event('makeMarkdown.table.onEnd',result);endEvent.setOutput(result)._setGlobals(globals)._setOptions(options).setMatches({node:node});endEvent=globals.converter.dispatch(endEvent);return endEvent.output;});showdown.subParser('makeMarkdown.tableCell',function(node,options,globals){'use strict';var startEvent=new showdown.Event('makeMarkdown.tableCell.onStart',node.outerHTML);startEvent.setOutput(null)._setGlobals(globals)._setOptions(options).setMatches({node:node});startEvent=globals.converter.dispatch(startEvent);var result;if(startEvent.output&&startEvent.output!==''){result=startEvent.output;}else{result=function(){var txt='';if(!node.hasChildNodes()){return'';}var children=node.childNodes,childrenLength=children.length;for(var i=0;i<childrenLength;++i){txt+=showdown.subParser('makeMarkdown.node')(children[i],options,globals,true);}return txt.trim();}();}var endEvent=new showdown.Event('makeMarkdown.tableCell.onEnd',result);endEvent.setOutput(result)._setGlobals(globals)._setOptions(options).setMatches({node:node});endEvent=globals.converter.dispatch(endEvent);return endEvent.output;});showdown.subParser('makeMarkdown.txt',function(node,options,globals){'use strict';var startEvent=new showdown.Event('makeMarkdown.txt.onStart',node.nodeValue);startEvent.setOutput(null)._setGlobals(globals)._setOptions(options).setMatches({node:node});startEvent=globals.converter.dispatch(startEvent);var result;if(startEvent.output&&startEvent.output!==''){result=startEvent.output;}else{result=function(){var txt=node.nodeValue;// multiple spaces are collapsed
txt=txt.replace(/ +/g,' ');// replace the custom ¨NBSP; with a space
txt=txt.replace(/¨NBSP;/g,' ');// ", <, > and & should replace escaped html entities
txt=showdown.helper.unescapeHTMLEntities(txt);// reverse the `ellipsis` option: … -> ...  (only when the converter would have produced it)
if(options.ellipsis){txt=txt.replace(/…/g,'...');}// reverse the `emoji` option: turn unicode emoji back into their `:code:` form.
// Done before the magic-char escaping below so an emoji whose value contains a markdown
// metachar (e.g. asterisk -> *️⃣) becomes :asterisk: instead of an escaped \*.
// Note: like underline, this is symmetric but opt-in - any literal emoji the source HTML
// already contained is also rewritten to a code when this option is on.
if(options.emoji){var emojiReverse=showdown.helper.emojiReverse();txt=txt.replace(emojiReverse.regex,function(wholeMatch){return':'+emojiReverse.unicode[wholeMatch]+':';});}// escape markdown magic characters
// emphasis, strong and strikethrough - can appear everywhere
// we also escape pipe (|) because of tables
// and escape ` because of code blocks and spans
txt=txt.replace(/([*_~|`])/g,'\\$1');// escape > because of blockquotes
txt=txt.replace(/^(\s*)>/g,'\\$1>');// hash character, only troublesome at the beginning of a line because of headers
txt=txt.replace(/^#/gm,'\\#');// horizontal rules
txt=txt.replace(/^(\s*)([-=]{3,})(\s*)$/,'$1\\$2$3');// dot, because of ordered lists, only troublesome at the beginning of a line when preceded by an integer
txt=txt.replace(/^( {0,3}\d+)\./gm,'$1\\.');// +, * and -, at the beginning of a line becomes a list, so we need to escape them also (asterisk was already escaped)
txt=txt.replace(/^( {0,3})([+-])/gm,'$1\\$2');// images and links, ] followed by ( is problematic, so we escape it
txt=txt.replace(/](\s*)\(/g,'\\]$1\\(');// reference URIs must also be escaped
txt=txt.replace(/^ {0,3}\[([\S \t]*?)]:/gm,'\\[$1]:');return txt;}();}var endEvent=new showdown.Event('makeMarkdown.txt.onEnd',result);endEvent.setOutput(result)._setGlobals(globals)._setOptions(options).setMatches({node:node});endEvent=globals.converter.dispatch(endEvent);return endEvent.output;});showdown.subParser('makeMarkdown.underline',function(node,options,globals){'use strict';var startEvent=new showdown.Event('makeMarkdown.underline.onStart',node.outerHTML);startEvent.setOutput(null)._setGlobals(globals)._setOptions(options).setMatches({node:node});startEvent=globals.converter.dispatch(startEvent);var result;if(startEvent.output&&startEvent.output!==''){result=startEvent.output;}else{result=function(){// showdown's forward parser emits <u> for the `underline` option (e.g. __text__).
// We mirror that here so <u> round-trips. Note: __ is standard-Markdown strong, so this
// only round-trips cleanly through a converter that has the `underline` option enabled.
var txt='';if(node.hasChildNodes()){txt+='__';var children=node.childNodes,childrenLength=children.length;for(var i=0;i<childrenLength;++i){txt+=showdown.subParser('makeMarkdown.node')(children[i],options,globals);}txt+='__';}return txt;}();}var endEvent=new showdown.Event('makeMarkdown.underline.onEnd',result);endEvent.setOutput(result)._setGlobals(globals)._setOptions(options).setMatches({node:node});endEvent=globals.converter.dispatch(endEvent);return endEvent.output;});/**
 * Created by Estevao on 31-05-2015.
 *//**
 * Module scope (shared across all converter instances): warn at most once per process per
 * deprecated extension type, rather than once per extension load.
 * @type {{}}
 */var deprecatedExtTypeWarned={};/**
 * Emit a one-time deprecation warning for a legacy (`lang`/`output`) extension type
 * @param {string} type
 * @param {string} event
 */function warnDeprecatedExtType(type,event){'use strict';if(deprecatedExtTypeWarned[type]){return;}deprecatedExtTypeWarned[type]=true;console.warn('DEPRECATION WARNING: "'+type+'" extensions are deprecated and will be '+'removed in a future version. Use a "listener" extension on the "'+event+'" event instead.');}/**
 * Showdown Converter class
 * @class
 * @param {object} [converterOptions]
 * @returns {Converter}
 */showdown.Converter=function(converterOptions){'use strict';var/**
       * Options used by this converter
       * @private
       * @type {{}}
       */options={},/**
       * Event listeners
       * @private
       * @type {{}}
       */listeners={},/**
       * The flavor set in this converter
       */setConvFlavor=setFlavor,/**
       * Metadata of the document
       * @type {{parsed: {}, raw: string, format: string}}
       */metadata={parsed:{},raw:'',format:''};_constructor();/**
   * Converter constructor
   * @private
   */function _constructor(){converterOptions=converterOptions||{};for(var gOpt in globalOptions){if(Object.prototype.hasOwnProperty.call(globalOptions,gOpt)){options[gOpt]=globalOptions[gOpt];}}// Merge options
if(_typeof(converterOptions)==='object'){for(var opt in converterOptions){if(Object.prototype.hasOwnProperty.call(converterOptions,opt)){options[opt]=converterOptions[opt];}}}else{throw Error('Converter expects the passed parameter to be an object, but '+_typeof(converterOptions)+' was passed instead.');}if(options.extensions){showdown.helper.forEach(options.extensions,_parseExtension);}options=showdown.helper.validateOptions(options);}/**
   * Parse extension
   * @param {*} ext
   * @param {string} [name='']
   * @private
   */function _parseExtension(ext,name){name=name||null;// If it's a string, the extension was previously loaded
if(showdown.helper.isString(ext)){ext=showdown.helper.stdExtName(ext);name=ext;if(!showdown.helper.isUndefined(extensions[ext])){ext=extensions[ext];}else{throw Error('Extension "'+ext+'" could not be loaded. It was either not found or is not a valid extension.');}}if(typeof ext==='function'){ext=ext();}if(!showdown.helper.isArray(ext)){ext=[ext];}var validExt=validate(ext,name);if(!validExt.valid){throw Error(validExt.error);}for(var i=0;i<ext.length;++i){// `lang` and `output` extensions are sugar over the event system: a `lang`
// extension is a listener on `makehtml.onPreParse` (runs after escaping, before the
// subparsers) and an `output` extension a listener on `makehtml.onEnd` (runs on the
// final HTML). They are deprecated in favor of writing listener extensions directly.
switch(ext[i].type){case'lang':warnDeprecatedExtType('lang','makehtml.onPreParse');listen('makehtml.onPreParse',_wrapLegacyExtension(ext[i]));break;case'output':warnDeprecatedExtType('output','makehtml.onEnd');listen('makehtml.onEnd',_wrapLegacyExtension(ext[i]));break;}if(Object.prototype.hasOwnProperty.call(ext[i],'listeners')){for(var ln in ext[i].listeners){if(Object.prototype.hasOwnProperty.call(ext[i].listeners,ln)){listen(ln,ext[i].listeners[ln]);}}}}}/**
   * Adapt a legacy `lang`/`output` extension to an event listener.
   * Returning a string makes `dispatch` update both `event.input` and `event.output`, so
   * several legacy extensions on the same event chain exactly like the old forEach did.
   * @param {{}} ext
   * @returns {function(showdown.Event): string}
   */function _wrapLegacyExtension(ext){return function(event){return showdown.subParser('makehtml.runExtension')(ext,event.input,event.options,{converter:event.converter});};}/**
   * Listen to an event
   * @param {string} name
   * @param {function} callback
   */function listen(name,callback){if(!showdown.helper.isString(name)){throw Error('Invalid argument in converter.listen() method: name must be a string, but '+_typeof(name)+' given');}if(typeof callback!=='function'){throw Error('Invalid argument in converter.listen() method: callback must be a function, but '+_typeof(callback)+' given');}name=name.toLowerCase();if(!Object.prototype.hasOwnProperty.call(listeners,name)){listeners[name]=[];}listeners[name].push(callback);}/**
   * Stop listening to an event
   * @param {string} name
   * @param {function} [callback] If omitted, removes every listener for the event
   */function unlisten(name,callback){if(!showdown.helper.isString(name)){throw Error('Invalid argument in converter.unlisten() method: name must be a string, but '+_typeof(name)+' given');}name=name.toLowerCase();if(!Object.prototype.hasOwnProperty.call(listeners,name)){return;}// no callback given: remove every listener registered for this event
if(typeof callback==='undefined'){delete listeners[name];return;}if(typeof callback!=='function'){throw Error('Invalid argument in converter.unlisten() method: callback must be a function, but '+_typeof(callback)+' given');}var idx=listeners[name].indexOf(callback);while(idx>-1){listeners[name].splice(idx,1);idx=listeners[name].indexOf(callback);}if(listeners[name].length===0){delete listeners[name];}}function rTrimInputText(text){var rsp=text.match(/^\s*/)[0].length,rgx=new RegExp('^\\s{0,'+rsp+'}','gm');return text.replace(rgx,'');}/**
   *
   * @param {showdown.Event} event
   * @returns showdown.Event
   */this.dispatch=function(event){if(!(event instanceof showdown.Event)){throw new TypeError('dispatch only accepts showdown.Event objects as param, but '+_typeof(event)+' given');}event.converter=this;if(Object.prototype.hasOwnProperty.call(listeners,event.name)){for(var i=0;i<listeners[event.name].length;++i){var listRet=listeners[event.name][i](event);if(showdown.helper.isString(listRet)){event.output=listRet;event.input=listRet;}else if(listRet instanceof showdown.Event&&listRet.name===event.name){event=listRet;}}}return event;};/**
   * Listen to an event
   * @param {string} name
   * @param {function} callback
   * @returns {showdown.Converter}
   */this.listen=function(name,callback){listen(name,callback);return this;};/**
   * Stop listening to an event
   * @param {string} name
   * @param {function} [callback] If omitted, removes every listener for the event
   * @returns {showdown.Converter}
   */this.unlisten=function(name,callback){unlisten(name,callback);return this;};/**
   * Converts a markdown string into HTML string
   * @param {string} text
   * @returns {*}
   */this.makeHtml=function(text){//check if text is not falsy
if(!text){return text;}var globals={gHtmlBlocks:[],gHtmlRawBlocks:[],gHtmlMdBlocks:[],gHtmlSpans:[],gUrls:{},gTitles:{},gDimensions:{},gListLevel:0,hashLinkCounts:{},converter:this,ghCodeBlocks:[],metadata:{parsed:{},raw:'',format:''}};// document level onStart event, emitted with the raw markdown before any escaping or
// normalization. Listeners here see literal `$`/`¨` (unlike onPreParse, which runs after
// escaping) and can rewrite the source wholesale.
var startEvent=new showdown.Event('makehtml.onStart',text);startEvent.setOutput(text)._setGlobals(globals)._setOptions(options);startEvent=this.dispatch(startEvent);text=startEvent.output;// This lets us use ¨ trema as an escape char to avoid md5 hashes
// The choice of character is arbitrary; anything that isn't
// magic in Markdown will work.
text=text.replace(/¨/g,'¨T');// Replace $ with ¨D
// RegExp interprets $ as a special character
// when it's in a replacement string
text=text.replace(/\$/g,'¨D');// Standardize line endings
text=text.replace(/\r\n/g,'\n');// DOS to Unix
text=text.replace(/\r/g,'\n');// Mac to Unix
// Stardardize line spaces
//text = text.replace(/\u00A0/g, '&nbsp;');
if(options.smartIndentationFix){text=rTrimInputText(text);}// Make sure text begins and ends with a couple of newlines:
text='\n\n'+text+'\n\n';// detab
//text = showdown.subParser('makehtml.detab')(text, options, globals);
text=showdown.helper.normalizeLeadingTabs(text);/**
     * Strip any lines consisting only of spaces and tabs.
     * This makes subsequent regexs easier to write, because we can
     * match consecutive blank lines with /\n+/ instead of something
     * contorted like /[ \t]*\n+/
     */text=text.replace(/^[ \t]+$/mg,'');// document level onPreParse event, emitted after escaping/normalization and immediately
// before the subparsers run. This is where `lang` extensions are invoked (as listeners).
// Input here contains the `¨D`/`¨T` placeholders for escaped `$`/`¨`.
var preParseEvent=new showdown.Event('makehtml.onPreParse',text);preParseEvent.setOutput(text)._setGlobals(globals)._setOptions(options);preParseEvent=this.dispatch(preParseEvent);text=preParseEvent.output;// run the sub parsers
text=showdown.subParser('makehtml.metadata')(text,options,globals);text=showdown.subParser('makehtml.hashPreCodeTags')(text,options,globals);if(options.cmSpec){// Container mode parses leaf blocks in document order: an open HTML block (e.g. a
// `<div>` with no following blank line) absorbs a fence that follows it, so the
// (fence-aware) HTML-block scan must run before githubCodeBlock. githubCodeBlock then
// only claims fences at indent 0; indent 1-3 fences nested in list items / block
// quotes are handled by the container parsers (and a later blockGamut pass for
// genuinely top-level indented fences).
if(options.cmSpec){text=showdown.helper.expandCmTabs(text);}text=showdown.subParser('makehtml.hashHTMLBlocks')(text,options,globals,true);text=showdown.subParser('makehtml.githubCodeBlock')(text,options,globals,true);}else{text=showdown.subParser('makehtml.githubCodeBlock')(text,options,globals);// CommonMark tab expansion runs after fenced/pre code is hashed (so their content
// tabs are protected) and before the block parsers, which key off indentation.
if(options.cmSpec){text=showdown.helper.expandCmTabs(text);}text=showdown.subParser('makehtml.hashHTMLBlocks')(text,options,globals,true);}text=showdown.subParser('makehtml.hashCodeTags')(text,options,globals);// Footnotes (GFM): collect `[^id]: ...` definitions and replace `[^id]` references
// before stripLinkDefinitions (whose scanner would otherwise claim `[^id]:` lines).
text=showdown.subParser('makehtml.footnotes')(text,options,globals,'strip');text=showdown.subParser('makehtml.stripLinkDefinitions')(text,options,globals);text=showdown.subParser('makehtml.blockGamut')(text,options,globals);text=showdown.subParser('makehtml.paragraphs')(text,options,globals);// Footnotes (GFM): render the referenced footnotes into a <section> and append it,
// before unhashHTMLSpans so hashed spans inside the rendered footnotes are restored.
text=showdown.subParser('makehtml.footnotes')(text,options,globals,'build');// decode character references (gated by the decodeEntities option) after inline parsing,
// while code spans/blocks are still hashed, so decoded chars are not re-parsed
text=showdown.subParser('makehtml.decodeEntities')(text,options,globals);// restore raw CommonMark HTML blocks now, after decodeEntities, so their verbatim
// content (e.g. `<a href="&ouml;&ouml;.html">`) keeps its entities undecoded
if(globals.gHtmlRawBlocks.length){text=text.replace(/¨R(\d+)R/g,function(wm,n){return globals.gHtmlRawBlocks[n];});}text=showdown.subParser('makehtml.unhashHTMLSpans')(text,options,globals);text=showdown.subParser('makehtml.unescapeSpecialChars')(text,options,globals);// GFM disallowed-raw-html tagfilter (opt-in): neutralize a small HTML tag blacklist in
// the now-restored raw HTML. Runs late so it sees the final tags, not placeholders.
text=showdown.subParser('makehtml.disallowedHtmlTags')(text,options,globals);// attacklab: Restore dollar signs
text=text.replace(/¨D/g,'$$');// attacklab: Restore tremas
text=text.replace(/¨T/g,'¨');// render a complete html document instead of a partial if the option is enabled
text=showdown.subParser('makehtml.completeHTMLDocument')(text,options,globals);// document level onEnd event, emitted with the final HTML. This is where `output`
// extensions are invoked (as listeners) and where listeners can post-process the output.
var endEvent=new showdown.Event('makehtml.onEnd',text);endEvent.setOutput(text)._setGlobals(globals)._setOptions(options);endEvent=this.dispatch(endEvent);text=endEvent.output;// update metadata
metadata=globals.metadata;return text;};/**
   * Converts an HTML string into a markdown string
   * @param src
   * @returns {string}
   */this.makeMarkdown=function(src){// replace \r\n with \n
src=src.replace(/\r\n/g,'\n');src=src.replace(/\r/g,'\n');// old macs
// document level onStart event (lets listeners rewrite the raw html before parsing)
var mdStartEvent=new showdown.Event('makeMarkdown.onStart',src);mdStartEvent.setOutput(src)._setOptions(options);mdStartEvent=this.dispatch(mdStartEvent);src=mdStartEvent.output;// due to an edge case, we need to find this: > <
// to prevent removing of non silent white spaces
// ex: <em>this is</em> <strong>sparta</strong>
src=src.replace(/>[ \t]+</,'>¨NBSP;<');var doc=showdown.helper.parseHTML(src);var globals={preList:substitutePreCodeTags(doc),converter:this};// remove all newlines and collapse spaces
clean(doc);// some stuff, like accidental reference links must now be escaped
// TODO
// doc.innerHTML = doc.innerHTML.replace(/\[[\S\t ]]/);
var nodes=doc.childNodes,mdDoc='';for(var i=0;i<nodes.length;i++){mdDoc+=showdown.subParser('makeMarkdown.node')(nodes[i],options,globals);}function clean(node){for(var n=0;n<node.childNodes.length;++n){var child=node.childNodes[n];if(child.nodeType===3){if(!/\S/.test(child.nodeValue)&&!/^ +$/.test(child.nodeValue)){node.removeChild(child);--n;}else{child.nodeValue=child.nodeValue.split('\n').join(' ');child.nodeValue=child.nodeValue.replace(/(\s)+/g,'$1');}}else if(child.nodeType===1){clean(child);}}}// find all pre tags and replace contents with placeholder
// we need this so that we can remove all indentation from html
// to ease up parsing
function substitutePreCodeTags(doc){var pres=doc.querySelectorAll('pre'),presPH=[];for(var _i7=0;_i7<pres.length;++_i7){if(pres[_i7].childElementCount===1&&pres[_i7].firstChild.tagName.toLowerCase()==='code'){var content=pres[_i7].firstChild.innerHTML.trim(),language=pres[_i7].firstChild.getAttribute('data-language')||'';// if data-language attribute is not defined, then we look for class language-*
if(language===''){var classes=pres[_i7].firstChild.className.split(' ');for(var c=0;c<classes.length;++c){var matches=classes[c].match(/^language-(.+)$/);if(matches!==null){language=matches[1];break;}}}// unescape html entities in content
content=showdown.helper.unescapeHTMLEntities(content);presPH.push(content);pres[_i7].outerHTML='<precode language="'+language+'" precodenum="'+_i7.toString()+'"></precode>';}else{presPH.push(pres[_i7].innerHTML);pres[_i7].innerHTML='';pres[_i7].setAttribute('prenum',_i7.toString());}}return presPH;}// document level onEnd event (lets listeners post-process the generated markdown)
var mdEndEvent=new showdown.Event('makeMarkdown.onEnd',mdDoc);mdEndEvent.setOutput(mdDoc)._setGlobals(globals)._setOptions(options);mdEndEvent=this.dispatch(mdEndEvent);mdDoc=mdEndEvent.output;return mdDoc;};/**
   * Set an option of this Converter instance
   * @param {string} key
   * @param {*} value
   */this.setOption=function(key,value){options[key]=value;};/**
   * Get the option of this Converter instance
   * @param {string} key
   * @returns {*}
   */this.getOption=function(key){return options[key];};/**
   * Get the options of this Converter instance
   * @returns {{}}
   */this.getOptions=function(){return options;};/**
   * Add extension to THIS converter
   * @param {{}} extension
   * @param {string} [name=null]
   */this.addExtension=function(extension,name){name=name||null;_parseExtension(extension,name);};/**
   * Use a global registered extension with THIS converter
   * @param {string} extensionName Name of the previously registered extension
   */this.useExtension=function(extensionName){_parseExtension(extensionName);};/**
   * Set the flavor THIS converter should use
   * @param {string} name
   */this.setFlavor=function(name){if(!Object.prototype.hasOwnProperty.call(flavor,name)){throw Error(name+' flavor was not found');}var preset=flavor[name];setConvFlavor=name;for(var option in preset){if(Object.prototype.hasOwnProperty.call(preset,option)){options[option]=preset[option];}}};/**
   * Get the currently set flavor of this converter
   * @returns {string}
   */this.getFlavor=function(){return setConvFlavor;};/**
   * Get the metadata of the previously parsed document
   * @param raw
   * @returns {string|{}}
   */this.getMetadata=function(raw){if(raw){return metadata.raw;}else{return metadata.parsed;}};/**
   * Get the metadata format of the previously parsed document
   * @returns {string}
   */this.getMetadataFormat=function(){return metadata.format;};/**
   * Private: set a single key, value metadata pair
   * @param {string} key
   * @param {string} value
   */this._setMetadataPair=function(key,value){metadata.parsed[key]=value;};/**
   * Private: set metadata format
   * @param {string} format
   */this._setMetadataFormat=function(format){metadata.format=format;};/**
   * Private: set metadata raw text
   * @param {string} raw
   */this._setMetadataRaw=function(raw){metadata.raw=raw;};};return showdown;}.call(globalThis);export default showdown;export{showdown};
